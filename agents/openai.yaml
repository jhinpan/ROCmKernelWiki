interface:
  display_name: "ROCm Kernel Wiki"
  short_description: "Search AMD GPU kernel optimization evidence"
  default_prompt: "Use $rocm-kernel-wiki to optimize this AMD GPU kernel for its target architecture."
policy:
  allow_implicit_invocation: true
