---
id: pr-sglang-23562
repo: sgl-project/sglang
pr: 23562
title: '[AMD] Enable preshuffle paged MQA and page_size=64 for NSA indexer'
author: 1am9trash
date: '2026-05-13'
url: https://github.com/sgl-project/sglang/pull/23562
source_category: upstream-code
architectures:
- gfx950
tags:
- attention
- fp8
- gfx950
- mla
- paged-attention
- quantization
techniques:
- preshuffle-layout
- wave-reduce
hardware_features:
- fp8
kernel_types:
- attention
- mla
- paged-attention
- quantization
languages:
- hip
- triton
- python
captured_at: '2026-05-15'
status: merged
merge_sha: a9359707c18d
inclusion_reason: kernel path 'python/sglang/srt/layers/attention/nsa/index_buf_accessor.py';
  keyword 'attention'
changed_paths:
- python/sglang/srt/layers/attention/nsa/index_buf_accessor.py
- python/sglang/srt/layers/attention/nsa/nsa_indexer.py
- python/sglang/srt/mem_cache/memory_pool.py
- python/sglang/srt/server_args.py
facet_source: inferred
related:
- kernel-paged-attention
- kernel-mla-decode
- technique-hip-graphs
---
# [AMD] Enable preshuffle paged MQA and page_size=64 for NSA indexer

**Repository:** [sgl-project/sglang](https://github.com/sgl-project/sglang) · **PR:** [#23562](https://github.com/sgl-project/sglang/pull/23562) · **Merged:** 2026-05-13 · **Author:** @1am9trash

**Inclusion reason:** kernel path 'python/sglang/srt/layers/attention/nsa/index_buf_accessor.py'; keyword 'attention'

## Summary (from upstream PR description)

Depends on aiter PR#2879 — must be merged first.

Motivation

In the NSA indexer, paged mqa computation is very slow at high concurrency. For example, at 8k1kcc64, it takes ~88 us per layer, consisting of two kernels (logits tensor init 11us + MQA kernel 77us).

The main bottleneck is the oversized block_table. The block_table shape is (batch_size, max_seq_len / page_size). With page_size=1 and max_seq_len=131k at cc64, the block_table shape is (64, 131072), totaling 64 × 131072 × 4B ≈ 32MB, with frequent indirect loads, causing poor MQA kernel performance.
Modifications

To reduce the block_table size, we cannot use page_size=1. In this PR, we change it to 64. This introduces the following changes:

The original MQA kernel does not support page_size>1. We switch to the MQA preshuffle kernel, which supports page_size as a multiple of 16.
Since the MQA preshuffle kernel requires a specific k-cache layout, the indexer's k-cache read/write paths must also store data accordingly. This was not supported in the existing kernels. In aiter PR#2879, we add preshuffle support to the indexer k-cache read/write kernels (controlled by preshuffle=True).

Other changes:

Replace torch.full(..., -

## Changed files (4 total, first 4 shown)

- `python/sglang/srt/layers/attention/nsa/index_buf_accessor.py`
- `python/sglang/srt/layers/attention/nsa/nsa_indexer.py`
- `python/sglang/srt/mem_cache/memory_pool.py`
- `python/sglang/srt/server_args.py`

## Provenance

- Merge commit: `a9359707c18d`
- Captured at knowledge cutoff: 2026-05-15
- Source of truth: the upstream PR linked above. This page summarizes upstream
  metadata; consult the PR for the authoritative diff.
