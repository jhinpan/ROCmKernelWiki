---
id: pr-sglang-26616
repo: sgl-project/sglang
pr: 26616
title: Let unittest._ShouldStop propagate through retry() so subTest+failfast works
author: ch-wan
date: '2026-05-28'
url: https://github.com/sgl-project/sglang/pull/26616
source_category: upstream-code
architectures:
- gfx942
tags:
- attention
- gfx942
- lds
techniques: []
hardware_features:
- lds
kernel_types:
- attention
languages:
- hip
- triton
- python
captured_at: '2026-05-15'
status: merged
merge_sha: 4f92e63c99fa
inclusion_reason: keyword 'attention'
changed_paths:
- python/sglang/srt/utils/common.py
---

# Let unittest._ShouldStop propagate through retry() so subTest+failfast works

**Repository:** [sgl-project/sglang](https://github.com/sgl-project/sglang) · **PR:** [#26616](https://github.com/sgl-project/sglang/pull/26616) · **Merged:** 2026-05-28 · **Author:** @ch-wan

**Inclusion reason:** keyword 'attention'

## Summary (from upstream PR description)

Motivation
retry() in sglang.srt.utils.common is used by CustomTestCase._callTestMethod to retry transient CI failures. It already special-cases SkipTest (re-raises without retry), but it doesn't know about unittest.case._ShouldStop — the internal failfast signal raised by subTest.__exit__ when result.failfast is True (CI invokes python3 file.py -f).
When a helper inside with self.subTest(...) calls self.skipTest():

subTest.__exit__ catches SkipTest via testPartExecutor and records the per-subtest skip.
Because result.failfast is True, subTest then raises unittest.case._ShouldStop to tell the outer testPartExecutor to stop the test method cleanly.
retry() only special-cases SkipTest; _ShouldStop falls into the generic except Exception branch, gets retried, and on retry the same thing happens — eventually wrapping it in a generic Exception so the test is reported as an error.

This surfaced as CI failures on the attention-backend unittest suite — any per-iteration self.skipTest() inside with self.subTest(...) (FA3's hardware gate, DSA's tilelang/trtllm/aiter capability gates, Mamba2 tree-verify, draft-extend backend-not-available, …) tripped the chain. Stacktrace was identical: the

## Changed files (1 total, first 1 shown)

- `python/sglang/srt/utils/common.py`

## Provenance

- Merge commit: `4f92e63c99fa`
- Captured at knowledge cutoff: 2026-05-15
- Source of truth: the upstream PR linked above. This page summarizes upstream
  metadata; consult the PR for the authoritative diff.
