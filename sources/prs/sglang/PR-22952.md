---
id: pr-sglang-22952
repo: sgl-project/sglang
pr: 22952
title: '[AMD] Add SGLANG_MORI_MOE_MAX_INPUT_TOKENS to truncate dispatch before MoE.'
author: Duyi-Wang
date: '2026-04-17'
url: https://github.com/sgl-project/sglang/pull/22952
source_category: upstream-code
architectures:
- gfx942
tags:
- fused-moe
- gfx942
- moe
techniques:
- kernel-fusion
hardware_features: []
kernel_types:
- fused-moe
- moe
languages:
- hip
- triton
- python
captured_at: '2026-05-15'
status: merged
merge_sha: 8c190f6b9183
inclusion_reason: kernel path 'python/sglang/srt/layers/moe/ep_moe/layer.py'; keyword
  'moe'
changed_paths:
- docs/references/environment_variables.md
- python/sglang/srt/layers/moe/ep_moe/layer.py
facet_source: inferred
related:
- kernel-fused-moe
- kernel-grouped-gemm
- technique-kernel-fusion
---
# [AMD] Add SGLANG_MORI_MOE_MAX_INPUT_TOKENS to truncate dispatch before MoE.

**Repository:** [sgl-project/sglang](https://github.com/sgl-project/sglang) · **PR:** [#22952](https://github.com/sgl-project/sglang/pull/22952) · **Merged:** 2026-04-17 · **Author:** @Duyi-Wang

**Inclusion reason:** kernel path 'python/sglang/srt/layers/moe/ep_moe/layer.py'; keyword 'moe'

## Summary (from upstream PR description)

Motivation
In the MoriEP MoE path, dispatch_a1 (the dispatch output hidden states) has a fixed first dimension of SGLANG_MORI_NUM_MAX_DISPATCH_TOKENS_PER_RANK * ep_size (or SGLANG_MORI_PREALLOC_MAX_RECV_TOKENS). However, the actual number of valid tokens (totalRecvTokenNum) is often much smaller than this buffer size.
aiter.fused_moe uses the first dimension of the input tensor (dispatch_a1.shape[0]) to select the kernel dispatch strategy. Because the padded buffer size is much larger than the actual totalRecvTokenNum, aiter.fused_moe selects a suboptimal kernel intended for large token counts, leading to degraded MoE performance.
This PR adds an environment variable SGLANG_MORI_MOE_MAX_INPUT_TOKENS to truncate the dispatch tensors before MoE computation, so that aiter.fused_moe sees a tensor size closer to the actual valid token count and selects the appropriate kernel.
Modifications


python/sglang/srt/layers/moe/ep_moe/layer.py:

Read SGLANG_MORI_MOE_MAX_INPUT_TOKENS env var in MoriEPMoE.__init__ (default 0 = disabled).
In run_moe_core, if the env var is set and smaller than the buffer size, truncate dispatch_a1, dispatch_scale, dispatch_ids, and dispatch_weights to [:limit] bef

## Changed files (2 total, first 2 shown)

- `docs/references/environment_variables.md`
- `python/sglang/srt/layers/moe/ep_moe/layer.py`

## Provenance

- Merge commit: `8c190f6b9183`
- Captured at knowledge cutoff: 2026-05-15
- Source of truth: the upstream PR linked above. This page summarizes upstream
  metadata; consult the PR for the authoritative diff.
