---
id: pr-sglang-23699
repo: sgl-project/sglang
pr: 23699
title: '[NSA] Fall back to fast_hadamard_transform when sgl_kernel lacks the symbol'
author: Fridge003
date: '2026-04-25'
url: https://github.com/sgl-project/sglang/pull/23699
source_category: upstream-code
architectures:
- gfx942
tags:
- attention
- flash-attention
- fp8
- gfx942
- lds
techniques: []
hardware_features:
- fp8
- lds
kernel_types:
- attention
- flash-attention
languages:
- hip
- triton
- python
captured_at: '2026-05-15'
status: merged
merge_sha: 4bf81c988a3c
inclusion_reason: kernel path 'python/sglang/srt/layers/attention/nsa/nsa_indexer.py';
  keyword 'attention'
changed_paths:
- python/sglang/srt/layers/attention/nsa/nsa_indexer.py
facet_source: inferred
related:
- kernel-flydsl-flash-attention
- kernel-flash-attention-ck
- lang-flydsl
---
# [NSA] Fall back to fast_hadamard_transform when sgl_kernel lacks the symbol

**Repository:** [sgl-project/sglang](https://github.com/sgl-project/sglang) · **PR:** [#23699](https://github.com/sgl-project/sglang/pull/23699) · **Merged:** 2026-04-25 · **Author:** @Fridge003

**Inclusion reason:** kernel path 'python/sglang/srt/layers/attention/nsa/nsa_indexer.py'; keyword 'attention'

## Summary (from upstream PR description)

Summary

On non-HIP / non-SM103 paths, nsa_indexer.rotate_activation does from sgl_kernel import hadamard_transform. Older sgl_kernel builds (e.g. 0.3.21) don't export this symbol, which causes ImportError at forward time and crashes the scheduler. Wrap the import in try/except and fall back to fast_hadamard_transform (already used on HIP / SM103), so older / partial sgl_kernel builds keep working.

Why

Reproducible on the official lmsysorg/sglang:deepseek-v4-grace-blackwell image only when running on non-GB300 CUDA hardware where _is_sm103=False. On GB300 it's masked because _is_sm103=True already routes to fast_hadamard_transform. But any non-Blackwell / non-GB300 CUDA platform hits this:

File ".../sglang/srt/layers/attention/nsa/nsa_indexer.py", line ..., in rotate_activation
    from sgl_kernel import hadamard_transform
ImportError: cannot import name 'hadamard_transform' from 'sgl_kernel'
[..] Received sigquit from a child process. It usually means the child failed.


fast_hadamard_transform is already an installed dependency on those code paths, so the fallback adds no new requirement.

Diff
def rotate_activation(x: torch.Tensor) -> torch.Tensor:
    if _is_hip or _is_sm103

## Changed files (1 total, first 1 shown)

- `python/sglang/srt/layers/attention/nsa/nsa_indexer.py`

## Provenance

- Merge commit: `4bf81c988a3c`
- Captured at knowledge cutoff: 2026-05-15
- Source of truth: the upstream PR linked above. This page summarizes upstream
  metadata; consult the PR for the authoritative diff.
