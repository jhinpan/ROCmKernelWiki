---
id: pr-aiter-2169
repo: ROCm/aiter
pr: 2169
title: Fix LRU cache pollution causing BLOCK_SIZE_S3 KeyError in gemm_afp4wfp4
author: Duyi-Wang
date: '2026-03-12'
url: https://github.com/ROCm/aiter/pull/2169
source_category: upstream-code
architectures:
- gfx942
tags:
- fp4
- gemm
- gfx942
- mxfp
- quantization
techniques:
- fine-grained-quantization
- kernel-fusion
- preshuffle-layout
hardware_features:
- fp4
- mxfp
kernel_types:
- gemm
- quantization
languages:
- hip
- cpp
- triton
captured_at: '2026-05-15'
status: merged
merge_sha: 7d29fce14768
inclusion_reason: kernel path 'aiter/ops/triton/gemm/fused/fused_gemm_afp4wfp4_split_cat.py';
  keyword 'gemm'
changed_paths:
- aiter/ops/triton/gemm/fused/fused_gemm_afp4wfp4_split_cat.py
- aiter/ops/triton/utils/gemm_config_utils.py
facet_source: inferred
related:
- technique-fine-grained-quantization
- kernel-flydsl-preshuffle-gemm
- kernel-fp8-gemm
---
# Fix LRU cache pollution causing BLOCK_SIZE_S3 KeyError in gemm_afp4wfp4

**Repository:** [ROCm/aiter](https://github.com/ROCm/aiter) · **PR:** [#2169](https://github.com/ROCm/aiter/pull/2169) · **Merged:** 2026-03-12 · **Author:** @Duyi-Wang

**Inclusion reason:** kernel path 'aiter/ops/triton/gemm/fused/fused_gemm_afp4wfp4_split_cat.py'; keyword 'gemm'

## Summary (from upstream PR description)

Motivation
Fix KeyError: 'Keyword argument BLOCK_SIZE_S3 was specified but unrecognised' crash in gemm_afp4wfp4 triggered when using quark_w4a4_mxfp4 quantization in SGLang.
Technical Details
get_gemm_config() in aiter/ops/triton/utils/gemm_config_utils.py uses @functools.lru_cache, which caches the returned (dict, bool) tuple by reference. The fused kernel fused_gemm_afp4wfp4_split_cat mutates the cached dict in-place by injecting BLOCK_SIZE_S3:
config["BLOCK_SIZE_S3"] = triton.next_power_of_2(...)
Subsequent calls to gemm_afp4wfp4 with the same (M, N, K) receive the same polluted dict from the cache. When unpacked via **config into a Triton kernel that doesn't accept BLOCK_SIZE_S3, it raises a KeyError.
Fix: Added config.pop("BLOCK_SIZE_S3", None) in the three affected functions in gemm_afp4wfp4.py (gemm_afp4wfp4_, gemm_afp4wfp4_preshuffled_scales, gemm_afp4wfp4_preshuffle) immediately after obtaining the config dict. This strips the polluted key before it reaches the kernel call, regardless of whether the cache was polluted or not.
This is a minimal workaround. A more thorough fix would be to have get_gemm_config() return defensive copies to prevent cache mutation entirely.
Test

## Changed files (2 total, first 2 shown)

- `aiter/ops/triton/gemm/fused/fused_gemm_afp4wfp4_split_cat.py`
- `aiter/ops/triton/utils/gemm_config_utils.py`

## Provenance

- Merge commit: `7d29fce14768`
- Captured at knowledge cutoff: 2026-05-15
- Source of truth: the upstream PR linked above. This page summarizes upstream
  metadata; consult the PR for the authoritative diff.
