---
id: pr-aiter-1191
repo: ROCm/aiter
pr: 1191
title: '[bug] fix qr when variable input'
author: lihaoyang-amd
date: '2025-10-15'
url: https://github.com/ROCm/aiter/pull/1191
source_category: upstream-code
architectures:
- gfx942
tags:
- all-reduce
- fp16
- fp8
- gfx942
- int8
techniques:
- wave-reduce
hardware_features:
- fp16
- fp8
- int8
kernel_types:
- all-reduce
languages:
- hip
- cpp
- triton
captured_at: '2026-05-15'
status: merged
merge_sha: 8455b8fbbc88
inclusion_reason: kernel path 'csrc/include/quick_all_reduce.cuh'; keyword 'fp8'
changed_paths:
- csrc/include/quick_all_reduce.cuh
- op_tests/multigpu_tests/test_quick_all_reduce.py
facet_source: inferred
related:
- kernel-rmsnorm
---
# [bug] fix qr when variable input

**Repository:** [ROCm/aiter](https://github.com/ROCm/aiter) · **PR:** [#1191](https://github.com/ROCm/aiter/pull/1191) · **Merged:** 2025-10-15 · **Author:** @lihaoyang-amd

**Inclusion reason:** kernel path 'csrc/include/quick_all_reduce.cuh'; keyword 'fp8'

## Summary (from upstream PR description)

When the tensor parallelism (TP) degree is set to 4 or 8, frequent changes in the input shape can cause QuickReduce to hang (this issue has been observed with the gpt_oss model).
We have identified that the root cause is overlapping flag memory addresses between consecutive AllReduce operations.
For most models, the hidden size remains relatively stable, so this issue does not occur.
Our current solution is to allocate separate memory regions for the flags and data of the two AllReduce phases in each operation.
(Note: The data region must also be separated, as overlapping would lead to correctness issues.)
To reproduce error
1.install aiter
2.python3 this_script.py
import torch
import multiprocessing
import argparse
import torch.distributed as dist
import aiter as ops
def worker(rank, world_size, comm_handles, comm_handle_dict):
    device = torch.device(f"cuda:{rank}")
    torch.cuda.set_device(device)
    qr_max_size = None # MB
    _ptr = ops.init_custom_qr(rank, world_size, qr_max_size)
    ranks = []
    for i in range(world_size):
        ranks.append(i)
    dist.init_process_group(
        backend="nccl",
        init_method="tcp://127.0.0.1:29500",
        rank=rank,
      

## Changed files (2 total, first 2 shown)

- `csrc/include/quick_all_reduce.cuh`
- `op_tests/multigpu_tests/test_quick_all_reduce.py`

## Provenance

- Merge commit: `8455b8fbbc88`
- Captured at knowledge cutoff: 2026-05-15
- Source of truth: the upstream PR linked above. This page summarizes upstream
  metadata; consult the PR for the authoritative diff.
