---
id: pr-aiter-3288
repo: ROCm/aiter
pr: 3288
title: '[Bugfix] a16w16 splitk: per-stream semaphore workspace to avoid deadlock'
author: valarLip
date: '2026-05-20'
url: https://github.com/ROCm/aiter/pull/3288
source_category: upstream-code
architectures:
- gfx942
tags:
- attention
- gemm
- gfx942
techniques:
- split-k
- wave-reduce
hardware_features: []
kernel_types:
- attention
- gemm
languages:
- hip
- cpp
- triton
captured_at: '2026-05-15'
status: merged
merge_sha: 163e6a025210
inclusion_reason: kernel path 'aiter/ops/gemm_op_a16w16.py'; keyword 'gemm'
changed_paths:
- aiter/ops/gemm_op_a16w16.py
facet_source: inferred
related:
- kernel-flash-attention-ck
- technique-stream-k
- technique-split-k
---
# [Bugfix] a16w16 splitk: per-stream semaphore workspace to avoid deadlock

**Repository:** [ROCm/aiter](https://github.com/ROCm/aiter) · **PR:** [#3288](https://github.com/ROCm/aiter/pull/3288) · **Merged:** 2026-05-20 · **Author:** @valarLip

**Inclusion reason:** kernel path 'aiter/ops/gemm_op_a16w16.py'; keyword 'gemm'

## Summary (from upstream PR description)

Summary
The a16w16 ASM splitk kernels use an atomic-counter protocol where the last workgroup performs the reduction phase. With a single shared semaphore workspace, concurrent launches on different CUDA streams contend on the same counter:

Stream A launches splitk GEMM → workgroups atomicAdd on shared counter
Stream B launches splitk GEMM concurrently → its workgroups also atomicAdd on the same counter
The counter now reflects both kernels' workgroups
Neither stream's last-writer check (`counter == this_kernel_expected_count`) ever fires
Reduction phase never runs → host blocks waiting for output → deadlock

Symptom: multi-stream attention paths (e.g. `default + alt_stream + compress_stream` in DSv4 attention) hang inside `*_splitk_clean` kernel.
Fix
Key the workspace cache by `(device, stream_ptr)` instead of just `device`, so each stream gets its own zero-initialized counter array.

New private `_get_semaphore_workspace_keyed(device, stream_id)` with `lru_cache(maxsize=64)`. Workspace is ~4 KB and process stream count is typically < 8, so the cap leaves wide headroom and avoids evicting an in-flight workspace.
Public `get_semaphore_workspace(device)` resolves the current stream

## Changed files (1 total, first 1 shown)

- `aiter/ops/gemm_op_a16w16.py`

## Provenance

- Merge commit: `163e6a025210`
- Captured at knowledge cutoff: 2026-05-15
- Source of truth: the upstream PR linked above. This page summarizes upstream
  metadata; consult the PR for the authoritative diff.
