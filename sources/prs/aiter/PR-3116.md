---
id: pr-aiter-3116
repo: ROCm/aiter
pr: 3116
title: 'perf(moe_gemm_a8w4): tune block_m=32 & block_m=128 config for MI355X / gpt-oss-120b'
author: JohnQinAMD
date: '2026-05-14'
url: https://github.com/ROCm/aiter/pull/3116
source_category: upstream-code
architectures:
- gfx950
tags:
- fp4
- gemm
- gfx950
- moe
- mxfp
techniques: []
hardware_features:
- fp4
- mxfp
kernel_types:
- moe
- gemm
languages:
- hip
- cpp
- triton
captured_at: '2026-05-15'
status: merged
merge_sha: 1fb097478c12
inclusion_reason: kernel path 'aiter/ops/triton/moe/moe_op_gemm_a8w4.py'; keyword
  'gemm'
changed_paths:
- aiter/ops/triton/moe/moe_op_gemm_a8w4.py
---

# perf(moe_gemm_a8w4): tune block_m=32 & block_m=128 config for MI355X / gpt-oss-120b

**Repository:** [ROCm/aiter](https://github.com/ROCm/aiter) · **PR:** [#3116](https://github.com/ROCm/aiter/pull/3116) · **Merged:** 2026-05-14 · **Author:** @JohnQinAMD

**Inclusion reason:** kernel path 'aiter/ops/triton/moe/moe_op_gemm_a8w4.py'; keyword 'gemm'

## Summary (from upstream PR description)

3-line change in get_kernel_config(): switches num_warps = 8 → 4 for block_m ∈ {32, 128} on gfx950. These are the routing block sizes selected by routing.block_m = max(16, min(next_pow_2(tokens_per_expt), 128)) for gpt-oss-120b (E=128, top_k=4): block_m=32 at batch=1024, block_m=128 at every batch ≥ 4096 (the dominant chunked-prefill case). Non-gfx950 and block_m ∈ {16, 64} paths are byte-for-byte unchanged.
     elif block_m == 32:
         if n <= 1024:
             block_n = 128
             num_warps = 4
         elif n <= 4096:
             block_n = 256
-            num_warps = 8
+            num_warps = 4 
         else:
             block_n = 512
-            num_warps = 8
+            num_warps = 4

     else:
         block_n = 512
-        num_warps = 8
+        num_warps = 4 if (block_m == 128) else 8

Gated on block_m == 128 (not the full outer else) because num_warps=4 regresses at block_m=64 by ~7% on this shape. The outer else handles both 64 and 128.

Microbench (MI355X gfx950, bench_moe_gemm_a8w4.py --num-weight-inits=10)
python bench_moe_gemm_a8w4.py --shape 2880 5760 --experts 128 4 --op-regex ".*<layer>.*" --num-weight-inits=10




batch
block_m (routing)
layer

## Changed files (1 total, first 1 shown)

- `aiter/ops/triton/moe/moe_op_gemm_a8w4.py`

## Provenance

- Merge commit: `1fb097478c12`
- Captured at knowledge cutoff: 2026-05-15
- Source of truth: the upstream PR linked above. This page summarizes upstream
  metadata; consult the PR for the authoritative diff.
