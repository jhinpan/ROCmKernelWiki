---
id: pr-aiter-1261
repo: ROCm/aiter
pr: 1261
title: '[TRITON] Instruction shape fix for Gluon gemm_a8w8_blockscale kernel'
author: eky-amd
date: '2025-11-07'
url: https://github.com/ROCm/aiter/pull/1261
source_category: upstream-code
architectures:
- gfx950
tags:
- block-scale
- gemm
- gfx950
- mfma
techniques:
- mfma-pipelining
hardware_features:
- block-scale
- mfma
kernel_types:
- gemm
languages:
- hip
- cpp
- triton
captured_at: '2026-05-15'
status: merged
merge_sha: fc37c619254a
inclusion_reason: kernel path 'aiter/ops/triton/gluon/gemm_a8w8_blockscale.py'; keyword
  'mfma'
changed_paths:
- aiter/ops/triton/gluon/gemm_a8w8_blockscale.py
- op_tests/triton_tests/test_gemm_a8w8_blockscale.py
facet_source: inferred
related:
- technique-mfma-pipelining
- technique-split-k
- technique-preshuffle-layout
---
# [TRITON] Instruction shape fix for Gluon gemm_a8w8_blockscale kernel

**Repository:** [ROCm/aiter](https://github.com/ROCm/aiter) · **PR:** [#1261](https://github.com/ROCm/aiter/pull/1261) · **Merged:** 2025-11-07 · **Author:** @eky-amd

**Inclusion reason:** kernel path 'aiter/ops/triton/gluon/gemm_a8w8_blockscale.py'; keyword 'mfma'

## Summary (from upstream PR description)

Motivation
Running the kernel in aiter/ops/triton/gluon/gemm_a8w8_blockscale.py throws the error:
triton.compiler.errors.CompilationError: at 93:32:
        size_per_thread=[threads_per_elem_mk, 16],
        threads_per_warp=[8, 8],
        warps_per_cta=[NUM_WARPS, 1],
        order=[1, 0],
    )
    blocked_kn: gl.constexpr = gl.BlockedLayout(
        size_per_thread=[16, threads_per_elem_kn],
        threads_per_warp=[8, 8],
        warps_per_cta=[1, NUM_WARPS],
        order=[0, 1],
    )
    mfma_layout: gl.constexpr = gl.amd.AMDMFMALayout(
                                ^
AssertionError('instr_shape must follow the (M, N, K) format')


Also, the following code snippet in op_tests/triton_tests/test_gemm_a8w8_blockscale is now unnecessary:
if K % block_shape_k != 0:
    pytest.skip(
        "Latest upstream compiler as of Aug 22 (necessary for Gluon) causes"
        " infinite hang when EVEN_K is false. Try seeing if it's fixed if it's been a while."
    )

The latest upstream Triton compiler no longer hangs for this case. There is no need to skip these tests.
Technical Details
The instr_shape argument needs to specify shapes according to the architecture's MFMA instruction se

## Changed files (2 total, first 2 shown)

- `aiter/ops/triton/gluon/gemm_a8w8_blockscale.py`
- `op_tests/triton_tests/test_gemm_a8w8_blockscale.py`

## Provenance

- Merge commit: `fc37c619254a`
- Captured at knowledge cutoff: 2026-05-15
- Source of truth: the upstream PR linked above. This page summarizes upstream
  metadata; consult the PR for the authoritative diff.
