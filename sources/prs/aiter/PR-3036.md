---
id: pr-aiter-3036
repo: ROCm/aiter
pr: 3036
title: Add -o flag to bench_gmm.py for CSV output
author: apicciau
date: '2026-05-11'
url: https://github.com/ROCm/aiter/pull/3036
source_category: upstream-code
architectures:
- gfx942
tags:
- bf16
- gemm
- gfx942
techniques: []
hardware_features:
- bf16
kernel_types:
- gemm
languages:
- hip
- cpp
- triton
captured_at: '2026-05-15'
status: merged
merge_sha: fde086e2297f
inclusion_reason: kernel path 'op_tests/op_benchmarks/triton/bench_gmm.py'; keyword
  'gemm'
changed_paths:
- op_tests/op_benchmarks/triton/bench_gmm.py
---

# Add -o flag to bench_gmm.py for CSV output

**Repository:** [ROCm/aiter](https://github.com/ROCm/aiter) · **PR:** [#3036](https://github.com/ROCm/aiter/pull/3036) · **Merged:** 2026-05-11 · **Author:** @apicciau

**Inclusion reason:** kernel path 'op_tests/op_benchmarks/triton/bench_gmm.py'; keyword 'gemm'

## Summary (from upstream PR description)

Motivation
bench_gmm.py is the only benchmark script under op_tests/op_benchmarks/triton/ that cannot write its results to a CSV file. All other benchmark scripts expose a -o flag that passes save_path="." to Triton's perf_report, causing results to be written as a CSV in the current directory. Without this, bench_gmm.py outputs only to stdout, making it impossible to use with automated sweep scripts that rely on collecting and merging per-run CSVs.
Technical Details
op_tests/op_benchmarks/triton/bench_gmm.py
Three changes, all following the pattern already used in bench_batched_gemm_*.py and other benchmark scripts:

Added -o flag to parse_args():

parser.add_argument(
    "-o",
    action="store_true",
    help="write performance results to CSV file in the current directory",
)


Added output: bool = False parameter to benchmark_gmm().


Changed the benchmark.run() call to pass save_path when -o is set:


benchmark.run(show_plots=False, print_data=True, save_path="." if output else "")
Test Plan
# Run with -o and verify a CSV is written to the current directory
cd /tmp && python3 op_tests/op_benchmarks/triton/bench_gmm.py \
  --gmm-type gmm --metric throughput -o
ls *.csv
Verify 

## Changed files (1 total, first 1 shown)

- `op_tests/op_benchmarks/triton/bench_gmm.py`

## Provenance

- Merge commit: `fde086e2297f`
- Captured at knowledge cutoff: 2026-05-15
- Source of truth: the upstream PR linked above. This page summarizes upstream
  metadata; consult the PR for the authoritative diff.
