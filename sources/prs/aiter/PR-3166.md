---
id: pr-aiter-3166
repo: ROCm/aiter
pr: 3166
title: '[Bugfix][Triton] Honor `transpose_bm` in batched_gemm_a16wfp4_ fake tensor'
author: xaguilar-amd
date: '2026-05-13'
url: https://github.com/ROCm/aiter/pull/3166
source_category: upstream-code
architectures:
- gfx950
tags:
- fp4
- gemm
- gfx950
- mla
- mxfp
- quantization
- rope
techniques: []
hardware_features:
- fp4
- mxfp
kernel_types:
- mla
- gemm
- rope
- quantization
languages:
- hip
- cpp
- triton
captured_at: '2026-05-15'
status: merged
merge_sha: 03c55d0cf744
inclusion_reason: kernel path 'aiter/ops/triton/gemm/batched/batched_gemm_a16wfp4.py';
  keyword 'gemm'
changed_paths:
- aiter/ops/triton/gemm/batched/batched_gemm_a16wfp4.py
- op_tests/triton_tests/gemm/batched/test_batched_gemm_a16wfp4.py
---

# [Bugfix][Triton] Honor `transpose_bm` in batched_gemm_a16wfp4_ fake tensor

**Repository:** [ROCm/aiter](https://github.com/ROCm/aiter) · **PR:** [#3166](https://github.com/ROCm/aiter/pull/3166) · **Merged:** 2026-05-13 · **Author:** @xaguilar-amd

**Inclusion reason:** kernel path 'aiter/ops/triton/gemm/batched/batched_gemm_a16wfp4.py'; keyword 'gemm'

## Summary (from upstream PR description)

Summary
The fake tensor function registered for aiter::batched_gemm_a16wfp4_ ignores
the transpose_bm argument and always returns (Bx, M, N), while the real
kernel returns (M, B, N) when transpose_bm=True. Under torch.compile,
this fake/real mismatch silently produces wrong output (or a GPU memory access
fault) for any caller whose downstream consumer constrains the BMM output's
leading dimension.
Fixes the fake to match the real kernel by branching on transpose_bm and
adds a torch.compile regression test that catches the bug deterministically.
Motivation
aiter/ops/triton/gemm/batched/batched_gemm_a16wfp4.py:


Real kernel allocation (lines 100-103, unchanged):
if y is None:
    if transpose_bm:
        y = torch.empty((M, B, N), dtype=dtype, device=x.device)
    else:
        y = torch.empty((B, M, N), dtype=dtype, device=x.device)


Registered fake (lines 31-46, before this PR):
def batched_gemm_a16wfp4_fake_tensor(...):
    if y is None:
        Bx, M, _ = x.shape
        _, N, _ = w.shape
        return torch.empty((Bx, M, N), dtype=dtype, device=x.device)  # ignores transpose_bm
    return y


Under torch.compile, AOTAutograd traces against the fake, so it propagates
(Bx, M, N

## Changed files (2 total, first 2 shown)

- `aiter/ops/triton/gemm/batched/batched_gemm_a16wfp4.py`
- `op_tests/triton_tests/gemm/batched/test_batched_gemm_a16wfp4.py`

## Provenance

- Merge commit: `03c55d0cf744`
- Captured at knowledge cutoff: 2026-05-15
- Source of truth: the upstream PR linked above. This page summarizes upstream
  metadata; consult the PR for the authoritative diff.
