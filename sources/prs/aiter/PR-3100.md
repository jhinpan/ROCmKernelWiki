---
id: pr-aiter-3100
repo: ROCm/aiter
pr: 3100
title: 'feat(topk): optimized topk_gating kernel with sigmoid/softmax support'
author: yzhou103
date: '2026-05-12'
url: https://github.com/ROCm/aiter/pull/3100
source_category: upstream-code
architectures:
- gfx950
tags:
- bf16
- dpp
- fp16
- gfx950
- lds
- moe
- softmax
techniques:
- kernel-fusion
- loop-unrolling
- register-blocking
- wave-reduce
hardware_features:
- bf16
- dpp
- fp16
- lds
kernel_types:
- moe
- softmax
languages:
- hip
- cpp
- triton
captured_at: '2026-05-15'
status: merged
merge_sha: ae80f53b8375
inclusion_reason: kernel path 'aiter/ops/topk.py'; keyword 'moe'
changed_paths:
- aiter/jit/optCompilerConfig.json
- aiter/ops/topk.py
- csrc/include/moe_op.h
- csrc/include/rocm_ops.hpp
- csrc/kernels/topk_gating_kernels.cu
- csrc/kernels/topk_softplus_kernels.cu
- op_tests/test_moe_topk_gating.py
facet_source: inferred
related:
- kernel-rmsnorm
- kernel-flash-attention-ck
- technique-wave-reduce
---
# feat(topk): optimized topk_gating kernel with sigmoid/softmax support

**Repository:** [ROCm/aiter](https://github.com/ROCm/aiter) · **PR:** [#3100](https://github.com/ROCm/aiter/pull/3100) · **Merged:** 2026-05-12 · **Author:** @yzhou103

**Inclusion reason:** kernel path 'aiter/ops/topk.py'; keyword 'moe'

## Summary (from upstream PR description)

Motivation
Replace topk_softplus with a unified topk_gating kernel that supports three scoring functions ("sqrtsoftplus" | "sigmoid" | "softmax") via a templated SCORE_FUNC parameter, plus a Python-facing topk_gating() wrapper.
Performance (vs reference kernels on MI355, bf16):
sqrtsoftplus E=384 T=1024 K=6: 81.3 us -> 8.7 us  (9.3x)
sigmoid     E=128 T=1   K=8 : CK   5.09 us -> 2.12 us (2.41x)
sigmoid     E=128 T=1024 K=8 : CK   6.06 us -> 3.73 us (1.62x)
softmax     E=256 T=1024 K=8 : vllm 7.95 us -> 6.32 us (1.26x)
(vllm topk_softmax still wins for E<=32 / K<=2;
we win for E>=64 + K>=4.)
Technical Details
Implementation highlights (register-only opt kernel):

1 warp = 1 token, no shared memory, no __syncthreads
Each thread holds EPT = NUM_EXPERTS / WARP_SIZE elements
Optimal sorting networks for N=2,3,4,6 (vs unrolled bubble sort)
Fused DPP warp argmax (multithread_reduce_max_dpp + __ballot + readlane) -> ~9 instructions per warp argmax, NaN-safe via ballot fallback
Cached unbiased scores so the merge phase reads no global memory
fmaxf(sum, 1e-20f) renorm clamp to handle pathological all-zero rows
Generic shared-memory fallback for non-power-of-WARP_SIZE expert counts (also hand

## Changed files (7 total, first 7 shown)

- `aiter/jit/optCompilerConfig.json`
- `aiter/ops/topk.py`
- `csrc/include/moe_op.h`
- `csrc/include/rocm_ops.hpp`
- `csrc/kernels/topk_gating_kernels.cu`
- `csrc/kernels/topk_softplus_kernels.cu`
- `op_tests/test_moe_topk_gating.py`

## Provenance

- Merge commit: `ae80f53b8375`
- Captured at knowledge cutoff: 2026-05-15
- Source of truth: the upstream PR linked above. This page summarizes upstream
  metadata; consult the PR for the authoritative diff.
