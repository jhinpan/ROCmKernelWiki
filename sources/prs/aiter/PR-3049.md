---
id: pr-aiter-3049
repo: ROCm/aiter
pr: 3049
title: '[Hotfix] aiter/__init__: do not swallow import errors on Linux (fixes silent
  rms*/quant*/gemm* truncation)'
author: ChuanLi1101
date: '2026-05-06'
url: https://github.com/ROCm/aiter/pull/3049
source_category: upstream-code
architectures:
- gfx942
tags:
- gemm
- gfx942
- layernorm
- lds
- quantization
- rmsnorm
techniques:
- fine-grained-quantization
hardware_features:
- lds
kernel_types:
- gemm
- layernorm
- quantization
- rmsnorm
languages:
- hip
- cpp
- triton
captured_at: '2026-05-15'
status: merged
merge_sha: 67a2190d242d
inclusion_reason: kernel path 'aiter/__init__.py'; keyword 'gemm'
changed_paths:
- aiter/__init__.py
facet_source: inferred
related:
- kernel-rmsnorm
- technique-wave-reduce
- technique-kernel-fusion
---
# [Hotfix] aiter/__init__: do not swallow import errors on Linux (fixes silent rms*/quant*/gemm* truncation)

**Repository:** [ROCm/aiter](https://github.com/ROCm/aiter) · **PR:** [#3049](https://github.com/ROCm/aiter/pull/3049) · **Merged:** 2026-05-06 · **Author:** @ChuanLi1101

**Inclusion reason:** kernel path 'aiter/__init__.py'; keyword 'gemm'

## Summary (from upstream PR description)

Commit b4b7516 (#2433, Triton FA Windows build support) wrapped the top-level Linux import block in
try:
from .jit import core ...
from .ops.* import *
...
except (ImportError, RuntimeError, OSError, KeyError) as e:
logger.warning("ROCm/HIP JIT runtime not available: ...", e)
to enable Windows / Triton-only builds.
Side effect on Linux: when any single import in the block fails, the whole try block aborts at that line, the except prints one warning, and every symbol declared after the failing line silently disappears from the top-level aiter namespace. import aiter still succeeds, so downstream code only discovers the missing symbol on first use.
Two visible regressions:


vLLM-ROCm: ImportError: cannot import name rmsnorm2d_fwd_with_add from aiter against aiter main on rocm/vllm-dev:nightly. Failure depends on which import below line 22 (.ops.rmsnorm) the host trips on; layernorm2d_* stays available because .ops.norm is line 4.


Pre-built downstream Docker images that were ABI-smoked on a non-GPU host (rocminfo unavailable) silently shipped with an empty rms*/quant*/gemm* surface, because .jit core import is the very first line in the try.


Fix: drop the try/except on Linux. Kee

## Changed files (1 total, first 1 shown)

- `aiter/__init__.py`

## Provenance

- Merge commit: `67a2190d242d`
- Captured at knowledge cutoff: 2026-05-15
- Source of truth: the upstream PR linked above. This page summarizes upstream
  metadata; consult the PR for the authoritative diff.
