---
id: pr-aiter-2867
repo: ROCm/aiter
pr: 2867
title: Fix Correction Issues in `batched_gemm_a8w8` for ASYNC_COPY Enabled
author: nidal567
date: '2026-04-27'
url: https://github.com/ROCm/aiter/pull/2867
source_category: upstream-code
architectures:
- gfx950
tags:
- async-copy
- bf16
- gemm
- gfx950
- lds
techniques:
- lds-double-buffering
- software-pipelining
hardware_features:
- async-copy
- bf16
- lds
kernel_types:
- gemm
languages:
- hip
- cpp
- triton
captured_at: '2026-05-15'
status: merged
merge_sha: 269357c3f02c
inclusion_reason: kernel path 'aiter/ops/triton/configs/gemm/gfx950-BATCHED_GEMM-A8W8.json';
  keyword 'gemm'
changed_paths:
- aiter/ops/triton/configs/gemm/gfx950-BATCHED_GEMM-A8W8.json
facet_source: inferred
related:
- kernel-flydsl-flash-attention
- technique-lds-double-buffering
- technique-mfma-pipelining
---
# Fix Correction Issues in `batched_gemm_a8w8` for ASYNC_COPY Enabled

**Repository:** [ROCm/aiter](https://github.com/ROCm/aiter) · **PR:** [#2867](https://github.com/ROCm/aiter/pull/2867) · **Merged:** 2026-04-27 · **Author:** @nidal567

**Inclusion reason:** kernel path 'aiter/ops/triton/configs/gemm/gfx950-BATCHED_GEMM-A8W8.json'; keyword 'gemm'

## Summary (from upstream PR description)

Motivation
This PR addresses a correctness failure in the batched_gemm_a8w8 Triton kernel when running with TRITON_HIP_USE_ASYNC_COPY enabled (the default in the current Triton compiler, which has caused breakage across kernels in UTs and potentially performance). The failures occur for two test cases:

test_batched_gemm_a8w8[bf16-16-9728-8192-65000-True]
test_batched_gemm_a8w8[bf16-16-9728-8192-65000-False]

Both test cases share the same shape (M=9728, N=8192, K=65000) with B=16. These were the only two failing cases out of 196 total, with ~98.7% of mismatched elements and absolute errors as large as 65024.0, which are far greater than the atol=0.01 tolerance.
Technical Details
The issue stems from incorrect behavior in the batched_gemm_a8w8 Triton kernel when TRITON_HIP_USE_ASYNC_COPY  is enabled. Under certain shapes, specifically (M=9728, N=8192, K=65000), the kernel produces large numerical errors due to how pipelined stages interact with boundary conditions in the K dimension.
The kernel configuration for the M_GEQ_4096 bucket uses:

BLOCK_SIZE_K = 64
"num_stages": 2

With num_stages=2, the Triton pipeline enables double-buffered prefetching into LDS. However, when K is not 

## Changed files (1 total, first 1 shown)

- `aiter/ops/triton/configs/gemm/gfx950-BATCHED_GEMM-A8W8.json`

## Provenance

- Merge commit: `269357c3f02c`
- Captured at knowledge cutoff: 2026-05-15
- Source of truth: the upstream PR linked above. This page summarizes upstream
  metadata; consult the PR for the authoritative diff.
