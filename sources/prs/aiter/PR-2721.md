---
id: pr-aiter-2721
repo: ROCm/aiter
pr: 2721
title: Ensure GemmTuner doesn't generate invalid input combinations for SplitK kernels
author: apicciau
date: '2026-04-29'
url: https://github.com/ROCm/aiter/pull/2721
source_category: upstream-code
architectures:
- gfx942
tags:
- gemm
- gfx942
techniques:
- split-k
hardware_features: []
kernel_types:
- gemm
languages:
- hip
- cpp
- triton
captured_at: '2026-05-15'
status: merged
merge_sha: 8497e11a2e34
inclusion_reason: kernel path 'aiter/ops/gemm_op_a16w16.py'; keyword 'gemm'
changed_paths:
- aiter/ops/gemm_op_a16w16.py
- gradlib/gradlib/GemmTuner.py
- op_tests/tuning_tests/README.md
- op_tests/tuning_tests/test_asm_splitk_guard.py
facet_source: inferred
related:
- technique-stream-k
- technique-split-k
- technique-vgpr-budgeting
---
# Ensure GemmTuner doesn't generate invalid input combinations for SplitK kernels

**Repository:** [ROCm/aiter](https://github.com/ROCm/aiter) · **PR:** [#2721](https://github.com/ROCm/aiter/pull/2721) · **Merged:** 2026-04-29 · **Author:** @apicciau

**Inclusion reason:** kernel path 'aiter/ops/gemm_op_a16w16.py'; keyword 'gemm'

## Summary (from upstream PR description)

Motivation
ASM SplitK kernels use a fixed semaphore workspace of ASM_SPLITK_MAX_GRID (1024) entries, indexed by grid position gdx * gdy. When the grid exceeds that limit, the kernel writes out of bounds, causing silent corruption or crashes
during tuning.
Large matrix shapes (e.g. M=4096, N=4096 with a 64×64 tile) produce gdx * gdy = 4096, well above the limit.
Technical Details
Extracts _SEMA_SHAPE and ASM_SPLITK_MAX_GRID from aiter/ops/gemm_op_a16w16.py so the semaphore allocation and the candidate filter share a single source of truth — previously the allocation shape (16, 64) and the guard literal
1024 were independent and could silently drift.
Adds a guard in Gemm.asm_gemm_all_solutions() (gradlib/gradlib/GemmTuner.py) that imports ASM_SPLITK_MAX_GRID and skips any SplitK candidate where gdx * gdy > ASM_SPLITK_MAX_GRID before appending to the task list. The check
only applies when splitK > 1 (non-SplitK candidates are unaffected).
Test Plan
Added offline unit tests in op_tests/tuning_tests/test_asm_splitk_guard.py (Level 1 — no GPU required) covering three cases:

Large grid (gdx*gdy > ASM_SPLITK_MAX_GRID): all generated tasks must satisfy the constraint
Small grid (gdx*gdy = 

## Changed files (4 total, first 4 shown)

- `aiter/ops/gemm_op_a16w16.py`
- `gradlib/gradlib/GemmTuner.py`
- `op_tests/tuning_tests/README.md`
- `op_tests/tuning_tests/test_asm_splitk_guard.py`

## Provenance

- Merge commit: `8497e11a2e34`
- Captured at knowledge cutoff: 2026-05-15
- Source of truth: the upstream PR linked above. This page summarizes upstream
  metadata; consult the PR for the authoritative diff.
