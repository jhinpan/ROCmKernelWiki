---
id: pr-aiter-3070
repo: ROCm/aiter
pr: 3070
title: Fix dsink bf16 noise in Triton MHA one-kernel backward
author: kyle-256
date: '2026-05-08'
url: https://github.com/ROCm/aiter/pull/3070
source_category: upstream-code
architectures:
- gfx942
tags:
- attention
- bf16
- flash-attention
- gfx942
techniques: []
hardware_features:
- bf16
kernel_types:
- attention
- flash-attention
languages:
- hip
- cpp
- triton
captured_at: '2026-05-15'
status: merged
merge_sha: b5e03ed191fc
inclusion_reason: kernel path 'aiter/ops/triton/_triton_kernels/attention/mha_onekernel_bwd.py';
  keyword 'attention'
changed_paths:
- aiter/ops/triton/_triton_kernels/attention/mha_onekernel_bwd.py
- op_tests/triton_tests/attention/test_mha_with_sink.py
facet_source: inferred
related:
- kernel-flash-attention-ck
- kernel-mla-decode
- technique-vgpr-budgeting
---
# Fix dsink bf16 noise in Triton MHA one-kernel backward

**Repository:** [ROCm/aiter](https://github.com/ROCm/aiter) · **PR:** [#3070](https://github.com/ROCm/aiter/pull/3070) · **Merged:** 2026-05-08 · **Author:** @kyle-256

**Inclusion reason:** kernel path 'aiter/ops/triton/_triton_kernels/attention/mha_onekernel_bwd.py'; keyword 'attention'

## Summary (from upstream PR description)

Motivation
ROCM-23870: flash_attn_onekernel_backward returns wrong dsink for sink + sliding-window with large enough Q. out/dq/dk/dv are fine — only dsink fails the test tolerance.
Failing shapes (bf16, causal, GQA 28/4, head_dim=128, S=1024):
B=4, window_left=32 → dsink atol=5e-2 fails
B=2, window_left=64 → dsink atol=5e-2 fails

Technical Details
dsink_partial = sum_t (-p_sink(t) * delta(t)) where delta = sum_d do_d * o_d.
Old path loaded delta from _bwd_preprocess, which reads bf16 o. The per-token bf16 round-trip is harmless for dq/dk/dv (each delta(t) is multiplied by p and contributes per-(q,k); bias cancels), but dsink sums delta(t) directly across batch * seqlen_q per head — bias accumulates linearly past tolerance.
Fix: recompute delta = sum_j(p_ij * dp_ij) inside _bwd_dq_inner from the fp32 p/dp already materialized for dq, gated on a new RECOMPUTE_DELTA constexpr (= ENABLE_SINK). Move the dsink atomic_add to after the masked + unmasked dq inner loops so it sees the fully-accumulated value. Same change in bwd_kernel_causal and bwd_kernel_noncausal.
Non-sink path is unchanged (constexpr-gated, same code at compile time).
Test Plan
Standalone reproducer from the ticket on M

## Changed files (2 total, first 2 shown)

- `aiter/ops/triton/_triton_kernels/attention/mha_onekernel_bwd.py`
- `op_tests/triton_tests/attention/test_mha_with_sink.py`

## Provenance

- Merge commit: `b5e03ed191fc`
- Captured at knowledge cutoff: 2026-05-15
- Source of truth: the upstream PR linked above. This page summarizes upstream
  metadata; consult the PR for the authoritative diff.
