---
id: pr-aiter-841
repo: ROCm/aiter
pr: 841
title: add aot kernel build for deepseek
author: ZJLi2013
date: '2025-08-20'
url: https://github.com/ROCm/aiter/pull/841
source_category: upstream-code
architectures:
- gfx942
tags:
- all-reduce
- bf16
- fused-moe
- gemm
- gfx942
- mla
- moe
- quantization
- rmsnorm
- rope
techniques: []
hardware_features:
- bf16
kernel_types:
- mla
- fused-moe
- moe
- gemm
- rmsnorm
- rope
- all-reduce
- quantization
languages:
- hip
- cpp
- triton
captured_at: '2026-05-15'
status: merged
merge_sha: 561387f764c1
inclusion_reason: kernel path 'aiter/aot/aot.py'; keyword 'gemm'
changed_paths:
- aiter/aot/aot.py
---

# add aot kernel build for deepseek

**Repository:** [ROCm/aiter](https://github.com/ROCm/aiter) · **PR:** [#841](https://github.com/ROCm/aiter/pull/841) · **Merged:** 2025-08-20 · **Author:** @ZJLi2013

**Inclusion reason:** kernel path 'aiter/aot/aot.py'; keyword 'gemm'

## Summary (from upstream PR description)

clean build for vllm integration
docker run -it --device /dev/dri --device /dev/kfd --network host --ipc host  --group-add video --security-opt seccomp=unconfined  -v /mnt/nvme1/david/:/workspace/ -v /mnt/nvme1/data:/data -w /workspace rocm/private:latest

# clean old aiter
pip uninstall aiter
pip list |grep aiter # show no more aiter package
# clean aiter build, so in case conflict
cd /opt/aiter/aiter/jit
rm -rf *.so
rm -rf build

# install aiter aot-kernels 
python3 -m aiter.aot.aot --out-dir /opt/aiter/aiter/jit/
export JIT_WORKSPACE_DIR=/opt/aiter/aiter/jit/
# install aiter itself
python3 setup.py develop 

# clean old vllm and rebuild
cd /opt/vllm/
rm -rf build 
rm -rf .deps
rm -rf vllm/.*so 
rm -rf vllm.egg-info/
rm -rf /root/.cache/vllm

# reinstall vllm 
python3 setup.py install --root=/opt/vllm/

## vllm server launch 

default kernels for aot_build
        "module_mha_varlen_fwd",
        "module_quant",
        "module_rmsnorm",
        "module_rocsolgemm",
        "module_hipbsolgemm",
        "module_rope_general_fwd",
        "module_rope_pos_fwd",
        "module_moe_asm",
        "module_moe_sorting",
        "module_fused_moe_bf16_asm",
        "module_gemm_a8w8",


## Changed files (1 total, first 1 shown)

- `aiter/aot/aot.py`

## Provenance

- Merge commit: `561387f764c1`
- Captured at knowledge cutoff: 2026-05-15
- Source of truth: the upstream PR linked above. This page summarizes upstream
  metadata; consult the PR for the authoritative diff.
