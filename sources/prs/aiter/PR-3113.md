---
id: pr-aiter-3113
repo: ROCm/aiter
pr: 3113
title: '[Misc] Add DSv4-Pro BF16 tuned GEMM configs (no hipblaslt)'
author: valarLip
date: '2026-05-11'
url: https://github.com/ROCm/aiter/pull/3113
source_category: upstream-code
architectures:
- gfx950
tags:
- bf16
- gemm
- gfx950
techniques: []
hardware_features:
- bf16
kernel_types:
- gemm
languages:
- hip
- cpp
- triton
captured_at: '2026-05-15'
status: merged
merge_sha: f4c5bde3fb01
inclusion_reason: kernel path 'aiter/configs/model_configs/dsv4_bf16_tuned_gemm.csv';
  keyword 'gemm'
changed_paths:
- aiter/configs/model_configs/dsv4_bf16_tuned_gemm.csv
- aiter/configs/model_configs/kimik2_bf16_tuned_gemm.csv
related:
- kernel-grouped-gemm
- kernel-flydsl-preshuffle-gemm
- kernel-ck-hgemm
---
# [Misc] Add DSv4-Pro BF16 tuned GEMM configs (no hipblaslt)

**Repository:** [ROCm/aiter](https://github.com/ROCm/aiter) · **PR:** [#3113](https://github.com/ROCm/aiter/pull/3113) · **Merged:** 2026-05-11 · **Author:** @valarLip

**Inclusion reason:** kernel path 'aiter/configs/model_configs/dsv4_bf16_tuned_gemm.csv'; keyword 'gemm'

## Summary (from upstream PR description)

Summary
Adds tuned BF16 GEMM configurations for DeepSeek-V4-Pro detected from atom_server log untuned warnings on gfx950 / cu_num=256.
Shapes (4)



N
K
dtype/otype




64
7168
bf16 → bf16


512
7168
bf16 → bf16


1024
7168
bf16 → bf16


2048
7168
bf16 → bf16



(otype=fp32 N=384 indexer intentionally skipped.)
M coverage
68 M values per shape: `1, 2, 4, 8` + `getPaddedM` gl=0 fine-grained set covering 16..2048, plus sparse `{4096, 6144, 8192, 10240, 12288, 14336, 16384, 32768}`.
Backend selection
Tuned with `--libtype asm,flydsl,skinny,torch,triton` — hipblaslt explicitly excluded. Resulting distribution:



backend
count




torch
120


asm
109


flydsl
60


triton
5


skinny
3



Side change
Removes 46 stale rows from `kimik2_bf16_tuned_gemm.csv` for `(N=1024, K=7168)` that overlap with — and are superseded by — the new dsv4 entries (the tuner's merge dedupe).
Test plan

 Tuner ran end-to-end producing rows with no failures and no hipblaslt selections
 Re-run DSv4-Pro server with the new tuned CSV to confirm the previously untuned shapes no longer print "will use default config" warnings

## Changed files (2 total, first 2 shown)

- `aiter/configs/model_configs/dsv4_bf16_tuned_gemm.csv`
- `aiter/configs/model_configs/kimik2_bf16_tuned_gemm.csv`

## Provenance

- Merge commit: `f4c5bde3fb01`
- Captured at knowledge cutoff: 2026-05-15
- Source of truth: the upstream PR linked above. This page summarizes upstream
  metadata; consult the PR for the authoritative diff.
