---
id: pr-vllm-42062
repo: vllm-project/vllm
pr: 42062
title: '[ROCm] Enable gluon paged MQA logits on gfx950 (MI355X)'
author: frida-andersson
date: '2026-05-14'
url: https://github.com/vllm-project/vllm/pull/42062
source_category: upstream-code
architectures:
- gfx942
- gfx950
tags:
- attention
- fp8
- gemm
- gfx942
- gfx950
- mla
- paged-attention
techniques: []
hardware_features:
- fp8
kernel_types:
- attention
- paged-attention
- mla
- gemm
languages:
- hip
- triton
- python
captured_at: '2026-05-15'
status: merged
merge_sha: f07b1da797cc
inclusion_reason: kernel path 'vllm/v1/attention/ops/rocm_aiter_mla_sparse.py'; keyword
  'gemm'
changed_paths:
- vllm/v1/attention/ops/rocm_aiter_mla_sparse.py
---

# [ROCm] Enable gluon paged MQA logits on gfx950 (MI355X)

**Repository:** [vllm-project/vllm](https://github.com/vllm-project/vllm) · **PR:** [#42062](https://github.com/vllm-project/vllm/pull/42062) · **Merged:** 2026-05-14 · **Author:** @frida-andersson

**Inclusion reason:** kernel path 'vllm/v1/attention/ops/rocm_aiter_mla_sparse.py'; keyword 'gemm'

## Summary (from upstream PR description)

Summary
rocm_fp8_paged_mqa_logits in rocm_aiter_mla_sparse.py had two separate branches:

_ON_GFX942 (MI300X, MI325X): called deepgemm_fp8_paged_mqa_logits → gluon single-kernel path - everything else (including MI355X / gfx950): fell through to deepgemm_fp8_paged_mqa_logits_stage1 → slow two-kernel path (stage1 + sum(dim=0))

The split was introduced in 628c436 (#40871). With Triton >= 3.5.0, deepgemm_fp8_paged_mqa_logits inside AITER already dispatches to the gluon kernel for both gfx942 and gfx950 (it asserts on both and sets cdna_version/TotalCuCount per GPU internally). The _ON_GFX942-only guard in vLLM was therefore incorrect — it was accidentally routing MI355X to the slow stage1 path.
Fix:
Extend the condition from if _ON_GFX942 to if _ON_GFX942 or _ON_GFX950 so MI355X takes the gluon path.
Before:

After:

Test plan

DeepSeek-V3.2 TP€ with HIP graphs and --block-size 64 on MI355X — confirm _gluon_deepgemm_fp8_paged_mqa_logits_preshuffle is dispatched (previously _stage1)
GSM8K 5-shot flexible-extract score

## Changed files (1 total, first 1 shown)

- `vllm/v1/attention/ops/rocm_aiter_mla_sparse.py`

## Provenance

- Merge commit: `f07b1da797cc`
- Captured at knowledge cutoff: 2026-05-15
- Source of truth: the upstream PR linked above. This page summarizes upstream
  metadata; consult the PR for the authoritative diff.
