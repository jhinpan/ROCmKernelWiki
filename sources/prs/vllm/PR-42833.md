---
id: pr-vllm-42833
repo: vllm-project/vllm
pr: 42833
title: '[ROCm][GPT-OSS] Avoid repeated compile-time `cos_sin_cache.to(bf16)` casts
  in rotary path'
author: akii96
date: '2026-05-27'
url: https://github.com/vllm-project/vllm/pull/42833
source_category: upstream-code
architectures:
- gfx942
tags:
- bf16
- gfx942
- rope
techniques: []
hardware_features:
- bf16
kernel_types:
- rope
languages:
- hip
- triton
- python
captured_at: '2026-05-15'
status: merged
merge_sha: de12f5ca0b8b
inclusion_reason: keyword 'decode'
changed_paths:
- vllm/model_executor/layers/rotary_embedding/base.py
---

# [ROCm][GPT-OSS] Avoid repeated compile-time `cos_sin_cache.to(bf16)` casts in rotary path

**Repository:** [vllm-project/vllm](https://github.com/vllm-project/vllm) · **PR:** [#42833](https://github.com/vllm-project/vllm/pull/42833) · **Merged:** 2026-05-27 · **Author:** @akii96

**Inclusion reason:** keyword 'decode'

## Summary (from upstream PR description)

Summary

Add a compile-friendly bf16 cache fast path for rotary cos/sin cache matching.
Materialize and reuse cos_sin_cache_bf16 when ROCm AITER rotary is active.
Avoid repeated per-layer bf16 cast nodes in compile mode while preserving non-compile behavior.

Motivation
In GPT-OSS decode compile graphs, repeated cos_sin_cache.to(query.device, dtype=query.dtype) can be emitted per layer in the rotary path.
Reusing a precomputed bf16 cache removes those repeated cast nodes while keeping existing runtime semantics.
Implementation

vllm/model_executor/layers/rotary_embedding/base.py

During cache init, register optional cos_sin_cache_bf16 buffer for AITER path.
In _match_cos_sin_cache_dtype, in AITER compile mode with bf16 query dtype, return cos_sin_cache_bf16 on same device before fallback cast.



Test Plan

 Verify no regressions in serve/warmup/perf flow
 Perf comparison shows throughput up and TPOT down vs baseline.
 LM-eval GSM8K (--limit 256, chat-template path) remains in baseline-equivalent range.
 FX dump comparison shows bf16 cache buffer reuse replaces repeated compile-time cast nodes.

Validation Results
Performance vs baseline (openai/gpt-oss-120b):

Throughput improves 

## Changed files (1 total, first 1 shown)

- `vllm/model_executor/layers/rotary_embedding/base.py`

## Provenance

- Merge commit: `de12f5ca0b8b`
- Captured at knowledge cutoff: 2026-05-15
- Source of truth: the upstream PR linked above. This page summarizes upstream
  metadata; consult the PR for the authoritative diff.
