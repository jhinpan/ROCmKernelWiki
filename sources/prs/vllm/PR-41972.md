---
id: pr-vllm-41972
repo: vllm-project/vllm
pr: 41972
title: '[ROCm] Fix AITER AR+RMSNorm no-residual fusion'
author: akii96
date: '2026-05-07'
url: https://github.com/vllm-project/vllm/pull/41972
source_category: upstream-code
architectures:
- gfx942
tags:
- all-reduce
- async-copy
- attention
- flash-attention
- gfx942
- rmsnorm
techniques: []
hardware_features:
- async-copy
kernel_types:
- flash-attention
- attention
- rmsnorm
- all-reduce
languages:
- hip
- triton
- python
captured_at: '2026-05-15'
status: merged
merge_sha: 3af561ec0a00
inclusion_reason: kernel path 'vllm/compilation/passes/fusion/allreduce_rms_fusion.py';
  keyword 'attention'
changed_paths:
- vllm/compilation/passes/fusion/allreduce_rms_fusion.py
---

# [ROCm] Fix AITER AR+RMSNorm no-residual fusion

**Repository:** [vllm-project/vllm](https://github.com/vllm-project/vllm) · **PR:** [#41972](https://github.com/vllm-project/vllm/pull/41972) · **Merged:** 2026-05-07 · **Author:** @akii96

**Inclusion reason:** kernel path 'vllm/compilation/passes/fusion/allreduce_rms_fusion.py'; keyword 'attention'

## Summary (from upstream PR description)

Purpose
Fix the ROCm AITER allreduce + RMSNorm fusion for the no-residual pattern.
AiterAllreduceFusedRMSNormPattern replaces an allreduce followed by RMSNorm without a residual input. However, the AITER fused kernel computes RMSNorm over allreduce(input) + residual, so the synthetic residual for this pattern must be zero.
The AITER replacement used torch.empty_like(input), which can add uninitialized memory into the layer output. This PR changes it to torch.zeros_like(input), matching the existing FlashInfer no-residual fusion patterns in the same file.
This restores MiniMax-M2.5 GSM8K accuracy while keeping the AITER fusion enabled.
Test Plan
Serve MiniMax-M2.5 with ROCm AITER and allreduce RMSNorm fusion enabled:
vllm serve MiniMaxAI/MiniMax-M2.5 \
  --tensor-parallel-size 4 \
  --attention-backend ROCM_AITER_UNIFIED_ATTN \
  --max-model-len 12288 \
  --block-size 64 \
  --max-num-seqs 512 \
  --max-num-batched-tokens 32768 \
  --gpu-memory-utilization 0.95 \
  --performance-mode balanced \
  --async-scheduling \
  --no-enable-prefix-caching \
  --kv-cache-dtype auto \
  --compilation-config '{"mode":3}'
Test Result
Before this fix, GSM8K accuracy collapsed with ROCm AITER allre

## Changed files (1 total, first 1 shown)

- `vllm/compilation/passes/fusion/allreduce_rms_fusion.py`

## Provenance

- Merge commit: `3af561ec0a00`
- Captured at knowledge cutoff: 2026-05-15
- Source of truth: the upstream PR linked above. This page summarizes upstream
  metadata; consult the PR for the authoritative diff.
