---
id: pr-vllm-43331
repo: vllm-project/vllm
pr: 43331
title: '[ROCm] Enable the aiter top-k/top-p sampler by default'
author: JohnQinAMD
date: '2026-05-28'
url: https://github.com/vllm-project/vllm/pull/43331
source_category: upstream-code
architectures:
- gfx950
tags:
- fp4
- fp8
- gfx950
- lds
- mxfp
- softmax
techniques: []
hardware_features:
- lds
- fp8
- fp4
- mxfp
kernel_types:
- softmax
languages:
- hip
- triton
- python
captured_at: '2026-05-15'
status: merged
merge_sha: ed7fe831da79
inclusion_reason: kernel path 'vllm/v1/sample/ops/topk_topp_sampler.py'; keyword 'fp8'
changed_paths:
- vllm/v1/sample/ops/topk_topp_sampler.py
---

# [ROCm] Enable the aiter top-k/top-p sampler by default

**Repository:** [vllm-project/vllm](https://github.com/vllm-project/vllm) · **PR:** [#43331](https://github.com/vllm-project/vllm/pull/43331) · **Merged:** 2026-05-28 · **Author:** @JohnQinAMD

**Inclusion reason:** kernel path 'vllm/v1/sample/ops/topk_topp_sampler.py'; keyword 'fp8'

## Summary (from upstream PR description)

Purpose
TopKTopPSampler.forward_hip (ROCm) was hard-disabled in #32413 (DISABLE_AITER_SAMPLER = True) to work around a top-p sampling accuracy issue observed on DeepSeek. That accuracy issue was fixed in ROCm/aiter#2035 (a prefix-sum lane bug in DeterministicInclusiveSum plus philox RNG seed/offset handling), so the work-around is obsolete and the aiter sampler can never be reached today.
This replaces the hardcoded flag with a VLLM_ROCM_USE_AITER_SAMPLER env var (default on; set =0 to fall back to the native sampler). When aiter is enabled (VLLM_ROCM_USE_AITER=1), top-k/top-p sampling uses the aiter rejection kernel
instead of the native softmax→sort→cumsum path. Per-request generators (seeds) and processed-logprobs modes still fall back to native; greedy bypasses the sampler entirely.
Test Plan / Results
Root cause fixed upstream: aiter#2035 Fix accuracy issues in top-p sampling kernels
 (2026-02-12) fixes the exact kernels forward_hip calls; included in current aiter builds.
Sampler-accuracy microbench — aiter vs native, DeepSeek-V3.2 config (vocab 129280, top_p 0.95), MI355X:



check
native
aiter




degenerate top_p→0 == argmax
1.0000
1.0000


out-of-nucleus rate (top_p=0.95)

## Changed files (1 total, first 1 shown)

- `vllm/v1/sample/ops/topk_topp_sampler.py`

## Provenance

- Merge commit: `ed7fe831da79`
- Captured at knowledge cutoff: 2026-05-15
- Source of truth: the upstream PR linked above. This page summarizes upstream
  metadata; consult the PR for the authoritative diff.
