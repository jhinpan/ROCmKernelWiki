---
id: pr-vllm-36042
repo: vllm-project/vllm
pr: 36042
title: Fix CUDA graph decode capture crash in AITER FlashAttention
author: iseeyuan
date: '2026-03-07'
url: https://github.com/vllm-project/vllm/pull/36042
source_category: upstream-code
architectures:
- gfx942
tags:
- attention
- decode
- flash-attention
- gfx942
- paged-attention
techniques: []
hardware_features: []
kernel_types:
- attention
- decode
- flash-attention
- paged-attention
languages:
- hip
- triton
- python
captured_at: '2026-05-15'
status: merged
merge_sha: 1a9718085c79
inclusion_reason: kernel path 'vllm/v1/attention/backends/rocm_aiter_fa.py'; keyword
  'attention'
changed_paths:
- vllm/v1/attention/backends/rocm_aiter_fa.py
facet_source: inferred
related:
- kernel-paged-attention
- kernel-mla-decode
- technique-hip-graphs
---
# Fix CUDA graph decode capture crash in AITER FlashAttention

**Repository:** [vllm-project/vllm](https://github.com/vllm-project/vllm) · **PR:** [#36042](https://github.com/vllm-project/vllm/pull/36042) · **Merged:** 2026-03-07 · **Author:** @iseeyuan

**Inclusion reason:** kernel path 'vllm/v1/attention/backends/rocm_aiter_fa.py'; keyword 'attention'

## Summary (from upstream PR description)

Summary:
Upstream vLLM commit #32877 added speculative decoding
support to the ROCm AITER FA backend by routing decode_max_query_len > 1
through unified_attention (a Triton kernel). However, it also added
self.sliding_window[0] != -1 to the same condition, which causes the
unified_attention path to be taken during CUDA graph decode FULL capture
even for non-speculative workloads.
unified_attention is not CUDA-graph-capture-safe because it performs
dynamic memory allocations (torch.empty) and runtime kernel selection
inside the wrapper. This causes Memory access fault by GPU node-X on address (nil) at 91% (10/11) of decode FULL graph capture.
The fix removes the sliding window condition from the unified_attention
gate, keeping it only for actual speculative decoding (`decode_max_query_len

1). Sliding window attention continues to work correctly through the existing paged_attention_v1` path, which has native sliding window support.

Test Plan:
Verified on (8x MI300X) with an internal model:
Before fix :

Memory access fault by GPU node-8 on address (nil) during
"Capturing CUDA graphs (decode, FULL): 91% (10/11)"

After fix:

Decode FULL capture completes 100% (11/11 graphs)
onStartS

## Changed files (1 total, first 1 shown)

- `vllm/v1/attention/backends/rocm_aiter_fa.py`

## Provenance

- Merge commit: `1a9718085c79`
- Captured at knowledge cutoff: 2026-05-15
- Source of truth: the upstream PR linked above. This page summarizes upstream
  metadata; consult the PR for the authoritative diff.
