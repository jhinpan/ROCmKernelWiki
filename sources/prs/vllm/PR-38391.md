---
id: pr-vllm-38391
repo: vllm-project/vllm
pr: 38391
title: '[CI Bugfix] Pre-download missing FlashInfer headers in Docker build'
author: mgoin
date: '2026-03-28'
url: https://github.com/vllm-project/vllm/pull/38391
source_category: upstream-code
architectures:
- gfx942
tags:
- flash-attention
- fp4
- fused-moe
- gemm
- gfx942
- lds
- moe
- mxfp
- rope
techniques: []
hardware_features:
- lds
- fp4
- mxfp
kernel_types:
- flash-attention
- fused-moe
- moe
- gemm
- rope
languages:
- hip
- triton
- python
captured_at: '2026-05-15'
status: merged
merge_sha: 2bf5b70ae862
inclusion_reason: keyword 'gemm'
changed_paths:
- docker/Dockerfile
---

# [CI Bugfix] Pre-download missing FlashInfer headers in Docker build

**Repository:** [vllm-project/vllm](https://github.com/vllm-project/vllm) · **PR:** [#38391](https://github.com/vllm-project/vllm/pull/38391) · **Merged:** 2026-03-28 · **Author:** @mgoin

**Inclusion reason:** keyword 'gemm'

## Summary (from upstream PR description)

Summary

NOTE: This is a temporary fix to unblock our CI - the proper fix should be upstream in flashinfer, hopefully flashinfer-ai/flashinfer#2903
Pre-download FlashInfer TRTLLM BMM headers at Docker build time to fix air-gapped/offline runtime failures
The flashinfer-cubin package ships headers at the artifact hash path (cubins/b55211623.../include/trtllmGen_bmm_export/), but runtime code (download_trtllm_headers) looks for them at cubins/flashinfer/trtllm/batched_gemm/trtllmGen_bmm_export/ — a path mismatch that causes network downloads on every startup
Without this fix, startup takes ~2min extra for serial header downloads, and completely fails in air-gapped environments or when NVIDIA's artifactory returns 403 (as seen with SfLayoutDecl.h)

Root Cause
flashinfer-cubin==0.6.6 includes all 17 BMM headers + checksums.txt, but only at the artifact hash path. The runtime download_trtllm_headers() function (called from flashinfer/jit/moe_utils.py and flashinfer/jit/fused_moe.py) writes to and reads from a different path under flashinfer/trtllm/batched_gemm/. Since get_file() → load_cubin() doesn't find the headers at the expected path, it always falls through to downloading from edg

## Changed files (1 total, first 1 shown)

- `docker/Dockerfile`

## Provenance

- Merge commit: `2bf5b70ae862`
- Captured at knowledge cutoff: 2026-05-15
- Source of truth: the upstream PR linked above. This page summarizes upstream
  metadata; consult the PR for the authoritative diff.
