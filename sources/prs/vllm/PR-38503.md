---
id: pr-vllm-38503
repo: vllm-project/vllm
pr: 38503
title: '[ROCm][Engine] Fix GPU memory leaks in engine shutdown and test workaround
  for async KV prefix cache reset'
author: AndreasKaratzas
date: '2026-04-25'
url: https://github.com/vllm-project/vllm/pull/38503
source_category: upstream-code
architectures:
- gfx942
tags:
- async-copy
- gfx942
- kv-cache
- lds
- prefill
- rope
techniques:
- wave-reduce
hardware_features:
- async-copy
- lds
kernel_types:
- kv-cache
- prefill
- rope
languages:
- hip
- triton
- python
captured_at: '2026-05-15'
status: merged
merge_sha: 95995bbef812
inclusion_reason: kernel path 'tests/entrypoints/openai/completion/test_shutdown.py';
  keyword 'lds'
changed_paths:
- docker/Dockerfile.rocm
- tests/entrypoints/openai/completion/test_shutdown.py
- tests/v1/kv_connector/unit/test_rixl_gpu_mem_diag.py
- tests/v1/kv_offload/test_cpu_offloading.py
- vllm/utils/func_utils.py
- vllm/v1/engine/core.py
- vllm/v1/worker/gpu_model_runner.py
- vllm/v1/worker/gpu_worker.py
facet_source: inferred
related:
- kernel-flash-attention-ck
- kernel-vector-add-asm
- technique-mfma-pipelining
---
# [ROCm][Engine] Fix GPU memory leaks in engine shutdown and test workaround for async KV prefix cache reset

**Repository:** [vllm-project/vllm](https://github.com/vllm-project/vllm) · **PR:** [#38503](https://github.com/vllm-project/vllm/pull/38503) · **Merged:** 2026-04-25 · **Author:** @AndreasKaratzas

**Inclusion reason:** kernel path 'tests/entrypoints/openai/completion/test_shutdown.py'; keyword 'lds'

## Summary (from upstream PR description)

When running in-process (VLLM_ENABLE_V1_MULTIPROCESSING=0), shutting down the engine leaked GPU memory because:

supports_kw LRU cache pinned model weights: The @lru_cache on supports_kw was keyed on bound methods (e.g. model.forward), which held strong references to the model instance and all its GPU tensors for the lifetime of the cache. Fixed by unwrapping bound methods to __func__ before caching.
gc.freeze() was never undone: EngineCore.__init__ calls gc.freeze() to reduce GC overhead during serving, but shutdown() never called gc.unfreeze(), making all engine objects invisible to the garbage collector.
No explicit GPU resource teardown: GPUWorker.shutdown() never freed model weights, KV caches, the rotary embedding cache (_ROPE_DICT), compilation contexts, or the workspace singleton.

Test workaround for prefix cache reset
reset_prefix_cache could return False indefinitely when async KV store transfers (CPU offloading) were in flight, because the engine stops stepping after all requests finish and never polls the worker for completed transfers. As a temporary workaround (per reviewer suggestion), _wait_for_prefix_cache_reset in the test now sends dummy single-token prefills be

## Changed files (8 total, first 8 shown)

- `docker/Dockerfile.rocm`
- `tests/entrypoints/openai/completion/test_shutdown.py`
- `tests/v1/kv_connector/unit/test_rixl_gpu_mem_diag.py`
- `tests/v1/kv_offload/test_cpu_offloading.py`
- `vllm/utils/func_utils.py`
- `vllm/v1/engine/core.py`
- `vllm/v1/worker/gpu_model_runner.py`
- `vllm/v1/worker/gpu_worker.py`

## Provenance

- Merge commit: `95995bbef812`
- Captured at knowledge cutoff: 2026-05-15
- Source of truth: the upstream PR linked above. This page summarizes upstream
  metadata; consult the PR for the authoritative diff.
