---
id: pr-vllm-39616
repo: vllm-project/vllm
pr: 39616
title: '[ROCm][Feature] Enable AITER MLA attention backend to work with Eagle3 speculative
  decoding on ROCm'
author: larryli2-amd
date: '2026-04-20'
url: https://github.com/vllm-project/vllm/pull/39616
source_category: upstream-code
architectures:
- gfx942
- gfx950
tags:
- attention
- decode
- flash-attention
- fp4
- gfx942
- gfx950
- kv-cache
- mla
- mxfp
techniques: []
hardware_features:
- fp4
- mxfp
kernel_types:
- attention
- decode
- flash-attention
- kv-cache
- mla
languages:
- hip
- triton
- python
captured_at: '2026-05-15'
status: merged
merge_sha: 7243e02aa1c6
inclusion_reason: kernel path 'docs/design/attention_backends.md'; keyword 'attention'
changed_paths:
- docs/design/attention_backends.md
- vllm/v1/attention/backends/mla/rocm_aiter_mla.py
facet_source: inferred
related:
- kernel-mla-decode
- kernel-paged-attention
- kernel-flydsl-flash-attention
---
# [ROCm][Feature] Enable AITER MLA attention backend to work with Eagle3 speculative decoding on ROCm

**Repository:** [vllm-project/vllm](https://github.com/vllm-project/vllm) · **PR:** [#39616](https://github.com/vllm-project/vllm/pull/39616) · **Merged:** 2026-04-20 · **Author:** @larryli2-amd

**Inclusion reason:** kernel path 'docs/design/attention_backends.md'; keyword 'attention'

## Summary (from upstream PR description)

Summary
Enable AITER MLA attention backend to work with Eagle3 speculative decoding on ROCm.
AITER MLA is the fastest MLA attention backend on AMD MI300X/MI355X GPUs (using AMD's ASM
kernels via the aiter library). However, it currently cannot be used with speculative decoding
methods like Eagle3 due to a kernel_block_size conflict:

AITER MLA declares get_supported_kernel_block_sizes() = [1]
Eagle3 draft model (flash_attn) requires block_size = MultipleOf(16)
Both share the same KV cache group, so select_common_block_size() fails — no block size      satisfies both constraints

This means users must choose between AITER MLA (fast decode, no spec-dec) or a slower backend
(spec-dec works but lower baseline performance). This PR removes that limitation.
Key Insight
The aiter MLA decode kernel always operates with page_size=1 internally — the vLLM wrapper
flattens kv_buffer via .view(-1, 1, 1, head_size) before calling the ASM kernel. The kernel
never sees multi-token pages. So AITER MLA can work with any kernel_block_size — we just need
to expand block-level indices into per-token flat indices in the metadata builder.
Changes
All changes are in a single file: vllm/v1/attention/backen

## Changed files (2 total, first 2 shown)

- `docs/design/attention_backends.md`
- `vllm/v1/attention/backends/mla/rocm_aiter_mla.py`

## Provenance

- Merge commit: `7243e02aa1c6`
- Captured at knowledge cutoff: 2026-05-15
- Source of truth: the upstream PR linked above. This page summarizes upstream
  metadata; consult the PR for the authoritative diff.
