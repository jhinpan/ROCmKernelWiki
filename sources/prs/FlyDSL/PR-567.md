---
id: pr-FlyDSL-567
repo: ROCm/FlyDSL
pr: 567
title: 'Enable gfx1151 (RDNA3.5 / Strix Halo): LDS, FP8 guards, f16/bf16 WMMA GEMM'
author: mgehre-amd
date: '2026-05-29'
url: https://github.com/ROCm/FlyDSL/pull/567
source_category: upstream-code
architectures:
- gfx1201
- gfx942
tags:
- bf16
- fp8
- gemm
- gfx1201
- gfx942
- lds
- rope
- wmma
techniques: []
hardware_features:
- wmma
- lds
- fp8
- bf16
kernel_types:
- gemm
- rope
languages:
- flydsl
- mlir
- python
captured_at: '2026-05-15'
artifact_dir: artifacts/prs/FlyDSL/PR-567
status: merged
merge_sha: 9861ab21bb7e
inclusion_reason: kernel path 'kernels/rdna3_f16_gemm.py'; keyword 'wmma'
changed_paths:
- kernels/rdna3_f16_gemm.py
- kernels/rdna_f16_gemm.py
- kernels/rdna_fp8_preshuffle_gemm.py
- python/flydsl/expr/typing.py
- python/flydsl/utils/smem_allocator.py
- scripts/run_benchmark.sh
- tests/kernels/benchmark_common.py
- tests/kernels/test_rdna_gemm.py
---

# Enable gfx1151 (RDNA3.5 / Strix Halo): LDS, FP8 guards, f16/bf16 WMMA GEMM

**Repository:** [ROCm/FlyDSL](https://github.com/ROCm/FlyDSL) · **PR:** [#567](https://github.com/ROCm/FlyDSL/pull/567) · **Merged:** 2026-05-29 · **Author:** @mgehre-amd

**Inclusion reason:** kernel path 'kernels/rdna3_f16_gemm.py'; keyword 'wmma'

## Summary (from upstream PR description)

Summary
Four commits enabling gfx1151 (Strix Halo / RDNA3.5):


Add gfx1151 LDS capacity — single entry in SMEM_CAPACITY_MAP (gfx1151: 65536). 64 KB per workgroup (the WGP has 128 KB of physical LDS split across 2 CUs); confirmed by HIP (sharedMemPerBlock = 65536) and by the AMDGPU backend rejecting kernels exceeding 65536 bytes with local memory (...) exceeds limit (65536).


Fail-fast on FP8 paths from gfx11 — gfx11 has no native FP8 instructions, but default_f8_type() was silently returning E4M3FNUZ and compile_fp8_gemm() was emitting rocdl.wmma_f32_16x16x16_fp8_fp8 directly, both surfacing as LLVM ERROR: cannot select intrinsic deep in ISel. Add early arch checks so the failure is clear at the entry point. No behavior change on gfx94*, gfx95*, gfx12*.


Add rdna3_f16_gemm.py: f16/bf16 WMMA GEMM for gfx11 — port of rdna_f16_gemm.py to the legacy v16-operand WMMA ABI. Two ABI-level differences from gfx12: (a) A/B operands are vector<16> not vector<8> (done as two v8 LDS loads + vector.shuffle concat, lanes 16-31 mirror lanes 0-15), and (b) the v8 accumulator's per-lane row distribution is stride-2 (lane L holds rows 2*si + L/16) instead of contiguous-8 (lane L holds rows 8*(L/16)

## Changed files (8 total, first 8 shown)

- `kernels/rdna3_f16_gemm.py`
- `kernels/rdna_f16_gemm.py`
- `kernels/rdna_fp8_preshuffle_gemm.py`
- `python/flydsl/expr/typing.py`
- `python/flydsl/utils/smem_allocator.py`
- `scripts/run_benchmark.sh`
- `tests/kernels/benchmark_common.py`
- `tests/kernels/test_rdna_gemm.py`

## Provenance

- Merge commit: `9861ab21bb7e`
- Captured at knowledge cutoff: 2026-05-15
- Source of truth: the upstream PR linked above. This page summarizes upstream
  metadata; consult the PR for the authoritative diff.
