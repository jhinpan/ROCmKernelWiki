---
id: pr-FlyDSL-309
repo: ROCm/FlyDSL
pr: 309
title: Support moe gemm2 bf16 output atomics on gfx942
author: MHYangAMD
date: '2026-03-31'
url: https://github.com/ROCm/FlyDSL/pull/309
source_category: upstream-code
architectures:
- gfx942
- gfx950
tags:
- bf16
- block-scale
- gemm
- gfx942
- gfx950
- moe
techniques: []
hardware_features:
- bf16
- block-scale
kernel_types:
- gemm
- moe
languages:
- flydsl
- mlir
- python
captured_at: '2026-05-15'
status: merged
merge_sha: a010e036a6c7
inclusion_reason: kernel path 'kernels/moe_blockscale_2stage.py'; keyword 'gemm'
changed_paths:
- kernels/moe_blockscale_2stage.py
- kernels/moe_gemm_2stage.py
- python/flydsl/expr/buffer_ops.py
- tests/kernels/test_moe_gemm.py
facet_source: inferred
related:
- kernel-grouped-gemm
- kernel-fused-moe
- technique-preshuffle-layout
---
# Support moe gemm2 bf16 output atomics on gfx942

**Repository:** [ROCm/FlyDSL](https://github.com/ROCm/FlyDSL) · **PR:** [#309](https://github.com/ROCm/FlyDSL/pull/309) · **Merged:** 2026-03-31 · **Author:** @MHYangAMD

**Inclusion reason:** kernel path 'kernels/moe_blockscale_2stage.py'; keyword 'gemm'

## Summary (from upstream PR description)

gfx942 (MI300) has buffer_atomic_pk_add_f16 but NOT buffer_atomic_pk_add_bf16 - the bf16 variant only exists on gfx950+. Therefore bf16 output atomics must use global_atomic_pk_add_bf16, which requires a raw pointer address instead of a buffer resource descriptor.
The bf16 codepath extracted the output base address via the standard memref.extract_aligned_pointer_as_index, but kernel arguments are !fly.memref (not standard memref), causing an MLIR verification error.
Fix:

Extend ExtractAlignedPointerAsIndexLowering (FlyToROCDL.cpp) to support integer/index result types via ptrtoint + index_cast, so fly.extract_aligned_pointer_as_index can return index directly. Previously the lowering only handled ptr-to-ptr (addrspacecast).
Add buffer_ops.extract_memref_base_index() helper that calls fly.extract_aligned_pointer_as_index with IndexType result.
Use the new helper in moe_gemm_2stage and moe_blockscale_2stage.
Enable bf16 in the test harness out_dtype mapping and CLI.

Motivation

Technical Details

Test Plan

Test Result

Submission Checklist

 Look over the contributing guidelines at https://github.com/ROCm/ROCm/blob/develop/CONTRIBUTING.md#pull-requests.

## Changed files (4 total, first 4 shown)

- `kernels/moe_blockscale_2stage.py`
- `kernels/moe_gemm_2stage.py`
- `python/flydsl/expr/buffer_ops.py`
- `tests/kernels/test_moe_gemm.py`

## Provenance

- Merge commit: `a010e036a6c7`
- Captured at knowledge cutoff: 2026-05-15
- Source of truth: the upstream PR linked above. This page summarizes upstream
  metadata; consult the PR for the authoritative diff.
