---
id: pr-composable_kernel-1764
repo: ROCm/composable_kernel
pr: 1764
title: 'fix: preprocessor directives logic error if/else'
author: deepsek
date: '2025-01-17'
url: https://github.com/ROCm/composable_kernel/pull/1764
source_category: upstream-code
architectures:
- gfx942
tags:
- bf16
- gemm
- gfx942
- grouped-gemm
- int8
techniques: []
hardware_features:
- bf16
- int8
kernel_types:
- grouped-gemm
- gemm
languages:
- composable-kernel
- hip
- cpp
captured_at: '2026-05-15'
status: merged
merge_sha: 0fcbb25f70e5
inclusion_reason: kernel path 'profiler/src/profile_grouped_gemm_fixed_nk.cpp'; keyword
  'gemm'
changed_paths:
- profiler/src/profile_grouped_gemm_fixed_nk.cpp
---

# fix: preprocessor directives logic error if/else

**Repository:** [ROCm/composable_kernel](https://github.com/ROCm/composable_kernel) · **PR:** [#1764](https://github.com/ROCm/composable_kernel/pull/1764) · **Merged:** 2025-01-17 · **Author:** @deepsek

**Inclusion reason:** kernel path 'profiler/src/profile_grouped_gemm_fixed_nk.cpp'; keyword 'gemm'

## Summary (from upstream PR description)

Fix for a bug in the ck profiler code for grouped_gemm_fixed_nk
line 102, 150..
If CK_ENABLE_BF16 or CK_ENABLE_INT8 is not defined and CK_ENABLE_F16 is defined, the code would still compile because it's still syntactically correct due to a preceding if condition for the if/else block.
But the logic breaks.
Couple of ways to solve this that I'm thinking of:

break down the if/else block and keep them as separate if/else statements with preprocessor macro since the args are on user inputs.
Just remove the preprocessor directives all together. I'm leaning toward this because it's user facing code, lower lvl fns will/should handle the wrong input if someone requests bf16 when lib doesn't support it.
just move the preprocessors into the if else conditions, that way the codeblock won't do anything. maybe add a #error for each within to handle it better

I've implemented the second solution. But we can choose another approach if preferred.

## Changed files (1 total, first 1 shown)

- `profiler/src/profile_grouped_gemm_fixed_nk.cpp`

## Provenance

- Merge commit: `0fcbb25f70e5`
- Captured at knowledge cutoff: 2026-05-15
- Source of truth: the upstream PR linked above. This page summarizes upstream
  metadata; consult the PR for the authoritative diff.
