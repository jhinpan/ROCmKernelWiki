---
id: pr-flash-attention-179
repo: ROCm/flash-attention
pr: 179
title: '[CK_TILE] Fix NaN for FMHA BWD When seq_q=0'
author: DDEle
date: '2026-04-01'
url: https://github.com/ROCm/flash-attention/pull/179
source_category: upstream-code
architectures:
- gfx942
- gfx950
tags:
- agpr
- attention
- bf16
- flash-attention
- fp16
- gfx942
- gfx950
- mfma
techniques:
- mfma-pipelining
- register-blocking
- software-pipelining
- vgpr-budgeting
hardware_features:
- agpr
- bf16
- fp16
- mfma
kernel_types:
- attention
- flash-attention
languages:
- composable-kernel
- triton
- hip
captured_at: '2026-05-15'
status: merged
merge_sha: 8f4a3990fe54
inclusion_reason: kernel path 'csrc/composable_kernel'; keyword 'mfma'
changed_paths:
- csrc/composable_kernel
- tests/test_flash_attn_ck.py
facet_source: inferred
related:
- kernel-flydsl-flash-attention
- kernel-flash-attention-ck
- technique-vgpr-budgeting
---
# [CK_TILE] Fix NaN for FMHA BWD When seq_q=0

**Repository:** [ROCm/flash-attention](https://github.com/ROCm/flash-attention) · **PR:** [#179](https://github.com/ROCm/flash-attention/pull/179) · **Merged:** 2026-04-01 · **Author:** @DDEle

**Inclusion reason:** kernel path 'csrc/composable_kernel'; keyword 'mfma'

## Summary (from upstream PR description)

Motivation
When the variable-length FMHA backward pass encounters a batch entry whose query sequence length is zero (seqlen_q == 0), the CK_TILE backward pipelines could produce NaN values in dK and dV.
The fix and its follow-ons form a chain of three PRs:


Root cause: early exit without zeroing accumulators (ROCm/rocm-libraries#5790) — the dK/dV pipelines did not exit early when num_total_loop <= 0, so accumulator registers with random values were written out as gradients. The group-mode kernel also returned early when seqlen_q == 0, preventing the zeroed dK/dV from ever being stored.


Exposed by fix 1: VGPR spill / register reuse (ROCm/rocm-libraries#5915) — once the early-exit path was added, num_total_loop was computed without amd_wave_read_first_lane(), so the compiler treated it as a VGPR even though it is uniform across all lanes. Under the resulting register pressure the compiler reused v52:v53 as both a B-matrix input for dK MFMAs and a temporary for dV, producing silently wrong gradients.


Also exposed by fix 1: AGPR misallocation in the IGLP pipeline (ROCm/rocm-libraries#5991) — the CFG change from fix 1 caused the LLVM register allocator to reuse AGPR accumulators as

## Changed files (2 total, first 2 shown)

- `csrc/composable_kernel`
- `tests/test_flash_attn_ck.py`

## Provenance

- Merge commit: `8f4a3990fe54`
- Captured at knowledge cutoff: 2026-05-15
- Source of truth: the upstream PR linked above. This page summarizes upstream
  metadata; consult the PR for the authoritative diff.
