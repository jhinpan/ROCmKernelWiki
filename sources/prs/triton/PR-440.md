---
id: pr-triton-440
repo: ROCm/triton
pr: 440
title: Dot slicing pass
author: oplavsic
date: '2024-01-16'
url: https://github.com/ROCm/triton/pull/440
source_category: upstream-code
architectures:
- gfx942
tags:
- attention
- flash-attention
- gemm
- gfx942
- mfma
techniques: []
hardware_features:
- mfma
kernel_types:
- flash-attention
- attention
- gemm
languages:
- triton
- mlir
captured_at: '2026-05-15'
status: merged
merge_sha: 760ac8441a62
inclusion_reason: kernel path 'python/triton/compiler/compiler.py'; keyword 'mfma'
changed_paths:
- include/triton/Dialect/TritonGPU/Transforms/Passes.h
- include/triton/Dialect/TritonGPU/Transforms/Passes.td
- lib/Dialect/TritonGPU/Transforms/CMakeLists.txt
- lib/Dialect/TritonGPU/Transforms/DotSlicing.cpp
- python/src/triton.cc
- python/triton/compiler/compiler.py
- python/triton/runtime/jit.py
- python/triton/third_party/hip/hip_backend.py
- python/tutorials/06-fused-attention.py
- test/TritonGPU/dot-slicing.mlir
---

# Dot slicing pass

**Repository:** [ROCm/triton](https://github.com/ROCm/triton) · **PR:** [#440](https://github.com/ROCm/triton/pull/440) · **Merged:** 2024-01-16 · **Author:** @oplavsic

**Inclusion reason:** kernel path 'python/triton/compiler/compiler.py'; keyword 'mfma'

## Summary (from upstream PR description)

This PR implements the dot slicing pass. It slices along the k dimension
in a dot operation (for dot A, B with shape(A) = (m,k), shape(B) = (k,n)).
The slice size along the k dimension is set by 'sliceKTile'.
The algorithm includes three main steps:


Modify load instruction layouts for dot operands.

This ensures tensor slices have the same layout as the original tensor,
allowing slicing without data exchange between threads.



Slice the dot operands.



Here, the original dot operands are sliced, starting from the load
instruction. This helps in hiding latency in global memory transactions.
Currently, the algorithm assumes the original tensor undergoes only
elementwise operations before being used as a dot operand. In other
words, the algorithm expects only elementwise operations and convert
layout operations to occur between the load and dot instructions.
However, It can be extended to handle other operations if necessary.


Slice the dot operation along the k dimension.


This involves calculating the number of slices from 'sliceKTile', and
then creating a new dot operation for each slice using the operands
sliced in the previous step. Sliced dots are concatenated so the resul

## Changed files (10 total, first 10 shown)

- `include/triton/Dialect/TritonGPU/Transforms/Passes.h`
- `include/triton/Dialect/TritonGPU/Transforms/Passes.td`
- `lib/Dialect/TritonGPU/Transforms/CMakeLists.txt`
- `lib/Dialect/TritonGPU/Transforms/DotSlicing.cpp`
- `python/src/triton.cc`
- `python/triton/compiler/compiler.py`
- `python/triton/runtime/jit.py`
- `python/triton/third_party/hip/hip_backend.py`
- `python/tutorials/06-fused-attention.py`
- `test/TritonGPU/dot-slicing.mlir`

## Provenance

- Merge commit: `760ac8441a62`
- Captured at knowledge cutoff: 2026-05-15
- Source of truth: the upstream PR linked above. This page summarizes upstream
  metadata; consult the PR for the authoritative diff.
