---
id: pr-triton-711
repo: ROCm/triton
pr: 711
title: Add workaround for pytorch device selection issue
author: AlexAUT
date: '2025-02-06'
url: https://github.com/ROCm/triton/pull/711
source_category: upstream-code
architectures:
- gfx942
tags:
- gemm
- gfx942
techniques:
- stream-k
hardware_features: []
kernel_types:
- gemm
languages:
- triton
- mlir
captured_at: '2026-05-15'
status: merged
merge_sha: eb7e0158f593
inclusion_reason: kernel path 'python/perf-kernels/streamk/tune_streamk.py'; keyword
  'gemm'
changed_paths:
- python/perf-kernels/streamk/tune_streamk.py
- python/perf-kernels/tools/tune_gemm/tune_gemm.py
facet_source: inferred
related:
- technique-mfma-pipelining
- technique-stream-k
- technique-persistent-kernel
---
# Add workaround for pytorch device selection issue

**Repository:** [ROCm/triton](https://github.com/ROCm/triton) · **PR:** [#711](https://github.com/ROCm/triton/pull/711) · **Merged:** 2025-02-06 · **Author:** @AlexAUT

**Inclusion reason:** kernel path 'python/perf-kernels/streamk/tune_streamk.py'; keyword 'gemm'

## Summary (from upstream PR description)

When switching to rocm/pytorch:latest we experience a perf CI crash. This PR fixes that by adding a workaround for the broken pytorch device selection.
Some pytorch versions only look at HIP_VISIBLE_DEVICES and therefore crash when we limit the devices with ROCR_VISIBLE_DEVICES because it computes the incorrect device count because it only checks HIP_VISIBLE_DEVICES.
pytorch/pytorch#140318
Right now tune_gemm/streamk correctly limits the visible devices via ROCR_VISIBLE_DEVICES to just the GPU we want to run on. Note that when we use more GPUs we launch one process per GPU. So HIP only sees one device per process which means we can always select the first gpu -> HIP_VISIBLE_DEVICES=0 does not have a behavior change but fixes the pytorch device count calculation.

## Changed files (2 total, first 2 shown)

- `python/perf-kernels/streamk/tune_streamk.py`
- `python/perf-kernels/tools/tune_gemm/tune_gemm.py`

## Provenance

- Merge commit: `eb7e0158f593`
- Captured at knowledge cutoff: 2026-05-15
- Source of truth: the upstream PR linked above. This page summarizes upstream
  metadata; consult the PR for the authoritative diff.
