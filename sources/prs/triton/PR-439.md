---
id: pr-triton-439
repo: ROCm/triton
pr: 439
title: '[Backend] Refactor sharedToDotOperandMFMA lowering'
author: zhanglx13
date: '2024-01-12'
url: https://github.com/ROCm/triton/pull/439
source_category: upstream-code
architectures:
- gfx942
tags:
- gemm
- gfx942
- lds
- mfma
techniques: []
hardware_features:
- mfma
- lds
kernel_types:
- gemm
languages:
- triton
- mlir
captured_at: '2026-05-15'
status: merged
merge_sha: 2e217c5a5cd9
inclusion_reason: kernel path 'lib/Conversion/TritonGPUToLLVM/ConvertLayoutOpToLLVM/SharedToDotOperandMFMA.cpp';
  keyword 'mfma'
changed_paths:
- include/triton/Dialect/TritonGPU/IR/TritonGPUAttrDefs.td
- lib/Conversion/TritonGPUToLLVM/ConvertLayoutOpToLLVM/SharedToDotOperandMFMA.cpp
- lib/Dialect/TritonGPU/Transforms/AccelerateAMDMatmul.cpp
- python/test/unit/language/test_core_amd.py
- test/Conversion/tritongpu_to_llvm.mlir
---

# [Backend] Refactor sharedToDotOperandMFMA lowering

**Repository:** [ROCm/triton](https://github.com/ROCm/triton) · **PR:** [#439](https://github.com/ROCm/triton/pull/439) · **Merged:** 2024-01-12 · **Author:** @zhanglx13

**Inclusion reason:** kernel path 'lib/Conversion/TritonGPUToLLVM/ConvertLayoutOpToLLVM/SharedToDotOperandMFMA.cpp'; keyword 'mfma'

## Summary (from upstream PR description)

This PR

Implements ROCm/frameworks-internal#5237
Supports mfma16 and mfma4 in the fast path
sets maxPhase=4 for mfma4. This is because the nonKDim of mfma4 is less than 16.
Choose warpsPerTile according to mfmaLayout's nonKDim
cleans up format

Before we enable vector access to LDS for non k-major tensors, here are our conventions about LDS accesses:

LDS write

For k-major tensors, swizzling is enabled, i.e. a vector of elements are swizzled in LDS. ds_write are vectorized
For non k-major tensors, a.k.a. mn-major tensors, swizzling is disabled, i.e. elements are written to LDS in their original order. The LLVM backend usually vectorizes ds_write for us, though we write one element at a time.


LDS read

For k-major tensors, normal path is taken since swizzling is enabled.
For non k-major tensors, fast path is taken, since the mapping from an element's coordinates in the tensor to its offsets in the LDS is linear.



Note

If the tensor is k-major, we assume swizzling is enabled. Therefore, we don't allow/test the case where swizzling is disabled for k-major tensors.

## Changed files (5 total, first 5 shown)

- `include/triton/Dialect/TritonGPU/IR/TritonGPUAttrDefs.td`
- `lib/Conversion/TritonGPUToLLVM/ConvertLayoutOpToLLVM/SharedToDotOperandMFMA.cpp`
- `lib/Dialect/TritonGPU/Transforms/AccelerateAMDMatmul.cpp`
- `python/test/unit/language/test_core_amd.py`
- `test/Conversion/tritongpu_to_llvm.mlir`

## Provenance

- Merge commit: `2e217c5a5cd9`
- Captured at knowledge cutoff: 2026-05-15
- Source of truth: the upstream PR linked above. This page summarizes upstream
  metadata; consult the PR for the authoritative diff.
