diff --git a/3rdparty/composable_kernel b/3rdparty/composable_kernel
index 0d4f145078..c318ec0778 160000
--- a/3rdparty/composable_kernel
+++ b/3rdparty/composable_kernel
@@ -1 +1 @@
-Subproject commit 0d4f14507818d118696fc345a3a7623b20470c4e
+Subproject commit c318ec0778f0b9db90618ac51185ff6f9dfab0e1
diff --git a/aiter/configs/tuned_fmoe.csv b/aiter/configs/tuned_fmoe.csv
index fe141f8c71..9d62ca397c 100644
--- a/aiter/configs/tuned_fmoe.csv
+++ b/aiter/configs/tuned_fmoe.csv
@@ -1,11 +1,12 @@
-token,model_dim,inter_dim,expert,topk,act_type,dtype,q_dtype_a,q_dtype_w,q_type,use_g1u1,block_m,ksplit,us,tag,err
-256,6144,4096,8,2,ActivationType.Silu,torch.bfloat16,torch.float8_e4m3fnuz,torch.float8_e4m3fnuz,QuantType.per_128x128,1,80,0,277.88116,fmoe_stage1_bf16_pertokenFp8_blockscale_g1u1_80x256_pf3,1.6%
-512,6144,4096,8,2,ActivationType.Silu,torch.bfloat16,torch.int8,torch.int8,QuantType.per_Tensor,1,48,0,343.17145,fmoe_stage1_bf16_pertokenInt8_g1u1_48x256_pf3,0.0%
-512,6144,4096,8,2,ActivationType.Silu,torch.bfloat16,torch.int8,torch.int8,QuantType.per_Token,1,48,0,336.2290100000001,fmoe_stage1_bf16_pertokenInt8_g1u1_48x256_pf3,0.0%
-512,6144,4096,8,2,ActivationType.Silu,torch.bfloat16,torch.float8_e4m3fnuz,torch.float8_e4m3fnuz,QuantType.per_Token,1,64,0,333.44902,fmoe_stage1_bf16_pertokenFp8_g1u1_64x256_pf3,0.0%
-512,6144,4096,8,2,ActivationType.Silu,torch.bfloat16,torch.float8_e4m3fnuz,torch.int4,QuantType.per_Tensor,1,128,0,838.4978900000001,ck_128,0.1%
-512,6144,4096,8,2,ActivationType.Gelu,torch.bfloat16,torch.int8,torch.int8,QuantType.per_Tensor,1,48,0,342.27102,fmoe_stage1_bf16_pertokenInt8_g1u1_48x256_pf3,0.0%
-512,6144,4096,8,2,ActivationType.Gelu,torch.bfloat16,torch.int8,torch.int8,QuantType.per_Token,1,48,0,337.96381,fmoe_stage1_bf16_pertokenInt8_g1u1_48x256_pf3,0.0%
-512,6144,4096,8,2,ActivationType.Gelu,torch.bfloat16,torch.float8_e4m3fnuz,torch.float8_e4m3fnuz,QuantType.per_Token,1,48,0,333.51427,fmoe_stage1_bf16_pertokenFp8_g1u1_48x256_pf3,0.0%
-512,6144,4096,8,2,ActivationType.Gelu,torch.bfloat16,torch.float8_e4m3fnuz,torch.int4,QuantType.per_Tensor,1,64,0,807.43612,ck_64,0.0%
-4,2304,1536,8,2,ActivationType.Gelu,torch.bfloat16,torch.float8_e4m3fnuz,torch.float8_e4m3fnuz,QuantType.per_Token,1,16,0,17.65486,fmoe_stage1_bf16_pertokenFp8_g1u1_16x64_5tg_pf3,0.0%
+token,model_dim,inter_dim,expert,topk,act_type,dtype,q_dtype_a,q_dtype_w,q_type,use_g1u1,doweight_stage1,block_m,ksplit,us,tag,err
+256,6144,4096,8,2,ActivationType.Silu,torch.bfloat16,torch.float8_e4m3fnuz,torch.float8_e4m3fnuz,QuantType.per_128x128,1,0,32,0,270.8542,fmoe_stage1_bf16_pertokenFp8_blockscale_g1u1_32x512_pf2,1.6%
+512,6144,4096,8,2,ActivationType.Silu,torch.bfloat16,torch.int8,torch.int8,QuantType.per_Tensor,1,0,64,0,346.93691000000007,fmoe_stage1_bf16_pertokenInt8_g1u1_64x256_pf3,0.0%
+512,6144,4096,8,2,ActivationType.Silu,torch.bfloat16,torch.int8,torch.int8,QuantType.per_Token,1,0,64,0,340.65007,fmoe_stage1_bf16_pertokenInt8_g1u1_64x256_pf3,0.0%
+512,6144,4096,8,2,ActivationType.Silu,torch.bfloat16,torch.float8_e4m3fnuz,torch.float8_e4m3fnuz,QuantType.per_Token,1,0,64,0,336.42205,fmoe_stage1_bf16_pertokenFp8_g1u1_64x256_pf3,0.0%
+512,6144,4096,8,2,ActivationType.Silu,torch.bfloat16,torch.float8_e4m3fnuz,torch.int4,QuantType.per_Tensor,1,0,128,0,699.5049699999998,ck_128,0.1%
+512,6144,4096,8,2,ActivationType.Gelu,torch.bfloat16,torch.int8,torch.int8,QuantType.per_Tensor,1,0,64,0,347.0561,fmoe_stage1_bf16_pertokenInt8_g1u1_64x256_pf3,0.0%
+512,6144,4096,8,2,ActivationType.Gelu,torch.bfloat16,torch.int8,torch.int8,QuantType.per_Token,1,0,64,0,334.47364,fmoe_stage1_bf16_pertokenInt8_g1u1_64x256_pf3,0.0%
+512,6144,4096,8,2,ActivationType.Gelu,torch.bfloat16,torch.float8_e4m3fnuz,torch.float8_e4m3fnuz,QuantType.per_Token,1,0,64,0,357.0689999999998,fmoe_stage1_bf16_pertokenFp8_g1u1_64x128_2tg_pf3,0.0%
+512,6144,4096,8,2,ActivationType.Gelu,torch.bfloat16,torch.float8_e4m3fnuz,torch.int4,QuantType.per_Tensor,1,0,128,0,837.6014699999998,ck_128,0.0%
+4,2304,1536,8,2,ActivationType.Gelu,torch.bfloat16,torch.float8_e4m3fnuz,torch.float8_e4m3fnuz,QuantType.per_Token,1,0,32,0,18.364460000000005,fmoe_stage1_bf16_pertokenFp8_g1u1_32x64_4tg_pf3,0.0%
+4,2304,1536,8,2,ActivationType.Gelu,torch.bfloat16,torch.float8_e4m3fnuz,torch.float8_e4m3fnuz,QuantType.per_Token,1,1,32,0,19.47728,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_32x64_4tg_pf3,0.0%
diff --git a/aiter/configs/untuned_fmoe.csv b/aiter/configs/untuned_fmoe.csv
index b381777e39..b9245af152 100644
--- a/aiter/configs/untuned_fmoe.csv
+++ b/aiter/configs/untuned_fmoe.csv
@@ -1,11 +1,12 @@
-token,model_dim,inter_dim,expert,topk,act_type,dtype,q_dtype_a,q_dtype_w,q_type,use_g1u1
-256,6144,4096,8,2,ActivationType.Silu,torch.bfloat16,torch.float8_e4m3fnuz,torch.float8_e4m3fnuz,QuantType.per_128x128,1
-512,6144,4096,8,2,ActivationType.Silu,torch.bfloat16,torch.int8,torch.int8,QuantType.per_Tensor,1
-512,6144,4096,8,2,ActivationType.Silu,torch.bfloat16,torch.int8,torch.int8,QuantType.per_Token,1
-512,6144,4096,8,2,ActivationType.Silu,torch.bfloat16,torch.float8_e4m3fnuz,torch.float8_e4m3fnuz,QuantType.per_Token,1
-512,6144,4096,8,2,ActivationType.Silu,torch.bfloat16,torch.float8_e4m3fnuz,torch.int4,QuantType.per_Tensor,1
-512,6144,4096,8,2,ActivationType.Gelu,torch.bfloat16,torch.int8,torch.int8,QuantType.per_Tensor,1
-512,6144,4096,8,2,ActivationType.Gelu,torch.bfloat16,torch.int8,torch.int8,QuantType.per_Token,1
-512,6144,4096,8,2,ActivationType.Gelu,torch.bfloat16,torch.float8_e4m3fnuz,torch.float8_e4m3fnuz,QuantType.per_Token,1
-512,6144,4096,8,2,ActivationType.Gelu,torch.bfloat16,torch.float8_e4m3fnuz,torch.int4,QuantType.per_Tensor,1
-4,2304,1536,8,2,ActivationType.Gelu,torch.bfloat16,torch.float8_e4m3fnuz,torch.float8_e4m3fnuz,QuantType.per_Token,1
\ No newline at end of file
+token,model_dim,inter_dim,expert,topk,act_type,dtype,q_dtype_a,q_dtype_w,q_type,use_g1u1,doweight_stage1
+256,6144,4096,8,2,ActivationType.Silu,torch.bfloat16,torch.float8_e4m3fnuz,torch.float8_e4m3fnuz,QuantType.per_128x128,1,0
+512,6144,4096,8,2,ActivationType.Silu,torch.bfloat16,torch.int8,torch.int8,QuantType.per_Tensor,1,0
+512,6144,4096,8,2,ActivationType.Silu,torch.bfloat16,torch.int8,torch.int8,QuantType.per_Token,1,0
+512,6144,4096,8,2,ActivationType.Silu,torch.bfloat16,torch.float8_e4m3fnuz,torch.float8_e4m3fnuz,QuantType.per_Token,1,0
+512,6144,4096,8,2,ActivationType.Silu,torch.bfloat16,torch.float8_e4m3fnuz,torch.int4,QuantType.per_Tensor,1,0
+512,6144,4096,8,2,ActivationType.Gelu,torch.bfloat16,torch.int8,torch.int8,QuantType.per_Tensor,1,0
+512,6144,4096,8,2,ActivationType.Gelu,torch.bfloat16,torch.int8,torch.int8,QuantType.per_Token,1,0
+512,6144,4096,8,2,ActivationType.Gelu,torch.bfloat16,torch.float8_e4m3fnuz,torch.float8_e4m3fnuz,QuantType.per_Token,1,0
+512,6144,4096,8,2,ActivationType.Gelu,torch.bfloat16,torch.float8_e4m3fnuz,torch.int4,QuantType.per_Tensor,1,0
+4,2304,1536,8,2,ActivationType.Gelu,torch.bfloat16,torch.float8_e4m3fnuz,torch.float8_e4m3fnuz,QuantType.per_Token,1,0
+4,2304,1536,8,2,ActivationType.Gelu,torch.bfloat16,torch.float8_e4m3fnuz,torch.float8_e4m3fnuz,QuantType.per_Token,1,1
\ No newline at end of file
diff --git a/aiter/fused_moe.py b/aiter/fused_moe.py
index f9cb582a2e..42dd236ec6 100644
--- a/aiter/fused_moe.py
+++ b/aiter/fused_moe.py
@@ -74,6 +74,7 @@ def fused_moe(
     expert_mask=None,  # EP
     activation=ActivationType.Silu,
     quant_type=QuantType.No,
+    doweight_stage1=False,
     # following for quant
     w1_scale=None,  # [expert(local_expert:EP), inter_dim, 1]
     w2_scale=None,  # [expert(local_expert:EP), model_dim, 1]
@@ -112,6 +113,7 @@ def fused_moe(
             quant_type,
             isG1U1,
             activation,
+            doweight_stage1,
         )
     run_1stage = M < 256
     run_1stage = False
@@ -122,6 +124,7 @@ def fused_moe(
     )
 
     if run_1stage:
+        assert doweight_stage1 == False, "doweight_stage1 not support in fused_moe_1stage"
         return fused_moe_1stage(
             hidden_states,
             w1,
@@ -158,6 +161,7 @@ def fused_moe(
             block_size_M,
             activation=activation,
             quant_type=quant_type,
+            doweight_stage1=doweight_stage1,
             q_dtype_a=q_dtype_a,
             q_dtype_w=q_dtype_w,
             w1_scale=w1_scale,
@@ -271,6 +275,7 @@ def get_2stage_cfgs(
     q_type,
     use_g1u1,
     activation,
+    doweight_stage1
 ):
     def get_cfg_2stages(tune_file):
         import pandas as pd
@@ -289,6 +294,7 @@ def get_cfg_2stages(tune_file):
                 "q_dtype_w",
                 "q_type",
                 "use_g1u1",
+                "doweight_stage1"
             ]
         ).to_dict("index")
         return cfg_2stages
@@ -297,6 +303,7 @@ def get_cfg_2stages(tune_file):
     config_path = f"{AITER_ROOT_DIR}/aiter/configs/"
     tune_file = os.path.join(config_path, "tuned_fmoe.csv")
     untune_file = os.path.join(config_path, "untuned_fmoe.csv")
+    profile_file = os.path.join(config_path, "profile_fmoe.csv")
     if cfg_2stages is None:
         cfg_2stages = get_cfg_2stages(tune_file)
     keys = (
@@ -311,17 +318,18 @@ def get_cfg_2stages(tune_file):
         str(q_dtype_w),
         str(q_type),
         use_g1u1,
+        doweight_stage1
     )
 
     def MainFunc():
         with open(untune_file, "a") as f:
             q_dtype_ws = q_dtype_w if q_dtype_w != torch.uint32 else "torch.int4"
             f.write(
-                f"\n{token},{model_dim},{inter_dim},{expert},{topk},{activation},{dtype},{q_dtype_a},{q_dtype_ws},{q_type},{int(use_g1u1)}"
+                f"\n{token},{model_dim},{inter_dim},{expert},{topk},{activation},{dtype},{q_dtype_a},{q_dtype_ws},{q_type},{int(use_g1u1)},{int(doweight_stage1)}"
             )
         logger.info("\033[34m Start tuning fmoe")
         os.system(
-            f"{PY} {AITER_ASM_DIR}/fmoe_2stages/tune.py -i {untune_file} -o {tune_file}"
+            f"{PY} {AITER_ASM_DIR}/fmoe_2stages/tune.py -i {untune_file} -o {tune_file} -o2 {profile_file} --last"
         )
 
     def FinalFunc():
@@ -353,7 +361,10 @@ def FinalFunc():
 
     if "ck" in tag:
         return (
-            ck_stage1,
+            functools.partial(
+                ck_stage1,
+                activation=activation,
+            ),
             aiter.ck_moe_stage2,
             block_m,
             ksplit,
@@ -392,6 +403,7 @@ def fused_moe_2stages(
     block_size_M,
     activation=ActivationType.Silu,
     quant_type=QuantType.No,
+    doweight_stage1=False,
     # following for quant
     q_dtype_a=None,
     q_dtype_w=None,
@@ -421,6 +433,7 @@ def fused_moe_2stages(
         quant_type,
         isG1U1,
         activation,
+        doweight_stage1
     )
 
     a1, a1_scale = quant_func(hidden_states, scale=a1_scale, quant_dtype=q_dtype_a)
@@ -437,7 +450,8 @@ def fused_moe_2stages(
             dtype=q_dtype_a,
             device=device,
         )
-    stage1(
+
+    a2 = stage1(
         a1,
         w1,
         w2,
@@ -448,6 +462,7 @@ def fused_moe_2stages(
         block_m=block_m,
         a1_scale=a1_scale,
         w1_scale=w1_scale,
+        sorted_weights=sorted_weights if doweight_stage1 else None,
     )
 
     if quant_type != QuantType.per_128x128:
@@ -472,19 +487,20 @@ def get1tensor(device):
             return torch.tensor(1.0, dtype=torch.float, device=device)
 
         a2_scale = get1tensor(device)
+
     stage2(
         a2,
         w1,
         w2,
         sorted_ids,
         sorted_expert_ids,
-        sorted_weights,
         num_valid_ids,
         moe_out,
         topk,
         w2_scale,
         a2_scale,
         block_size_M,
+        sorted_weights=sorted_weights if not doweight_stage1 else None,
     )
 
     return moe_out
@@ -513,8 +529,10 @@ def asm_stage1(
     quant_type=QuantType.No,
     a1_scale=None,
     w1_scale=None,
+    sorted_weights=None
 ):
-    dtype = out.dtype
+    dtype = torch.bfloat16 # out.dtype, asm only support bf16
+    out = out.view(dtype)
     device = out.device
     token_num, topk, _ = out.shape
     E, model_dim, inter_dim = get_inter_dim(w1.shape, w2.shape)
@@ -547,7 +565,8 @@ def asm_stage1(
         activation=activation,
         quant_type=quant_type,
         a1_scale=a1_scale,
-        w1_scale=w1_scale,
+        w1_scale=w1_scale, 
+        sorted_weights=sorted_weights,
     )
     if ksplit > 0:
         if activation == ActivationType.Silu:
@@ -569,6 +588,7 @@ def ck_stage1(
     activation=ActivationType.Silu,
     a1_scale=None,
     w1_scale=None,
+    sorted_weights=None
 ):
     topk = out.shape[1]
     expert, topk, _ = out.shape
@@ -589,6 +609,7 @@ def ck_stage1(
         w1_scale,
         a1_scale,
         block_m,
+        sorted_weights,
     )
     if activation == ActivationType.Silu:
         aiter.silu_and_mul(out, tmp)
@@ -673,6 +694,7 @@ def torch_moe_stage1(
     # following for quant
     a1_scale=None,  # [token, 1]
     w1_scale=None,  # [expert, inter_dim, 1]
+    doweight=False
 ):
     ctype = torch.float  # compute type
     hidden_states = hidden_states.to(ctype)
@@ -718,6 +740,8 @@ def torch_moe_stage1(
         if mask.sum():
             sub_tokens = hidden_states[mask]
             act_input = sub_tokens @ (w1[E_id].transpose(0, 1))
+            if doweight:
+                act_input = act_input * topk_weight[mask].view(-1, 1)
             out[mask] = act_input
 
     use_g1u1 = w1.shape[1] == (2 * inter_dim)
@@ -739,7 +763,8 @@ def torch_moe_stage2(
     dtype=torch.float16,
     quant_type=QuantType.No,
     w2_scale=None,  # [1]
-    a2_scale=None,  # [expert]]
+    a2_scale=None,  # [expert]]'
+    doweight=True
 ):
     ctype = torch.float  # compute type
     hidden_states = hidden_states.to(ctype)
@@ -779,7 +804,9 @@ def torch_moe_stage2(
             sub_tokens = hidden_states[mask]
             act_input = sub_tokens @ (w2[E_id].transpose(0, 1))
             out[mask] = act_input
-    return (out * topk_weights.view(token_num, -1, 1)).sum(1).to(dtype)
+    if doweight:
+        out = out * topk_weights.view(token_num, -1, 1)
+    return out.sum(1).to(dtype)
 
 
 def fused_topk(
diff --git a/aiter/fused_moe_bf16_asm.py b/aiter/fused_moe_bf16_asm.py
index 438550e04e..bd3b32d829 100755
--- a/aiter/fused_moe_bf16_asm.py
+++ b/aiter/fused_moe_bf16_asm.py
@@ -315,7 +315,7 @@ def ck_moe_2stages(a1,
     aiter.ck_moe_stage1(a1, w1, w2,
                         sorted_ids, sorted_expert_ids, num_valid_ids,
                         a2, topk,
-                        fc1_scale, a1_scale, block_size)
+                        fc1_scale, a1_scale, block_size, sorted_weights)
 
     # g1u0
     if (w2.shape[2] != w1.shape[1]) and (w2.dtype == torch.float8_e4m3fnuz):
@@ -342,8 +342,8 @@ def ck_moe_2stages(a1,
         a2_scale = ck_moe_2stages.one_float_tensor
 
     aiter.ck_moe_stage2(a2, w1, w2, sorted_ids,
-                        sorted_expert_ids, sorted_weights,
-                        num_valid_ids, moe_buf, topk, fc2_scale, a2_scale, block_size)
+                        sorted_expert_ids,
+                        num_valid_ids, moe_buf, topk, fc2_scale, a2_scale, block_size, sorted_weights)
 
     return moe_buf
 
diff --git a/aiter/ops/moe_op.py b/aiter/ops/moe_op.py
index 5ab8a0477a..c8ce970416 100755
--- a/aiter/ops/moe_op.py
+++ b/aiter/ops/moe_op.py
@@ -48,7 +48,7 @@ def fmoe(
     gate: Tensor,
     down: Tensor,
     sorted_token_ids: Tensor,
-    sorted_weight_buf: Tensor,
+    sorted_weights: Tensor,
     sorted_expert_ids: Tensor,
     num_valid_ids: Tensor,
     topk: int,
@@ -62,7 +62,7 @@ def fmoe_int8_g1u0(
     gate: Tensor,
     down: Tensor,
     sorted_token_ids: Tensor,
-    sorted_weight_buf: Tensor,
+    sorted_weights: Tensor,
     sorted_expert_ids: Tensor,
     num_valid_ids: Tensor,
     topk: int,
@@ -81,7 +81,7 @@ def fmoe_g1u1(
     gate: Tensor,
     down: Tensor,
     sorted_token_ids: Tensor,
-    sorted_weight_buf: Tensor,
+    sorted_weights: Tensor,
     sorted_expert_ids: Tensor,
     num_valid_ids: Tensor,
     topk: int,
@@ -100,7 +100,7 @@ def fmoe_g1u1_tkw1(
     gate: Tensor,
     down: Tensor,
     sorted_token_ids: Tensor,
-    sorted_weight_buf: Tensor,
+    sorted_weights: Tensor,
     sorted_expert_ids: Tensor,
     num_valid_ids: Tensor,
     topk: int,
@@ -119,7 +119,7 @@ def fmoe_int8_g1u0_a16(
     gate: Tensor,
     down: Tensor,
     sorted_token_ids: Tensor,
-    sorted_weight_buf: Tensor,
+    sorted_weights: Tensor,
     sorted_expert_ids: Tensor,
     num_valid_ids: Tensor,
     topk: int,
@@ -137,7 +137,7 @@ def fmoe_g1u1_a16(
     gate: Tensor,
     down: Tensor,
     sorted_token_ids: Tensor,
-    sorted_weight_buf: Tensor,
+    sorted_weights: Tensor,
     sorted_expert_ids: Tensor,
     num_valid_ids: Tensor,
     topk: int,
@@ -155,7 +155,7 @@ def fmoe_fp8_blockscale_g1u1(
     gate: Tensor,
     down: Tensor,
     sorted_token_ids: Tensor,
-    sorted_weight_buf: Tensor,
+    sorted_weights: Tensor,
     sorted_expert_ids: Tensor,
     num_valid_ids: Tensor,
     topk: int,
@@ -182,10 +182,12 @@ def moe_stage1_g1u1(
     kernelName: str,
     block_m: int,
     ksplit: int = 0,
-    activation: ActivationType = ActivationType.Silu,
-    quant_type: QuantType = QuantType.No,
-    a1_scale: Optional[torch.Tensor] = None,
-    w1_scale: Optional[torch.Tensor] = None,
+    activation: Enum = ActivationType.Silu,
+    quant_type: Enum = QuantType.No,
+    a1_scale: Optional[Tensor] = None,
+    w1_scale: Optional[Tensor] = None,
+    doweight_stage1: bool = False,
+    sorted_weights: Optional[Tensor] = None # do sorted weight multiply in stage1
 ): ...
 
 
@@ -218,6 +220,7 @@ def ck_moe_stage1(
     w1_scale: Optional[Tensor] = None,
     a1_scale: Optional[Tensor] = None,
     block_m: Optional[int] = 32,
+    sorted_weights: Optional[Tensor] = None,
 ): ...
 
 
@@ -228,11 +231,11 @@ def ck_moe_stage2(
     w2: Tensor,
     sorted_token_ids: Tensor,
     sorted_expert_ids: Tensor,
-    sorted_weights: Tensor,
     num_valid_ids: Tensor,
     out: Tensor,
     topk: int,
     w2_scale: Optional[Tensor] = None,
     a2_scale: Optional[Tensor] = None,
     block_m: Optional[int] = 32,
+    sorted_weights: Optional[Tensor] = None,
 ): ...
diff --git a/aiter/ops/quant.py b/aiter/ops/quant.py
index b2cf1be062..c5966b2d84 100644
--- a/aiter/ops/quant.py
+++ b/aiter/ops/quant.py
@@ -143,7 +143,7 @@ def per_tensor_quant_hip(x, scale=None, quant_dtype=torch.float8_e4m3fnuz):
             static_scaled_fp8_quant(y, x, scale)
     else:
         raise ValueError(f"unsupported: {quant_dtype=}")
-    return y, scale
+    return y, scale.view(1)
 
 
 def per_token_quant_triton(x, scale=None, quant_dtype=torch.int8):
diff --git a/aiter/test_common.py b/aiter/test_common.py
index 9bda581a16..b4269f675b 100644
--- a/aiter/test_common.py
+++ b/aiter/test_common.py
@@ -18,20 +18,23 @@ def wrapper(*args, **kwargs):
             num = num_rotate_args
             if num < 1:
                 gpu_id = torch.cuda.current_device()
-                inputSize = (
-                    sum([el.nbytes for el in args if isinstance(el, torch.Tensor)]) + 1
-                )
+
+                iter_used_memory, inputSize, _, _ = device_memory_profiling(func, *args, **kwargs)
+
                 properties = torch.cuda.get_device_properties(gpu_id)
+                free_memory = torch.cuda.mem_get_info(gpu_id)[0]
                 cache_size = min(
                     properties.L2_cache_size * 64 * 128,
-                    properties.total_memory - inputSize,
+                    (free_memory - iter_used_memory + inputSize) * 0.9,
                 )
-                num = (cache_size + inputSize - 1) // inputSize
+                cache_size = max(cache_size, 0)
+                num = int((cache_size + inputSize - 1) // inputSize)
+                # print(f"{iter_used_memory=}, {inputSize=}, {cache_size=}, {free_memory=}, {num=}")
             num = min(num, num_iters)
 
-            rotate_args = [
-                (copy.deepcopy(args), copy.deepcopy(kwargs)) for _ in range(num)
-            ]
+            rotate_args =[
+                (copy.deepcopy(args), copy.deepcopy(kwargs)) for _ in range(num-1)
+            ] +  [(args, kwargs)]
 
             run_iters(num_warmup, func, *args, **kwargs)
             if int(os.environ.get("AITER_LOG_MORE", 0)):
@@ -97,6 +100,40 @@ def wrapper(*args, **kwargs):
     return decorator
 
 
+def device_memory_profiling(func, *args, **kwargs):
+    gpu_id = torch.cuda.current_device()
+    inputSize = (
+        sum([el.nbytes for el in args if isinstance(el, torch.Tensor) and el.device.index == gpu_id]) + 1
+    )
+    torch.cuda.reset_peak_memory_stats(gpu_id)
+    cuda_memory_before = (
+        torch.cuda.mem_get_info(gpu_id)[1] - torch.cuda.mem_get_info(gpu_id)[0]
+    )
+    torch_memory_before = torch.cuda.memory_reserved(gpu_id)
+    torch_peak_before = torch.cuda.memory_stats(gpu_id).get(
+        "allocated_bytes.all.peak", 0
+    )
+    non_torch_memory_before = cuda_memory_before - torch_memory_before
+
+    data = func(*args, **kwargs)
+
+    torch.cuda.reset_peak_memory_stats(gpu_id)
+    cuda_memory_after = (
+        torch.cuda.mem_get_info(gpu_id)[1] - torch.cuda.mem_get_info(gpu_id)[0]
+    )
+    torch_memory_after = torch.cuda.memory_reserved(gpu_id)
+    torch_peak_after = torch.cuda.memory_stats(gpu_id).get(
+        "allocated_bytes.all.peak", 0
+    )
+    non_torch_memory_after = cuda_memory_after - torch_memory_after
+
+    torch_peak_increase = torch_peak_after - torch_peak_before
+    non_torch_increase = non_torch_memory_after - non_torch_memory_before
+    iter_used_memory = torch_peak_increase + non_torch_increase + inputSize
+
+    return iter_used_memory, inputSize, torch_peak_increase, non_torch_increase
+
+
 def run_iters(num_iters, func, *args, **kwargs):
     data = None
     for _ in range(num_iters):
diff --git a/aiter/utility/mp_tuner.py b/aiter/utility/mp_tuner.py
index 3cd754c0d8..a0f86992a8 100644
--- a/aiter/utility/mp_tuner.py
+++ b/aiter/utility/mp_tuner.py
@@ -19,10 +19,16 @@ def worker(gpuIDMap, tag, func, args, **kwargs):
     args = [el.to(device) if isinstance(el, torch.Tensor) else el for el in args]
     torch.cuda.synchronize()
 
-    _, us = run_perftest(func, *args, **kwargs)
-    torch.cuda.synchronize()
-
-    return tag, us, _.to("cpu")
+    try:
+        _, us = run_perftest(func, *args, **kwargs)
+        torch.cuda.synchronize()
+        _ = _.to("cpu")
+    except Exception as e:
+        print(f"Error in process {pid}: {e}")
+        us = float("inf")
+        _ = None
+
+    return tag, us, _
 
 
 def get_pid():
diff --git a/csrc/include/moe_ck.h b/csrc/include/moe_ck.h
index 3c50f300ec..1f219e88d2 100644
--- a/csrc/include/moe_ck.h
+++ b/csrc/include/moe_ck.h
@@ -26,17 +26,18 @@ void ck_moe_stage1(torch::Tensor &hidden_states,     // [m, k], input token
                    int topk,
                    std::optional<torch::Tensor> w1_scale, // [e, 1, n], gate(up) scale
                    std::optional<torch::Tensor> a1_scale, // [m, 1], token scale
-                   std::optional<int> block_m);
+                   std::optional<int> block_m,
+                   std::optional<torch::Tensor> sorted_weights);    // [max_num_tokens_padded]);
 
 void ck_moe_stage2(torch::Tensor &inter_states,      // [m, k], input token
                    torch::Tensor &w1,                // [e, n, k]/[e, 2*n, k], pre-shuffle([e, nr, kr, w])
                    torch::Tensor &w2,                // [e, n, k], pre-shuffle([e, nr, kr, w])
                    torch::Tensor &sorted_token_ids,  // [max_num_tokens_padded]
                    torch::Tensor &sorted_expert_ids, // [max_num_m_blocks]
-                   torch::Tensor &sorted_weights,    // [max_num_tokens_padded]
                    torch::Tensor &num_valid_ids,     // [1]
                    torch::Tensor &out,               // [max_num_tokens_padded, inter_dim]
                    int topk,
                    std::optional<torch::Tensor> w2_scale, // [e, 1, n], gate(up) scale
                    std::optional<torch::Tensor> a2_scale, // [m, 1], token scale
-                   std::optional<int> block_m);
+                   std::optional<int> block_m,
+                   std::optional<torch::Tensor> sorted_weights);    // [max_num_tokens_padded]);
diff --git a/csrc/include/moe_op.h b/csrc/include/moe_op.h
index 114659e3c2..451b04adef 100755
--- a/csrc/include/moe_op.h
+++ b/csrc/include/moe_op.h
@@ -40,7 +40,7 @@ void fmoe(torch::Tensor &out,               // [token_cnt, dim]
           torch::Tensor &gate,              // [expert, hidden_dim, dim] N,K
           torch::Tensor &down,              // [expert, hidden_dim, dim]
           torch::Tensor &sorted_token_ids,  // [max_num_tokens_padded]
-          torch::Tensor &sorted_weight_buf, // [max_num_tokens_padded]
+          torch::Tensor &sorted_weights,    // [max_num_tokens_padded]
           torch::Tensor &sorted_expert_ids, // [max_num_m_blocks]
           torch::Tensor &num_valid_ids,     // [1]
           uint32_t topk                     //
@@ -51,7 +51,7 @@ void fmoe_int8_g1u0(torch::Tensor &out,               // [token_cnt, dim]
                     torch::Tensor &gate,              // [expert, hidden_dim, dim] N,K
                     torch::Tensor &down,              // [expert, hidden_dim, dim]
                     torch::Tensor &sorted_token_ids,  // [max_num_tokens_padded]
-                    torch::Tensor &sorted_weight_buf, // [max_num_tokens_padded]
+                    torch::Tensor &sorted_weights,    // [max_num_tokens_padded]
                     torch::Tensor &sorted_expert_ids, // [max_num_m_blocks]
                     torch::Tensor &num_valid_ids,     // [1]
                     uint32_t topk,                    //
@@ -66,7 +66,7 @@ void fmoe_g1u1(torch::Tensor &out,                                           //
                torch::Tensor &gate,                                          // [expert, hidden_dim*2, dim] N,K
                torch::Tensor &down,                                          // [expert, hidden_dim, dim]
                torch::Tensor &sorted_token_ids,                              // [max_num_tokens_padded]
-               torch::Tensor &sorted_weight_buf,                             // [max_num_tokens_padded]
+               torch::Tensor &sorted_weights,                                // [max_num_tokens_padded]
                torch::Tensor &sorted_expert_ids,                             // [max_num_m_blocks]
                torch::Tensor &num_valid_ids,                                 // [1]
                uint32_t topk,                                                //
@@ -75,28 +75,28 @@ void fmoe_g1u1(torch::Tensor &out,                                           //
                torch::Tensor &fc2_scale,                                     // [expert, 1, dim]
                std::optional<torch::Tensor> fc2_smooth_scale = std::nullopt, // [expert, 1, hidden_dim]
                ActivationType activation = ActivationType::Silu);
-               
+
 void fmoe_g1u1_tkw1(torch::Tensor &out,                                           // [token_cnt, dim]
-                torch::Tensor &input,                                         // [token_cnt, dim] M,K
-                torch::Tensor &gate,                                          // [expert, hidden_dim*2, dim] N,K
-                torch::Tensor &down,                                          // [expert, hidden_dim, dim]
-                torch::Tensor &sorted_token_ids,                              // [max_num_tokens_padded]
-                torch::Tensor &sorted_weight_buf,                             // [max_num_tokens_padded]
-                torch::Tensor &sorted_expert_ids,                             // [max_num_m_blocks]
-                torch::Tensor &num_valid_ids,                                 // [1]
-                uint32_t topk,                                                //
-                torch::Tensor &input_scale,                                   // [token_cnt, 1]
-                torch::Tensor &fc1_scale,                                     // [expert, 1, hidden_dim]
-                torch::Tensor &fc2_scale,                                     // [expert, 1, dim]
-                std::optional<torch::Tensor> fc2_smooth_scale = std::nullopt, // [expert, 1, hidden_dim]
-                ActivationType activation = ActivationType::Silu);
+                    torch::Tensor &input,                                         // [token_cnt, dim] M,K
+                    torch::Tensor &gate,                                          // [expert, hidden_dim*2, dim] N,K
+                    torch::Tensor &down,                                          // [expert, hidden_dim, dim]
+                    torch::Tensor &sorted_token_ids,                              // [max_num_tokens_padded]
+                    torch::Tensor &sorted_weights,                                // [max_num_tokens_padded]
+                    torch::Tensor &sorted_expert_ids,                             // [max_num_m_blocks]
+                    torch::Tensor &num_valid_ids,                                 // [1]
+                    uint32_t topk,                                                //
+                    torch::Tensor &input_scale,                                   // [token_cnt, 1]
+                    torch::Tensor &fc1_scale,                                     // [expert, 1, hidden_dim]
+                    torch::Tensor &fc2_scale,                                     // [expert, 1, dim]
+                    std::optional<torch::Tensor> fc2_smooth_scale = std::nullopt, // [expert, 1, hidden_dim]
+                    ActivationType activation = ActivationType::Silu);
 
 void fmoe_int8_g1u0_a16(torch::Tensor &out,               // [token_cnt, dim]
                         torch::Tensor &input,             // [token_cnt, dim] M,K
                         torch::Tensor &gate,              // [expert, inter_dim, dim] N,K
                         torch::Tensor &down,              // [expert, dim, inter_dim]
                         torch::Tensor &sorted_token_ids,  // [max_num_tokens_padded]
-                        torch::Tensor &sorted_weight_buf, // [max_num_tokens_padded]
+                        torch::Tensor &sorted_weights,    // [max_num_tokens_padded]
                         torch::Tensor &sorted_expert_ids, // [max_num_m_blocks]
                         torch::Tensor &num_valid_ids,     // [1]
                         uint32_t topk,                    //
@@ -110,7 +110,7 @@ void fmoe_g1u1_a16(torch::Tensor &out,               // [token_cnt, dim]
                    torch::Tensor &gate,              // [expert, inter_dim, dim] N,K
                    torch::Tensor &down,              // [expert, dim, inter_dim]
                    torch::Tensor &sorted_token_ids,  // [max_num_tokens_padded]
-                   torch::Tensor &sorted_weight_buf, // [max_num_tokens_padded]
+                   torch::Tensor &sorted_weights,    // [max_num_tokens_padded]
                    torch::Tensor &sorted_expert_ids, // [max_num_m_blocks]
                    torch::Tensor &num_valid_ids,     // [1]
                    uint32_t topk,                    //
@@ -125,7 +125,7 @@ void fmoe_fp8_blockscale_g1u1(torch::Tensor &out,
                               torch::Tensor &gate,                                          // [expert, inter_dim*2, dim] N,K
                               torch::Tensor &down,                                          // [expert, dim, inter_dim]
                               torch::Tensor &sorted_token_ids,                              // [max_num_tokens_padded]
-                              torch::Tensor &sorted_weight_buf,                             // [max_num_tokens_padded]
+                              torch::Tensor &sorted_weights,                                // [max_num_tokens_padded]
                               torch::Tensor &sorted_expert_ids,                             // [max_num_m_blocks]
                               torch::Tensor &num_valid_ids,                                 // [1]
                               uint32_t topk,                                                //
@@ -151,7 +151,7 @@ void moe_stage1_g1u1(torch::Tensor &input,             // [token_cnt, model_dim]
                      ActivationType activation,
                      QuantType quant_type,
                      std::optional<torch::Tensor> a1_scale, // [token_cnt, 1], token scale
-                     std::optional<torch::Tensor> w1_scale  // [expert, 1, inter_dim], gate(up) scale
-);
+                     std::optional<torch::Tensor> w1_scale, // [expert, 1, inter_dim], gate(up) scale
+                     std::optional<torch::Tensor> sorted_weights);
 
 void moe_sum(torch::Tensor &input, torch::Tensor &output);
diff --git a/csrc/include/rocm_ops.hpp b/csrc/include/rocm_ops.hpp
index 720394ca72..8fbd3b1e00 100755
--- a/csrc/include/rocm_ops.hpp
+++ b/csrc/include/rocm_ops.hpp
@@ -335,33 +335,34 @@
             py::arg("alibi_slopes") = std::nullopt,               \
             py::arg("gen") = std::nullopt);
 
-#define MOE_CK_2STAGES_PYBIND                   \
-      m.def("ck_moe_stage1", &ck_moe_stage1,    \
-            py::arg("hidden_states"),           \
-            py::arg("w1"),                      \
-            py::arg("w2"),                      \
-            py::arg("sorted_token_ids"),        \
-            py::arg("sorted_expert_ids"),       \
-            py::arg("num_valid_ids"),           \
-            py::arg("out"),                     \
-            py::arg("topk"),                    \
-            py::arg("w1_scale") = std::nullopt, \
-            py::arg("a1_scale") = std::nullopt, \
-            py::arg("block_m") = 32);           \
-                                                \
-      m.def("ck_moe_stage2", &ck_moe_stage2,    \
-            py::arg("inter_states"),            \
-            py::arg("w1"),                      \
-            py::arg("w2"),                      \
-            py::arg("sorted_token_ids"),        \
-            py::arg("sorted_expert_ids"),       \
-            py::arg("sorted_weights"),          \
-            py::arg("num_valid_ids"),           \
-            py::arg("out"),                     \
-            py::arg("topk"),                    \
-            py::arg("w2_scale") = std::nullopt, \
-            py::arg("a2_scale") = std::nullopt, \
-            py::arg("block_m") = 32);
+#define MOE_CK_2STAGES_PYBIND                          \
+      m.def("ck_moe_stage1", &ck_moe_stage1,           \
+            py::arg("hidden_states"),                  \
+            py::arg("w1"),                             \
+            py::arg("w2"),                             \
+            py::arg("sorted_token_ids"),               \
+            py::arg("sorted_expert_ids"),              \
+            py::arg("num_valid_ids"),                  \
+            py::arg("out"),                            \
+            py::arg("topk"),                           \
+            py::arg("w1_scale") = std::nullopt,        \
+            py::arg("a1_scale") = std::nullopt,        \
+            py::arg("block_m") = 32,                   \
+            py::arg("sorted_weights") = std::nullopt); \
+                                                       \
+      m.def("ck_moe_stage2", &ck_moe_stage2,           \
+            py::arg("inter_states"),                   \
+            py::arg("w1"),                             \
+            py::arg("w2"),                             \
+            py::arg("sorted_token_ids"),               \
+            py::arg("sorted_expert_ids"),              \
+            py::arg("num_valid_ids"),                  \
+            py::arg("out"),                            \
+            py::arg("topk"),                           \
+            py::arg("w2_scale") = std::nullopt,        \
+            py::arg("a2_scale") = std::nullopt,        \
+            py::arg("block_m") = 32,                   \
+            py::arg("sorted_weights") = std::nullopt); \
 
 #define MOE_CK_PYBIND                                                               \
       m.def("ck_moe", &ck_moe,                                                      \
@@ -396,7 +397,7 @@
       m.def("fmoe_int8_g1u0", &fmoe_int8_g1u0,                                   \
             py::arg("out"), py::arg("input"),                                    \
             py::arg("gate"), py::arg("down"),                                    \
-            py::arg("sorted_token_ids"), py::arg("sorted_weight_buf"),           \
+            py::arg("sorted_token_ids"), py::arg("sorted_weights"),              \
             py::arg("sorted_expert_ids"), py::arg("num_valid_ids"),              \
             py::arg("topk"), py::arg("input_scale"),                             \
             py::arg("fc1_scale"), py::arg("fc2_scale"),                          \
@@ -405,7 +406,7 @@
       m.def("fmoe_g1u1", &fmoe_g1u1,                                             \
             py::arg("out"), py::arg("input"),                                    \
             py::arg("gate"), py::arg("down"),                                    \
-            py::arg("sorted_token_ids"), py::arg("sorted_weight_buf"),           \
+            py::arg("sorted_token_ids"), py::arg("sorted_weights"),              \
             py::arg("sorted_expert_ids"), py::arg("num_valid_ids"),              \
             py::arg("topk"), py::arg("input_scale"),                             \
             py::arg("fc1_scale"), py::arg("fc2_scale"),                          \
@@ -414,7 +415,7 @@
       m.def("fmoe_g1u1_tkw1", &fmoe_g1u1_tkw1,                                   \
             py::arg("out"), py::arg("input"),                                    \
             py::arg("gate"), py::arg("down"),                                    \
-            py::arg("sorted_token_ids"), py::arg("sorted_weight_buf"),           \
+            py::arg("sorted_token_ids"), py::arg("sorted_weights"),              \
             py::arg("sorted_expert_ids"), py::arg("num_valid_ids"),              \
             py::arg("topk"), py::arg("input_scale"),                             \
             py::arg("fc1_scale"), py::arg("fc2_scale"),                          \
@@ -425,7 +426,7 @@
       m.def("fmoe_fp8_blockscale_g1u1", &fmoe_fp8_blockscale_g1u1,               \
             py::arg("out"), py::arg("input"),                                    \
             py::arg("gate"), py::arg("down"),                                    \
-            py::arg("sorted_token_ids"), py::arg("sorted_weight_buf"),           \
+            py::arg("sorted_token_ids"), py::arg("sorted_weights"),              \
             py::arg("sorted_expert_ids"), py::arg("num_valid_ids"),              \
             py::arg("topk"),                                                     \
             py::arg("input_scale"),                                              \
@@ -446,7 +447,8 @@
             py::arg("activation") = ActivationType::Silu,                        \
             py::arg("quant_type") = QuantType::No,                               \
             py::arg("a1_scale") = std::nullopt,                                  \
-            py::arg("w1_scale") = std::nullopt);                                 \
+            py::arg("w1_scale") = std::nullopt,                                  \
+            py::arg("sorted_weights") = std::nullopt);                           \
       m.def("moe_sum", &moe_sum, "moe_sum(Tensor! input, Tensor output) -> ()");
 
 #define MOE_SORTING_PYBIND                                          \
diff --git a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm.hpp b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm.hpp
index 0dce5af014..e79cc7c942 100644
--- a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm.hpp
+++ b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm.hpp
@@ -32,38 +32,42 @@ using Col = ck::tensor_layout::gemm::ColumnMajor;
 
 struct TypeCast
 {
-    template <typename E, typename C, typename D0, typename D1>
+    template <typename E, typename C, typename D0, typename D1, typename D2>
     __host__ __device__ constexpr void
-    operator()(E &e, const C &c, const D0 &d0, const D1 &d1) const;
+    operator()(E &e, const C &c, const D0 &d0, const D1 &d1, const D2 &d2) const;
 
     template <>
-    __host__ __device__ constexpr void operator()<F16, float, float, float>(F16 &e, const float &c,
+    __host__ __device__ constexpr void operator()<F16, float, float, float, float>(F16 &e, const float &c,
                                                                             const float &d0,
-                                                                            const float &d1) const
+                                                                            const float &d1,
+                                                                            const float &d2) const
     {
         e = ck::type_convert<F16>(c);
     }
 
     template <>
-    __host__ __device__ constexpr void operator()<B16, float, float, float>(B16 &e, const float &c,
+    __host__ __device__ constexpr void operator()<B16, float, float, float, float>(B16 &e, const float &c,
                                                                             const float &d0,
-                                                                            const float &d1) const
+                                                                            const float &d1,
+                                                                            const float &d2) const
     {
         e = ck::type_convert<B16>(c);
     }
 
     template <>
-    __host__ __device__ constexpr void operator()<F16, int, float, float>(F16 &e, const int &c,
+    __host__ __device__ constexpr void operator()<F16, int, float, float, float>(F16 &e, const int &c,
                                                                             const float &d0,
-                                                                            const float &d1) const
+                                                                            const float &d1,
+                                                                            const float &d2) const
     {
         e = ck::type_convert<F16>(c);
     }
 
     template <>
-    __host__ __device__ constexpr void operator()<B16, int, float, float>(B16 &e, const int &c,
+    __host__ __device__ constexpr void operator()<B16, int, float, float, float>(B16 &e, const int &c,
                                                                             const float &d0,
-                                                                            const float &d1) const
+                                                                            const float &d1,
+                                                                            const float &d2) const
     {
         e = ck::type_convert<B16>(c);
     }
@@ -72,87 +76,95 @@ struct TypeCast
 // for gate, a_scale, b_scale
 struct MulABScale
 {
-    template <typename E, typename C, typename D0, typename D1>
+    template <typename E, typename C, typename D0, typename D1, typename D2>
     __host__ __device__ constexpr void
-    operator()(E &e, const C &c, const D0 &d0, const D1 &d1) const;
+    operator()(E &e, const C &c, const D0 &d0, const D1 &d1, const D2 &d2) const;
 
     template <>
-    __host__ __device__ constexpr void operator()<F16, float, float, float>(F16 &e,
+    __host__ __device__ constexpr void operator()<F16, float, float, float, float>(F16 &e,
                                                                             const float &c,
                                                                             const float &d0,
-                                                                            const float &d1) const
+                                                                            const float &d1,
+                                                                            const float &d2) const
     {
-        e = ck::type_convert<F16>(c * d1 * d0);
+        e = ck::type_convert<F16>(c * d1 * d2);
     }
 
     template <>
-    __host__ __device__ constexpr void operator()<B16, float, float, float>(B16 &e,
+    __host__ __device__ constexpr void operator()<B16, float, float, float, float>(B16 &e,
                                                                             const float &c,
                                                                             const float &d0,
-                                                                            const float &d1) const
+                                                                            const float &d1,
+                                                                            const float &d2) const
     {
-        e = ck::type_convert<B16>(c * d1 * d0);
+        e = ck::type_convert<B16>(c * d1 * d2);
     }
 
     template <>
-    __host__ __device__ constexpr void operator()<F16, int, float, float>(F16 &e,
+    __host__ __device__ constexpr void operator()<F16, int, float, float, float>(F16 &e,
                                                                           const int &c,
                                                                           const float &d0,
-                                                                          const float &d1) const
+                                                                          const float &d1,
+                                                                          const float &d2) const
     {
-        e = ck::type_convert<F16>(ck::type_convert<F32>(c) * d1 * d0);
+        e = ck::type_convert<F16>(ck::type_convert<F32>(c) * d1 * d2);
     }
 
     template <>
-    __host__ __device__ constexpr void operator()<B16, int, float, float>(B16 &e,
+    __host__ __device__ constexpr void operator()<B16, int, float, float, float>(B16 &e,
                                                                           const int &c,
                                                                           const float &d0,
-                                                                          const float &d1) const
+                                                                          const float &d1,
+                                                                          const float &d2) const
     {
-        e = ck::type_convert<B16>(ck::type_convert<F32>(c) * d1 * d0);
+        e = ck::type_convert<B16>(ck::type_convert<F32>(c) * d1 * d2);
     }
 };
 
 struct MulABScaleWint4
 {
-    template <typename E, typename C, typename D0, typename D1>
+    template <typename E, typename C, typename D0, typename D1, typename D2>
     __host__ __device__ constexpr void
-    operator()(E &e, const C &c, const D0 &d0, const D1 &d1) const;
+    operator()(E &e, const C &c, const D0 &d0, const D1 &d1, const D2 &d2) const;
 
     template <>
-    __host__ __device__ constexpr void operator()<F16, float, float, float>(F16 &e,
+    __host__ __device__ constexpr void operator()<F16, float, float, float, float>(F16 &e,
                                                                             const float &c,
                                                                             const float &d0,
-                                                                            const float &d1) const
+                                                                            const float &d1,
+                                                                            const float &d2) const
     {
-        e = ck::type_convert<F16>(c * d1 * d0 * 16.f);
+        e = ck::type_convert<F16>(c * d1 * d2 * 16.f);
     }
 
     template <>
-    __host__ __device__ constexpr void operator()<B16, float, float, float>(B16 &e,
+    __host__ __device__ constexpr void operator()<B16, float, float, float, float>(B16 &e,
                                                                             const float &c,
                                                                             const float &d0,
-                                                                            const float &d1) const
+                                                                            const float &d1,
+                                                                            const float &d2) const
     {
-        e = ck::type_convert<B16>(c * d1 * d0 * 16.f);
+        e = ck::type_convert<B16>(c * d1 * d2 * 16.f);
     }
 
     template <>
-    __host__ __device__ constexpr void operator()<F16, int, float, float>(F16 &e,
+    __host__ __device__ constexpr void operator()<F16, int, float, float, float>(F16 &e,
                                                                           const int &c,
                                                                           const float &d0,
-                                                                          const float &d1) const
+                                                                          const float &d1,
+                                                                          const float &d2) const
     {
-        e = ck::type_convert<F16>(ck::type_convert<F32>(c) * d1 * d0 * 16.f);
+        e = ck::type_convert<F16>(ck::type_convert<F32>(c) * d1 * d2 * 16.f);
     }
 
     template <>
-    __host__ __device__ constexpr void operator()<B16, int, float, float>(B16 &e,
+    __host__ __device__ constexpr void operator()<B16, int, float, float, float>(B16 &e,
                                                                           const int &c,
                                                                           const float &d0,
-                                                                          const float &d1) const
+                                                                          const float &d1,
+                                                                          const float &d2) const
     {
-        e = ck::type_convert<B16>(ck::type_convert<F32>(c) * d1 * d0 * 16.f);
+        e = ck::type_convert<B16>(ck::type_convert<F32>(c) * d1 * d2 * 16.f);
     }
 };
 
@@ -292,7 +304,7 @@ struct MulABScaleExpertWeightWin4
     }
 };
 
-template <typename A0DataType, typename B0DataType, typename AccDataType, typename EDataType, typename CDEElementOp, int MPerBlock, int KPerBlock, int MWaves, int NWaves, bool Nswizzle, bool PerTensorQuant>
+template <typename A0DataType, typename B0DataType, typename AccDataType, typename EDataType, typename CDEElementOp, int MPerBlock, int KPerBlock, int MWaves, int NWaves, bool Nswizzle, bool PerTensorQuant, bool MulRoutedWeight>
 void ck_moe_stage1_gemm(const hipStream_t &stream, int tokens, int sorted_size, int N, int K,
                         int topk,
                         void *&hidden_states,                          // [m, k], input token
@@ -300,13 +312,14 @@ void ck_moe_stage1_gemm(const hipStream_t &stream, int tokens, int sorted_size,
                         void *&w2,                                     // [expert, dim, inter_dim], pre-shuffle([e, nr, kr, w])
                         void *&sorted_token_ids,                       // [max_num_tokens_padded]
                         void *&sorted_expert_ids,                      // [max_num_m_blocks]
+                        void *&sorted_weights,                         // [max_num_tokens_padded]
                         void *&num_valid_ids,                          //[1]
                         void *&out,                                    // [max_num_tokens_padded, inter_dim]
                         std::optional<void *> w1_scale = std::nullopt, // [e, 1, n], gate(up) scale
                         std::optional<void *> a1_scale = std::nullopt  // [m, 1], token scale
 );
 
-template <typename A0DataType, typename B0DataType, typename AccDataType, typename EDataType, typename CDEElementOp, int MPerBlock, int KPerBlock, int MWaves, int NWaves, bool Nswizzle, bool PerTensorQuant>
+template <typename A0DataType, typename B0DataType, typename AccDataType, typename EDataType, typename CDEElementOp, int MPerBlock, int KPerBlock, int MWaves, int NWaves, bool Nswizzle, bool PerTensorQuant, bool MulRoutedWeight>
 void ck_moe_stage2_gemm(const hipStream_t &stream, int tokens, int sorted_size, int N, int K,
                         int topk,
                         void *&inter_states,                           // [max_num_tokens_padded, k], input token
diff --git a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertensor_b16.cu b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertensor_b16.cu
index 5aae271913..670073ec37 100644
--- a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertensor_b16.cu
+++ b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertensor_b16.cu
@@ -9,6 +9,6 @@ using EDataType = B16;
 using CDEElementOp = TypeCast;
 const bool Nswizzle = false;
 const bool PerTensorQuant = true;
-CK_MOE_STAGE1_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE1_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2)
+CK_MOE_STAGE1_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE1_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, false)
\ No newline at end of file
diff --git a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertensor_b16_f8.cu b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertensor_b16_f8.cu
index f5d25e8977..d237646e88 100644
--- a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertensor_b16_f8.cu
+++ b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertensor_b16_f8.cu
@@ -9,6 +9,10 @@ using EDataType = B16;
 using CDEElementOp = MulABScale;
 const bool Nswizzle = false;
 const bool PerTensorQuant = true;
-CK_MOE_STAGE1_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE1_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2)
+CK_MOE_STAGE1_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE1_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, true)
+
+CK_MOE_STAGE1_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE1_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, false)
diff --git a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertensor_b16_f8_wint4.cu b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertensor_b16_f8_wint4.cu
index c97d5e3f34..362e1d1835 100644
--- a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertensor_b16_f8_wint4.cu
+++ b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertensor_b16_f8_wint4.cu
@@ -9,6 +9,10 @@ using EDataType = B16;
 using CDEElementOp = MulABScaleWint4;
 const bool Nswizzle = false;
 const bool PerTensorQuant = true;
-CK_MOE_STAGE1_GEMM_DEFINE(32, 128/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE1_GEMM_DEFINE(64, 128/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 1, 4)
+CK_MOE_STAGE1_GEMM_DEFINE(32, 128/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE1_GEMM_DEFINE(64, 128/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 1, 4, true)
+
+CK_MOE_STAGE1_GEMM_DEFINE(32, 128/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE1_GEMM_DEFINE(64, 128/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 1, 4, false)
diff --git a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertensor_b16_i8.cu b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertensor_b16_i8.cu
index 9b15210dce..5c0e905048 100644
--- a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertensor_b16_i8.cu
+++ b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertensor_b16_i8.cu
@@ -9,6 +9,10 @@ using EDataType = B16;
 using CDEElementOp = MulABScale;
 const bool Nswizzle = false;
 const bool PerTensorQuant = true;
-CK_MOE_STAGE1_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE1_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2)
+CK_MOE_STAGE1_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE1_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, true)
+
+CK_MOE_STAGE1_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE1_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, false)
\ No newline at end of file
diff --git a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertensor_f16.cu b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertensor_f16.cu
index cf834c7098..e100da3473 100644
--- a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertensor_f16.cu
+++ b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertensor_f16.cu
@@ -9,7 +9,6 @@ using EDataType = F16;
 using CDEElementOp = TypeCast;
 const bool Nswizzle = false;
 const bool PerTensorQuant = true;
-CK_MOE_STAGE1_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE1_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2)
-
+CK_MOE_STAGE1_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE1_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, false)
diff --git a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertensor_f16_f8.cu b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertensor_f16_f8.cu
index 218b2a5e89..e2566d1432 100644
--- a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertensor_f16_f8.cu
+++ b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertensor_f16_f8.cu
@@ -9,6 +9,10 @@ using EDataType = F16;
 using CDEElementOp = MulABScale;
 const bool Nswizzle = false;
 const bool PerTensorQuant = true;
-CK_MOE_STAGE1_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE1_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2)
+CK_MOE_STAGE1_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE1_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, true)
+
+CK_MOE_STAGE1_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE1_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, false)
diff --git a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertensor_f16_f8_win4.cu b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertensor_f16_f8_win4.cu
index 8be8a8d6ad..6e166378c1 100644
--- a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertensor_f16_f8_win4.cu
+++ b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertensor_f16_f8_win4.cu
@@ -9,6 +9,10 @@ using EDataType = F16;
 using CDEElementOp = MulABScaleWint4;
 const bool Nswizzle = false;
 const bool PerTensorQuant = true;
-CK_MOE_STAGE1_GEMM_DEFINE(32, 128/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE1_GEMM_DEFINE(64, 128/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 1, 4)
+CK_MOE_STAGE1_GEMM_DEFINE(32, 128/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE1_GEMM_DEFINE(64, 128/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 1, 4, true)
+
+CK_MOE_STAGE1_GEMM_DEFINE(32, 128/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE1_GEMM_DEFINE(64, 128/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 1, 4, false)
diff --git a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertensor_f16_i8.cu b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertensor_f16_i8.cu
index 5d06c6d9ab..4d1c2e4299 100644
--- a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertensor_f16_i8.cu
+++ b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertensor_f16_i8.cu
@@ -9,6 +9,10 @@ using EDataType = F16;
 using CDEElementOp = MulABScale;
 const bool Nswizzle = false;
 const bool PerTensorQuant = true;
-CK_MOE_STAGE1_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE1_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2)
+CK_MOE_STAGE1_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE1_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, true)
+
+CK_MOE_STAGE1_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE1_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, false)
diff --git a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertensor_mulweight_b16.cu b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertensor_mulweight_b16.cu
new file mode 100644
index 0000000000..eab8252182
--- /dev/null
+++ b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertensor_mulweight_b16.cu
@@ -0,0 +1,14 @@
+// SPDX-License-Identifier: MIT
+// Copyright (c) 2024, Advanced Micro Devices, Inc. All rights reserved.
+#include "moe_ck_gemm_common.cuh"
+
+using A0DataType = B16;
+using B0DataType = B16;
+using AccDataType = F32;
+using EDataType = B16;
+using CDEElementOp = TypeCastExpertWeight;
+const bool Nswizzle = false;
+const bool PerTensorQuant = true;
+CK_MOE_STAGE1_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE1_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, true)
diff --git a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertensor_mulweight_f16.cu b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertensor_mulweight_f16.cu
new file mode 100644
index 0000000000..046ae1c9b8
--- /dev/null
+++ b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertensor_mulweight_f16.cu
@@ -0,0 +1,15 @@
+// SPDX-License-Identifier: MIT
+// Copyright (c) 2024, Advanced Micro Devices, Inc. All rights reserved.
+#include "moe_ck_gemm_common.cuh"
+
+using A0DataType = F16;
+using B0DataType = F16;
+using AccDataType = F32;
+using EDataType = F16;
+using CDEElementOp = TypeCastExpertWeight;
+const bool Nswizzle = false;
+const bool PerTensorQuant = true;
+CK_MOE_STAGE1_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE1_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, true)
+
diff --git a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertoken_b16.cu b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertoken_b16.cu
index fa8e17bb68..222f4d4b60 100644
--- a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertoken_b16.cu
+++ b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertoken_b16.cu
@@ -9,7 +9,6 @@ using EDataType = B16;
 using CDEElementOp = TypeCast;
 const bool Nswizzle = false;
 const bool PerTensorQuant = false;
-CK_MOE_STAGE1_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE1_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2)
-
+CK_MOE_STAGE1_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE1_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, false)
diff --git a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertoken_b16_f8.cu b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertoken_b16_f8.cu
index 3ec6146180..0244052d38 100644
--- a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertoken_b16_f8.cu
+++ b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertoken_b16_f8.cu
@@ -9,7 +9,10 @@ using EDataType = B16;
 using CDEElementOp = MulABScale;
 const bool Nswizzle = false;
 const bool PerTensorQuant = false;
-CK_MOE_STAGE1_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE1_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2)
+CK_MOE_STAGE1_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE1_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, true)
 
+CK_MOE_STAGE1_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE1_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, false)
diff --git a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertoken_b16_f8_wint4.cu b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertoken_b16_f8_wint4.cu
index 06732f6542..c0f8dfd439 100644
--- a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertoken_b16_f8_wint4.cu
+++ b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertoken_b16_f8_wint4.cu
@@ -9,6 +9,10 @@ using EDataType = B16;
 using CDEElementOp = MulABScaleWint4;
 const bool Nswizzle = false;
 const bool PerTensorQuant = false;
-CK_MOE_STAGE1_GEMM_DEFINE(32, 128/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE1_GEMM_DEFINE(64, 128/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 1, 4)
+CK_MOE_STAGE1_GEMM_DEFINE(32, 128/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE1_GEMM_DEFINE(64, 128/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 1, 4, true)
+
+CK_MOE_STAGE1_GEMM_DEFINE(32, 128/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE1_GEMM_DEFINE(64, 128/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 1, 4, false)
diff --git a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertoken_b16_i8.cu b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertoken_b16_i8.cu
index 3083060624..8fb62d55d6 100644
--- a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertoken_b16_i8.cu
+++ b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertoken_b16_i8.cu
@@ -9,7 +9,10 @@ using EDataType = B16;
 using CDEElementOp = MulABScale;
 const bool Nswizzle = false;
 const bool PerTensorQuant = false;
-CK_MOE_STAGE1_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE1_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2)
+CK_MOE_STAGE1_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE1_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, true)
 
+CK_MOE_STAGE1_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE1_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, false)
diff --git a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertoken_f16.cu b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertoken_f16.cu
index d865efcd1a..2543daaf7c 100644
--- a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertoken_f16.cu
+++ b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertoken_f16.cu
@@ -9,7 +9,6 @@ using EDataType = F16;
 using CDEElementOp = TypeCast;
 const bool Nswizzle = false;
 const bool PerTensorQuant = false;
-CK_MOE_STAGE1_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE1_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2)
-
+CK_MOE_STAGE1_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE1_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, false)
diff --git a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertoken_f16_f8.cu b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertoken_f16_f8.cu
index 103fa17a76..50422a7029 100644
--- a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertoken_f16_f8.cu
+++ b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertoken_f16_f8.cu
@@ -9,7 +9,10 @@ using EDataType = F16;
 using CDEElementOp = MulABScale;
 const bool Nswizzle = false;
 const bool PerTensorQuant = false;
-CK_MOE_STAGE1_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE1_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2)
+CK_MOE_STAGE1_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE1_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, true)
 
+CK_MOE_STAGE1_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE1_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, false)
diff --git a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertoken_f16_f8_win4.cu b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertoken_f16_f8_win4.cu
index 8c5c6d9d3e..4ce5bf8535 100644
--- a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertoken_f16_f8_win4.cu
+++ b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertoken_f16_f8_win4.cu
@@ -9,6 +9,10 @@ using EDataType = F16;
 using CDEElementOp = MulABScaleWint4;
 const bool Nswizzle = false;
 const bool PerTensorQuant = false;
-CK_MOE_STAGE1_GEMM_DEFINE(32, 128/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE1_GEMM_DEFINE(64, 128/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 1, 4)
+CK_MOE_STAGE1_GEMM_DEFINE(32, 128/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE1_GEMM_DEFINE(64, 128/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 1, 4, true)
+
+CK_MOE_STAGE1_GEMM_DEFINE(32, 128/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE1_GEMM_DEFINE(64, 128/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 1, 4, false)
diff --git a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertoken_f16_i8.cu b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertoken_f16_i8.cu
index ad6d512012..26ff1f6591 100644
--- a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertoken_f16_i8.cu
+++ b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertoken_f16_i8.cu
@@ -9,7 +9,10 @@ using EDataType = F16;
 using CDEElementOp = MulABScale;
 const bool Nswizzle = false;
 const bool PerTensorQuant = false;
-CK_MOE_STAGE1_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE1_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2)
+CK_MOE_STAGE1_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE1_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, true)
 
+CK_MOE_STAGE1_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE1_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, false)
diff --git a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertoken_mulweight_b16.cu b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertoken_mulweight_b16.cu
new file mode 100644
index 0000000000..2dbbef58e5
--- /dev/null
+++ b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertoken_mulweight_b16.cu
@@ -0,0 +1,14 @@
+// SPDX-License-Identifier: MIT
+// Copyright (c) 2024, Advanced Micro Devices, Inc. All rights reserved.
+#include "moe_ck_gemm_common.cuh"
+
+using A0DataType = B16;
+using B0DataType = B16;
+using AccDataType = F32;
+using EDataType = B16;
+using CDEElementOp = TypeCastExpertWeight;
+const bool Nswizzle = false;
+const bool PerTensorQuant = false;
+CK_MOE_STAGE1_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE1_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, true)
diff --git a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertoken_mulweight_f16.cu b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertoken_mulweight_f16.cu
new file mode 100644
index 0000000000..7cd3f53cd3
--- /dev/null
+++ b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm1_instance_pertoken_mulweight_f16.cu
@@ -0,0 +1,14 @@
+// SPDX-License-Identifier: MIT
+// Copyright (c) 2024, Advanced Micro Devices, Inc. All rights reserved.
+#include "moe_ck_gemm_common.cuh"
+
+using A0DataType = F16;
+using B0DataType = F16;
+using AccDataType = F32;
+using EDataType = F16;
+using CDEElementOp = TypeCastExpertWeight;
+const bool Nswizzle = false;
+const bool PerTensorQuant = false;
+CK_MOE_STAGE1_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE1_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE1_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, true)
diff --git a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertensor_b16..cu b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertensor_b16..cu
index 14a43b8601..4b9e07b46a 100644
--- a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertensor_b16..cu
+++ b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertensor_b16..cu
@@ -6,10 +6,9 @@ using A0DataType = B16;
 using B0DataType = B16;
 using AccDataType = F32;
 using EDataType = B16;
-using CDEElementOp = TypeCastExpertWeight;
+using CDEElementOp = TypeCast;
 const bool Nswizzle = false;
 const bool PerTensorQuant = true;
-CK_MOE_STAGE2_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE2_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2)
-
+CK_MOE_STAGE2_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE2_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, false)
\ No newline at end of file
diff --git a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertensor_b16_f8.cu b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertensor_b16_f8.cu
index 6d8705a9e0..ca1f7f05f3 100644
--- a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertensor_b16_f8.cu
+++ b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertensor_b16_f8.cu
@@ -9,7 +9,10 @@ using EDataType = B16;
 using CDEElementOp = MulABScaleExpertWeight;
 const bool Nswizzle = false;
 const bool PerTensorQuant = true;
-CK_MOE_STAGE2_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE2_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2)
+CK_MOE_STAGE2_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE2_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, true)
 
+CK_MOE_STAGE2_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE2_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, false)
diff --git a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertensor_b16_f8_wint4.cu b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertensor_b16_f8_wint4.cu
index 8997d73494..55790ce1f0 100644
--- a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertensor_b16_f8_wint4.cu
+++ b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertensor_b16_f8_wint4.cu
@@ -9,6 +9,10 @@ using EDataType = B16;
 using CDEElementOp = MulABScaleExpertWeightWin4; 
 const bool Nswizzle = false;
 const bool PerTensorQuant = true;
-CK_MOE_STAGE2_GEMM_DEFINE(32, 128/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE2_GEMM_DEFINE(64, 128/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 1, 4)
+CK_MOE_STAGE2_GEMM_DEFINE(32, 128/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE2_GEMM_DEFINE(64, 128/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 1, 4, true)
+
+CK_MOE_STAGE2_GEMM_DEFINE(32, 128/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE2_GEMM_DEFINE(64, 128/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 1, 4, false)
diff --git a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertensor_b16_i8.cu b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertensor_b16_i8.cu
index feaf6157a5..995b59ac8d 100644
--- a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertensor_b16_i8.cu
+++ b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertensor_b16_i8.cu
@@ -9,7 +9,10 @@ using EDataType = B16;
 using CDEElementOp = MulABScaleExpertWeight;
 const bool Nswizzle = false;
 const bool PerTensorQuant = true;
-CK_MOE_STAGE2_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE2_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2)
+CK_MOE_STAGE2_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE2_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, true)
 
+CK_MOE_STAGE2_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE2_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, false)
\ No newline at end of file
diff --git a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertensor_f16.cu b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertensor_f16.cu
index aa5be9918e..336feaee1d 100644
--- a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertensor_f16.cu
+++ b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertensor_f16.cu
@@ -6,11 +6,9 @@ using A0DataType = F16;
 using B0DataType = F16;
 using AccDataType = F32;
 using EDataType = F16;
-using CDEElementOp = TypeCastExpertWeight;
+using CDEElementOp = TypeCast;
 const bool Nswizzle = false;
 const bool PerTensorQuant = true;
-CK_MOE_STAGE2_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE2_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2)
-
-
+CK_MOE_STAGE2_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE2_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, false)
\ No newline at end of file
diff --git a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertensor_f16_f8.cu b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertensor_f16_f8.cu
index b4f7b1ea25..082dde87cb 100644
--- a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertensor_f16_f8.cu
+++ b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertensor_f16_f8.cu
@@ -9,7 +9,10 @@ using EDataType = F16;
 using CDEElementOp = MulABScaleExpertWeight;
 const bool Nswizzle = false;
 const bool PerTensorQuant = true;
-CK_MOE_STAGE2_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE2_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2)
+CK_MOE_STAGE2_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE2_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, true)
 
+CK_MOE_STAGE2_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE2_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, false)
\ No newline at end of file
diff --git a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertensor_f16_f8_wint4.cu b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertensor_f16_f8_wint4.cu
index 6ef870d356..f8585af8cc 100644
--- a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertensor_f16_f8_wint4.cu
+++ b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertensor_f16_f8_wint4.cu
@@ -9,6 +9,10 @@ using EDataType = F16;
 using CDEElementOp = MulABScaleExpertWeightWin4;
 const bool Nswizzle = false;
 const bool PerTensorQuant = true;
-CK_MOE_STAGE2_GEMM_DEFINE(32, 128/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE2_GEMM_DEFINE(64, 128/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 1, 4)
+CK_MOE_STAGE2_GEMM_DEFINE(32, 128/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE2_GEMM_DEFINE(64, 128/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 1, 4, true)
+
+CK_MOE_STAGE2_GEMM_DEFINE(32, 128/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE2_GEMM_DEFINE(64, 128/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 1, 4, false)
diff --git a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertensor_f16_i8.cu b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertensor_f16_i8.cu
index 590f6b521d..67a4f491f2 100644
--- a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertensor_f16_i8.cu
+++ b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertensor_f16_i8.cu
@@ -9,7 +9,10 @@ using EDataType = F16;
 using CDEElementOp = MulABScaleExpertWeight;
 const bool Nswizzle = false;
 const bool PerTensorQuant = true;
-CK_MOE_STAGE2_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE2_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2)
+CK_MOE_STAGE2_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE2_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, true)
 
+CK_MOE_STAGE2_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE2_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, false)
\ No newline at end of file
diff --git a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertensor_mulweight_b16.cu b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertensor_mulweight_b16.cu
new file mode 100644
index 0000000000..8c258eabf1
--- /dev/null
+++ b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertensor_mulweight_b16.cu
@@ -0,0 +1,14 @@
+// SPDX-License-Identifier: MIT
+// Copyright (c) 2024, Advanced Micro Devices, Inc. All rights reserved.
+#include "moe_ck_gemm_common.cuh"
+
+using A0DataType = B16;
+using B0DataType = B16;
+using AccDataType = F32;
+using EDataType = B16;
+using CDEElementOp = TypeCastExpertWeight;
+const bool Nswizzle = false;
+const bool PerTensorQuant = true;
+CK_MOE_STAGE2_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE2_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, true)
diff --git a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertensor_mulweight_f16.cu b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertensor_mulweight_f16.cu
new file mode 100644
index 0000000000..82eecc5834
--- /dev/null
+++ b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertensor_mulweight_f16.cu
@@ -0,0 +1,15 @@
+// SPDX-License-Identifier: MIT
+// Copyright (c) 2024, Advanced Micro Devices, Inc. All rights reserved.
+#include "moe_ck_gemm_common.cuh"
+
+using A0DataType = F16;
+using B0DataType = F16;
+using AccDataType = F32;
+using EDataType = F16;
+using CDEElementOp = TypeCastExpertWeight;
+const bool Nswizzle = false;
+const bool PerTensorQuant = true;
+CK_MOE_STAGE2_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE2_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, true)
+
diff --git a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertoken_b16..cu b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertoken_b16..cu
index d63ab95104..5d9515ac1a 100644
--- a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertoken_b16..cu
+++ b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertoken_b16..cu
@@ -6,10 +6,9 @@ using A0DataType = B16;
 using B0DataType = B16;
 using AccDataType = F32;
 using EDataType = B16;
-using CDEElementOp = TypeCastExpertWeight;
+using CDEElementOp = TypeCast;
 const bool Nswizzle = false;
 const bool PerTensorQuant = false;
-CK_MOE_STAGE2_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE2_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2)
-
+CK_MOE_STAGE2_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE2_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, false)
\ No newline at end of file
diff --git a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertoken_b16_f8.cu b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertoken_b16_f8.cu
index 8e82b29a7b..8fb54c3b21 100644
--- a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertoken_b16_f8.cu
+++ b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertoken_b16_f8.cu
@@ -9,7 +9,10 @@ using EDataType = B16;
 using CDEElementOp = MulABScaleExpertWeight;
 const bool Nswizzle = false;
 const bool PerTensorQuant = false;
-CK_MOE_STAGE2_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE2_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2)
+CK_MOE_STAGE2_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE2_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, true)
 
+CK_MOE_STAGE2_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE2_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, false)
\ No newline at end of file
diff --git a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertoken_b16_f8_wint4.cu b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertoken_b16_f8_wint4.cu
index ef3592db07..1cdb980259 100644
--- a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertoken_b16_f8_wint4.cu
+++ b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertoken_b16_f8_wint4.cu
@@ -9,6 +9,10 @@ using EDataType = B16;
 using CDEElementOp = MulABScaleExpertWeightWin4; 
 const bool Nswizzle = false;
 const bool PerTensorQuant = false;
-CK_MOE_STAGE2_GEMM_DEFINE(32, 128/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE2_GEMM_DEFINE(64, 128/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 1, 4)
+CK_MOE_STAGE2_GEMM_DEFINE(32, 128/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE2_GEMM_DEFINE(64, 128/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 1, 4, true)
+
+CK_MOE_STAGE2_GEMM_DEFINE(32, 128/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE2_GEMM_DEFINE(64, 128/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 1, 4, false)
diff --git a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertoken_b16_i8.cu b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertoken_b16_i8.cu
index dc598bc90b..1b470f0b25 100644
--- a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertoken_b16_i8.cu
+++ b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertoken_b16_i8.cu
@@ -9,7 +9,10 @@ using EDataType = B16;
 using CDEElementOp = MulABScaleExpertWeight;
 const bool Nswizzle = false;
 const bool PerTensorQuant = false;
-CK_MOE_STAGE2_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE2_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2)
+CK_MOE_STAGE2_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE2_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, true)
 
+CK_MOE_STAGE2_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE2_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, false)
\ No newline at end of file
diff --git a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertoken_f16.cu b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertoken_f16.cu
index 28d5d3aa1f..23bbd0990e 100644
--- a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertoken_f16.cu
+++ b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertoken_f16.cu
@@ -6,10 +6,9 @@ using A0DataType = F16;
 using B0DataType = F16;
 using AccDataType = F32;
 using EDataType = F16;
-using CDEElementOp = TypeCastExpertWeight;
+using CDEElementOp = TypeCast;
 const bool Nswizzle = false;
 const bool PerTensorQuant = false;
-CK_MOE_STAGE2_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE2_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2)
-
+CK_MOE_STAGE2_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE2_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, false)
\ No newline at end of file
diff --git a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertoken_f16_f8.cu b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertoken_f16_f8.cu
index 1fbd5c7faf..658f84f05c 100644
--- a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertoken_f16_f8.cu
+++ b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertoken_f16_f8.cu
@@ -9,7 +9,10 @@ using EDataType = F16;
 using CDEElementOp = MulABScaleExpertWeight;
 const bool Nswizzle = false;
 const bool PerTensorQuant = false;
-CK_MOE_STAGE2_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE2_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2)
+CK_MOE_STAGE2_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE2_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, true)
 
+CK_MOE_STAGE2_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE2_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, false)
\ No newline at end of file
diff --git a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertoken_f16_f8_wint4.cu b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertoken_f16_f8_wint4.cu
index d4927be614..c962c5c627 100644
--- a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertoken_f16_f8_wint4.cu
+++ b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertoken_f16_f8_wint4.cu
@@ -9,6 +9,10 @@ using EDataType = F16;
 using CDEElementOp = MulABScaleExpertWeightWin4;
 const bool Nswizzle = false;
 const bool PerTensorQuant = false;
-CK_MOE_STAGE2_GEMM_DEFINE(32, 128/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE2_GEMM_DEFINE(64, 128/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 1, 4)
+CK_MOE_STAGE2_GEMM_DEFINE(32, 128/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE2_GEMM_DEFINE(64, 128/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 1, 4, true)
+
+CK_MOE_STAGE2_GEMM_DEFINE(32, 128/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE2_GEMM_DEFINE(64, 128/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 1, 4, false)
diff --git a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertoken_f16_i8.cu b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertoken_f16_i8.cu
index 8eed2d28b7..07b19c2da7 100644
--- a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertoken_f16_i8.cu
+++ b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertoken_f16_i8.cu
@@ -9,7 +9,10 @@ using EDataType = F16;
 using CDEElementOp = MulABScaleExpertWeight;
 const bool Nswizzle = false;
 const bool PerTensorQuant = false;
-CK_MOE_STAGE2_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE2_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4)
-CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2)
+CK_MOE_STAGE2_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE2_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, true)
 
+CK_MOE_STAGE2_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE2_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, false)
+CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, false)
\ No newline at end of file
diff --git a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertoken_mulweight_b16.cu b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertoken_mulweight_b16.cu
new file mode 100644
index 0000000000..aa9185e1aa
--- /dev/null
+++ b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertoken_mulweight_b16.cu
@@ -0,0 +1,14 @@
+// SPDX-License-Identifier: MIT
+// Copyright (c) 2024, Advanced Micro Devices, Inc. All rights reserved.
+#include "moe_ck_gemm_common.cuh"
+
+using A0DataType = B16;
+using B0DataType = B16;
+using AccDataType = F32;
+using EDataType = B16;
+using CDEElementOp = TypeCastExpertWeight;
+const bool Nswizzle = false;
+const bool PerTensorQuant = false;
+CK_MOE_STAGE2_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE2_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, true)
diff --git a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertoken_mulweight_f16.cu b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertoken_mulweight_f16.cu
new file mode 100644
index 0000000000..58ad613dfa
--- /dev/null
+++ b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm2_instance_pertoken_mulweight_f16.cu
@@ -0,0 +1,14 @@
+// SPDX-License-Identifier: MIT
+// Copyright (c) 2024, Advanced Micro Devices, Inc. All rights reserved.
+#include "moe_ck_gemm_common.cuh"
+
+using A0DataType = F16;
+using B0DataType = F16;
+using AccDataType = F32;
+using EDataType = F16;
+using CDEElementOp = TypeCastExpertWeight;
+const bool Nswizzle = false;
+const bool PerTensorQuant = false;
+CK_MOE_STAGE2_GEMM_DEFINE(32, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE2_GEMM_DEFINE(64, 256/sizeof(A0DataType), 1, 4, true)
+CK_MOE_STAGE2_GEMM_DEFINE(128, 128/sizeof(A0DataType), 2, 2, true)
diff --git a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm_common.cuh b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm_common.cuh
index 9d6cb5d77c..345cb0d381 100644
--- a/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm_common.cuh
+++ b/csrc/py_itfs_ck/moe_ck_2stages_gemm_impl/moe_ck_gemm_common.cuh
@@ -3,7 +3,7 @@
 #pragma once
 #include "moe_ck_gemm.hpp"
 #include <iostream>
-template <typename A0DataType, typename B0DataType, typename AccDataType, typename EDataType, typename CDEElementOp, int MPerBlock, int KPerBlock, int MWaves, int NWaves, bool Nswizzle, bool PerTensorQuant>
+template <typename A0DataType, typename B0DataType, typename AccDataType, typename EDataType, typename CDEElementOp, int MPerBlock, int KPerBlock, int MWaves, int NWaves, bool Nswizzle, bool PerTensorQuant, bool MulRoutedWeight>
 void ck_moe_stage1_gemm(const hipStream_t &stream, int tokens, int sorted_size, int N, int K,
                         int topk,
                         void *&hidden_states,           // [m, k], input token
@@ -11,6 +11,7 @@ void ck_moe_stage1_gemm(const hipStream_t &stream, int tokens, int sorted_size,
                         void *&w2,                      // [expert, dim, inter_dim], pre-shuffle([e, nr, kr, w])
                         void *&sorted_token_ids,        // [max_num_tokens_padded]
                         void *&sorted_expert_ids,       // [max_num_m_blocks]
+                        void *&sorted_weights,
                         void *&num_valid_ids,           //[1]
                         void *&out,                     // [max_num_tokens_padded, inter_dim]
                         std::optional<void *> w1_scale, // [e, 1, n], gate(up) scale
@@ -25,14 +26,15 @@ void ck_moe_stage1_gemm(const hipStream_t &stream, int tokens, int sorted_size,
     ck::index_t KBatch = 1;
     // using AccDataType = F32;
     using CShuffleDataType = F32;
-    using DsDataType = ck::Tuple<F32, F32>;
+    using DsDataType = ck::Tuple<F32, F32, F32>;
 
     using A0Layout = Row;
     using B0Layout = Col;
     using D0Layout = Row;
     using D1Layout = Col;
-    using DsLayout = ck::Tuple<D0Layout, D1Layout>;
-    using ELayout = Row;
+    using ELayout  = Row;
+    using D2Layout = ELayout;
+    using DsLayout = ck::Tuple<D0Layout, D1Layout, D2Layout>;
 
     using PassThrough = ck::tensor_operation::element_wise::PassThrough;
     using AElementOp = PassThrough;
@@ -61,6 +63,7 @@ void ck_moe_stage1_gemm(const hipStream_t &stream, int tokens, int sorted_size,
     static constexpr ck::index_t K0_N_B = BLOCKSIZE / K0_B;
     static constexpr ck::index_t D0Vec = 1;
     static constexpr ck::index_t D1Vec = PerTensorQuant ? 1 : EVec;
+    static constexpr ck::index_t D2Vec = 1;
 
     using DeviceOpInstance = ck::tensor_operation::device::DeviceMoeGemm
         // clang-format off
@@ -90,8 +93,8 @@ void ck_moe_stage1_gemm(const hipStream_t &stream, int tokens, int sorted_size,
                //    CShuffle|    CShuffle| CBlockTransferClusterLengths|  CBlockTransfer|
                //    MXdlPerWave| NXdlPerWave|         _MBlock_MWaveMPerXdl| ScalarPerVector|
                 //  PerShuffle|  PerShuffle|         _NBlock_NWaveNPerXdl|   _NWaveNPerXdl|
-               CShuffleMXDLPerWave,    CShuffleNXDLPerWave,   S<1, 32, 1, 8>, S<EVec, D0Vec, D1Vec>,
-               ck::BlockGemmPipelineScheduler::Intrawave, ck::BlockGemmPipelineVersion::v1, Nswizzle, true, A0DataType>;
+               CShuffleMXDLPerWave,    CShuffleNXDLPerWave,   S<1, 32, 1, 8>, S<EVec, D0Vec, D1Vec, D2Vec>,
+               ck::BlockGemmPipelineScheduler::Intrawave, ck::BlockGemmPipelineVersion::v1, Nswizzle, true, MulRoutedWeight, A0DataType>;
         // kernel 2: 128->32x128x128
         //  <      Row,      Col, DsLayout, ELayout, A0DataType, B0DataType, DsDataType, EDataType, AccDataType, CShuffleDataType,  AElementOp,  BElementOp, CDEElementOp,       GemmSpec,   128,   32,   128,    128,  16,  16,  32,   32,    1,    2,     S<8, 16, 1>,     S<1, 0, 2>,    S<1, 0, 2>,               2,             16,             16,          0,     S<8, 16, 1>,    S<1, 0, 2>,     S<1, 0, 2>,             2,              16,             16,          0,          1,           1,               S<1, 16, 1, 8>,      S<8, 8, 1>,  ck::BlockGemmPipelineScheduler::Interwave, ck::BlockGemmPipelineVersion::v1, EDataType>;
 
@@ -118,7 +121,8 @@ void ck_moe_stage1_gemm(const hipStream_t &stream, int tokens, int sorted_size,
                                hidden_states,
                                w1,
                                std::array<const void *, NumDTensor>{a1_scale.has_value() ? a1_scale.value() : nullptr,
-                                                                    w1_scale.has_value() ? w1_scale.value() : nullptr},
+                                                                    w1_scale.has_value() ? w1_scale.value() : nullptr,
+                                                                    MulRoutedWeight ? sorted_weights : nullptr},
                                out,
                                tokens,
                                topk,
@@ -127,7 +131,7 @@ void ck_moe_stage1_gemm(const hipStream_t &stream, int tokens, int sorted_size,
                                K,
                                StrideA,
                                StrideB,
-                               std::array<ck::index_t, NumDTensor>{DStride, DStride},
+                               std::array<ck::index_t, NumDTensor>{DStride, DStride, I0},
                                StrideE,
                                KBatch,
                                a_element_op,
@@ -144,8 +148,8 @@ void ck_moe_stage1_gemm(const hipStream_t &stream, int tokens, int sorted_size,
     invoker.Run(argument, StreamConfig{stream});
 }
 
-#define CK_MOE_STAGE1_GEMM_DEFINE(MPerfBlock, KPerBlock, MWaves, NWaves)                                                                                             \
-    template void ck_moe_stage1_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, MPerfBlock, KPerBlock, MWaves, NWaves, Nswizzle, PerTensorQuant>( \
+#define CK_MOE_STAGE1_GEMM_DEFINE(MPerfBlock, KPerBlock, MWaves, NWaves, MulRoutedWeight)                                                                                             \
+    template void ck_moe_stage1_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, MPerfBlock, KPerBlock, MWaves, NWaves, Nswizzle, PerTensorQuant, MulRoutedWeight>( \
         const hipStream_t &stream,                                                                                                                                   \
         int tokens, int sorted_size, int N, int K,                                                                                                                   \
         int topk,                                                                                                                                                    \
@@ -154,12 +158,13 @@ void ck_moe_stage1_gemm(const hipStream_t &stream, int tokens, int sorted_size,
         void *&w2,                                                                                                                                                   \
         void *&sorted_token_ids,                                                                                                                                     \
         void *&sorted_expert_ids,                                                                                                                                    \
+        void *&sorted_weights,                                                                                                                         \
         void *&num_valid_ids,                                                                                                                                        \
         void *&out,                                                                                                                                                  \
         std::optional<void *> w1_scale,                                                                                                                              \
         std::optional<void *> a1_scale);
 
-template <typename A0DataType, typename B0DataType, typename AccDataType, typename EDataType, typename CDEElementOp, int MPerBlock, int KPerBlock, int MWaves, int NWaves, bool Nswizzle, bool PerTensorQuant>
+template <typename A0DataType, typename B0DataType, typename AccDataType, typename EDataType, typename CDEElementOp, int MPerBlock, int KPerBlock, int MWaves, int NWaves, bool Nswizzle, bool PerTensorQuant, bool MulRoutedWeight>
 void ck_moe_stage2_gemm(const hipStream_t &stream, int tokens, int sorted_size, int N, int K,
                         int topk,
                         void *&inter_states,            // [max_num_tokens_padded, k], input token
@@ -248,7 +253,7 @@ void ck_moe_stage2_gemm(const hipStream_t &stream, int tokens, int sorted_size,
               //    MXdlPerWave| NXdlPerWave|         _MBlock_MWaveMPerXdl| ScalarPerVector|
                //  PerShuffle|  PerShuffle|         _NBlock_NWaveNPerXdl|   _NWaveNPerXdl|
               CShuffleMXDLPerWave,    CShuffleNXDLPerWave,   S<1, CShuffleMLane, 1, CShuffleNLane>, S<EVec, D0Vec, D1Vec, D2Vec>,
-              ck::BlockGemmPipelineScheduler::Intrawave, ck::BlockGemmPipelineVersion::v1, Nswizzle, false, A0DataType>;
+              ck::BlockGemmPipelineScheduler::Intrawave, ck::BlockGemmPipelineVersion::v1, Nswizzle, false, MulRoutedWeight, A0DataType>;
        // kernel 2: 128->32x128x128
        //  <      Row,      Col, DsLayout, ELayout, A0DataType, B0DataType, DsDataType, EDataType, AccDataType, CShuffleDataType,  AElementOp,  BElementOp, CDEElementOp,       GemmSpec,   128,   32,   128,    128,  16,  16,  32,   32,    1,    2,     S<8, 16, 1>,     S<1, 0, 2>,    S<1, 0, 2>,               2,             16,             16,          0,     S<8, 16, 1>,    S<1, 0, 2>,     S<1, 0, 2>,             2,              16,             16,          0,          1,           1,               S<1, 16, 1, 8>,      S<8, 8, 1>,  ck::BlockGemmPipelineScheduler::Interwave, ck::BlockGemmPipelineVersion::v1, EDataType>;
 
@@ -275,7 +280,7 @@ void ck_moe_stage2_gemm(const hipStream_t &stream, int tokens, int sorted_size,
                                w2,
                                std::array<const void *, NumDTensor>{a2_scale.has_value() ? a2_scale.value() : nullptr,
                                                                     w2_scale.has_value() ? w2_scale.value() : nullptr,
-                                                                    sorted_weights},
+                                                                    MulRoutedWeight ? sorted_weights : nullptr},
                                out,
                                tokens,
                                topk,
@@ -300,8 +305,8 @@ void ck_moe_stage2_gemm(const hipStream_t &stream, int tokens, int sorted_size,
     invoker.Run(argument, StreamConfig{stream});
 }
 
-#define CK_MOE_STAGE2_GEMM_DEFINE(MPerfBlock, KPerBlock, MWaves, NWaves)                                                                                             \
-    template void ck_moe_stage2_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, MPerfBlock, KPerBlock, MWaves, NWaves, Nswizzle, PerTensorQuant>( \
+#define CK_MOE_STAGE2_GEMM_DEFINE(MPerfBlock, KPerBlock, MWaves, NWaves, MulRoutedWeight)                                                                                             \
+    template void ck_moe_stage2_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, MPerfBlock, KPerBlock, MWaves, NWaves, Nswizzle, PerTensorQuant, MulRoutedWeight>( \
         const hipStream_t &stream,                                                                                                                                   \
         int tokens, int sorted_size, int N, int K,                                                                                                                   \
         int topk,                                                                                                                                                    \
diff --git a/csrc/py_itfs_ck/moe_ck_2stages_kernel.cu b/csrc/py_itfs_ck/moe_ck_2stages_kernel.cu
index c5b0ad385e..6f4c172f5a 100644
--- a/csrc/py_itfs_ck/moe_ck_2stages_kernel.cu
+++ b/csrc/py_itfs_ck/moe_ck_2stages_kernel.cu
@@ -6,45 +6,93 @@
 #include "py_itfs_common.h"
 #include "moe_ck_gemm.hpp"
 
-#define CK_MOE_STAGE1_GEMM_IMPL(A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, Nswizzle, isPerTensorQuant, MPerBlock)                                                                                                                                                                                                                                    \
-    if (isPerTensorQuant)                                                                                                                                                                                                                                                                                                                                               \
-    {                                                                                                                                                                                                                                                                                                                                                                   \
-        if (MPerBlock == 32)                                                                                                                                                                                                                                                                                                                                            \
-            ck_moe_stage1_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 32, 256 / sizeof(A0DataType), 1, 4, Nswizzle, true>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, hidden_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, num_valid_ids_ptr, out_ptr, w1_scale_ptr, a1_scale_ptr);   \
-        else if (MPerBlock == 64)                                                                                                                                                                                                                                                                                                                                       \
-            ck_moe_stage1_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 64, 256 / sizeof(A0DataType), 1, 4, Nswizzle, true>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, hidden_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, num_valid_ids_ptr, out_ptr, w1_scale_ptr, a1_scale_ptr);   \
-        else if (MPerBlock == 128)                                                                                                                                                                                                                                                                                                                                      \
-            ck_moe_stage1_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 128, 128 / sizeof(A0DataType), 2, 2, Nswizzle, true>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, hidden_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, num_valid_ids_ptr, out_ptr, w1_scale_ptr, a1_scale_ptr);  \
-    }                                                                                                                                                                                                                                                                                                                                                                   \
-    else                                                                                                                                                                                                                                                                                                                                                                \
-    {                                                                                                                                                                                                                                                                                                                                                                   \
-        if (MPerBlock == 32)                                                                                                                                                                                                                                                                                                                                            \
-            ck_moe_stage1_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 32, 256 / sizeof(A0DataType), 1, 4, Nswizzle, false>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, hidden_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, num_valid_ids_ptr, out_ptr, w1_scale_ptr, a1_scale_ptr);  \
-        else if (MPerBlock == 64)                                                                                                                                                                                                                                                                                                                                       \
-            ck_moe_stage1_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 64, 256 / sizeof(A0DataType), 1, 4, Nswizzle, false>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, hidden_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, num_valid_ids_ptr, out_ptr, w1_scale_ptr, a1_scale_ptr);  \
-        else if (MPerBlock == 128)                                                                                                                                                                                                                                                                                                                                      \
-            ck_moe_stage1_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 128, 128 / sizeof(A0DataType), 2, 2, Nswizzle, false>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, hidden_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, num_valid_ids_ptr, out_ptr, w1_scale_ptr, a1_scale_ptr); \
-    }
-
-#define CK_MOE_STAGE1_GEMM_IMPL_INT4(A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, Nswizzle, isPerTensorQuant, MPerBlock)                                                                                                                                                                                                                               \
-    if (isPerTensorQuant)                                                                                                                                                                                                                                                                                                                                               \
-    {                                                                                                                                                                                                                                                                                                                                                                   \
-        if (MPerBlock == 32)                                                                                                                                                                                                                                                                                                                                            \
-            ck_moe_stage1_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 32, 128 / sizeof(A0DataType), 1, 4, Nswizzle, true>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, hidden_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, num_valid_ids_ptr, out_ptr, w1_scale_ptr, a1_scale_ptr);   \
-        else if (MPerBlock == 64)                                                                                                                                                                                                                                                                                                                                       \
-            ck_moe_stage1_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 64, 128 / sizeof(A0DataType), 1, 4, Nswizzle, true>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, hidden_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, num_valid_ids_ptr, out_ptr, w1_scale_ptr, a1_scale_ptr);   \
-        else if (MPerBlock == 128)                                                                                                                                                                                                                                                                                                                                      \
-            ck_moe_stage1_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 128, 128 / sizeof(A0DataType), 1, 4, Nswizzle, true>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, hidden_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, num_valid_ids_ptr, out_ptr, w1_scale_ptr, a1_scale_ptr);  \
-    }                                                                                                                                                                                                                                                                                                                                                                   \
-    else                                                                                                                                                                                                                                                                                                                                                                \
-    {                                                                                                                                                                                                                                                                                                                                                                   \
-        if (MPerBlock == 32)                                                                                                                                                                                                                                                                                                                                            \
-            ck_moe_stage1_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 32, 128 / sizeof(A0DataType), 1, 4, Nswizzle, false>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, hidden_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, num_valid_ids_ptr, out_ptr, w1_scale_ptr, a1_scale_ptr);  \
-        else if (MPerBlock == 64)                                                                                                                                                                                                                                                                                                                                       \
-            ck_moe_stage1_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 64, 128 / sizeof(A0DataType), 1, 4, Nswizzle, false>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, hidden_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, num_valid_ids_ptr, out_ptr, w1_scale_ptr, a1_scale_ptr);  \
-        else if (MPerBlock == 128)                                                                                                                                                                                                                                                                                                                                      \
-            ck_moe_stage1_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 128, 128 / sizeof(A0DataType), 1, 4, Nswizzle, false>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, hidden_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, num_valid_ids_ptr, out_ptr, w1_scale_ptr, a1_scale_ptr); \
+#define CK_MOE_STAGE1_GEMM_IMPL(A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, Nswizzle, isPerTensorQuant, MPerBlock)                                                                                                                                                                                                                                        \
+    if (MulRoutedWeight)                                                                                                                                                                                                                                                                                                                                                    \
+    {                                                                                                                                                                                                                                                                                                                                                                       \
+        if (isPerTensorQuant)                                                                                                                                                                                                                                                                                                                                               \
+        {                                                                                                                                                                                                                                                                                                                                                                   \
+            if (MPerBlock == 32)                                                                                                                                                                                                                                                                                                                                            \
+                ck_moe_stage1_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 32, 256 / sizeof(A0DataType), 1, 4, Nswizzle, true, true>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, hidden_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w1_scale_ptr, a1_scale_ptr);   \
+            else if (MPerBlock == 64)                                                                                                                                                                                                                                                                                                                                       \
+                ck_moe_stage1_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 64, 256 / sizeof(A0DataType), 1, 4, Nswizzle, true, true>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, hidden_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w1_scale_ptr, a1_scale_ptr);   \
+            else if (MPerBlock == 128)                                                                                                                                                                                                                                                                                                                                      \
+                ck_moe_stage1_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 128, 128 / sizeof(A0DataType), 2, 2, Nswizzle, true, true>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, hidden_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w1_scale_ptr, a1_scale_ptr);  \
+        }                                                                                                                                                                                                                                                                                                                                                                   \
+        else                                                                                                                                                                                                                                                                                                                                                                \
+        {                                                                                                                                                                                                                                                                                                                                                                   \
+            if (MPerBlock == 32)                                                                                                                                                                                                                                                                                                                                            \
+                ck_moe_stage1_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 32, 256 / sizeof(A0DataType), 1, 4, Nswizzle, false, true>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, hidden_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w1_scale_ptr, a1_scale_ptr);  \
+            else if (MPerBlock == 64)                                                                                                                                                                                                                                                                                                                                       \
+                ck_moe_stage1_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 64, 256 / sizeof(A0DataType), 1, 4, Nswizzle, false, true>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, hidden_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w1_scale_ptr, a1_scale_ptr);  \
+            else if (MPerBlock == 128)                                                                                                                                                                                                                                                                                                                                      \
+                ck_moe_stage1_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 128, 128 / sizeof(A0DataType), 2, 2, Nswizzle, false, true>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, hidden_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w1_scale_ptr, a1_scale_ptr); \
+        }                                                                                                                                                                                                                                                                                                                                                                   \
+    }                                                                                                                                                                                                                                                                                                                                                                       \
+    else                                                                                                                                                                                                                                                                                                                                                                    \
+    {                                                                                                                                                                                                                                                                                                                                                                       \
+        if (isPerTensorQuant)                                                                                                                                                                                                                                                                                                                                               \
+        {                                                                                                                                                                                                                                                                                                                                                                   \
+            if (MPerBlock == 32)                                                                                                                                                                                                                                                                                                                                            \
+                ck_moe_stage1_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 32, 256 / sizeof(A0DataType), 1, 4, Nswizzle, true, false>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, hidden_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w1_scale_ptr, a1_scale_ptr);   \
+            else if (MPerBlock == 64)                                                                                                                                                                                                                                                                                                                                       \
+                ck_moe_stage1_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 64, 256 / sizeof(A0DataType), 1, 4, Nswizzle, true, false>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, hidden_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w1_scale_ptr, a1_scale_ptr);   \
+            else if (MPerBlock == 128)                                                                                                                                                                                                                                                                                                                                      \
+                ck_moe_stage1_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 128, 128 / sizeof(A0DataType), 2, 2, Nswizzle, true, false>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, hidden_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w1_scale_ptr, a1_scale_ptr);  \
+        }                                                                                                                                                                                                                                                                                                                                                                   \
+        else                                                                                                                                                                                                                                                                                                                                                                \
+        {                                                                                                                                                                                                                                                                                                                                                                   \
+            if (MPerBlock == 32)                                                                                                                                                                                                                                                                                                                                            \
+                ck_moe_stage1_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 32, 256 / sizeof(A0DataType), 1, 4, Nswizzle, false, false>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, hidden_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w1_scale_ptr, a1_scale_ptr);  \
+            else if (MPerBlock == 64)                                                                                                                                                                                                                                                                                                                                       \
+                ck_moe_stage1_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 64, 256 / sizeof(A0DataType), 1, 4, Nswizzle, false, false>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, hidden_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w1_scale_ptr, a1_scale_ptr);  \
+            else if (MPerBlock == 128)                                                                                                                                                                                                                                                                                                                                      \
+                ck_moe_stage1_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 128, 128 / sizeof(A0DataType), 2, 2, Nswizzle, false, false>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, hidden_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w1_scale_ptr, a1_scale_ptr); \
+        }                                                                                                                                                                                                                                                                                                                                                                   \
     }
+   
+#define CK_MOE_STAGE1_GEMM_IMPL_INT4(A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, Nswizzle, isPerTensorQuant, MPerBlock)                                                                                                                                                                                                                                   \
+    if (MulRoutedWeight)                                                                                                                                                                                                                                                                                                                                                    \
+    {                                                                                                                                                                                                                                                                                                                                                                       \
+        if (isPerTensorQuant)                                                                                                                                                                                                                                                                                                                                               \
+        {                                                                                                                                                                                                                                                                                                                                                                   \
+            if (MPerBlock == 32)                                                                                                                                                                                                                                                                                                                                            \
+                ck_moe_stage1_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 32, 128 / sizeof(A0DataType), 1, 4, Nswizzle, true, true>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, hidden_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w1_scale_ptr, a1_scale_ptr);   \
+            else if (MPerBlock == 64)                                                                                                                                                                                                                                                                                                                                       \
+                ck_moe_stage1_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 64, 128 / sizeof(A0DataType), 1, 4, Nswizzle, true, true>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, hidden_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w1_scale_ptr, a1_scale_ptr);   \
+            else if (MPerBlock == 128)                                                                                                                                                                                                                                                                                                                                      \
+                ck_moe_stage1_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 128, 128 / sizeof(A0DataType), 1, 4, Nswizzle, true, true>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, hidden_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w1_scale_ptr, a1_scale_ptr);  \
+        }                                                                                                                                                                                                                                                                                                                                                                   \
+        else                                                                                                                                                                                                                                                                                                                                                                \
+        {                                                                                                                                                                                                                                                                                                                                                                   \
+            if (MPerBlock == 32)                                                                                                                                                                                                                                                                                                                                            \
+                ck_moe_stage1_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 32, 128 / sizeof(A0DataType), 1, 4, Nswizzle, false, true>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, hidden_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w1_scale_ptr, a1_scale_ptr);  \
+            else if (MPerBlock == 64)                                                                                                                                                                                                                                                                                                                                       \
+                ck_moe_stage1_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 64, 128 / sizeof(A0DataType), 1, 4, Nswizzle, false, true>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, hidden_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w1_scale_ptr, a1_scale_ptr);  \
+            else if (MPerBlock == 128)                                                                                                                                                                                                                                                                                                                                      \
+                ck_moe_stage1_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 128, 128 / sizeof(A0DataType), 1, 4, Nswizzle, false, true>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, hidden_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w1_scale_ptr, a1_scale_ptr); \
+        }                                                                                                                                                                                                                                                                                                                                                                   \
+    }                                                                                                                                                                                                                                                                                                                                                                       \
+    else                                                                                                                                                                                                                                                                                                                                                    \
+    {                                                                                                                                                                                                                                                                                                                                                                       \
+        if (isPerTensorQuant)                                                                                                                                                                                                                                                                                                                                               \
+        {                                                                                                                                                                                                                                                                                                                                                                   \
+            if (MPerBlock == 32)                                                                                                                                                                                                                                                                                                                                            \
+                ck_moe_stage1_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 32, 128 / sizeof(A0DataType), 1, 4, Nswizzle, true, false>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, hidden_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w1_scale_ptr, a1_scale_ptr);   \
+            else if (MPerBlock == 64)                                                                                                                                                                                                                                                                                                                                       \
+                ck_moe_stage1_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 64, 128 / sizeof(A0DataType), 1, 4, Nswizzle, true, false>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, hidden_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w1_scale_ptr, a1_scale_ptr);   \
+            else if (MPerBlock == 128)                                                                                                                                                                                                                                                                                                                                      \
+                ck_moe_stage1_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 128, 128 / sizeof(A0DataType), 1, 4, Nswizzle, true, false>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, hidden_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w1_scale_ptr, a1_scale_ptr);  \
+        }                                                                                                                                                                                                                                                                                                                                                                   \
+        else                                                                                                                                                                                                                                                                                                                                                                \
+        {                                                                                                                                                                                                                                                                                                                                                                   \
+            if (MPerBlock == 32)                                                                                                                                                                                                                                                                                                                                            \
+                ck_moe_stage1_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 32, 128 / sizeof(A0DataType), 1, 4, Nswizzle, false, false>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, hidden_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w1_scale_ptr, a1_scale_ptr);  \
+            else if (MPerBlock == 64)                                                                                                                                                                                                                                                                                                                                       \
+                ck_moe_stage1_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 64, 128 / sizeof(A0DataType), 1, 4, Nswizzle, false, false>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, hidden_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w1_scale_ptr, a1_scale_ptr);  \
+            else if (MPerBlock == 128)                                                                                                                                                                                                                                                                                                                                      \
+                ck_moe_stage1_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 128, 128 / sizeof(A0DataType), 1, 4, Nswizzle, false, false>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, hidden_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w1_scale_ptr, a1_scale_ptr); \
+        }                                                                                                                                                                                                                                                                                                                                                                   \
+    }                                                                                                                                                                                                                                                                                                                                                                       \
 
 void ck_moe_stage1(torch::Tensor &hidden_states,     // [m, k], input token
                    torch::Tensor &w1,                // [e, n, k]/[e, 2*n, k], pre-shuffle([e, nr, kr, w])
@@ -56,7 +104,8 @@ void ck_moe_stage1(torch::Tensor &hidden_states,     // [m, k], input token
                    int topk,
                    std::optional<torch::Tensor> w1_scale = std::nullopt, // [e, 1, n], gate(up) scale
                    std::optional<torch::Tensor> a1_scale = std::nullopt, // [m, 1], token scale
-                   std::optional<int> block_m = 32)
+                   std::optional<int> block_m = 32,
+                   std::optional<torch::Tensor> sorted_weights = std::nullopt)    // [max_num_tokens_padded])
 {
     const at::cuda::OptionalCUDAGuard device_guard(device_of(out));
     at::cuda::getCurrentCUDAStream().stream();
@@ -75,6 +124,7 @@ void ck_moe_stage1(torch::Tensor &hidden_states,     // [m, k], input token
     // int agvtokens_per_expert = max_num_tokens_padded / E;
     int MPerBlock = block_m.value();
     bool isPerTensorQuant = (!w1_scale.has_value()) || (w1_scale.value().numel() == E);
+    bool MulRoutedWeight = sorted_weights.has_value();
 
     // int M = agvtokens_per_expert < 32 ? 32 : (agvtokens_per_expert < 64 ? 64 : 128);
 
@@ -83,9 +133,10 @@ void ck_moe_stage1(torch::Tensor &hidden_states,     // [m, k], input token
     void *w2_ptr = w2.data_ptr();
     void *sorted_token_ids_ptr = sorted_token_ids.data_ptr();
     void *sorted_expert_ids_ptr = sorted_expert_ids.data_ptr();
+    void *sorted_weights_ptr = MulRoutedWeight ? sorted_weights.value().data_ptr() : nullptr;
     void *num_valid_ids_ptr = num_valid_ids.data_ptr();
     void *out_ptr = out.data_ptr();
-    void *w1_scale_ptr = w1_scale.has_value() ? w1_scale.value().transpose(0, 1).data_ptr() : nullptr;
+    void *w1_scale_ptr = w1_scale.has_value() ? w1_scale.value().data_ptr() : nullptr;
     void *a1_scale_ptr = a1_scale.has_value() ? a1_scale.value().data_ptr() : nullptr;
 
     // BF16
@@ -95,9 +146,17 @@ void ck_moe_stage1(torch::Tensor &hidden_states,     // [m, k], input token
         using B0DataType = B16;
         using AccDataType = F32;
         using EDataType = B16;
-        using CDEElementOp = TypeCast;
         const bool Nswizzle = false;
-        CK_MOE_STAGE1_GEMM_IMPL(A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, Nswizzle, isPerTensorQuant, MPerBlock);
+        if (MulRoutedWeight) 
+        {
+            using CDEElementOp = TypeCastExpertWeight;
+            CK_MOE_STAGE1_GEMM_IMPL(A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, Nswizzle, isPerTensorQuant, MPerBlock);
+        }
+        else 
+        {
+            using CDEElementOp = TypeCast;
+            CK_MOE_STAGE1_GEMM_IMPL(A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, Nswizzle, isPerTensorQuant, MPerBlock);
+        }
     }
     // FP16
     else if (hidden_states.dtype() == at::ScalarType::Half)
@@ -106,9 +165,17 @@ void ck_moe_stage1(torch::Tensor &hidden_states,     // [m, k], input token
         using B0DataType = F16;
         using AccDataType = F32;
         using EDataType = F16;
-        using CDEElementOp = TypeCast;
         const bool Nswizzle = false;
-        CK_MOE_STAGE1_GEMM_IMPL(A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, Nswizzle, isPerTensorQuant, MPerBlock);
+        if (MulRoutedWeight) 
+        {
+            using CDEElementOp = TypeCastExpertWeight;
+            CK_MOE_STAGE1_GEMM_IMPL(A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, Nswizzle, isPerTensorQuant, MPerBlock);
+        }
+        else 
+        {
+            using CDEElementOp = TypeCast;
+            CK_MOE_STAGE1_GEMM_IMPL(A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, Nswizzle, isPerTensorQuant, MPerBlock);
+        }
     }
     // FP8 Wint4
     else if (hidden_states.dtype() == at::ScalarType::Float8_e4m3fnuz && w1.dtype() == at::ScalarType::UInt32)
@@ -176,57 +243,105 @@ void ck_moe_stage1(torch::Tensor &hidden_states,     // [m, k], input token
 }
 
 #define CK_MOE_STAGE2_GEMM_IMPL(A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, Nswizzle, isPerTensorQuant, MPerBlock)                                                                                                                                                                                                                                                       \
-    if (isPerTensorQuant)                                                                                                                                                                                                                                                                                                                                                                  \
-    {                                                                                                                                                                                                                                                                                                                                                                                      \
-        if (MPerBlock == 32)                                                                                                                                                                                                                                                                                                                                                               \
-            ck_moe_stage2_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 32, 256 / sizeof(A0DataType), 1, 4, Nswizzle, true>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, inter_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w2_scale_ptr, a2_scale_ptr);   \
-        else if (MPerBlock == 64)                                                                                                                                                                                                                                                                                                                                                          \
-            ck_moe_stage2_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 64, 256 / sizeof(A0DataType), 1, 4, Nswizzle, true>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, inter_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w2_scale_ptr, a2_scale_ptr);   \
-        else if (MPerBlock == 128)                                                                                                                                                                                                                                                                                                                                                         \
-            ck_moe_stage2_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 128, 128 / sizeof(A0DataType), 2, 2, Nswizzle, true>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, inter_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w2_scale_ptr, a2_scale_ptr);  \
-    }                                                                                                                                                                                                                                                                                                                                                                                      \
-    else                                                                                                                                                                                                                                                                                                                                                                                   \
-    {                                                                                                                                                                                                                                                                                                                                                                                      \
-        if (MPerBlock == 32)                                                                                                                                                                                                                                                                                                                                                               \
-            ck_moe_stage2_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 32, 256 / sizeof(A0DataType), 1, 4, Nswizzle, false>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, inter_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w2_scale_ptr, a2_scale_ptr);  \
-        else if (MPerBlock == 64)                                                                                                                                                                                                                                                                                                                                                          \
-            ck_moe_stage2_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 64, 256 / sizeof(A0DataType), 1, 4, Nswizzle, false>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, inter_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w2_scale_ptr, a2_scale_ptr);  \
-        else if (MPerBlock == 128)                                                                                                                                                                                                                                                                                                                                                         \
-            ck_moe_stage2_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 128, 128 / sizeof(A0DataType), 2, 2, Nswizzle, false>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, inter_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w2_scale_ptr, a2_scale_ptr); \
+    if (MulRoutedWeight)                                                                                                                                                                                                                                                                                                                                                    \
+    {                                                                                                                                                                                                                                                                                                                                                                       \
+        if (isPerTensorQuant)                                                                                                                                                                                                                                                                                                                                               \
+        {                                                                                                                                                                                                                                                                                                                                                                   \
+            if (MPerBlock == 32)                                                                                                                                                                                                                                                                                                                                            \
+                ck_moe_stage2_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 32, 256 / sizeof(A0DataType), 1, 4, Nswizzle, true, true>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, inter_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w2_scale_ptr, a2_scale_ptr);   \
+            else if (MPerBlock == 64)                                                                                                                                                                                                                                                                                                                                       \
+                ck_moe_stage2_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 64, 256 / sizeof(A0DataType), 1, 4, Nswizzle, true, true>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, inter_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w2_scale_ptr, a2_scale_ptr);   \
+            else if (MPerBlock == 128)                                                                                                                                                                                                                                                                                                                                      \
+                ck_moe_stage2_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 128, 128 / sizeof(A0DataType), 2, 2, Nswizzle, true, true>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, inter_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w2_scale_ptr, a2_scale_ptr);  \
+        }                                                                                                                                                                                                                                                                                                                                                                   \
+        else                                                                                                                                                                                                                                                                                                                                                                \
+        {                                                                                                                                                                                                                                                                                                                                                                   \
+            if (MPerBlock == 32)                                                                                                                                                                                                                                                                                                                                            \
+                ck_moe_stage2_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 32, 256 / sizeof(A0DataType), 1, 4, Nswizzle, false, true>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, inter_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w2_scale_ptr, a2_scale_ptr);  \
+            else if (MPerBlock == 64)                                                                                                                                                                                                                                                                                                                                       \
+                ck_moe_stage2_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 64, 256 / sizeof(A0DataType), 1, 4, Nswizzle, false, true>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, inter_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w2_scale_ptr, a2_scale_ptr);  \
+            else if (MPerBlock == 128)                                                                                                                                                                                                                                                                                                                                      \
+                ck_moe_stage2_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 128, 128 / sizeof(A0DataType), 2, 2, Nswizzle, false, true>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, inter_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w2_scale_ptr, a2_scale_ptr); \
+        }                                                                                                                                                                                                                                                                                                                                                                   \
+    }                                                                                                                                                                                                                                                                                                                                                                       \
+    else                                                                                                                                                                                                                                                                                                                                                                    \
+    {                                                                                                                                                                                                                                                                                                                                                                       \
+        if (isPerTensorQuant)                                                                                                                                                                                                                                                                                                                                               \
+        {                                                                                                                                                                                                                                                                                                                                                                   \
+            if (MPerBlock == 32)                                                                                                                                                                                                                                                                                                                                            \
+                ck_moe_stage2_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 32, 256 / sizeof(A0DataType), 1, 4, Nswizzle, true, false>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, inter_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w2_scale_ptr, a2_scale_ptr);   \
+            else if (MPerBlock == 64)                                                                                                                                                                                                                                                                                                                                       \
+                ck_moe_stage2_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 64, 256 / sizeof(A0DataType), 1, 4, Nswizzle, true, false>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, inter_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w2_scale_ptr, a2_scale_ptr);   \
+            else if (MPerBlock == 128)                                                                                                                                                                                                                                                                                                                                      \
+                ck_moe_stage2_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 128, 128 / sizeof(A0DataType), 2, 2, Nswizzle, true, false>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, inter_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w2_scale_ptr, a2_scale_ptr);  \
+        }                                                                                                                                                                                                                                                                                                                                                                   \
+        else                                                                                                                                                                                                                                                                                                                                                                \
+        {                                                                                                                                                                                                                                                                                                                                                                   \
+            if (MPerBlock == 32)                                                                                                                                                                                                                                                                                                                                            \
+                ck_moe_stage2_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 32, 256 / sizeof(A0DataType), 1, 4, Nswizzle, false, false>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, inter_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w2_scale_ptr, a2_scale_ptr);  \
+            else if (MPerBlock == 64)                                                                                                                                                                                                                                                                                                                                       \
+                ck_moe_stage2_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 64, 256 / sizeof(A0DataType), 1, 4, Nswizzle, false, false>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, inter_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w2_scale_ptr, a2_scale_ptr);  \
+            else if (MPerBlock == 128)                                                                                                                                                                                                                                                                                                                                      \
+                ck_moe_stage2_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 128, 128 / sizeof(A0DataType), 2, 2, Nswizzle, false, false>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, inter_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w2_scale_ptr, a2_scale_ptr); \
+        }                                                                                                                                                                                                                                                                                                                                                                   \
     }
 
 #define CK_MOE_STAGE2_GEMM_IMPL_INT4(A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, Nswizzle, isPerTensorQuant, MPerBlock)                                                                                                                                                                                                                                                  \
-    if (isPerTensorQuant)                                                                                                                                                                                                                                                                                                                                                                  \
-    {                                                                                                                                                                                                                                                                                                                                                                                      \
-        if (MPerBlock == 32)                                                                                                                                                                                                                                                                                                                                                               \
-            ck_moe_stage2_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 32, 128 / sizeof(A0DataType), 1, 4, Nswizzle, true>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, inter_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w2_scale_ptr, a2_scale_ptr);   \
-        else if (MPerBlock == 64)                                                                                                                                                                                                                                                                                                                                                          \
-            ck_moe_stage2_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 64, 128 / sizeof(A0DataType), 1, 4, Nswizzle, true>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, inter_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w2_scale_ptr, a2_scale_ptr);   \
-        else if (MPerBlock == 128)                                                                                                                                                                                                                                                                                                                                                         \
-            ck_moe_stage2_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 128, 128 / sizeof(A0DataType), 1, 4, Nswizzle, true>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, inter_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w2_scale_ptr, a2_scale_ptr);  \
-    }                                                                                                                                                                                                                                                                                                                                                                                      \
-    else                                                                                                                                                                                                                                                                                                                                                                                   \
-    {                                                                                                                                                                                                                                                                                                                                                                                      \
-        if (MPerBlock == 32)                                                                                                                                                                                                                                                                                                                                                               \
-            ck_moe_stage2_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 32, 128 / sizeof(A0DataType), 1, 4, Nswizzle, false>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, inter_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w2_scale_ptr, a2_scale_ptr);  \
-        else if (MPerBlock == 64)                                                                                                                                                                                                                                                                                                                                                          \
-            ck_moe_stage2_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 64, 128 / sizeof(A0DataType), 1, 4, Nswizzle, false>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, inter_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w2_scale_ptr, a2_scale_ptr);  \
-        else if (MPerBlock == 128)                                                                                                                                                                                                                                                                                                                                                         \
-            ck_moe_stage2_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 128, 128 / sizeof(A0DataType), 1, 4, Nswizzle, false>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, inter_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w2_scale_ptr, a2_scale_ptr); \
-    }
+    if (MulRoutedWeight)                                                                                                                                                                                                                                                                                                                                                    \
+    {                                                                                                                                                                                                                                                                                                                                                                       \
+        if (isPerTensorQuant)                                                                                                                                                                                                                                                                                                                                               \
+        {                                                                                                                                                                                                                                                                                                                                                                   \
+            if (MPerBlock == 32)                                                                                                                                                                                                                                                                                                                                            \
+                ck_moe_stage2_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 32, 128 / sizeof(A0DataType), 1, 4, Nswizzle, true, true>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, inter_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w2_scale_ptr, a2_scale_ptr);   \
+            else if (MPerBlock == 64)                                                                                                                                                                                                                                                                                                                                       \
+                ck_moe_stage2_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 64, 128 / sizeof(A0DataType), 1, 4, Nswizzle, true, true>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, inter_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w2_scale_ptr, a2_scale_ptr);   \
+            else if (MPerBlock == 128)                                                                                                                                                                                                                                                                                                                                      \
+                ck_moe_stage2_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 128, 128 / sizeof(A0DataType), 1, 4, Nswizzle, true, true>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, inter_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w2_scale_ptr, a2_scale_ptr);  \
+        }                                                                                                                                                                                                                                                                                                                                                                   \
+        else                                                                                                                                                                                                                                                                                                                                                                \
+        {                                                                                                                                                                                                                                                                                                                                                                   \
+            if (MPerBlock == 32)                                                                                                                                                                                                                                                                                                                                            \
+                ck_moe_stage2_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 32, 128 / sizeof(A0DataType), 1, 4, Nswizzle, false, true>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, inter_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w2_scale_ptr, a2_scale_ptr);  \
+            else if (MPerBlock == 64)                                                                                                                                                                                                                                                                                                                                       \
+                ck_moe_stage2_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 64, 128 / sizeof(A0DataType), 1, 4, Nswizzle, false, true>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, inter_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w2_scale_ptr, a2_scale_ptr);  \
+            else if (MPerBlock == 128)                                                                                                                                                                                                                                                                                                                                      \
+                ck_moe_stage2_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 128, 128 / sizeof(A0DataType), 1, 4, Nswizzle, false, true>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, inter_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w2_scale_ptr, a2_scale_ptr); \
+        }                                                                                                                                                                                                                                                                                                                                                                   \
+    }                                                                                                                                                                                                                                                                                                                                                                       \
+    else                                                                                                                                                                                                                                                                                                                                                    \
+    {                                                                                                                                                                                                                                                                                                                                                                       \
+        if (isPerTensorQuant)                                                                                                                                                                                                                                                                                                                                               \
+        {                                                                                                                                                                                                                                                                                                                                                                   \
+            if (MPerBlock == 32)                                                                                                                                                                                                                                                                                                                                            \
+                ck_moe_stage2_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 32, 128 / sizeof(A0DataType), 1, 4, Nswizzle, true, false>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, inter_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w2_scale_ptr, a2_scale_ptr);   \
+            else if (MPerBlock == 64)                                                                                                                                                                                                                                                                                                                                       \
+                ck_moe_stage2_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 64, 128 / sizeof(A0DataType), 1, 4, Nswizzle, true, false>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, inter_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w2_scale_ptr, a2_scale_ptr);   \
+            else if (MPerBlock == 128)                                                                                                                                                                                                                                                                                                                                      \
+                ck_moe_stage2_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 128, 128 / sizeof(A0DataType), 1, 4, Nswizzle, true, false>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, inter_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w2_scale_ptr, a2_scale_ptr);  \
+        }                                                                                                                                                                                                                                                                                                                                                                   \
+        else                                                                                                                                                                                                                                                                                                                                                                \
+        {                                                                                                                                                                                                                                                                                                                                                                   \
+            if (MPerBlock == 32)                                                                                                                                                                                                                                                                                                                                            \
+                ck_moe_stage2_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 32, 128 / sizeof(A0DataType), 1, 4, Nswizzle, false, false>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, inter_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w2_scale_ptr, a2_scale_ptr);  \
+            else if (MPerBlock == 64)                                                                                                                                                                                                                                                                                                                                       \
+                ck_moe_stage2_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 64, 128 / sizeof(A0DataType), 1, 4, Nswizzle, false, false>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, inter_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w2_scale_ptr, a2_scale_ptr);  \
+            else if (MPerBlock == 128)                                                                                                                                                                                                                                                                                                                                      \
+                ck_moe_stage2_gemm<A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, 128, 128 / sizeof(A0DataType), 1, 4, Nswizzle, false, false>(at::cuda::getCurrentCUDAStream().stream(), tokens, sorted_size, N, K, topk, inter_states_ptr, w1_ptr, w2_ptr, sorted_token_ids_ptr, sorted_expert_ids_ptr, sorted_weights_ptr, num_valid_ids_ptr, out_ptr, w2_scale_ptr, a2_scale_ptr); \
+        }                                                                                                                                                                                                                                                                                                                                                                   \
+    }  
 
 void ck_moe_stage2(torch::Tensor &inter_states,      // [m, k], input token
                    torch::Tensor &w1,                // [e, n, k]/[e, 2*n, k], pre-shuffle([e, nr, kr, w])
                    torch::Tensor &w2,                // [expert, dim, inter_dim], pre-shuffle([e, nr, kr, w])
                    torch::Tensor &sorted_token_ids,  // [max_num_tokens_padded]
                    torch::Tensor &sorted_expert_ids, // [max_num_m_blocks]
-                   torch::Tensor &sorted_weights,    // [max_num_tokens_padded]
                    torch::Tensor &num_valid_ids,     // [1]
                    torch::Tensor &out,               // [max_num_tokens_padded, inter_dim]
                    int topk,
                    std::optional<torch::Tensor> w2_scale = std::nullopt, // [e, 1, n], gate(up) scale
                    std::optional<torch::Tensor> a2_scale = std::nullopt, // [m, 1], token scale
-                   std::optional<int> block_m = 32)
+                   std::optional<int> block_m = 32,
+                   std::optional<torch::Tensor> sorted_weights = std::nullopt)    // [max_num_tokens_padded])
 {
     // TORCH_CHECK(inter_states.dtype() == w2.dtype(),
     //             "Weights and activations should both be same dtype!");
@@ -244,13 +359,15 @@ void ck_moe_stage2(torch::Tensor &inter_states,      // [m, k], input token
     int MPerBlock = block_m.value();
     // int M = agvtokens_per_expert < 32 ? 32 : (agvtokens_per_expert < 64 ? 64 : 128);
     bool isPerTensorQuant = (!w2_scale.has_value()) || (w2_scale.value().numel() == E);
+    bool MulRoutedWeight = sorted_weights.has_value();
+    
 
     void *inter_states_ptr = inter_states.data_ptr();
     void *w1_ptr = w1.data_ptr();
     void *w2_ptr = w2.data_ptr();
     void *sorted_token_ids_ptr = sorted_token_ids.data_ptr();
     void *sorted_expert_ids_ptr = sorted_expert_ids.data_ptr();
-    void *sorted_weights_ptr = sorted_weights.data_ptr();
+    void *sorted_weights_ptr = MulRoutedWeight ? sorted_weights.value().data_ptr() : nullptr;
     void *num_valid_ids_ptr = num_valid_ids.data_ptr();
     void *out_ptr = out.data_ptr();
     void *w2_scale_ptr = w2_scale.has_value() ? w2_scale.value().data_ptr() : nullptr;
@@ -263,9 +380,17 @@ void ck_moe_stage2(torch::Tensor &inter_states,      // [m, k], input token
         using B0DataType = B16;
         using AccDataType = F32;
         using EDataType = B16;
-        using CDEElementOp = TypeCastExpertWeight;
         const bool Nswizzle = false;
-        CK_MOE_STAGE2_GEMM_IMPL(A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, Nswizzle, isPerTensorQuant, MPerBlock);
+        if (MulRoutedWeight) 
+        {
+            using CDEElementOp = TypeCastExpertWeight;
+            CK_MOE_STAGE2_GEMM_IMPL(A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, Nswizzle, isPerTensorQuant, MPerBlock);
+        }
+        else 
+        {
+            using CDEElementOp = TypeCast;
+            CK_MOE_STAGE2_GEMM_IMPL(A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, Nswizzle, isPerTensorQuant, MPerBlock);
+        }
     }
     // FP16
     else if (inter_states.dtype() == at::ScalarType::Half)
@@ -274,9 +399,17 @@ void ck_moe_stage2(torch::Tensor &inter_states,      // [m, k], input token
         using B0DataType = F16;
         using AccDataType = F32;
         using EDataType = F16;
-        using CDEElementOp = TypeCastExpertWeight;
         const bool Nswizzle = false;
-        CK_MOE_STAGE2_GEMM_IMPL(A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, Nswizzle, isPerTensorQuant, MPerBlock);
+        if (MulRoutedWeight) 
+        {
+            using CDEElementOp = TypeCastExpertWeight;
+            CK_MOE_STAGE2_GEMM_IMPL(A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, Nswizzle, isPerTensorQuant, MPerBlock);
+        }
+        else 
+        {
+            using CDEElementOp = TypeCast;
+            CK_MOE_STAGE2_GEMM_IMPL(A0DataType, B0DataType, AccDataType, EDataType, CDEElementOp, Nswizzle, isPerTensorQuant, MPerBlock);
+        }
     }
     // FP8 wint4
     else if (inter_states.dtype() == at::ScalarType::Float8_e4m3fnuz && w1.dtype() == at::ScalarType::UInt32)
diff --git a/csrc/py_itfs_cu/asm_fmoe.cpp b/csrc/py_itfs_cu/asm_fmoe.cpp
index 4e3608233f..50fced9594 100755
--- a/csrc/py_itfs_cu/asm_fmoe.cpp
+++ b/csrc/py_itfs_cu/asm_fmoe.cpp
@@ -94,7 +94,7 @@ class FMoeKernel
                        torch::Tensor &w1,                // [expert, inter_dim, dim] N,K
                        torch::Tensor &w2,                // [expert, dim, inter_dim]
                        torch::Tensor &sorted_token_ids,  // [max_num_tokens_padded]
-                       torch::Tensor &sorted_weight_buf, // [max_num_tokens_padded]
+                       torch::Tensor &sorted_weights,    // [max_num_tokens_padded]
                        torch::Tensor &sorted_expert_ids, // [max_num_m_blocks]
                        torch::Tensor &num_valid_ids,     // [1]
                        uint32_t topk,                    //
@@ -155,7 +155,7 @@ class FMoeKernel
             args.ptr_SMQ = nullptr;
         }
         args.ptr_STP = sorted_token_ids.data_ptr();
-        args.ptr_SW = sorted_weight_buf.data_ptr();
+        args.ptr_SW = sorted_weights.data_ptr();
         args.ptr_SEP = sorted_expert_ids.data_ptr();
         args.dim = dim;
         args.inter_dim = inter_dim;
@@ -258,7 +258,7 @@ void fmoe(torch::Tensor &out,               // [token_cnt, dim]
           torch::Tensor &gate,              // [expert, inter_dim, dim] N,K
           torch::Tensor &down,              // [expert, dim, inter_dim]
           torch::Tensor &sorted_token_ids,  // [max_num_tokens_padded]
-          torch::Tensor &sorted_weight_buf, // [max_num_tokens_padded]
+          torch::Tensor &sorted_weights,    // [max_num_tokens_padded]
           torch::Tensor &sorted_expert_ids, // [max_num_m_blocks]
           torch::Tensor &num_valid_ids,     // [1]
           uint32_t topk                     //
@@ -283,7 +283,7 @@ void fmoe(torch::Tensor &out,               // [token_cnt, dim]
                                                 gate,
                                                 down,
                                                 sorted_token_ids,
-                                                sorted_weight_buf,
+                                                sorted_weights,
                                                 sorted_expert_ids,
                                                 num_valid_ids,
                                                 topk);
@@ -294,7 +294,7 @@ void fmoe_int8_g1u0(torch::Tensor &out,               // [token_cnt, dim]
                     torch::Tensor &gate,              // [expert, inter_dim, dim] N,K
                     torch::Tensor &down,              // [expert, dim, inter_dim]
                     torch::Tensor &sorted_token_ids,  // [max_num_tokens_padded]
-                    torch::Tensor &sorted_weight_buf, // [max_num_tokens_padded]
+                    torch::Tensor &sorted_weights,    // [max_num_tokens_padded]
                     torch::Tensor &sorted_expert_ids, // [max_num_m_blocks]
                     torch::Tensor &num_valid_ids,     // [1]
                     uint32_t topk,                    //
@@ -385,7 +385,7 @@ void fmoe_int8_g1u0(torch::Tensor &out,               // [token_cnt, dim]
                                                gate,
                                                down,
                                                sorted_token_ids,
-                                               sorted_weight_buf,
+                                               sorted_weights,
                                                sorted_expert_ids,
                                                num_valid_ids,
                                                topk,
@@ -400,7 +400,7 @@ void fmoe_g1u1(torch::Tensor &out,                            // [token_cnt, dim
                torch::Tensor &gate,                           // [expert, inter_dim*2, dim] N,K
                torch::Tensor &down,                           // [expert, dim, inter_dim]
                torch::Tensor &sorted_token_ids,               // [max_num_tokens_padded]
-               torch::Tensor &sorted_weight_buf,              // [max_num_tokens_padded]
+               torch::Tensor &sorted_weights,                 // [max_num_tokens_padded]
                torch::Tensor &sorted_expert_ids,              // [max_num_m_blocks]
                torch::Tensor &num_valid_ids,                  // [1]
                uint32_t topk,                                 //
@@ -593,7 +593,7 @@ void fmoe_g1u1(torch::Tensor &out,                            // [token_cnt, dim
                                                gate,
                                                down,
                                                sorted_token_ids,
-                                               sorted_weight_buf,
+                                               sorted_weights,
                                                sorted_expert_ids,
                                                num_valid_ids,
                                                topk,
@@ -605,19 +605,19 @@ void fmoe_g1u1(torch::Tensor &out,                            // [token_cnt, dim
 }
 
 void fmoe_g1u1_tkw1(torch::Tensor &out,                            // [token_cnt, dim]
-               torch::Tensor &input,                          // [token_cnt, dim] M,K
-               torch::Tensor &gate,                           // [expert, inter_dim*2, dim] N,K
-               torch::Tensor &down,                           // [expert, dim, inter_dim]
-               torch::Tensor &sorted_token_ids,               // [max_num_tokens_padded]
-               torch::Tensor &sorted_weight_buf,              // [max_num_tokens_padded]
-               torch::Tensor &sorted_expert_ids,              // [max_num_m_blocks]
-               torch::Tensor &num_valid_ids,                  // [1]
-               uint32_t topk,                                 //
-               torch::Tensor &input_scale,                    // [token_cnt, 1]
-               torch::Tensor &fc1_scale,                      // [expert, 1, inter_dim]
-               torch::Tensor &fc2_scale,                      // [expert, 1, dim]
-               std::optional<torch::Tensor> fc2_smooth_scale, // [expert, 1, inter_dim]
-               ActivationType activation)
+                    torch::Tensor &input,                          // [token_cnt, dim] M,K
+                    torch::Tensor &gate,                           // [expert, inter_dim*2, dim] N,K
+                    torch::Tensor &down,                           // [expert, dim, inter_dim]
+                    torch::Tensor &sorted_token_ids,               // [max_num_tokens_padded]
+                    torch::Tensor &sorted_weights,                 // [max_num_tokens_padded]
+                    torch::Tensor &sorted_expert_ids,              // [max_num_m_blocks]
+                    torch::Tensor &num_valid_ids,                  // [1]
+                    uint32_t topk,                                 //
+                    torch::Tensor &input_scale,                    // [token_cnt, 1]
+                    torch::Tensor &fc1_scale,                      // [expert, 1, inter_dim]
+                    torch::Tensor &fc2_scale,                      // [expert, 1, dim]
+                    std::optional<torch::Tensor> fc2_smooth_scale, // [expert, 1, inter_dim]
+                    ActivationType activation)
 {
     struct FMoeKernelConfig
     {
@@ -704,7 +704,7 @@ void fmoe_g1u1_tkw1(torch::Tensor &out,                            // [token_cnt
                                                gate,
                                                down,
                                                sorted_token_ids,
-                                               sorted_weight_buf,
+                                               sorted_weights,
                                                sorted_expert_ids,
                                                num_valid_ids,
                                                topk,
@@ -720,7 +720,7 @@ void fmoe_int8_g1u0_a16(torch::Tensor &out,               // [token_cnt, dim]
                         torch::Tensor &gate,              // [expert, inter_dim, dim] N,K
                         torch::Tensor &down,              // [expert, dim, inter_dim]
                         torch::Tensor &sorted_token_ids,  // [max_num_tokens_padded]
-                        torch::Tensor &sorted_weight_buf, // [max_num_tokens_padded]
+                        torch::Tensor &sorted_weights,    // [max_num_tokens_padded]
                         torch::Tensor &sorted_expert_ids, // [max_num_m_blocks]
                         torch::Tensor &num_valid_ids,     // [1]
                         uint32_t topk,                    //
@@ -736,7 +736,7 @@ void fmoe_int8_g1u0_a16(torch::Tensor &out,               // [token_cnt, dim]
                                                 gate,
                                                 down,
                                                 sorted_token_ids,
-                                                sorted_weight_buf,
+                                                sorted_weights,
                                                 sorted_expert_ids,
                                                 num_valid_ids,
                                                 topk,
@@ -752,7 +752,7 @@ void fmoe_g1u1_a16(torch::Tensor &out,               // [token_cnt, dim]
                    torch::Tensor &gate,              // [expert, inter_dim*2, dim] N,K
                    torch::Tensor &down,              // [expert, dim, inter_dim]
                    torch::Tensor &sorted_token_ids,  // [max_num_tokens_padded]
-                   torch::Tensor &sorted_weight_buf, // [max_num_tokens_padded]
+                   torch::Tensor &sorted_weights,    // [max_num_tokens_padded]
                    torch::Tensor &sorted_expert_ids, // [max_num_m_blocks]
                    torch::Tensor &num_valid_ids,     // [1]
                    uint32_t topk,                    //
@@ -798,7 +798,7 @@ void fmoe_g1u1_a16(torch::Tensor &out,               // [token_cnt, dim]
                                                      gate,
                                                      down,
                                                      sorted_token_ids,
-                                                     sorted_weight_buf,
+                                                     sorted_weights,
                                                      sorted_expert_ids,
                                                      num_valid_ids,
                                                      topk,
@@ -814,7 +814,7 @@ void fmoe_fp8_blockscale_g1u1(torch::Tensor &out,               // [token_cnt, d
                               torch::Tensor &gate,              // [expert, inter_dim*2, dim] N,K
                               torch::Tensor &down,              // [expert, dim, inter_dim]
                               torch::Tensor &sorted_token_ids,  // [max_num_tokens_padded]
-                              torch::Tensor &sorted_weight_buf, // [max_num_tokens_padded]
+                              torch::Tensor &sorted_weights,    // [max_num_tokens_padded]
                               torch::Tensor &sorted_expert_ids, // [max_num_m_blocks]
                               torch::Tensor &num_valid_ids,     // [1]
                               uint32_t topk,                    //
@@ -853,7 +853,7 @@ void fmoe_fp8_blockscale_g1u1(torch::Tensor &out,               // [token_cnt, d
                                                       gate,
                                                       down,
                                                       sorted_token_ids,
-                                                      sorted_weight_buf,
+                                                      sorted_weights,
                                                       sorted_expert_ids,
                                                       num_valid_ids,
                                                       topk,
diff --git a/csrc/py_itfs_cu/asm_moe_2stage.cpp b/csrc/py_itfs_cu/asm_moe_2stage.cpp
index 3ed4e59d8b..38a93da05d 100644
--- a/csrc/py_itfs_cu/asm_moe_2stage.cpp
+++ b/csrc/py_itfs_cu/asm_moe_2stage.cpp
@@ -55,16 +55,27 @@ struct __attribute__((packed)) KernelArgs
     p3 _p20;
     unsigned int activation;
     p3 _p21;
+    void *ptr_SW;
+    p2 _p22;
 };
 
-static CFG *get_cfg(torch::Tensor &inp, torch::Tensor &out, torch::Tensor &w1, QuantType &quant_type)
+static CFG *get_cfg(torch::Tensor &inp, torch::Tensor &out, torch::Tensor &w1, QuantType &quant_type, bool do_weight)
 {
     int E = w1.size(0);
     int dim1 = w1.size(1);
+
     if (inp.scalar_type() == at::ScalarType::Float8_e4m3fnuz &&
         w1.scalar_type() == at::ScalarType::Float8_e4m3fnuz &&
         out.scalar_type() == at::ScalarType::BFloat16 &&
-        quant_type == QuantType::per_Token)
+        quant_type == QuantType::per_Token &&
+        do_weight)
+    {
+        return &cfg_fmoe_stage1_bf16_pertokenFp8_doweight_g1u1;
+    }
+    else if (inp.scalar_type() == at::ScalarType::Float8_e4m3fnuz &&
+             w1.scalar_type() == at::ScalarType::Float8_e4m3fnuz &&
+             out.scalar_type() == at::ScalarType::BFloat16 &&
+             quant_type == QuantType::per_Token)
     {
         return &cfg_fmoe_stage1_bf16_pertokenFp8_g1u1;
     }
@@ -84,7 +95,8 @@ static CFG *get_cfg(torch::Tensor &inp, torch::Tensor &out, torch::Tensor &w1, Q
     }
     else
     {
-        TORCH_CHECK(false, "Unsupported input_type:", inp.scalar_type(), " weight_type:", w1.scalar_type(), ", out_type:", out.scalar_type(), ", quant_type:", static_cast<int>(quant_type));
+        TORCH_CHECK(false, "Unsupported input_type:", inp.scalar_type(), ", weight_type:", w1.scalar_type(),
+                    ", out_type:", out.scalar_type(), ", quant_type:", static_cast<int>(quant_type), ", do_weight:", do_weight);
     }
 };
 
@@ -98,12 +110,12 @@ std::string get_heuristic_kernel(int m_num, int N, int blockk_size, CFG *cfgs)
     uint32_t empty_cu = num_cu;
     uint32_t tg_num = 0;
     uint32_t round = 0xffffffff;
-    std::string selected;
+    std::string selected = "inter_dim = " + std::to_string(N);
 
     for (const auto &el : *cfgs)
     {
         const auto &cfg = el.second;
-        if (cfg.tile_M != blockk_size)
+        if (cfg.tile_M != blockk_size || N % cfg.tile_N != 0)
         {
             continue;
         }
@@ -142,14 +154,15 @@ void moe_stage1_g1u1(
     int ksplit = 0,
     ActivationType activation = ActivationType::Silu,
     QuantType quant_type = QuantType::No,
-    std::optional<torch::Tensor> a1_scale = std::nullopt, // [token_cnt, 1], token scale
-    std::optional<torch::Tensor> w1_scale = std::nullopt  // [expert, 1, inter_dim], gate(up) scale
+    std::optional<torch::Tensor> a1_scale = std::nullopt,      // [token_cnt, 1], token scale
+    std::optional<torch::Tensor> w1_scale = std::nullopt,      // [expert, 1, inter_dim], gate(up) scale
+    std::optional<torch::Tensor> sorted_weights = std::nullopt // [max_num_tokens_padded], do_weight==true need
 )
 {
     const at::cuda::OptionalCUDAGuard device_guard(device_of(input));
     const cudaStream_t stream = at::cuda::getCurrentCUDAStream();
 
-    CFG *config_map = get_cfg(input, out, w1, quant_type);
+    CFG *config_map = get_cfg(input, out, w1, quant_type, sorted_weights.has_value());
     static std::unordered_map<std::string, std::unique_ptr<AiterAsmKernel>> impl_ptr_map;
     int model_dim = input.size(1);
     int hidden_dim = inter_dim;
@@ -167,6 +180,8 @@ void moe_stage1_g1u1(
         const char *name = cfg.name.c_str();
         const char *co_name = cfg.co_name.c_str();
 
+        TORCH_CHECK(inter_dim % cfg.tile_N == 0, "ASM kernel " + std::string(name) + " is not supported for inter_dim = " + std::to_string(inter_dim));
+
         auto result = impl_ptr_map.emplace(name, nullptr);
         if (result.second)
         {
@@ -226,6 +241,7 @@ void moe_stage1_g1u1(
     args.topk = topk;
     args.splitk = ksplit;
     args.activation = static_cast<int>(activation);
+    args.ptr_SW = sorted_weights.has_value() ? sorted_weights.value().data_ptr() : nullptr;
 
     uint32_t k_num = 1 << ksplit;
     TORCH_CHECK(model_dim % k_num == 0, __func__, " Unsupported ksplit for model_dim:", model_dim, " k_num:", k_num);
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1.csv b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1.csv
new file mode 100644
index 0000000000..2767f10e5a
--- /dev/null
+++ b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1.csv
@@ -0,0 +1,55 @@
+tile_m,tile_n,knl_name
+144,64,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_144x64_pf3
+16,128,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_16x128_4tg_pf3
+16,128,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_16x128_4tg_pf2
+64,64,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_64x64_2tg_pf2
+144,64,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_144x64_pf2
+48,512,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_48x512_pf2
+128,128,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_128x128_pf3
+64,128,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_64x128_2tg_pf3
+112,128,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_112x128_pf3
+144,128,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_144x128_pf3
+128,64,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_128x64_pf3
+64,256,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_64x256_pf3
+64,128,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_64x128_2tg_pf2
+32,512,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_32x512_pf3
+80,256,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_80x256_pf3
+32,64,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_32x64_4tg_pf2
+96,128,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_96x128_pf3
+80,64,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_80x64_2tg_pf2
+160,64,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_160x64_pf3
+48,256,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_48x256_pf3
+80,128,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_80x128_pf3
+16,256,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_16x256_2tg_pf3
+48,128,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_48x128_2tg_pf3
+16,256,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_16x256_3tg_pf2
+80,64,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_80x64_2tg_pf3
+112,128,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_112x128_pf2
+80,256,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_80x256_pf2
+16,512,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_16x512_2tg_pf2
+128,128,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_128x128_pf2
+112,64,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_112x64_pf2
+16,64,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_16x64_6tg_pf2
+80,128,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_80x128_pf2
+48,256,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_48x256_pf2
+16,512,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_16x512_pf3
+64,64,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_64x64_2tg_pf3
+112,64,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_112x64_pf3
+160,64,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_160x64_pf2
+96,64,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_96x64_2tg_pf2
+32,128,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_32x128_3tg_pf3
+48,64,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_48x64_3tg_pf2
+64,256,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_64x256_pf2
+96,64,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_96x64_pf3
+96,128,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_96x128_pf2
+16,64,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_16x64_5tg_pf3
+128,64,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_128x64_pf2
+48,64,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_48x64_3tg_pf3
+48,512,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_48x512_pf3
+32,128,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_32x128_3tg_pf2
+32,256,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_32x256_2tg_pf2
+32,256,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_32x256_2tg_pf3
+32,64,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_32x64_4tg_pf3
+32,512,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_32x512_pf2
+48,128,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_48x128_2tg_pf2
+144,128,fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_144x128_pf2
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_112x128_pf2.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_112x128_pf2.co
new file mode 100755
index 0000000000..4ac01a9b8e
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_112x128_pf2.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_112x128_pf3.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_112x128_pf3.co
new file mode 100755
index 0000000000..a5e4e66ed9
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_112x128_pf3.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_112x64_pf2.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_112x64_pf2.co
new file mode 100755
index 0000000000..af75ec0853
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_112x64_pf2.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_112x64_pf3.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_112x64_pf3.co
new file mode 100755
index 0000000000..256de9d0e7
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_112x64_pf3.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_128x128_pf2.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_128x128_pf2.co
new file mode 100755
index 0000000000..4f9ee138cf
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_128x128_pf2.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_128x128_pf3.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_128x128_pf3.co
new file mode 100755
index 0000000000..d88dc2e394
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_128x128_pf3.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_128x64_pf2.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_128x64_pf2.co
new file mode 100755
index 0000000000..31ba611c90
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_128x64_pf2.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_128x64_pf3.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_128x64_pf3.co
new file mode 100755
index 0000000000..1adf1f0e14
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_128x64_pf3.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_144x128_pf2.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_144x128_pf2.co
new file mode 100755
index 0000000000..533ada5a28
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_144x128_pf2.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_144x128_pf3.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_144x128_pf3.co
new file mode 100755
index 0000000000..d4cfca2180
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_144x128_pf3.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_144x64_pf2.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_144x64_pf2.co
new file mode 100755
index 0000000000..437453f8f5
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_144x64_pf2.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_144x64_pf3.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_144x64_pf3.co
new file mode 100755
index 0000000000..b40e273b4a
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_144x64_pf3.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_160x64_pf2.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_160x64_pf2.co
new file mode 100755
index 0000000000..c0b4ff14ca
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_160x64_pf2.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_160x64_pf3.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_160x64_pf3.co
new file mode 100755
index 0000000000..6687290143
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_160x64_pf3.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_16x128_4tg_pf2.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_16x128_4tg_pf2.co
new file mode 100755
index 0000000000..f1c10d9f06
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_16x128_4tg_pf2.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_16x128_4tg_pf3.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_16x128_4tg_pf3.co
new file mode 100755
index 0000000000..1e3003f780
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_16x128_4tg_pf3.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_16x256_2tg_pf3.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_16x256_2tg_pf3.co
new file mode 100755
index 0000000000..6030c97ae0
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_16x256_2tg_pf3.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_16x256_3tg_pf2.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_16x256_3tg_pf2.co
new file mode 100755
index 0000000000..0757b2f67f
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_16x256_3tg_pf2.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_16x512_2tg_pf2.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_16x512_2tg_pf2.co
new file mode 100755
index 0000000000..128463edfb
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_16x512_2tg_pf2.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_16x512_pf3.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_16x512_pf3.co
new file mode 100755
index 0000000000..b283cdd2f1
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_16x512_pf3.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_16x64_5tg_pf3.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_16x64_5tg_pf3.co
new file mode 100755
index 0000000000..addf3d04d6
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_16x64_5tg_pf3.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_16x64_6tg_pf2.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_16x64_6tg_pf2.co
new file mode 100755
index 0000000000..69d72db80a
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_16x64_6tg_pf2.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_32x128_3tg_pf2.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_32x128_3tg_pf2.co
new file mode 100755
index 0000000000..3e7ae54cf8
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_32x128_3tg_pf2.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_32x128_3tg_pf3.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_32x128_3tg_pf3.co
new file mode 100755
index 0000000000..19cf9f6269
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_32x128_3tg_pf3.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_32x256_2tg_pf2.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_32x256_2tg_pf2.co
new file mode 100755
index 0000000000..449a321b67
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_32x256_2tg_pf2.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_32x256_2tg_pf3.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_32x256_2tg_pf3.co
new file mode 100755
index 0000000000..be104b5f01
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_32x256_2tg_pf3.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_32x512_pf2.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_32x512_pf2.co
new file mode 100755
index 0000000000..2e3c8c903e
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_32x512_pf2.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_32x512_pf3.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_32x512_pf3.co
new file mode 100755
index 0000000000..77fb000ffd
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_32x512_pf3.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_32x64_4tg_pf2.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_32x64_4tg_pf2.co
new file mode 100755
index 0000000000..0429793380
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_32x64_4tg_pf2.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_32x64_4tg_pf3.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_32x64_4tg_pf3.co
new file mode 100755
index 0000000000..09db9713f4
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_32x64_4tg_pf3.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_48x128_2tg_pf2.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_48x128_2tg_pf2.co
new file mode 100755
index 0000000000..87f0118d87
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_48x128_2tg_pf2.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_48x128_2tg_pf3.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_48x128_2tg_pf3.co
new file mode 100755
index 0000000000..2574a4a55e
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_48x128_2tg_pf3.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_48x256_pf2.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_48x256_pf2.co
new file mode 100755
index 0000000000..822899d99b
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_48x256_pf2.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_48x256_pf3.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_48x256_pf3.co
new file mode 100755
index 0000000000..c2526714e0
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_48x256_pf3.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_48x512_pf2.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_48x512_pf2.co
new file mode 100755
index 0000000000..2d7cda62f4
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_48x512_pf2.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_48x512_pf3.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_48x512_pf3.co
new file mode 100755
index 0000000000..8bdce16ba1
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_48x512_pf3.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_48x64_3tg_pf2.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_48x64_3tg_pf2.co
new file mode 100755
index 0000000000..1816b7ac96
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_48x64_3tg_pf2.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_48x64_3tg_pf3.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_48x64_3tg_pf3.co
new file mode 100755
index 0000000000..abc05c2ccf
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_48x64_3tg_pf3.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_64x128_2tg_pf2.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_64x128_2tg_pf2.co
new file mode 100755
index 0000000000..f43301d615
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_64x128_2tg_pf2.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_64x128_2tg_pf3.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_64x128_2tg_pf3.co
new file mode 100755
index 0000000000..ad909200e0
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_64x128_2tg_pf3.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_64x256_pf2.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_64x256_pf2.co
new file mode 100755
index 0000000000..e2523295e4
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_64x256_pf2.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_64x256_pf3.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_64x256_pf3.co
new file mode 100755
index 0000000000..1f54120bd1
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_64x256_pf3.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_64x64_2tg_pf2.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_64x64_2tg_pf2.co
new file mode 100755
index 0000000000..f26a6fa6b7
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_64x64_2tg_pf2.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_64x64_2tg_pf3.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_64x64_2tg_pf3.co
new file mode 100755
index 0000000000..816ba24a17
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_64x64_2tg_pf3.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_80x128_pf2.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_80x128_pf2.co
new file mode 100755
index 0000000000..6bcedc8b02
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_80x128_pf2.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_80x128_pf3.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_80x128_pf3.co
new file mode 100755
index 0000000000..057133bcc0
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_80x128_pf3.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_80x256_pf2.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_80x256_pf2.co
new file mode 100755
index 0000000000..717704351f
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_80x256_pf2.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_80x256_pf3.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_80x256_pf3.co
new file mode 100755
index 0000000000..3c492179ea
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_80x256_pf3.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_80x64_2tg_pf2.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_80x64_2tg_pf2.co
new file mode 100755
index 0000000000..c789874f59
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_80x64_2tg_pf2.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_80x64_2tg_pf3.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_80x64_2tg_pf3.co
new file mode 100755
index 0000000000..eed100d0fc
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_80x64_2tg_pf3.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_96x128_pf2.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_96x128_pf2.co
new file mode 100755
index 0000000000..f51c99d0ee
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_96x128_pf2.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_96x128_pf3.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_96x128_pf3.co
new file mode 100755
index 0000000000..8a85cf10d5
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_96x128_pf3.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_96x64_2tg_pf2.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_96x64_2tg_pf2.co
new file mode 100755
index 0000000000..90f14aeb08
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_96x64_2tg_pf2.co differ
diff --git a/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_96x64_pf3.co b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_96x64_pf3.co
new file mode 100755
index 0000000000..fdb64eef2b
Binary files /dev/null and b/hsa/fmoe_2stages/fmoe_stage1_bf16_pertokenFp8_doweight_g1u1_96x64_pf3.co differ
diff --git a/hsa/fmoe_2stages/tune.py b/hsa/fmoe_2stages/tune.py
index b043afc386..b7263373e0 100644
--- a/hsa/fmoe_2stages/tune.py
+++ b/hsa/fmoe_2stages/tune.py
@@ -92,6 +92,7 @@ def go(
         "q_dtype_w",
         "q_type",
         "use_g1u1",
+        "doweight_stage1"
     ]
     print(untunedf[args])
     prorfiles = []
@@ -109,6 +110,7 @@ def go(
             q_dtype_w,
             q_type,
             use_g1u1,
+            doweight_stage1
         ) = line
         dtype = eval(dtype)
         q_dtype_a = eval(q_dtype_a)
@@ -140,6 +142,7 @@ def go(
         else:
             torch_quant = aiter.get_torch_quant(q_type)
             a1_qt, a1_scale = torch_quant(input, quant_dtype=q_dtype_a)
+        del input, w1, w2, score
 
         ref = torch_moe_stage1(
             a1_qt,
@@ -152,6 +155,7 @@ def go(
             dtype=dtype,
             a1_scale=a1_scale,
             w1_scale=w1_scale,
+            doweight=doweight_stage1
         )
         if q_type == QuantType.per_128x128:
             ref, ref_scale = aiter.pertoken_quant(
@@ -173,7 +177,12 @@ def get_kernels_dict(file):
             kernel_dict = df.groupby("tile_m")["knl_name"].apply(list).to_dict()
             return kernel_dict
 
-        extraInfo = "_blockscale" if q_type == QuantType.per_128x128 else ""
+        extraInfo = ""
+        if q_type == QuantType.per_128x128:
+            extraInfo += "_blockscale"
+        if doweight_stage1:
+            extraInfo += "_doweight"
+
         if q_dtype_a == torch.int8:
             quantDtype = "Int8"
         elif q_dtype_a == torch.float8_e4m3fnuz:
@@ -201,7 +210,7 @@ def get_kernels_dict(file):
                     dtype=q_dtype_a,
                 )
 
-            if use_g1u1 and dtype == torch.bfloat16 and q_dtype_w != torch.int4:
+            if use_g1u1 and q_dtype_w != torch.int4:
                 for el in asm_kernels.get(blockM, []):
                     tasks.append(
                         (
@@ -214,7 +223,7 @@ def get_kernels_dict(file):
                                 sorted_ids,
                                 sorted_expert_ids,
                                 num_valid_ids,
-                                out,
+                                out.view(torch.bfloat16),
                                 blockM,
                                 el,
                                 0,
@@ -226,6 +235,7 @@ def get_kernels_dict(file):
                                     else a1_scale
                                 ),
                                 w1_scale,
+                                sorted_weights if doweight_stage1 else None,
                             ),
                         )
                     )
@@ -256,6 +266,7 @@ def get_kernels_dict(file):
                             act_type,
                             a1_scale,
                             w1_scale,
+                            sorted_weights if doweight_stage1 else None,
                         ),
                     )
                 )
@@ -266,6 +277,8 @@ def get_kernels_dict(file):
 
         profileDF = []
         for (tag, block_m), us, _ in rets:
+            if us == float("inf"):
+                continue
             if q_type == QuantType.per_128x128:
                 scale = (
                     _[token:, ...]
@@ -275,7 +288,7 @@ def get_kernels_dict(file):
                 )
                 _ = _[:token, :, :].to(torch.float32)
             err = checkAllclose(
-                ref.to("cpu"), _, msg=f"[{tag:<50}]: {us:.2f}us ......      "
+                ref.to("cpu"), _.to(ref.dtype), msg=f"[{tag:<50}]: {us:.2f}us ......      "
             )
             profileDF.append(
                 [
@@ -290,6 +303,7 @@ def get_kernels_dict(file):
                     q_dtype_w if q_dtype_w != torch.int4 else "torch.int4",
                     q_type,
                     use_g1u1,
+                    doweight_stage1,
                     block_m,
                     0,
                     us,
@@ -350,27 +364,42 @@ def get_kernels_dict(file):
         help="All the kernels are tuned, if not, only kernels that are not in the tuned_fmoe.csv are tuned",
     )
 
+    parser.add_argument(
+        "--last",
+        action="store_true",
+        required=False,
+        help="Only last kernel is tuned, if not, only kernels that are not in the tuned_fmoe.csv are tuned",
+    )
+
     args = parser.parse_args()
     untunedf = pd.read_csv(args.untune_file)
-    untunedf = untunedf.drop_duplicates()
-    if not args.all:
+    untunedf = untunedf.drop_duplicates(keep="last")
+    
+    if not args.all or args.last:
         if os.path.exists(args.tune_file):
             old_tunedf = pd.read_csv(args.tune_file)
-            untunedf_cols = untunedf.columns
-            mask = untunedf.apply(tuple, axis=1).isin(
-                old_tunedf[untunedf_cols].apply(tuple, axis=1)
-            )
-            untunedf = untunedf[~mask]
         else:
             old_tunedf = None
     else:
         old_tunedf = None
+
+    if args.last:
+        untunedf = untunedf.iloc[-1:]
+    elif old_tunedf is not None and not args.all:
+        untunedf_cols = untunedf.columns
+        mask = untunedf.apply(tuple, axis=1).isin(
+            old_tunedf[untunedf_cols].apply(tuple, axis=1)
+        )
+        untunedf = untunedf[~mask]
+    
     tunedf = None
     # tunedf = pd.read_csv(args.tune_file)
     profiles, tunedf = go(untunedf, tunedf)
     if old_tunedf is not None and tunedf is not None:
         tunedf = pd.concat([old_tunedf, tunedf], axis=0)
     if tunedf is not None:
+        tunedf = tunedf.astype(str).drop_duplicates(subset=["token","model_dim","inter_dim","expert","topk","act_type","dtype",
+                                                "q_dtype_a","q_dtype_w","q_type","use_g1u1","doweight_stage1"], keep="last")
         tunedf.to_csv(args.tune_file, index=False)
     if profiles is not None:
         profiles.to_csv(args.profile_file, index=False)
diff --git a/op_tests/test_moe_2stage.py b/op_tests/test_moe_2stage.py
index e4a55b0a6f..e4579cf7f2 100644
--- a/op_tests/test_moe_2stage.py
+++ b/op_tests/test_moe_2stage.py
@@ -3,7 +3,7 @@
 
 import torch
 import torch.nn.functional as F
-import triton.language as tl
+import math
 import sys
 import os
 from typing import Any, Callable, Dict, Optional, Tuple
@@ -68,13 +68,13 @@ def ck_moe_stage2(
         w2,
         sorted_token_ids,
         sorted_expert_ids,
-        sorted_weights,
         num_valid_ids,
         out,
         topk,
         w2_scale,
         a2_scale,
         block_size,
+        sorted_weights,
     )
     return out
 
@@ -92,10 +92,11 @@ def test_fmoe(
     AQDType,
     WQDType,
     use_g1u1=False,
+    doweight_stage1=False,
 ):
     torch_quant = aiter.get_torch_quant(qType)
     torch_act = aiter.get_torch_act(actType)
-    input = torch.randn((token, model_dim), dtype=dtype)
+    input = torch.randn((token, model_dim), dtype=dtype) / math.sqrt(inter_dim)
     if use_g1u1:
         w1 = torch.randn((E, inter_dim * 2, model_dim), dtype=dtype)
     else:
@@ -138,6 +139,7 @@ def test_fmoe(
         a1_scale=a1_scale,
         w1_scale=w1_scale,
         num_iters=3,
+        doweight=doweight_stage1,
     )
 
     if WQDType == torch.int4:  # int4 w quant
@@ -217,6 +219,7 @@ def test_fmoe(
         w2_scale=w2_scale,
         a2_scale=a2_scale,
         num_iters=3,
+        doweight=not doweight_stage1,
     )
     # # out_ref = torch_moe(
     # #     input,
@@ -257,6 +260,7 @@ def test_fmoe(
     # ######################## stage 2 end ###########
 
     # ######################## fused 2 stage #########
+
     out2_aiter, us_fuse = run_perftest(
         fused_moe,
         input,
@@ -268,6 +272,7 @@ def test_fmoe(
         w2_scale=w2_scale,
         quant_type=qType,
         activation=actType,
+        doweight_stage1=doweight_stage1,
     )
 
     err = checkAllclose(
@@ -278,7 +283,7 @@ def test_fmoe(
     return {"us": us_fuse, "err": err}
 
 
-list_dtype = [torch.bfloat16]
+list_dtype = [torch.bfloat16, torch.float16][:1]
 list_dim = [(6144, 4096)]
 list_tokenNum = [
     1,
@@ -297,9 +302,10 @@ def test_fmoe(
     (aiter.QuantType.No, None, None),  # a16w16
     (aiter.QuantType.per_Tensor, torch.float8_e4m3fnuz, torch.float8_e4m3fnuz),  # a8w8
     (aiter.QuantType.per_Token, torch.float8_e4m3fnuz, torch.float8_e4m3fnuz),  # a8w8
-    # (aiter.QuantType.per_Token, torch.float8_e4m3fnuz, torch.int4),  # a8w4
+    (aiter.QuantType.per_Token, torch.float8_e4m3fnuz, torch.int4),  # a8w4
 ]
 list_act = [aiter.ActivationType.Silu, aiter.ActivationType.Gelu][:1]
+list_doweight_stage1 = [False, True]
 expert, topk = 8, 2
 
 import pandas as pd
@@ -309,7 +315,8 @@ def test_fmoe(
     act_type,
     (quant_type, aq_dtype, wq_dtype),
     (model_dim, inter_dim),
-) in itertools.product(list_dtype, list_act, list_quant, list_dim):
+    doweight_stage1
+) in itertools.product(list_dtype, list_act, list_quant, list_dim, list_doweight_stage1):
     df = []
     for m in list_tokenNum:
         ret = test_fmoe(
@@ -324,6 +331,7 @@ def test_fmoe(
             aq_dtype,
             wq_dtype,
             use_g1u1=True,
+            doweight_stage1=doweight_stage1,
         )
         df.append(ret)
     df = pd.DataFrame(df)
diff --git a/op_tests/test_moe_blockscale.py b/op_tests/test_moe_blockscale.py
index acd9eb29c9..ce80fda2f5 100644
--- a/op_tests/test_moe_blockscale.py
+++ b/op_tests/test_moe_blockscale.py
@@ -136,7 +136,7 @@ def asm_moe_test(
     model_dim = hidden_states.shape[-1]
     topk = topk_ids.shape[-1]
     E = w1.shape[0]
-    sorted_token_ids, sorted_weight_buf, sorted_expert_ids, num_valid_ids, out_asm = (
+    sorted_token_ids, sorted_weights, sorted_expert_ids, num_valid_ids, out_asm = (
         moe_sorting(topk_ids, topk_weights, E, model_dim, dtype)
     )
     scale_blk_n, scale_blk_k = scale_blk
@@ -146,7 +146,7 @@ def asm_moe_test(
         w1,
         w2,
         sorted_token_ids,
-        sorted_weight_buf,
+        sorted_weights,
         sorted_expert_ids,
         num_valid_ids,
         topk,
