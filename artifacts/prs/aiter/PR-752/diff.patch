diff --git a/op_tests/test_batched_gemm_a8w8.py b/op_tests/test_batched_gemm_a8w8.py
index cb1c58e221..3ef2fef3cd 100644
--- a/op_tests/test_batched_gemm_a8w8.py
+++ b/op_tests/test_batched_gemm_a8w8.py
@@ -99,13 +99,15 @@ def test_gemm(dtype, b, m, n, k):
     e.g.: -b 16""",
 )
 parser.add_argument(
-    "-mnk",
+    "-s",
+    "--mnk",
     type=dtypes.str2tuple,
     nargs="?",
     const=None,
     default=None,
-    help="""shape of mnk.
-    e.g. -mnk 1280,8192,1024""",
+    help="""Shape of mnk.
+    e.g.:   -s 1024,8192,1024
+            --mnk 1024,8192,1024""",
 )
 
 args = parser.parse_args()
@@ -114,9 +116,9 @@ def test_gemm(dtype, b, m, n, k):
 else:
     l_dtype = [dtypes.d_dtypes[args.dtype]]
 if args.batch is not None:
-    l_b = [args.bitch]
+    l_b = [args.batch]
 if args.mnk is not None:
-    l_mnk = [args.m]
+    l_mnk = [args.mnk]
 
 
 for dtype in l_dtype:
diff --git a/op_tests/test_batched_gemm_bf16.py b/op_tests/test_batched_gemm_bf16.py
index 92c5295314..74331f8793 100644
--- a/op_tests/test_batched_gemm_bf16.py
+++ b/op_tests/test_batched_gemm_bf16.py
@@ -97,13 +97,15 @@ def test_gemm(dtype, b, m, n, k):
     e.g.: -b 16""",
 )
 parser.add_argument(
-    "-mnk",
+    "-s",
+    "--mnk",
     type=dtypes.str2tuple,
     nargs="?",
     const=None,
     default=None,
-    help="""shape of mnk.
-    e.g. -mnk 1280,8192,1024""",
+    help="""Shape of mnk.
+    e.g.    -s 1024,8192,1024
+            --mnk 1024,8192,1024""",
 )
 args = parser.parse_args()
 if args.dtype is None:
@@ -111,9 +113,9 @@ def test_gemm(dtype, b, m, n, k):
 else:
     l_dtype = [dtypes.d_dtypes[args.dtype]]
 if args.batch is not None:
-    l_b = [args.bitch]
+    l_b = [args.batch]
 if args.mnk is not None:
-    l_mnk = [args.m]
+    l_mnk = [args.mnk]
 
 
 for dtype in l_dtype:
diff --git a/op_tests/test_gemm_a4w4.py b/op_tests/test_gemm_a4w4.py
index 3e0a1ffec7..8c3f12ef17 100644
--- a/op_tests/test_gemm_a4w4.py
+++ b/op_tests/test_gemm_a4w4.py
@@ -271,7 +271,7 @@ def test_gemm(dtype, M, N, K):
     nargs="?",
     const=None,
     default=None,
-    help="""shape of mnk.
+    help="""Shape of mnk.
     e.g. -mnk 1280,8192,1024""",
 )
 
diff --git a/op_tests/test_gemm_a8w8.py b/op_tests/test_gemm_a8w8.py
index bdfd59f3e8..d3e8eff88d 100644
--- a/op_tests/test_gemm_a8w8.py
+++ b/op_tests/test_gemm_a8w8.py
@@ -362,7 +362,7 @@ def test_skinny_gemm_a8w8_pertoken_quant():
     nargs="?",
     const=None,
     default=None,
-    help="""shape of mnk.
+    help="""Shape of mnk.
     e.g. -mnk 1280,8192,1024""",
 )
 
diff --git a/op_tests/test_kvcache_blockscale.py b/op_tests/test_kvcache_blockscale.py
index d18709a279..8c42d9c8e4 100644
--- a/op_tests/test_kvcache_blockscale.py
+++ b/op_tests/test_kvcache_blockscale.py
@@ -668,7 +668,7 @@ def test_reshape_and_cache(
         "bf16tofp8_asm_pa128",
         "bf16tofp8_asm_pa256",
     ],
-    help="""select which test to run, default is all
+    help="""Select which test to run, default is all
     e.g.: -t fp16tofp8""",
 )
 args = parser.parse_args()
diff --git a/op_tests/test_moe.py b/op_tests/test_moe.py
index afb0a50f83..ee4bb8a987 100755
--- a/op_tests/test_moe.py
+++ b/op_tests/test_moe.py
@@ -351,6 +351,15 @@ def calculateTensorsSize(*args):
     help="""Token Num.
     e.g.: -m 128""",
 )
+parser.add_argument(
+    "-hd",
+    "--hidden_dim",
+    type=int,
+    nargs="*",
+    default=None,
+    help="""Hidden states dim.
+    e.g.: -hd 4096""",
+)
 parser.add_argument(
     "-id",
     "--inter_dim",
@@ -358,16 +367,25 @@ def calculateTensorsSize(*args):
     nargs="*",
     default=None,
     help="""Intermediate dim.
-    e.g.: -id 4096""",
+    e.g.: -id 1024""",
 )
 parser.add_argument(
-    "-hd",
-    "--hidden_dim",
+    "-e",
+    "--expert",
     type=int,
-    nargs="*",
+    nargs="?",
     default=None,
-    help="""Hidden states dim.
-    e.g.: -hd 1024""",
+    help="""Number of experts.
+    e.g.: -e 32""",
+)
+parser.add_argument(
+    "-k",
+    "--topk",
+    type=int,
+    nargs="?",
+    default=None,
+    help="""Top-k value.
+    e.g.: -k 5""",
 )
 
 args = parser.parse_args()
@@ -384,10 +402,13 @@ def calculateTensorsSize(*args):
             else [dtypes.d_dtypes[args.dtype]]
         ):
             for m in [128, 256] if args.token is None else args.token:
-                for dim in [4096, 8192] if args.inter_dim is None else args.inter_dim:
-                    for hdim in [1024] if args.hidden_dim is None else args.hidden_dim:
-                        # test_fmoe(dtype, m, dim, hdim, 32, 5)
-                        test_fmoe(dtype, m, dim, hdim, 32, 5, quant="No")
+                for hdim in (
+                    [4096, 8192] if args.hidden_dim is None else args.hidden_dim
+                ):
+                    for idim in [1024] if args.inter_dim is None else args.inter_dim:
+                        expert = 32 if args.expert is None else args.expert
+                        topk = 5 if args.topk is None else args.topk
+                        test_fmoe(dtype, m, hdim, idim, expert, topk, quant="No")
     elif test == "g1u1_no_quant":
         for dtype in (
             [dtypes.fp16, dtypes.bf16]
@@ -395,24 +416,40 @@ def calculateTensorsSize(*args):
             else [dtypes.d_dtypes[args.dtype]]
         ):
             for m in [128, 256] if args.token is None else args.token:
-                for dim in [4096, 8192] if args.inter_dim is None else args.inter_dim:
-                    for hdim in [1024] if args.hidden_dim is None else args.hidden_dim:
-                        # test_fmoe(dtype, m, dim, hdim, 32, 5)
-                        test_fmoe(dtype, m, dim, hdim, 32, 5, quant="No", use_g1u1=True)
+                for hdim in (
+                    [4096, 8192] if args.hidden_dim is None else args.hidden_dim
+                ):
+                    for idim in [1024] if args.inter_dim is None else args.inter_dim:
+                        expert = 32 if args.expert is None else args.expert
+                        topk = 5 if args.topk is None else args.topk
+                        test_fmoe(
+                            dtype,
+                            m,
+                            hdim,
+                            idim,
+                            expert,
+                            topk,
+                            quant="No",
+                            use_g1u1=True,
+                        )
     elif test == "g1u1_int8quant":
         for dtype in (
             [dtypes.bf16] if args.dtype is None else [dtypes.d_dtypes[args.dtype]]
         ):
             for m in [128, 256] if args.token is None else args.token:
-                for dim in [4096, 8192] if args.inter_dim is None else args.inter_dim:
-                    for hdim in [1024] if args.hidden_dim is None else args.hidden_dim:
+                for hdim in (
+                    [4096, 8192] if args.hidden_dim is None else args.hidden_dim
+                ):
+                    for idim in [1024] if args.inter_dim is None else args.inter_dim:
+                        expert = 32 if args.expert is None else args.expert
+                        topk = 5 if args.topk is None else args.topk
                         test_fmoe(
                             dtype,
                             m,
-                            dim,
                             hdim,
-                            32,
-                            5,
+                            idim,
+                            expert,
+                            topk,
                             #   quant='int8quant', use_g1u1=True, shared_E=0, activation=ActivationType.Gelu)
                             quant="int8quant",
                             use_g1u1=True,
@@ -423,15 +460,19 @@ def calculateTensorsSize(*args):
             [dtypes.bf16] if args.dtype is None else [dtypes.d_dtypes[args.dtype]]
         ):
             for m in [128, 256] if args.token is None else args.token:
-                for dim in [4096, 8192] if args.inter_dim is None else args.inter_dim:
-                    for hdim in [1024] if args.hidden_dim is None else args.hidden_dim:
+                for hdim in (
+                    [4096, 8192] if args.hidden_dim is None else args.hidden_dim
+                ):
+                    for idim in [1024] if args.inter_dim is None else args.inter_dim:
+                        expert = 32 if args.expert is None else args.expert
+                        topk = 5 if args.topk is None else args.topk
                         test_fmoe(
                             dtype,
                             m,
-                            dim,
                             hdim,
-                            32,
-                            5,
+                            idim,
+                            expert,
+                            topk,
                             quant="fp8quant",
                             use_g1u1=True,
                             shared_E=0,
@@ -444,19 +485,21 @@ def calculateTensorsSize(*args):
             [dtypes.bf16] if args.dtype is None else [dtypes.d_dtypes[args.dtype]]
         ):
             for m in [128] if args.token is None else args.token:
-                for dim in (
-                    [4096, 6144, 8192] if args.inter_dim is None else args.inter_dim
+                for hdim in (
+                    [4096, 6144, 8192] if args.hidden_dim is None else args.hidden_dim
                 ):
-                    for hdim in (
-                        [512, 1024] if args.hidden_dim is None else args.hidden_dim
+                    for idim in (
+                        [512, 1024] if args.inter_dim is None else args.inter_dim
                     ):
+                        expert = 32 if args.expert is None else args.expert
+                        topk = 5 if args.topk is None else args.topk
                         test_fmoe(
                             dtype,
                             m,
-                            dim,
                             hdim,
-                            32,
-                            5,
+                            idim,
+                            expert,
+                            topk,
                             quant="int8smoothquant",
                             use_g1u1=False,
                         )
@@ -466,21 +509,23 @@ def calculateTensorsSize(*args):
             [dtypes.bf16] if args.dtype is None else [dtypes.d_dtypes[args.dtype]]
         ):
             for m in [128] if args.token is None else args.token:
-                for dim in (
-                    [4096, 6144, 8192] if args.inter_dim is None else args.inter_dim
+                for hdim in (
+                    [4096, 6144, 8192] if args.hidden_dim is None else args.hidden_dim
                 ):
-                    for hdim in (
+                    for idim in (
                         [512, 1024, 1280, 1536]
-                        if args.hidden_dim is None
-                        else args.hidden_dim
+                        if args.inter_dim is None
+                        else args.inter_dim
                     ):
+                        expert = 32 if args.expert is None else args.expert
+                        topk = 5 if args.topk is None else args.topk
                         test_fmoe(
                             dtype,
                             m,
-                            dim,
                             hdim,
-                            32,
-                            5,
+                            idim,
+                            expert,
+                            topk,
                             quant="int8smoothquant",
                             use_g1u1=True,
                         )
@@ -490,21 +535,21 @@ def calculateTensorsSize(*args):
             [dtypes.bf16] if args.dtype is None else [dtypes.d_dtypes[args.dtype]]
         ):
             for m in [128] if args.token is None else args.token:
-                for dim in (
-                    [4096, 6144, 8192] if args.inter_dim is None else args.inter_dim
+                for hdim in (
+                    [4096, 6144, 8192] if args.hidden_dim is None else args.hidden_dim
                 ):
-                    for hdim in (
-                        [512, 1024, 1280]
-                        if args.hidden_dim is None
-                        else args.hidden_dim
+                    for idim in (
+                        [512, 1024, 1280] if args.inter_dim is None else args.inter_dim
                     ):
+                        expert = 32 if args.expert is None else args.expert
+                        topk = 5 if args.topk is None else args.topk
                         test_fmoe(
                             dtype,
                             m,
-                            dim,
                             hdim,
-                            32,
-                            5,
+                            idim,
+                            expert,
+                            topk,
                             quant="fp8smoothquant",
                             use_g1u1=True,
                         )
@@ -513,17 +558,21 @@ def calculateTensorsSize(*args):
             [dtypes.bf16] if args.dtype is None else [dtypes.d_dtypes[args.dtype]]
         ):
             for m in [32, 128] if args.token is None else args.token:
-                for dim in [4096, 6144] if args.inter_dim is None else args.inter_dim:
-                    for hdim in (
-                        [1024, 4096] if args.hidden_dim is None else args.hidden_dim
+                for hdim in (
+                    [4096, 6144] if args.hidden_dim is None else args.hidden_dim
+                ):
+                    for idim in (
+                        [1024, 4096] if args.inter_dim is None else args.inter_dim
                     ):
+                        expert = 8 if args.expert is None else args.expert
+                        topk = 3 if args.topk is None else args.topk
                         test_fmoe(
                             dtype,
                             m,
-                            dim,
                             hdim,
-                            8,
-                            3,
+                            idim,
+                            expert,
+                            topk,
                             quant="wint4afp8smoothquant",
                             use_g1u1=True,
                         )
diff --git a/op_tests/test_moe_blockscale.py b/op_tests/test_moe_blockscale.py
index 493e4a7a06..bbfb29a445 100644
--- a/op_tests/test_moe_blockscale.py
+++ b/op_tests/test_moe_blockscale.py
@@ -331,6 +331,22 @@ def test_fmoe(
     help="""Intermediate dimension. Default is 256.
     e.g.: -idim 256""",
 )
+parser.add_argument(
+    "-e",
+    "--expert",
+    type=int,
+    default=256,
+    help="""Number of experts. Default is 256.
+    e.g.: -e 256""",
+)
+parser.add_argument(
+    "-k",
+    "--topk",
+    type=int,
+    default=8,
+    help="""Top-k value. Default is 8.
+    e.g.: -k 8""",
+)
 
 args = parser.parse_args()
 if args.dtype is None:
@@ -347,5 +363,13 @@ def test_fmoe(
             for idim in [args.idim]:
                 scale_blks = (128, 128)
                 test_fmoe(
-                    dtype, m, dim, idim, scale_blks, 256, 8, quant="No", use_g1u1=True
+                    dtype,
+                    m,
+                    dim,
+                    idim,
+                    scale_blks,
+                    args.expert,
+                    args.topk,
+                    quant="No",
+                    use_g1u1=True,
                 )
