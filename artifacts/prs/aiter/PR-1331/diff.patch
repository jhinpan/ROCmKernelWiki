diff --git a/aiter/ops/triton/_triton_kernels/batched_gemm_afp4wfp4.py b/aiter/ops/triton/_triton_kernels/batched_gemm_afp4wfp4.py
index d1a7415497..aa81ede314 100755
--- a/aiter/ops/triton/_triton_kernels/batched_gemm_afp4wfp4.py
+++ b/aiter/ops/triton/_triton_kernels/batched_gemm_afp4wfp4.py
@@ -327,7 +327,7 @@ def _get_config(
     from ..utils.gemm_config_utils import get_gemm_config
 
     # Note: Config files use K=2*K in their naming
-    config = get_gemm_config("BATCHED_GEMM-AFP4WFP4", M, N, 2 * K)
+    config, is_tunned = get_gemm_config("BATCHED_GEMM-AFP4WFP4", M, N, 2 * K)
 
     # Apply custom split-K logic for AFP4WFP4
     if config["NUM_KSPLIT"] > 1:
@@ -345,4 +345,4 @@ def _get_config(
         config["BLOCK_SIZE_K"] = triton.next_power_of_2(2 * K)
         config["SPLITK_BLOCK_SIZE"] = 2 * K
 
-    return config
+    return config, is_tunned
diff --git a/aiter/ops/triton/_triton_kernels/gemm_a16w16.py b/aiter/ops/triton/_triton_kernels/gemm_a16w16.py
index eb70c95557..b2520cfbed 100644
--- a/aiter/ops/triton/_triton_kernels/gemm_a16w16.py
+++ b/aiter/ops/triton/_triton_kernels/gemm_a16w16.py
@@ -251,5 +251,5 @@ def _get_config(
     N: int,
     K: int,
 ):
-    config = get_gemm_config("GEMM-A16W16", M, N, K)
-    return compute_splitk_params(config, K)
+    config, is_tunned = get_gemm_config("GEMM-A16W16", M, N, K)
+    return compute_splitk_params(config, K), is_tunned
diff --git a/aiter/ops/triton/_triton_kernels/gemm_a8wfp4.py b/aiter/ops/triton/_triton_kernels/gemm_a8wfp4.py
index 4441cf51fe..2d74eeff7d 100644
--- a/aiter/ops/triton/_triton_kernels/gemm_a8wfp4.py
+++ b/aiter/ops/triton/_triton_kernels/gemm_a8wfp4.py
@@ -268,7 +268,7 @@ def _get_config(
 ):
     from ..utils.gemm_config_utils import get_gemm_config
 
-    config = get_gemm_config("GEMM-A8WFP4", M, N, K)
+    config, is_tunned = get_gemm_config("GEMM-A8WFP4", M, N, K)
 
     if M <= 128:
         SPLITK_BLOCK_SIZE, BLOCK_SIZE_K, NUM_KSPLIT = _get_splitk(
@@ -280,7 +280,7 @@ def _get_config(
     else:
         config["SPLITK_BLOCK_SIZE"] = 2 * K
 
-    return config
+    return config, is_tunned
 
 
 def _get_splitk(K: int, BLOCK_SIZE_K: int, NUM_KSPLIT: int):
diff --git a/aiter/ops/triton/batched_gemm_a16wfp4.py b/aiter/ops/triton/batched_gemm_a16wfp4.py
index ffd8b0ba3d..8c91fe6427 100755
--- a/aiter/ops/triton/batched_gemm_a16wfp4.py
+++ b/aiter/ops/triton/batched_gemm_a16wfp4.py
@@ -92,7 +92,7 @@ def batched_gemm_a16wfp4(
     B = Bx
 
     if config is None:
-        config = _get_config(M, N, K)
+        config, _ = _get_config(M, N, K)
     else:
         config = deserialize_str(config)
 
diff --git a/aiter/ops/triton/batched_gemm_a8w8.py b/aiter/ops/triton/batched_gemm_a8w8.py
index 1ac5e6fe3a..453c190e14 100644
--- a/aiter/ops/triton/batched_gemm_a8w8.py
+++ b/aiter/ops/triton/batched_gemm_a8w8.py
@@ -73,7 +73,7 @@ def batched_gemm_a8w8(
         YQ = torch.empty((B, M, N), dtype=dtype, device=XQ.device)
 
     if config is None:
-        config = _get_config(M, N, K)
+        config, _ = _get_config(M, N, K)
 
     grid = lambda META: (  # noqa: E731
         B,
diff --git a/aiter/ops/triton/batched_gemm_a8w8_a_per_token_group_prequant_w_per_batched_tensor_quant.py b/aiter/ops/triton/batched_gemm_a8w8_a_per_token_group_prequant_w_per_batched_tensor_quant.py
index 7701fb249e..aaa62f9675 100644
--- a/aiter/ops/triton/batched_gemm_a8w8_a_per_token_group_prequant_w_per_batched_tensor_quant.py
+++ b/aiter/ops/triton/batched_gemm_a8w8_a_per_token_group_prequant_w_per_batched_tensor_quant.py
@@ -86,7 +86,7 @@ def batched_gemm_a8w8_a_per_token_group_prequant_w_per_batched_tensor_quant(
             ), "Output dimension error"
 
     if config is None:
-        config = _get_config(M, N, K)
+        config, _ = _get_config(M, N, K)
     config["BLOCK_SIZE_K"] = group_size
     config["kpack"] = 1
 
diff --git a/aiter/ops/triton/batched_gemm_afp4wfp4.py b/aiter/ops/triton/batched_gemm_afp4wfp4.py
index 046ee0040a..bb4e13a69c 100755
--- a/aiter/ops/triton/batched_gemm_afp4wfp4.py
+++ b/aiter/ops/triton/batched_gemm_afp4wfp4.py
@@ -64,7 +64,7 @@ def batched_gemm_afp4wfp4(
     Batch = Bx
 
     if config is None:
-        config = _get_config(M, N, K)
+        config, _ = _get_config(M, N, K)
 
     if config["NUM_KSPLIT"] > 1:
         if _USE_GEMM_SPLITK_BF16:
diff --git a/aiter/ops/triton/batched_gemm_bf16.py b/aiter/ops/triton/batched_gemm_bf16.py
index 6948b142ae..a06833f948 100644
--- a/aiter/ops/triton/batched_gemm_bf16.py
+++ b/aiter/ops/triton/batched_gemm_bf16.py
@@ -66,7 +66,7 @@ def batched_gemm_bf16(
         YQ = torch.empty((B, M, N), dtype=dtype, device=XQ.device)
 
     if config is None:
-        config = _get_config(M, N, K)
+        config, _ = _get_config(M, N, K)
 
     grid = lambda META: (  # noqa: E731
         B,
diff --git a/aiter/ops/triton/ff_a16w16_fused_gated.py b/aiter/ops/triton/ff_a16w16_fused_gated.py
index 656452066b..2790245afd 100644
--- a/aiter/ops/triton/ff_a16w16_fused_gated.py
+++ b/aiter/ops/triton/ff_a16w16_fused_gated.py
@@ -73,7 +73,7 @@ def ff_a16w16_fused_gated(
         )  # zeros, as this does atomic adds on top
 
     if config is None:
-        config = _get_config(M, N, K)
+        config, _ = _get_config(M, N, K)
 
     grid = lambda META: (  # noqa: E731
         triton.cdiv(M, META["BLOCK_SIZE_M"]) * triton.cdiv(N, META["BLOCK_SIZE_N"]),
diff --git a/aiter/ops/triton/ff_a16w16_fused_ungated.py b/aiter/ops/triton/ff_a16w16_fused_ungated.py
index 0830afa14f..e3abadbb3a 100644
--- a/aiter/ops/triton/ff_a16w16_fused_ungated.py
+++ b/aiter/ops/triton/ff_a16w16_fused_ungated.py
@@ -70,7 +70,7 @@ def ff_a16w16_fused_ungated(
         )  # zeros, as this does atomic adds on top
 
     if config is None:
-        config = _get_config(M, N, K)
+        config, _ = _get_config(M, N, K)
 
     grid = lambda META: (  # noqa: E731
         triton.cdiv(M, META["BLOCK_SIZE_M"]) * triton.cdiv(N, META["BLOCK_SIZE_N"]),
diff --git a/aiter/ops/triton/fused_gemm_a8w8_blockscale_a16w16.py b/aiter/ops/triton/fused_gemm_a8w8_blockscale_a16w16.py
index 90b50bd066..2f454fbf6f 100644
--- a/aiter/ops/triton/fused_gemm_a8w8_blockscale_a16w16.py
+++ b/aiter/ops/triton/fused_gemm_a8w8_blockscale_a16w16.py
@@ -83,7 +83,7 @@ def fused_gemm_a8w8_blockscale_a16w16(
     w_fp8_scale = w_fp8_scale.T
 
     if config is None:
-        config = _get_config(M, N_fp8, N_bf16, K)
+        config, _ = _get_config(M, N_fp8, N_bf16, K)
 
     if y_fp8 is None and (config["NUM_KSPLIT"] == 1 or not skip_reduce):
         y_fp8 = torch.empty((M, N_fp8), dtype=dtype, device=x_fp8.device)
diff --git a/aiter/ops/triton/fused_gemm_afp4wfp4_mul_add.py b/aiter/ops/triton/fused_gemm_afp4wfp4_mul_add.py
index 3d5d3d8640..376ae29732 100644
--- a/aiter/ops/triton/fused_gemm_afp4wfp4_mul_add.py
+++ b/aiter/ops/triton/fused_gemm_afp4wfp4_mul_add.py
@@ -135,7 +135,7 @@ def fused_gemm_afp4wfp4_mul_add(
         y = torch.empty((M, N), dtype=dtype, device=x.device)
 
     if config is None:
-        config = _get_config(M, N, K)
+        config, _ = _get_config(M, N, K)
 
     if config["NUM_KSPLIT"] > 1:
         SPLITK_BLOCK_SIZE, BLOCK_SIZE_K, NUM_KSPLIT = get_splitk(
@@ -310,7 +310,7 @@ def fused_gemm_afp4wfp4_preshuffle_add_mul(
         y = torch.empty((M, N), dtype=dtype, device=x.device)
 
     if config is None:
-        config = _get_config(M, N, K, True)
+        config, _ = _get_config(M, N, K, True)
 
     if config["NUM_KSPLIT"] > 1:
         SPLITK_BLOCK_SIZE, BLOCK_SIZE_K, NUM_KSPLIT = get_splitk(
diff --git a/aiter/ops/triton/fused_gemm_afp4wfp4_split_cat.py b/aiter/ops/triton/fused_gemm_afp4wfp4_split_cat.py
index b474f9720b..38b62b0d28 100644
--- a/aiter/ops/triton/fused_gemm_afp4wfp4_split_cat.py
+++ b/aiter/ops/triton/fused_gemm_afp4wfp4_split_cat.py
@@ -72,7 +72,7 @@ def fused_gemm_afp4wfp4_split_cat(
     w_scale = w_scale.T  # (scale_k, scale_n)
 
     if config is None:
-        config = _get_config(M, N, K)
+        config, _ = _get_config(M, N, K)
 
     c1 = torch.empty((M, D, S1 + S3), dtype=dtype, device=x.device)
     c2 = torch.empty((M, D, S2), dtype=dtype, device=x.device)
diff --git a/aiter/ops/triton/gemm_a16w16.py b/aiter/ops/triton/gemm_a16w16.py
index 549ddd36d8..552090a64b 100644
--- a/aiter/ops/triton/gemm_a16w16.py
+++ b/aiter/ops/triton/gemm_a16w16.py
@@ -56,7 +56,7 @@ def gemm_a16w16(
     w = w.T
 
     if config is None:
-        config = _get_config(M, N, K)
+        config, _ = _get_config(M, N, K)
 
     if y is None and (config["NUM_KSPLIT"] == 1 or not skip_reduce):
         y = torch.empty((M, N), dtype=dtype, device=x.device)
diff --git a/aiter/ops/triton/gemm_a16w16_atomic.py b/aiter/ops/triton/gemm_a16w16_atomic.py
index 38341b3efb..641f0d6ff8 100644
--- a/aiter/ops/triton/gemm_a16w16_atomic.py
+++ b/aiter/ops/triton/gemm_a16w16_atomic.py
@@ -49,7 +49,7 @@ def gemm_a16w16_atomic_(
     K, N = w.shape
 
     if config is None:
-        config = _get_config(M, N, K)
+        config, _ = _get_config(M, N, K)
     else:
         config = deserialize_str(config)
 
diff --git a/aiter/ops/triton/gemm_a16w16_gated.py b/aiter/ops/triton/gemm_a16w16_gated.py
index 8871daebbc..242595cd07 100644
--- a/aiter/ops/triton/gemm_a16w16_gated.py
+++ b/aiter/ops/triton/gemm_a16w16_gated.py
@@ -55,7 +55,7 @@ def gemm_a16w16_gated(
         y = torch.empty((M, N // 2), dtype=dtype, device=x.device)
 
     if config is None:
-        config = _get_config(M, N, K)
+        config, _ = _get_config(M, N, K)
 
     grid = lambda META: (  # noqa: E731
         triton.cdiv(M, META["BLOCK_SIZE_M"]) * triton.cdiv(N, META["BLOCK_SIZE_N"]),
diff --git a/aiter/ops/triton/gemm_a16w8_blockscale.py b/aiter/ops/triton/gemm_a16w8_blockscale.py
index 3dbf8db721..f25715cffc 100644
--- a/aiter/ops/triton/gemm_a16w8_blockscale.py
+++ b/aiter/ops/triton/gemm_a16w8_blockscale.py
@@ -57,7 +57,7 @@ def gemm_a16w8_blockscale(
         y = torch.empty((M, N), dtype=dtype, device=x.device)
 
     if config is None:
-        config = _get_config(M, N, K)
+        config, _ = _get_config(M, N, K)
 
     config["SPLITK_BLOCK_SIZE"] = triton.cdiv(K, config["NUM_KSPLIT"])
     if config["NUM_KSPLIT"] > 1:
diff --git a/aiter/ops/triton/gemm_a16wfp4.py b/aiter/ops/triton/gemm_a16wfp4.py
index 2bc0983119..b118df184a 100644
--- a/aiter/ops/triton/gemm_a16wfp4.py
+++ b/aiter/ops/triton/gemm_a16wfp4.py
@@ -78,7 +78,7 @@ def gemm_a16wfp4(
     w = w.T
 
     if config is None:
-        config = _get_config(M, N, K)
+        config, _ = _get_config(M, N, K)
     else:
         config = deserialize_str(config)
 
diff --git a/aiter/ops/triton/gemm_a8w8.py b/aiter/ops/triton/gemm_a8w8.py
index 3602ef2ff6..e530a51779 100644
--- a/aiter/ops/triton/gemm_a8w8.py
+++ b/aiter/ops/triton/gemm_a8w8.py
@@ -62,7 +62,7 @@ def gemm_a8w8(
         y = torch.empty((M, N), dtype=dtype, device=x.device)
 
     if config is None:
-        config = _get_config(M, N, K)
+        config, _ = _get_config(M, N, K)
 
     grid = (
         triton.cdiv(M, config["BLOCK_SIZE_M"]) * triton.cdiv(N, config["BLOCK_SIZE_N"]),
diff --git a/aiter/ops/triton/gemm_a8w8_blockscale.py b/aiter/ops/triton/gemm_a8w8_blockscale.py
index ee2d072822..05328c2d98 100644
--- a/aiter/ops/triton/gemm_a8w8_blockscale.py
+++ b/aiter/ops/triton/gemm_a8w8_blockscale.py
@@ -61,7 +61,7 @@ def gemm_a8w8_blockscale(
     w_scale = w_scale.T  # (scale_k, scale_n)
 
     if config is None:
-        config = _get_config(M, N, K)
+        config, _ = _get_config(M, N, K)
 
     if y is None and (config["NUM_KSPLIT"] == 1 or not skip_reduce):
         y = torch.empty((M, N), dtype=dtype, device=x.device)
diff --git a/aiter/ops/triton/gemm_a8w8_per_token_scale.py b/aiter/ops/triton/gemm_a8w8_per_token_scale.py
index e8032bdbeb..3a4a164598 100644
--- a/aiter/ops/triton/gemm_a8w8_per_token_scale.py
+++ b/aiter/ops/triton/gemm_a8w8_per_token_scale.py
@@ -54,7 +54,7 @@ def gemm_a8w8_per_token_scale(
         y = torch.empty((M, N), dtype=dtype, device=x.device)
 
     if config is None:
-        config = _get_config(M, N, K)
+        config, _ = _get_config(M, N, K)
 
     config["SPLITK_BLOCK_SIZE"] = triton.cdiv(K, config["NUM_KSPLIT"])
     if config["NUM_KSPLIT"] > 1:
diff --git a/aiter/ops/triton/gemm_a8wfp4.py b/aiter/ops/triton/gemm_a8wfp4.py
index ffa4b7b6d8..8961fe53c6 100644
--- a/aiter/ops/triton/gemm_a8wfp4.py
+++ b/aiter/ops/triton/gemm_a8wfp4.py
@@ -75,7 +75,7 @@ def gemm_a8wfp4(
     ), f"Scale shapes incorrect: x_scales should be ({M}, 1), got {x_scales.shape}; w_scales should be ({N}, {K//32}), got {w_scales.shape}"
 
     if config is None:
-        config = _get_config(M, N, K)
+        config, _ = _get_config(M, N, K)
 
     if M <= 128:
         if _USE_GEMM_SPLITK_BF16:
diff --git a/aiter/ops/triton/gemm_afp4wfp4.py b/aiter/ops/triton/gemm_afp4wfp4.py
index 1085dd5d12..338586f9fa 100644
--- a/aiter/ops/triton/gemm_afp4wfp4.py
+++ b/aiter/ops/triton/gemm_afp4wfp4.py
@@ -122,7 +122,7 @@ def gemm_afp4wfp4_(
     w = w.T
 
     if config is None:
-        config = _get_config(M, N, K)
+        config, _ = _get_config(M, N, K)
     else:
         config = deserialize_str(config)
 
@@ -267,7 +267,7 @@ def gemm_afp4wfp4_preshuffled_scales(
         y = torch.empty((M, N), dtype=dtype, device=x.device)
 
     if config is None:
-        config = _get_config(M, N, K)
+        config, _ = _get_config(M, N, K)
 
     if config["NUM_KSPLIT"] > 1:
         SPLITK_BLOCK_SIZE, BLOCK_SIZE_K, NUM_KSPLIT = get_splitk(
@@ -401,7 +401,7 @@ def gemm_afp4wfp4_preshuffle(
         y = torch.empty((M, N), dtype=dtype, device=x.device)
 
     if config is None:
-        config = _get_config(M, N, K, True)
+        config, _ = _get_config(M, N, K, True)
 
     if config["NUM_KSPLIT"] > 1:
         SPLITK_BLOCK_SIZE, BLOCK_SIZE_K, NUM_KSPLIT = get_splitk(
diff --git a/aiter/ops/triton/utils/_triton/gemm_tune_check.py b/aiter/ops/triton/utils/_triton/gemm_tune_check.py
new file mode 100644
index 0000000000..52726162d2
--- /dev/null
+++ b/aiter/ops/triton/utils/_triton/gemm_tune_check.py
@@ -0,0 +1,62 @@
+import importlib
+import inspect
+from typing import Optional
+from aiter.ops.triton.utils.logger import AiterTritonLogger
+
+_LOGGER = AiterTritonLogger()
+
+
+def gemm_tune_check(
+    func: callable,
+    N: int,
+    K: int,
+    M: Optional[int] = None,
+    shuffle: Optional[bool] = None,
+):
+    """
+    This function returns if a AITER Triton GEMM is tunned for a specific shape
+
+    example 1: FP4 GEMM preshuffled weight scales for shape (16, 1280, 8192)
+        from aiter.ops.triton.utils._triton.gemm_tune_check import gemm_tune_check
+        from aiter.ops.triton.gemm_afp4wfp4 import gemm_afp4wfp4_preshuffle
+        is_tunned = gemm_tune_check(gemm_afp4wfp4_preshuffle, N=1280, K=8192//2, M=16, shuffle=True)
+        print(is_tunned) # return True or False
+
+    example 2: FP8 GEMM blockscale for shape (16, 1280, 8192)
+        from aiter.ops.triton.utils._triton.gemm_tune_check import gemm_tune_check
+        from aiter.ops.triton.gemm_a8w8_blockscale import gemm_a8w8_blockscale
+        is_tunned = gemm_tune_check(gemm_a8w8_blockscale, N=1024, K=8192, M=16)
+        print(is_tunned) # return True or False
+    """
+
+    module_pth = func.__module__.split(".")
+    module_pth = module_pth[:-1] + ["_triton_kernels"] + module_pth[-1:]
+    module_pth = ".".join(module_pth)
+    module = importlib.import_module(module_pth)
+    _LOGGER.info(f"Function {func} found at {module}")
+
+    if hasattr(module, "_get_config"):
+
+        get_config_func = getattr(module, "_get_config")
+        sig = inspect.signature(get_config_func)
+
+        if (
+            sig.parameters.get("M") is not None
+            and sig.parameters.get("N") is not None
+            and sig.parameters.get("K") is not None
+        ):
+            if sig.parameters.get("shuffle") is None:
+                _, is_tunned = get_config_func(M, N, K)
+            else:
+                if shuffle is not None:
+                    _, is_tunned = get_config_func(M, N, K, shuffle=shuffle)
+                else:
+                    raise Exception(f"Please specify shuffle (True/False) for {module}")
+
+            return is_tunned
+
+        raise NotImplementedError(
+            f"{module} _get_config not yet supported for inspection"
+        )
+
+    raise Exception(f"{module} does not have _get_config function")
diff --git a/aiter/ops/triton/utils/gemm_config_utils.py b/aiter/ops/triton/utils/gemm_config_utils.py
index 56ed8a20c9..fa99a2c14c 100644
--- a/aiter/ops/triton/utils/gemm_config_utils.py
+++ b/aiter/ops/triton/utils/gemm_config_utils.py
@@ -51,7 +51,7 @@ def get_gemm_config(
     K: int | None = None,
     bounds: tuple[int, ...] | None = None,
     specialized_filename: str | None = None,
-) -> dict:
+) -> tuple[dict, bool]:
     """
     Load a GEMM configuration using the standardized M_LEQ_x/M_GEQ_y/any format.
 
@@ -141,16 +141,16 @@ def get_gemm_config(
     for bound in search_bounds:
         key = f"M_LEQ_{bound}"
         if M <= bound and key in config_dict:
-            return dict(config_dict[key])
+            return dict(config_dict[key]), config_dict_key != "default"
 
     # Search for M_GEQ_x keys
     for bound in reversed(search_bounds):
         key = f"M_GEQ_{bound}"
         if M >= bound and key in config_dict:
-            return dict(config_dict[key])
+            return dict(config_dict[key]), config_dict_key != "default"
 
     if "any" in config_dict:
-        return dict(config_dict["any"])
+        return dict(config_dict["any"]), False
 
     raise KeyError(
         f"No matching configuration found for M={M}, N={N}, K={K} in config '{config_name}'."
