diff --git a/aiter/jit/core.py b/aiter/jit/core.py
index c0d35055ae..712feea0d3 100644
--- a/aiter/jit/core.py
+++ b/aiter/jit/core.py
@@ -614,7 +614,13 @@ def decorator(func):
             for name, param in sig.parameters.items():
                 if param.annotation is torch.Tensor:
                     mutates_args.append(name)
-            sig = torch.library.infer_schema(func, mutates_args="unknown")
+            if hasattr(torch.library, "infer_schema"):
+                sig = torch.library.infer_schema(func, mutates_args="unknown")
+            else:
+                # for pytorch 2.4
+                import torch._custom_op.impl
+
+                sig = torch._custom_op.impl.infer_schema(func, mutates_args)
             schema = f"{sig}"
         loadName = func.__name__
 
diff --git a/aiter/ops/attention.py b/aiter/ops/attention.py
index 36668169eb..07e030dca1 100644
--- a/aiter/ops/attention.py
+++ b/aiter/ops/attention.py
@@ -59,7 +59,7 @@ def gen_pa_fwd_asm(
     high_precision: Optional[
         int
     ] = 1,  # [0, 1, 2] 2 is the highest precision, this is only for fp8 kvcache
-    kernelName: str = "",
+    kernelName: Optional[str] = None,
 ):
     if out_ is not None:
         return out_
@@ -108,7 +108,7 @@ def pa_fwd_asm(
     high_precision: Optional[
         int
     ] = 1,  # [0, 1, 2] 2 is the highest precision, this is only for fp8 kvcache
-    kernelName: str = "",
+    kernelName: Optional[str] = None,
 ) -> torch.Tensor: ...
 
 
diff --git a/aiter/ops/gemm_op_a8w8.py b/aiter/ops/gemm_op_a8w8.py
index 2570a047c4..c058630d6c 100644
--- a/aiter/ops/gemm_op_a8w8.py
+++ b/aiter/ops/gemm_op_a8w8.py
@@ -191,17 +191,28 @@ def get_CKGEMM_config_fake(
     return None
 
 
-op_name = "aiter::get_CKGEMM_config_"
+@functools.lru_cache(maxsize=1024)
+def get_CKGEMM_config(M: int, N: int, K: int, tuned_file="a8w8_tuned_gemm.csv"):
+    import torch
 
-schema_str = torch.library.infer_schema(get_CKGEMM_config_, mutates_args=())
-torch.library.define(op_name, schema_str, lib=aiter_lib)
-torch.library.impl(op_name, "cuda", get_CKGEMM_config_, lib=aiter_lib)
-torch.library.register_fake(op_name, get_CKGEMM_config_fake, lib=aiter_lib)
+    op_name = "aiter::get_CKGEMM_config_"
+    if not hasattr(torch.ops.aiter, "get_CKGEMM_config_"):
+        if hasattr(torch.library, "infer_schema"):
+            schema_str = torch.library.infer_schema(
+                get_CKGEMM_config_, mutates_args="unknown"
+            )
+        else:
+            # for pytorch 2.4
+            import torch._custom_op.impl
 
+            schema_str = torch._custom_op.impl.infer_schema(get_CKGEMM_config_, ["X"])
 
-@functools.lru_cache(maxsize=1024)
-def get_CKGEMM_config(M: int, N: int, K: int, tuned_file="a8w8_tuned_gemm.csv"):
-    torch.ops.aiter.get_CKGEMM_config_(torch.empty(1, device="cuda"), tuned_file)
+        torch.library.define(op_name, schema_str, lib=aiter_lib)
+        torch.library.impl(op_name, "cuda", get_CKGEMM_config_, lib=aiter_lib)
+        torch.library.register_fake(op_name, get_CKGEMM_config_, lib=aiter_lib)
+
+    x = torch.empty(1, device="cuda")
+    getattr(torch.ops.aiter, "get_CKGEMM_config_")(x, tuned_file)
 
     cu_num = get_cu_num()
 
diff --git a/aiter/ops/moe_op.py b/aiter/ops/moe_op.py
index 7d8dab4828..4103ac650d 100755
--- a/aiter/ops/moe_op.py
+++ b/aiter/ops/moe_op.py
@@ -197,7 +197,7 @@ def cmdGenFunc_ck_moe_stage(
     num_valid_ids: Tensor,
     out: Tensor,
     topk: int,
-    kernelName: str = "",
+    kernelName: Optional[str] = None,
     w1_scale: Optional[Tensor] = None,
     a1_scale: Optional[Tensor] = None,
     block_m: Optional[int] = 32,
@@ -230,7 +230,7 @@ def cmdGenFunc_ck_moe_stage2(
     num_valid_ids: Tensor,
     out: Tensor,
     topk: int,
-    kernelName: str = "",
+    kernelName: Optional[str] = None,
     w1_scale: Optional[Tensor] = None,
     a1_scale: Optional[Tensor] = None,
     block_m: Optional[int] = 32,
@@ -264,7 +264,7 @@ def ck_moe_stage1(
     num_valid_ids: Tensor,
     out: Tensor,
     topk: int,
-    kernelName: str = "",
+    kernelName: Optional[str] = None,
     w1_scale: Optional[Tensor] = None,
     a1_scale: Optional[Tensor] = None,
     block_m: Optional[int] = 32,
@@ -284,7 +284,7 @@ def ck_moe_stage2(
     num_valid_ids: Tensor,
     out: Tensor,
     topk: int,
-    kernelName: str = "",
+    kernelName: Optional[str] = None,
     w2_scale: Optional[Tensor] = None,
     a2_scale: Optional[Tensor] = None,
     block_m: Optional[int] = 32,
diff --git a/aiter/ops/topk.py b/aiter/ops/topk.py
index 0fd3f51690..2b477f3694 100644
--- a/aiter/ops/topk.py
+++ b/aiter/ops/topk.py
@@ -33,7 +33,7 @@ def grouped_topk(
     num_expert_group: int,
     topk_group: int,
     need_renorm: bool,
-    scoring_func: str = "softmax",
+    scoring_func: bool = True,
     routed_scaling_factor: float = 1.0,
 ) -> None: ...
 
diff --git a/csrc/include/attention_asm.h b/csrc/include/attention_asm.h
index 709984e11f..af09e2d2a0 100644
--- a/csrc/include/attention_asm.h
+++ b/csrc/include/attention_asm.h
@@ -15,4 +15,4 @@ torch::Tensor pa_fwd(torch::Tensor& Q, //   [num_seqs, num_heads, head_size]
                      std::optional<torch::Tensor> out_      = std::nullopt,
                      std::optional<torch::Tensor> qo_indptr = std::nullopt,
                      std::optional<int> high_precision      = 1,
-                     std::string kernelName                 = "");
+                     std::optional<std::string> kernelName_  = std::nullopt);
diff --git a/csrc/include/moe_op.h b/csrc/include/moe_op.h
index c3652c249e..e6963bb09d 100755
--- a/csrc/include/moe_op.h
+++ b/csrc/include/moe_op.h
@@ -19,7 +19,7 @@ void grouped_topk(torch::Tensor& gating_output, // [num_tokens, num_experts]
                   int num_expert_group,
                   int topk_grp,
                   bool need_renorm,
-                  std::string scoring_func          = "softmax",
+                  bool scoring_func = true,
                   const float routed_scaling_factor = 1.);
 
 std::vector<at::Tensor> moe_fused_gate(at::Tensor& input,
diff --git a/csrc/include/rocm_ops.hpp b/csrc/include/rocm_ops.hpp
index dbc37f554e..e110a5da8f 100644
--- a/csrc/include/rocm_ops.hpp
+++ b/csrc/include/rocm_ops.hpp
@@ -66,7 +66,7 @@
           py::arg("out_")           = std::nullopt, \
           py::arg("qo_indptr")      = std::nullopt, \
           py::arg("high_precision") = 1,            \
-          py::arg("kernelName")     = "");
+          py::arg("kernelName")     = std::nullopt);
 
 #define ATTENTION_CK_PYBIND            \
     m.def("pa_fwd_naive",              \
@@ -558,7 +558,7 @@
           py::arg("num_valid_ids"),                  \
           py::arg("out"),                            \
           py::arg("topk"),                           \
-          py::arg("kernelName")     = "",            \
+          py::arg("kernelName")     = std::nullopt,  \
           py::arg("w1_scale")       = std::nullopt,  \
           py::arg("a1_scale")       = std::nullopt,  \
           py::arg("block_m")        = 32,            \
@@ -577,7 +577,7 @@
           py::arg("num_valid_ids"),                  \
           py::arg("out"),                            \
           py::arg("topk"),                           \
-          py::arg("kernelName")     = "",            \
+          py::arg("kernelName")     = std::nullopt,  \
           py::arg("w2_scale")       = std::nullopt,  \
           py::arg("a2_scale")       = std::nullopt,  \
           py::arg("block_m")        = 32,            \
@@ -647,7 +647,7 @@
           py::arg("num_expert_group"),                                                        \
           py::arg("topk_grp"),                                                                \
           py::arg("need_renorm"),                                                             \
-          py::arg("scoring_func")          = "softmax",                                       \
+          py::arg("scoring_func") = true,                                                     \
           py::arg("routed_scaling_factor") = 1.0f,                                            \
           "Apply grouped topk softmax/sigmodd to the gating outputs.");                       \
     m.def("biased_grouped_topk",                                                              \
diff --git a/csrc/kernels/topk_softmax_kernels_group.cu b/csrc/kernels/topk_softmax_kernels_group.cu
index cb90fa8f5f..7bd29061ba 100644
--- a/csrc/kernels/topk_softmax_kernels_group.cu
+++ b/csrc/kernels/topk_softmax_kernels_group.cu
@@ -467,12 +467,11 @@ void grouped_topk(
     int num_expert_group,
     int topk_grp,
     bool need_renorm,
-    std::string scoring_func = "softmax",
+    bool scoring_func = true,
     const float routed_scaling_factor = 1.)
 {
-    TORCH_CHECK((scoring_func == "softmax") || (scoring_func == "sigmoid"), "grouped_topk scoring_func only suppot softmax or sigmoid");
     const bool isBiased = false;
-    bool isSoftmax = scoring_func == "softmax" ? true : false;
+    bool isSoftmax = scoring_func;
     int num_tokens = gating_output.size(0);
     int num_experts = gating_output.size(1);
     int topk = topk_ids.size(1);
diff --git a/csrc/py_itfs_cu/asm_pa.cu b/csrc/py_itfs_cu/asm_pa.cu
index 61f9e638f3..680159ad66 100644
--- a/csrc/py_itfs_cu/asm_pa.cu
+++ b/csrc/py_itfs_cu/asm_pa.cu
@@ -88,8 +88,9 @@ torch::Tensor pa_fwd(torch::Tensor& Q, //   [num_seqs, num_heads, head_size]
                      std::optional<torch::Tensor> out_      = std::nullopt,
                      std::optional<torch::Tensor> qo_indptr = std::nullopt,
                      std::optional<int> high_precision      = 1,
-                     std::string kernelName                 = "")
+                     std::optional<std::string> kernelName_  = std::nullopt)
 {
+    std::string kernelName = kernelName_.value_or("");
     torch::Tensor output = out_.value_or(torch::empty_like(Q));
     int batch            = context_lens.size(0);
     // int max_num_blocks = block_tables.size(1);
diff --git a/op_tests/test_moeTopkSoftmax.py b/op_tests/test_moeTopkSoftmax.py
index 7abf7e9a8d..70d60e670d 100644
--- a/op_tests/test_moeTopkSoftmax.py
+++ b/op_tests/test_moeTopkSoftmax.py
@@ -188,6 +188,7 @@ def test_grouped_topk(
     w_ref = w_ref * scale_factor
     w_aiter = torch.empty_strided((token, topk), (topk + 10, 1), dtype=dtypes.fp32)
     id_aiter = torch.empty_strided((token, topk), (topk + 10, 1), dtype=dtypes.i32)
+    is_softmax = True if scoring_func == "softmax" else False
     _, us_aiter = run_perftest(
         aiter.grouped_topk,
         gating_output,
@@ -196,7 +197,7 @@ def test_grouped_topk(
         group,
         topk_group,
         need_renorm,
-        scoring_func,
+        is_softmax,
         scale_factor,
     )
     id_ref, _ref = torch.sort(id_ref)
