diff --git a/ater/__init__.py b/aiter/__init__.py
similarity index 86%
rename from ater/__init__.py
rename to aiter/__init__.py
index 03473f392a..9d28b768d8 100644
--- a/ater/__init__.py
+++ b/aiter/__init__.py
@@ -4,10 +4,10 @@
 import torch
 import os
 import logging
-logger = logging.getLogger("ater")
+logger = logging.getLogger("aiter")
 import importlib.util
-if importlib.util.find_spec('ater_') is not None:
-    from ater_ import *
+if importlib.util.find_spec('aiter_') is not None:
+    from aiter_ import *
 if importlib.util.find_spec('hipbsolidxgemm_') is not None:
     from hipbsolidxgemm_ import *
 if importlib.util.find_spec('rocsolidxgemm_') is not None:
@@ -15,7 +15,7 @@
 from .ops.norm import *
 from .ops.quant import *
 from .ops.gemm_op_a8w8 import *
-from .ops.ater_operator import *
+from .ops.aiter_operator import *
 from .ops.activation import *
 from .ops.attention import *
 from .ops.custom import *
@@ -34,7 +34,7 @@ def getLogger():
         logger.setLevel(logging.DEBUG)
 
         console_handler = logging.StreamHandler()
-        if int(os.environ.get('ATER_LOG_MORE', 0)):
+        if int(os.environ.get('AITER_LOG_MORE', 0)):
             formatter = logging.Formatter(
                 fmt="[%(name)s %(levelname)s] %(asctime)s.%(msecs)03d - %(process)d:%(processName)s - %(pathname)s:%(lineno)d - %(funcName)s\n%(message)s",
                 datefmt="%Y-%m-%d %H:%M:%S",
diff --git a/ater/configs/a8w8_tuned_gemm.csv b/aiter/configs/a8w8_tuned_gemm.csv
similarity index 100%
rename from ater/configs/a8w8_tuned_gemm.csv
rename to aiter/configs/a8w8_tuned_gemm.csv
diff --git a/ater/configs/a8w8_untuned_gemm.csv b/aiter/configs/a8w8_untuned_gemm.csv
similarity index 100%
rename from ater/configs/a8w8_untuned_gemm.csv
rename to aiter/configs/a8w8_untuned_gemm.csv
diff --git a/ater/configs/asm_a8w8_gemm.csv b/aiter/configs/asm_a8w8_gemm.csv
similarity index 100%
rename from ater/configs/asm_a8w8_gemm.csv
rename to aiter/configs/asm_a8w8_gemm.csv
diff --git a/ater/configs/tuned_gemm.csv b/aiter/configs/tuned_gemm.csv
similarity index 100%
rename from ater/configs/tuned_gemm.csv
rename to aiter/configs/tuned_gemm.csv
diff --git a/ater/configs/untuned_gemm.csv b/aiter/configs/untuned_gemm.csv
similarity index 100%
rename from ater/configs/untuned_gemm.csv
rename to aiter/configs/untuned_gemm.csv
diff --git a/ater/dist/communication_op.py b/aiter/dist/communication_op.py
similarity index 100%
rename from ater/dist/communication_op.py
rename to aiter/dist/communication_op.py
diff --git a/ater/dist/cuda_wrapper.py b/aiter/dist/cuda_wrapper.py
similarity index 100%
rename from ater/dist/cuda_wrapper.py
rename to aiter/dist/cuda_wrapper.py
diff --git a/ater/dist/custom_all_reduce.py b/aiter/dist/custom_all_reduce.py
similarity index 99%
rename from ater/dist/custom_all_reduce.py
rename to aiter/dist/custom_all_reduce.py
index 9a9532c3ec..177bf80b80 100644
--- a/ater/dist/custom_all_reduce.py
+++ b/aiter/dist/custom_all_reduce.py
@@ -23,12 +23,12 @@
 
 # import vllm.envs as envs
 # from vllm import _custom_ops as ops
-import ater as ops
+import aiter as ops
 import os
 from .custom_all_reduce_utils import (
     gpu_p2p_access_check)
 from .parallel_state import in_the_same_node_as
-from ater import logger
+from aiter import logger
 
 try:
     ops.meta_size()
diff --git a/ater/dist/custom_all_reduce_utils.py b/aiter/dist/custom_all_reduce_utils.py
similarity index 99%
rename from ater/dist/custom_all_reduce_utils.py
rename to aiter/dist/custom_all_reduce_utils.py
index 38daa8d78c..141d8d44d8 100644
--- a/ater/dist/custom_all_reduce_utils.py
+++ b/aiter/dist/custom_all_reduce_utils.py
@@ -17,7 +17,7 @@
 from functools import lru_cache, partial, wraps
 from .utils import (cuda_device_count_stateless,
                     update_environment_variables)
-from ater import logger
+from aiter import logger
 from .cuda_wrapper import CudaRTLibrary
 import ctypes
 import json
diff --git a/ater/dist/parallel_state.py b/aiter/dist/parallel_state.py
similarity index 99%
rename from ater/dist/parallel_state.py
rename to aiter/dist/parallel_state.py
index bcc355a94a..9fa4632cd9 100644
--- a/ater/dist/parallel_state.py
+++ b/aiter/dist/parallel_state.py
@@ -34,7 +34,7 @@
 from torch.distributed import Backend, ProcessGroup
 
 import os
-from ater import logger
+from aiter import logger
 
 
 def supports_custom_op(): return True
diff --git a/ater/dist/shm_broadcast.py b/aiter/dist/shm_broadcast.py
similarity index 99%
rename from ater/dist/shm_broadcast.py
rename to aiter/dist/shm_broadcast.py
index 2fe314df04..74e4e64fb4 100644
--- a/ater/dist/shm_broadcast.py
+++ b/aiter/dist/shm_broadcast.py
@@ -28,7 +28,7 @@
 from zmq import IPV6  # type: ignore
 from zmq import SUB, SUBSCRIBE, XPUB, XPUB_VERBOSE, Context  # type: ignore
 
-from ater import logger
+from aiter import logger
 from .utils import get_ip, get_open_port, is_valid_ipv6_address
 
 VLLM_RINGBUFFER_WARNING_INTERVAL = 60
diff --git a/ater/dist/utils.py b/aiter/dist/utils.py
similarity index 99%
rename from ater/dist/utils.py
rename to aiter/dist/utils.py
index 5df2082a0f..51a62a18ea 100644
--- a/ater/dist/utils.py
+++ b/aiter/dist/utils.py
@@ -49,7 +49,7 @@
 from packaging.version import Version
 from typing_extensions import ParamSpec, TypeIs, assert_never
 
-from ater import logger
+from aiter import logger
 
 
 # Exception strings for non-implemented encoder/decoder scenarios
diff --git a/ater/fused_moe.py b/aiter/fused_moe.py
similarity index 99%
rename from ater/fused_moe.py
rename to aiter/fused_moe.py
index 8558fa4b24..83282992e2 100644
--- a/ater/fused_moe.py
+++ b/aiter/fused_moe.py
@@ -24,7 +24,7 @@
 import triton
 import triton.language as tl
 
-import ater as moe_kernels
+import aiter as moe_kernels
 
 
 logger = moe_kernels.getLogger()
diff --git a/ater/fused_moe_bf16_asm.py b/aiter/fused_moe_bf16_asm.py
similarity index 91%
rename from ater/fused_moe_bf16_asm.py
rename to aiter/fused_moe_bf16_asm.py
index a31b9cef8a..ca8b6663f8 100644
--- a/ater/fused_moe_bf16_asm.py
+++ b/aiter/fused_moe_bf16_asm.py
@@ -7,8 +7,8 @@
 import sys
 import os
 from typing import Any, Callable, Dict, Optional, Tuple
-import ater
-from ater import logger
+import aiter
+from aiter import logger
 BLOCK_SIZE_M = 32
 
 
@@ -34,7 +34,7 @@ def moe_sorting_ck(topk_ids, topk_weights, num_experts, model_dim, moebuf_dtype)
     moe_buf = torch.empty((M, model_dim),
                           dtype=moebuf_dtype,
                           device=device)
-    ater.moe_sorting_fwd(topk_ids, topk_weights, sorted_ids, sorted_weights,  sorted_expert_ids,
+    aiter.moe_sorting_fwd(topk_ids, topk_weights, sorted_ids, sorted_weights,  sorted_expert_ids,
                          num_tokens_post_pad, moe_buf, num_experts, BLOCK_SIZE_M)
     return sorted_ids, sorted_weights, sorted_expert_ids, num_tokens_post_pad, moe_buf
 
@@ -55,10 +55,10 @@ def asm_moe(hidden_states, w1, w2, topk_weight, topk_ids,
                                                                                                     model_dim, dtype)
     if fc1_scale is None:
         # pure bf16
-        ater.fmoe(moe_buf, hidden_states, w1, w2, sorted_ids,
+        aiter.fmoe(moe_buf, hidden_states, w1, w2, sorted_ids,
                   sorted_weights, sorted_expert_ids, num_tokens_post_padded, topk)
     elif a16:
-        ater.fmoe_int8_g1u0_a16(moe_buf, hidden_states, w1, w2, sorted_ids,
+        aiter.fmoe_int8_g1u0_a16(moe_buf, hidden_states, w1, w2, sorted_ids,
                                 sorted_weights, sorted_expert_ids, num_tokens_post_padded,
                                 topk,
                                 fc1_scale,
@@ -72,22 +72,22 @@ def asm_moe(hidden_states, w1, w2, topk_weight, topk_ids,
                             dtype=torch.int8, device=device)
             a8_scale = torch.empty((topk * M), dtype=torch.float, device=device)
 
-            ater.moe_smoothquant_fwd(
+            aiter.moe_smoothquant_fwd(
                 a8, hidden_states, fc1_smooth_scale, topk_ids, a8_scale)
         else:
             if w1.dtype == torch.float8_e4m3fnuz:
                 a8 = torch.empty((M, model_dim), dtype=w1.dtype, device=device)
                 a8_scale = torch.empty(M, dtype=torch.float, device=device)
-                ater.dynamic_per_token_scaled_fp8_quant(
+                aiter.dynamic_per_token_scaled_fp8_quant(
                     a8, hidden_states, a8_scale)
             else:
                 logger.warning(f'FMOE fall into pure torch quant...')
-                a8, a8_scale = ater.pertoken_quant(hidden_states, torch.float, quant_dtype=w1.dtype)
+                a8, a8_scale = aiter.pertoken_quant(hidden_states, torch.float, quant_dtype=w1.dtype)
 
         if w2.shape[2] == w1.shape[1]:
-            fmoe_func = ater.fmoe_int8_g1u0
+            fmoe_func = aiter.fmoe_int8_g1u0
         elif w2.shape[2]*2 == w1.shape[1]:
-            fmoe_func = ater.fmoe_g1u1
+            fmoe_func = aiter.fmoe_g1u1
         else:
             raise ValueError(f"Invalid MoE weight: {w1.shape=} {w2.shape=}")
 
diff --git a/ater/fused_moe_gelu.py b/aiter/fused_moe_gelu.py
similarity index 99%
rename from ater/fused_moe_gelu.py
rename to aiter/fused_moe_gelu.py
index ad4e0d475d..dad5415455 100644
--- a/ater/fused_moe_gelu.py
+++ b/aiter/fused_moe_gelu.py
@@ -24,7 +24,7 @@
 import triton
 import triton.language as tl
 
-import ater as moe_kernels
+import aiter as moe_kernels
 
 
 logger = moe_kernels.getLogger()
diff --git a/ater/fused_moe_int8_a8w8.py b/aiter/fused_moe_int8_a8w8.py
similarity index 99%
rename from ater/fused_moe_int8_a8w8.py
rename to aiter/fused_moe_int8_a8w8.py
index dcf4e63cbc..5446b29224 100644
--- a/ater/fused_moe_int8_a8w8.py
+++ b/aiter/fused_moe_int8_a8w8.py
@@ -24,7 +24,7 @@
 import triton
 import triton.language as tl
 
-import ater as moe_kernels
+import aiter as moe_kernels
 
 logger = moe_kernels.getLogger()
 VLLM_MOE_PADDING = bool(int(os.getenv("VLLM_MOE_PADDING", "1")))
diff --git a/ater/jit/core.py b/aiter/jit/core.py
similarity index 94%
rename from ater/jit/core.py
rename to aiter/jit/core.py
index 4520fcd271..0ae1b87d75 100644
--- a/ater/jit/core.py
+++ b/aiter/jit/core.py
@@ -15,17 +15,17 @@
 import json
 
 PREBUILD_KERNELS = False
-if os.path.exists(os.path.dirname(os.path.abspath(__file__))+"/ater_.so"):
-    ater_ = importlib.import_module(f'{__package__}.ater_')
+if os.path.exists(os.path.dirname(os.path.abspath(__file__))+"/aiter_.so"):
+    aiter_ = importlib.import_module(f'{__package__}.aiter_')
     PREBUILD_KERNELS = True
-logger = logging.getLogger("ater")
+logger = logging.getLogger("aiter")
 
 PY = sys.executable
 this_dir = os.path.dirname(os.path.abspath(__file__))
-ATER_ROOT_DIR = os.path.abspath(f"{this_dir}/../../")
-ATER_CSRC_DIR = f'{ATER_ROOT_DIR}/csrc'
+AITER_ROOT_DIR = os.path.abspath(f"{this_dir}/../../")
+AITER_CSRC_DIR = f'{AITER_ROOT_DIR}/csrc'
 CK_DIR = os.environ.get("CK_DIR",
-                        f"{ATER_ROOT_DIR}/3rdparty/composable_kernel")
+                        f"{AITER_ROOT_DIR}/3rdparty/composable_kernel")
 bd_dir = f"{this_dir}/build"
 # copy ck to build, thus hippify under bd_dir
 shutil.copytree(CK_DIR, f'{bd_dir}/ck', dirs_exist_ok=True)
@@ -151,7 +151,7 @@ def exec_blob(blob_gen_cmd, op_dir, src_dir, sources):
 
         bd_include_dir = f'{op_dir}/build/include'
         os.makedirs(bd_include_dir, exist_ok=True)
-        rename_cpp_to_cu([f"{ATER_CSRC_DIR}/include"],
+        rename_cpp_to_cu([f"{AITER_CSRC_DIR}/include"],
                          bd_include_dir)
         extra_include_paths = [
             f"{CK_DIR}/include",
@@ -167,7 +167,7 @@ def exec_blob(blob_gen_cmd, op_dir, src_dir, sources):
             extra_ldflags=extra_ldflags,
             extra_include_paths=extra_include_paths,
             build_directory=opbd_dir,
-            verbose=verbose or int(os.getenv("ATER_LOG_MORE", 0)) > 0,
+            verbose=verbose or int(os.getenv("AITER_LOG_MORE", 0)) > 0,
             with_cuda=True,
             is_python_module=True,
         )
@@ -198,7 +198,7 @@ def convert(d_ops: dict):
         # judge isASM
         if d_ops["isASM"].lower() == "true":
             d_ops["flags_extra_hip"].append(
-                "rf'-DATER_ASM_DIR=\\\"{ATER_ROOT_DIR}/hsa/\\\"'")
+                "rf'-DAITER_ASM_DIR=\\\"{AITER_ROOT_DIR}/hsa/\\\"'")
         del d_ops["isASM"]
         for k, val in d_ops.items():
             if isinstance(val, list):
@@ -266,8 +266,8 @@ def wrapper(*args, **kwargs):
             try:
                 module = None
                 if PREBUILD_KERNELS:
-                    if hasattr(ater_, loadName):
-                        module = ater_
+                    if hasattr(aiter_, loadName):
+                        module = aiter_
                 if module is None:
                     module = get_module(md_name)
             except Exception as e:
diff --git a/ater/jit/optCompilerConfig.json b/aiter/jit/optCompilerConfig.json
similarity index 66%
rename from ater/jit/optCompilerConfig.json
rename to aiter/jit/optCompilerConfig.json
index 95b6654ecd..344323ce5f 100644
--- a/ater/jit/optCompilerConfig.json
+++ b/aiter/jit/optCompilerConfig.json
@@ -1,8 +1,8 @@
 {
     "module_activation": {
         "srcs": [
-            "f'{ATER_CSRC_DIR}/pybind/activation_pybind.cu'",
-            "f'{ATER_CSRC_DIR}/kernels/activation_kernels.cu'"
+            "f'{AITER_CSRC_DIR}/pybind/activation_pybind.cu'",
+            "f'{AITER_CSRC_DIR}/kernels/activation_kernels.cu'"
         ],
         "md_name": "'module_activation'",
         "flags_extra_cc": [],
@@ -15,8 +15,8 @@
     },
     "module_attention": {
         "srcs": [
-            "f'{ATER_CSRC_DIR}/py_itfs_ck/attention_kernels.cu'",
-            "f'{ATER_CSRC_DIR}/pybind/attention_ck_pybind.cu'"
+            "f'{AITER_CSRC_DIR}/py_itfs_ck/attention_kernels.cu'",
+            "f'{AITER_CSRC_DIR}/pybind/attention_ck_pybind.cu'"
         ],
         "md_name": "'module_attention'",
         "flags_extra_cc": [],
@@ -29,8 +29,8 @@
     },
     "module_attention_asm": {
         "srcs": [
-            "f'{ATER_CSRC_DIR}/py_itfs_cu/asm_pa.cpp'",
-            "f'{ATER_CSRC_DIR}/pybind/attention_asm_pybind.cu'"
+            "f'{AITER_CSRC_DIR}/py_itfs_cu/asm_pa.cpp'",
+            "f'{AITER_CSRC_DIR}/pybind/attention_asm_pybind.cu'"
         ],
         "md_name": "'module_attention_asm'",
         "flags_extra_cc": [],
@@ -43,8 +43,8 @@
     },
     "module_pa": {
         "srcs": [
-            "f'{ATER_CSRC_DIR}/pybind/attention_pybind.cu'",
-            "f'{ATER_CSRC_DIR}/kernels/attention.cu'"
+            "f'{AITER_CSRC_DIR}/pybind/attention_pybind.cu'",
+            "f'{AITER_CSRC_DIR}/kernels/attention.cu'"
         ],
         "md_name": "'module_pa'",
         "flags_extra_cc": [],
@@ -59,8 +59,8 @@
     },
     "module_cache": {
         "srcs": [
-            "f'{ATER_CSRC_DIR}/pybind/cache_pybind.cu'",
-            "f'{ATER_CSRC_DIR}/kernels/cache_kernels.cu'"
+            "f'{AITER_CSRC_DIR}/pybind/cache_pybind.cu'",
+            "f'{AITER_CSRC_DIR}/kernels/cache_kernels.cu'"
         ],
         "md_name": "'module_cache'",
         "flags_extra_cc": [],
@@ -73,9 +73,9 @@
     },
     "module_custom_all_reduce": {
         "srcs": [
-            "f'{ATER_CSRC_DIR}/pybind/custom_all_reduce_pybind.cu'",
-            "f'{ATER_CSRC_DIR}/kernels/custom_all_reduce.cu'",
-            "f'{ATER_CSRC_DIR}/py_itfs_cu/asm_communication.cpp'"
+            "f'{AITER_CSRC_DIR}/pybind/custom_all_reduce_pybind.cu'",
+            "f'{AITER_CSRC_DIR}/kernels/custom_all_reduce.cu'",
+            "f'{AITER_CSRC_DIR}/py_itfs_cu/asm_communication.cpp'"
         ],
         "md_name": "'module_custom_all_reduce'",
         "flags_extra_cc": [],
@@ -88,9 +88,9 @@
     },
     "module_custom": {
         "srcs": [
-            "f'{ATER_CSRC_DIR}/pybind/custom_pybind.cu'",
-            "f'{ATER_CSRC_DIR}/py_itfs_cu/custom.cu'",
-            "f'{ATER_CSRC_DIR}/kernels/custom_kernels.cu'"
+            "f'{AITER_CSRC_DIR}/pybind/custom_pybind.cu'",
+            "f'{AITER_CSRC_DIR}/py_itfs_cu/custom.cu'",
+            "f'{AITER_CSRC_DIR}/kernels/custom_kernels.cu'"
         ],
         "md_name": "'module_custom'",
         "flags_extra_cc": [],
@@ -103,9 +103,9 @@
     },
     "module_gemm_a8w8": {
         "srcs": [
-            "f'{ATER_CSRC_DIR}/ck_gemm_a8w8/include'",
-            "f'{ATER_CSRC_DIR}/pybind/gemm_a8w8_pybind.cu'",
-            "f'{ATER_CSRC_DIR}/ck_gemm_a8w8/gemm_a8w8.cu'"
+            "f'{AITER_CSRC_DIR}/ck_gemm_a8w8/include'",
+            "f'{AITER_CSRC_DIR}/pybind/gemm_a8w8_pybind.cu'",
+            "f'{AITER_CSRC_DIR}/ck_gemm_a8w8/gemm_a8w8.cu'"
         ],
         "md_name": "'module_gemm_a8w8'",
         "flags_extra_cc": [],
@@ -114,12 +114,12 @@
         "extra_include": [],
         "isASM": "False",
         "verbose": "False",
-        "blob_gen_cmd": "f'{ATER_CSRC_DIR}/ck_gemm_a8w8/gen_instances.py --working_path {{}} --tune_file {ATER_ROOT_DIR}/ater/configs/a8w8_tuned_gemm.csv'"
+        "blob_gen_cmd": "f'{AITER_CSRC_DIR}/ck_gemm_a8w8/gen_instances.py --working_path {{}} --tune_file {AITER_ROOT_DIR}/aiter/configs/a8w8_tuned_gemm.csv'"
     },
     "module_gemm_a8w8_asm": {
         "srcs": [
-            "f'{ATER_CSRC_DIR}/pybind/gemm_a8w8_asm_pybind.cu'",
-            "f'{ATER_CSRC_DIR}/py_itfs_cu/asm_gemm_a8w8.cpp'"
+            "f'{AITER_CSRC_DIR}/pybind/gemm_a8w8_asm_pybind.cu'",
+            "f'{AITER_CSRC_DIR}/py_itfs_cu/asm_gemm_a8w8.cpp'"
         ],
         "md_name": "'module_gemm_a8w8_asm'",
         "flags_extra_cc": [],
@@ -132,10 +132,10 @@
     },
     "module_moe_asm": {
         "srcs": [
-            "f'{ATER_CSRC_DIR}/pybind/moe_op_pybind.cu'",
-            "f'{ATER_CSRC_DIR}/kernels/topk_softmax_kernels.cu'",
-            "f'{ATER_CSRC_DIR}/kernels/moe_align_block_size_kernels.cu'",
-            "f'{ATER_CSRC_DIR}/py_itfs_cu/asm_fmoe.cpp'"
+            "f'{AITER_CSRC_DIR}/pybind/moe_op_pybind.cu'",
+            "f'{AITER_CSRC_DIR}/kernels/topk_softmax_kernels.cu'",
+            "f'{AITER_CSRC_DIR}/kernels/moe_align_block_size_kernels.cu'",
+            "f'{AITER_CSRC_DIR}/py_itfs_cu/asm_fmoe.cpp'"
         ],
         "md_name": "'module_moe_asm'",
         "flags_extra_cc": [],
@@ -148,9 +148,9 @@
     },
     "module_moe": {
         "srcs": [
-            "f'{ATER_CSRC_DIR}/pybind/moe_ck_pybind.cu'",
+            "f'{AITER_CSRC_DIR}/pybind/moe_ck_pybind.cu'",
             "f'{CK_DIR}/example/ck_tile/15_fused_moe/instances'",
-            "f'{ATER_CSRC_DIR}/py_itfs_ck/moe_kernels.cu'"
+            "f'{AITER_CSRC_DIR}/py_itfs_ck/moe_kernels.cu'"
         ],
         "md_name": "'module_moe'",
         "flags_extra_cc": [],
@@ -165,8 +165,8 @@
     },
     "module_moe_sorting": {
         "srcs": [
-            "f'{ATER_CSRC_DIR}/py_itfs_ck/moe_sorting_kernels.cu'",
-            "f'{ATER_CSRC_DIR}/pybind/moe_sorting_pybind.cu'",
+            "f'{AITER_CSRC_DIR}/py_itfs_ck/moe_sorting_kernels.cu'",
+            "f'{AITER_CSRC_DIR}/pybind/moe_sorting_pybind.cu'",
             "f'{CK_DIR}/example/ck_tile/13_moe_sorting/'"
         ],
         "md_name": "'module_moe_sorting'",
@@ -182,9 +182,9 @@
     },
     "module_norm": {
         "srcs": [
-            "f'{ATER_CSRC_DIR}/py_itfs_ck/norm_kernels.cu'",
-            "f'{ATER_CSRC_DIR}/pybind/norm_pybind.cu'",
-            "f'{ATER_CSRC_DIR}/py_itfs_cu/asm_layernorm.cpp'"
+            "f'{AITER_CSRC_DIR}/py_itfs_ck/norm_kernels.cu'",
+            "f'{AITER_CSRC_DIR}/pybind/norm_pybind.cu'",
+            "f'{AITER_CSRC_DIR}/py_itfs_cu/asm_layernorm.cpp'"
         ],
         "md_name": "'module_norm'",
         "flags_extra_cc": [],
@@ -199,8 +199,8 @@
     },
     "module_pos_encoding": {
         "srcs": [
-            "f'{ATER_CSRC_DIR}/pybind/pos_encoding_pybind.cu'",
-            "f'{ATER_CSRC_DIR}/kernels/pos_encoding_kernels.cu'"
+            "f'{AITER_CSRC_DIR}/pybind/pos_encoding_pybind.cu'",
+            "f'{AITER_CSRC_DIR}/kernels/pos_encoding_kernels.cu'"
         ],
         "md_name": "'module_pos_encoding'",
         "flags_extra_cc": [],
@@ -213,9 +213,9 @@
     },
     "module_rmsnorm": {
         "srcs": [
-            "f'{ATER_CSRC_DIR}/kernels/rmsnorm_kernels.cu'",
-            "f'{ATER_CSRC_DIR}/py_itfs_ck/rmsnorm_ck_kernels.cu'",
-            "f'{ATER_CSRC_DIR}/pybind/rmsnorm_pybind.cu'"
+            "f'{AITER_CSRC_DIR}/kernels/rmsnorm_kernels.cu'",
+            "f'{AITER_CSRC_DIR}/py_itfs_ck/rmsnorm_ck_kernels.cu'",
+            "f'{AITER_CSRC_DIR}/pybind/rmsnorm_pybind.cu'"
         ],
         "md_name": "'module_rmsnorm'",
         "flags_extra_cc": [],
@@ -230,8 +230,8 @@
     },
     "module_smoothquant": {
         "srcs": [
-            "f'{ATER_CSRC_DIR}/py_itfs_ck/smoothquant_kernels.cu'",
-            "f'{ATER_CSRC_DIR}/pybind/smoothquant_pybind.cu'",
+            "f'{AITER_CSRC_DIR}/py_itfs_ck/smoothquant_kernels.cu'",
+            "f'{AITER_CSRC_DIR}/pybind/smoothquant_pybind.cu'",
             "f'{CK_DIR}/example/ck_tile/12_smoothquant/instances'",
             "f'{CK_DIR}/example/ck_tile/14_moe_smoothquant/instances'"
         ],
@@ -249,9 +249,9 @@
     },
     "module_transpose_operator": {
         "srcs": [
-            "f'{ATER_CSRC_DIR}/pybind/transpose_operator_pybind.cu'",
-            "f'{ATER_CSRC_DIR}/include/transpose_operator.h.cu'",
-            "f'{ATER_CSRC_DIR}/kernels/transpose_operator.cu'"
+            "f'{AITER_CSRC_DIR}/pybind/transpose_operator_pybind.cu'",
+            "f'{AITER_CSRC_DIR}/include/transpose_operator.h.cu'",
+            "f'{AITER_CSRC_DIR}/kernels/transpose_operator.cu'"
         ],
         "md_name": "'module_transpose_operator'",
         "flags_extra_cc": [],
@@ -264,9 +264,9 @@
     },
     "module_gemm_a8w8_tune": {
         "srcs": [
-            "f'{ATER_CSRC_DIR}/pybind/gemm_a8w8_tune_pybind.cu'",
-            "f'{ATER_CSRC_DIR}/ck_gemm_a8w8/gemm_a8w8_tune.cu'",
-            "f'{ATER_CSRC_DIR}/ck_gemm_a8w8/include'"
+            "f'{AITER_CSRC_DIR}/pybind/gemm_a8w8_tune_pybind.cu'",
+            "f'{AITER_CSRC_DIR}/ck_gemm_a8w8/gemm_a8w8_tune.cu'",
+            "f'{AITER_CSRC_DIR}/ck_gemm_a8w8/include'"
         ],
         "md_name": "'module_gemm_a8w8_tune'",
         "flags_extra_cc": [],
@@ -275,15 +275,15 @@
         "extra_include": [],
         "isASM": "False",
         "verbose": "False",
-        "blob_gen_cmd": "f'{ATER_CSRC_DIR}/ck_gemm_a8w8/gen_instances.py --working_path {{}} --tune'"
+        "blob_gen_cmd": "f'{AITER_CSRC_DIR}/ck_gemm_a8w8/gen_instances.py --working_path {{}} --tune'"
     },
-    "module_ater_operator": {
+    "module_aiter_operator": {
         "srcs": [
-            "f'{ATER_CSRC_DIR}/pybind/ater_operator_pybind.cu'",
-            "f'{ATER_CSRC_DIR}/include/ater_operator.h'",
-            "f'{ATER_CSRC_DIR}/kernels/ater_operator.cu'"
+            "f'{AITER_CSRC_DIR}/pybind/aiter_operator_pybind.cu'",
+            "f'{AITER_CSRC_DIR}/include/aiter_operator.h'",
+            "f'{AITER_CSRC_DIR}/kernels/aiter_operator.cu'"
         ],
-        "md_name": "'module_ater_operator'",
+        "md_name": "'module_aiter_operator'",
         "flags_extra_cc": [],
         "flags_extra_hip": [],
         "extra_ldflags": "None",
@@ -294,8 +294,8 @@
     },
     "module_quant": {
         "srcs": [
-            "f'{ATER_CSRC_DIR}/pybind/quant_pybind.cu'",
-            "f'{ATER_CSRC_DIR}/kernels/quant_kernels.cu'"
+            "f'{AITER_CSRC_DIR}/pybind/quant_pybind.cu'",
+            "f'{AITER_CSRC_DIR}/kernels/quant_kernels.cu'"
         ],
         "md_name": "'module_quant'",
         "flags_extra_cc": [],
diff --git a/ater/ops/activation.py b/aiter/ops/activation.py
similarity index 89%
rename from ater/ops/activation.py
rename to aiter/ops/activation.py
index 41ad0e1701..5401770510 100644
--- a/ater/ops/activation.py
+++ b/aiter/ops/activation.py
@@ -3,7 +3,7 @@
 
 from torch import Tensor
 from typing import List, Optional
-from ..jit.core import compile_ops, CK_DIR, ATER_CSRC_DIR
+from ..jit.core import compile_ops, CK_DIR, AITER_CSRC_DIR
 import torch.nn.functional as F
 
 MD_NAME = "module_activation"
diff --git a/ater/ops/ater_operator.py b/aiter/ops/aiter_operator.py
similarity index 62%
rename from ater/ops/ater_operator.py
rename to aiter/ops/aiter_operator.py
index dd1b45e969..07f608afde 100644
--- a/ater/ops/ater_operator.py
+++ b/aiter/ops/aiter_operator.py
@@ -3,22 +3,22 @@
 
 from torch import Tensor
 from typing import List, Optional
-from ..jit.core import compile_ops, CK_DIR, ATER_CSRC_DIR
+from ..jit.core import compile_ops, CK_DIR, AITER_CSRC_DIR
 import torch.nn.functional as F
 
-MD_NAME = "module_ater_operator"
+MD_NAME = "module_aiter_operator"
 
-@compile_ops("module_ater_operator")
+@compile_ops("module_aiter_operator")
 def add(input: Tensor, other: Tensor) -> Tensor: ...
 
 
-@compile_ops("module_ater_operator")
+@compile_ops("module_aiter_operator")
 def sub(input: Tensor, other: Tensor) -> Tensor: ...
 
 
-@compile_ops("module_ater_operator")
+@compile_ops("module_aiter_operator")
 def mul(input: Tensor, other: Tensor) -> Tensor: ...
 
 
-@compile_ops("module_ater_operator")
+@compile_ops("module_aiter_operator")
 def div(input: Tensor, other: Tensor) -> Tensor: ...
diff --git a/ater/ops/attention.py b/aiter/ops/attention.py
similarity index 96%
rename from ater/ops/attention.py
rename to aiter/ops/attention.py
index c0caf23cad..d545359651 100644
--- a/ater/ops/attention.py
+++ b/aiter/ops/attention.py
@@ -4,7 +4,7 @@
 import os
 import torch
 from typing import List, Optional
-from ..jit.core import compile_ops, CK_DIR, ATER_CSRC_DIR, ATER_ROOT_DIR
+from ..jit.core import compile_ops, CK_DIR, AITER_CSRC_DIR, AITER_ROOT_DIR
 MD_NAME = 'module_attention'
 
 
diff --git a/ater/ops/cache.py b/aiter/ops/cache.py
similarity index 95%
rename from ater/ops/cache.py
rename to aiter/ops/cache.py
index 80b7e431d7..96c55a21f2 100644
--- a/ater/ops/cache.py
+++ b/aiter/ops/cache.py
@@ -3,7 +3,7 @@
 
 from torch import Tensor
 from typing import List, Optional
-from ..jit.core import compile_ops, CK_DIR, ATER_CSRC_DIR
+from ..jit.core import compile_ops, CK_DIR, AITER_CSRC_DIR
 import torch.nn.functional as F
 
 MD_NAME = "module_cache"
diff --git a/ater/ops/communication.py b/aiter/ops/communication.py
similarity index 88%
rename from ater/ops/communication.py
rename to aiter/ops/communication.py
index af60358b87..513a934361 100644
--- a/ater/ops/communication.py
+++ b/aiter/ops/communication.py
@@ -5,7 +5,7 @@
 from torch import Tensor
 import torch.distributed as dist
 from typing import List, Optional
-from ..jit.core import compile_ops, CK_DIR, ATER_CSRC_DIR, ATER_ROOT_DIR
+from ..jit.core import compile_ops, CK_DIR, AITER_CSRC_DIR, AITER_ROOT_DIR
 from ..dist.parallel_state import (ensure_model_parallel_initialized,
                                    init_distributed_environment,
                                    set_custom_all_reduce,
@@ -16,9 +16,9 @@
 from ..dist.utils import (get_open_port,
                           get_distributed_init_method,
                           get_ip)
-import ater
+import aiter
 import logging
-logger = logging.getLogger("ater")
+logger = logging.getLogger("aiter")
 
 
 def init_dist_env(world_size, rankID):
@@ -56,7 +56,7 @@ def all_reduce_asm(inp: torch.Tensor):
 
     if ca._IS_CAPTURING:
         if torch.cuda.is_current_stream_capturing():
-            return ater.all_reduce_asm_(inp,
+            return aiter.all_reduce_asm_(inp,
                                         ca._ptr, ca.signal, ca.buffer, ca._IS_CAPTURING)
         else:
             # if warm up, mimic the allocation pattern
@@ -67,7 +67,7 @@ def all_reduce_asm(inp: torch.Tensor):
         # custom allreduce incurs a cost of cudaMemcpy, which should
         # be small(<=1% of overall latency) compared to the performance
         # gains of using custom kernels
-        return ater.all_reduce_asm_(inp,
+        return aiter.all_reduce_asm_(inp,
                                     ca._ptr, ca.signal, ca.buffer, ca._IS_CAPTURING)
 
 
@@ -79,7 +79,7 @@ def all_reduce_rmsnorm(input: Tensor,
     tp_grp = get_tp_group()
     ca = tp_grp.ca_comm
 
-    return ater.all_reduce_rmsnorm_(input, residual_in, weight, bias, epsilon,
+    return aiter.all_reduce_rmsnorm_(input, residual_in, weight, bias, epsilon,
                                     ca._ptr, ca.signal, ca.buffer, ca._IS_CAPTURING)
 
 
@@ -92,5 +92,5 @@ def all_reduce_rmsnorm_quant(input: Tensor,
     tp_grp = get_tp_group()
     ca = tp_grp.ca_comm
 
-    return ater.all_reduce_rmsnorm_quant_(input, residual_in, xscale, weight, bias, epsilon,
+    return aiter.all_reduce_rmsnorm_quant_(input, residual_in, xscale, weight, bias, epsilon,
                                           ca._ptr, ca.signal, ca.buffer, ca._IS_CAPTURING)
diff --git a/ater/ops/custom.py b/aiter/ops/custom.py
similarity index 88%
rename from ater/ops/custom.py
rename to aiter/ops/custom.py
index 0bf6143e43..f73c576cdc 100644
--- a/ater/ops/custom.py
+++ b/aiter/ops/custom.py
@@ -3,7 +3,7 @@
 
 from torch import Tensor
 from typing import List, Optional
-from ..jit.core import compile_ops, CK_DIR, ATER_CSRC_DIR
+from ..jit.core import compile_ops, CK_DIR, AITER_CSRC_DIR
 import torch.nn.functional as F
 
 MD_NAME = "module_custom"
diff --git a/ater/ops/custom_all_reduce.py b/aiter/ops/custom_all_reduce.py
similarity index 96%
rename from ater/ops/custom_all_reduce.py
rename to aiter/ops/custom_all_reduce.py
index c0c5203453..29609d72f9 100644
--- a/ater/ops/custom_all_reduce.py
+++ b/aiter/ops/custom_all_reduce.py
@@ -3,7 +3,7 @@
 
 from torch import Tensor
 from typing import List, Optional
-from ..jit.core import compile_ops, CK_DIR, ATER_CSRC_DIR, ATER_ROOT_DIR
+from ..jit.core import compile_ops, CK_DIR, AITER_CSRC_DIR, AITER_ROOT_DIR
 import torch.nn.functional as F
 
 MD_NAME = "module_custom_all_reduce"
diff --git a/ater/ops/gemm_op_a8w8.py b/aiter/ops/gemm_op_a8w8.py
similarity index 94%
rename from ater/ops/gemm_op_a8w8.py
rename to aiter/ops/gemm_op_a8w8.py
index c464144288..c73b1d35a9 100644
--- a/ater/ops/gemm_op_a8w8.py
+++ b/aiter/ops/gemm_op_a8w8.py
@@ -6,7 +6,7 @@
 from typing import List, Optional
 import functools
 import pandas as pd
-from ..jit.core import compile_ops, CK_DIR, ATER_CSRC_DIR, ATER_ROOT_DIR
+from ..jit.core import compile_ops, CK_DIR, AITER_CSRC_DIR, AITER_ROOT_DIR
 
 
 @compile_ops("module_gemm_a8w8", fc_name="gemm_a8w8")
@@ -64,7 +64,7 @@ def get_CKGEMM_config(
     K: int,
 ):
     if not hasattr(get_CKGEMM_config, "ckgemm_dict"):
-        ckgemm_dict = pd.read_csv(f"{ATER_ROOT_DIR}/ater/configs/a8w8_tuned_gemm.csv").drop_duplicates()
+        ckgemm_dict = pd.read_csv(f"{AITER_ROOT_DIR}/aiter/configs/a8w8_tuned_gemm.csv").drop_duplicates()
         get_CKGEMM_config.ckgemm_dict = ckgemm_dict.set_index(['M','N','K']).to_dict('index')
     config = get_CKGEMM_config.ckgemm_dict.get((M,N,K), None)
     if config != None:
@@ -84,7 +84,7 @@ def get_ASMGEMM_config(
     dtype: torch.dtype
 ):
     if not hasattr(get_ASMGEMM_config, "asmgemm_dict"):
-        asmGemmDictDf = pd.read_csv(f"{ATER_ROOT_DIR}/ater/configs/asm_a8w8_gemm.csv").drop_duplicates()
+        asmGemmDictDf = pd.read_csv(f"{AITER_ROOT_DIR}/aiter/configs/asm_a8w8_gemm.csv").drop_duplicates()
         asmGemmDictDf.bias = asmGemmDictDf.bias.apply(lambda s: True if s in ['True',1,'true'] else False)
         get_ASMGEMM_config.asmgemm_dict = asmGemmDictDf.set_index(['M','N','K','bias','outdtype']).to_dict('index')
     return get_ASMGEMM_config.asmgemm_dict.get((M,N,K,bias,str(dtype)), None)
diff --git a/ater/ops/moe_op.py b/aiter/ops/moe_op.py
similarity index 97%
rename from ater/ops/moe_op.py
rename to aiter/ops/moe_op.py
index a1b03db957..d3647c152a 100644
--- a/ater/ops/moe_op.py
+++ b/aiter/ops/moe_op.py
@@ -3,7 +3,7 @@
 
 from torch import Tensor
 from typing import List, Optional
-from ..jit.core import compile_ops, CK_DIR, ATER_CSRC_DIR, ATER_ROOT_DIR
+from ..jit.core import compile_ops, CK_DIR, AITER_CSRC_DIR, AITER_ROOT_DIR
 import torch.nn.functional as F
 
 
diff --git a/ater/ops/moe_sorting.py b/aiter/ops/moe_sorting.py
similarity index 86%
rename from ater/ops/moe_sorting.py
rename to aiter/ops/moe_sorting.py
index 845c10e120..e1d39733b0 100644
--- a/ater/ops/moe_sorting.py
+++ b/aiter/ops/moe_sorting.py
@@ -4,7 +4,7 @@
 import torch
 from torch import Tensor
 from typing import List, Optional
-from ..jit.core import compile_ops, CK_DIR, ATER_CSRC_DIR
+from ..jit.core import compile_ops, CK_DIR, AITER_CSRC_DIR
 import torch.nn.functional as F
 
 MD_NAME = "module_moe_sorting"
diff --git a/ater/ops/norm.py b/aiter/ops/norm.py
similarity index 97%
rename from ater/ops/norm.py
rename to aiter/ops/norm.py
index a78cda4569..231ca47133 100644
--- a/ater/ops/norm.py
+++ b/aiter/ops/norm.py
@@ -3,7 +3,7 @@
 
 from torch import Tensor
 from typing import List, Optional
-from ..jit.core import compile_ops, CK_DIR, ATER_CSRC_DIR, ATER_ROOT_DIR
+from ..jit.core import compile_ops, CK_DIR, AITER_CSRC_DIR, AITER_ROOT_DIR
 import torch.nn.functional as F
 
 MD_NAME = "module_norm"
diff --git a/ater/ops/pos_encoding.py b/aiter/ops/pos_encoding.py
similarity index 90%
rename from ater/ops/pos_encoding.py
rename to aiter/ops/pos_encoding.py
index 40d221cf85..c43deb38ae 100644
--- a/ater/ops/pos_encoding.py
+++ b/aiter/ops/pos_encoding.py
@@ -3,7 +3,7 @@
 
 from torch import Tensor
 from typing import List, Optional
-from ..jit.core import compile_ops, CK_DIR, ATER_CSRC_DIR, ATER_ROOT_DIR
+from ..jit.core import compile_ops, CK_DIR, AITER_CSRC_DIR, AITER_ROOT_DIR
 import torch.nn.functional as F
 
 MD_NAME = "module_pos_encoding"
diff --git a/ater/ops/quant.py b/aiter/ops/quant.py
similarity index 96%
rename from ater/ops/quant.py
rename to aiter/ops/quant.py
index caf8d0582e..74cf86beae 100644
--- a/ater/ops/quant.py
+++ b/aiter/ops/quant.py
@@ -4,7 +4,7 @@
 import torch
 from torch import Tensor
 from typing import List, Optional
-from ..jit.core import compile_ops, CK_DIR, ATER_CSRC_DIR
+from ..jit.core import compile_ops, CK_DIR, AITER_CSRC_DIR
 
 
 @compile_ops("module_smoothquant")
diff --git a/ater/ops/rmsnorm.py b/aiter/ops/rmsnorm.py
similarity index 97%
rename from ater/ops/rmsnorm.py
rename to aiter/ops/rmsnorm.py
index 9831be3384..f2e7923f5d 100644
--- a/ater/ops/rmsnorm.py
+++ b/aiter/ops/rmsnorm.py
@@ -3,7 +3,7 @@
 
 from torch import Tensor
 from typing import List, Optional
-from ..jit.core import compile_ops, CK_DIR, ATER_CSRC_DIR
+from ..jit.core import compile_ops, CK_DIR, AITER_CSRC_DIR
 import torch.nn.functional as F
 
 MD_NAME = "module_rmsnorm"
diff --git a/ater/ops/shuffle.py b/aiter/ops/shuffle.py
similarity index 100%
rename from ater/ops/shuffle.py
rename to aiter/ops/shuffle.py
diff --git a/ater/paged_attn.py b/aiter/paged_attn.py
similarity index 99%
rename from ater/paged_attn.py
rename to aiter/paged_attn.py
index 4da0ec8e56..660e4f4eb4 100644
--- a/ater/paged_attn.py
+++ b/aiter/paged_attn.py
@@ -16,7 +16,7 @@
 
 from typing import List, Optional, Tuple, Union
 import torch
-import ater as ops
+import aiter as ops
 
 from dataclasses import dataclass
 # from vllm.utils import is_hip
diff --git a/ater/rotary_embedding.py b/aiter/rotary_embedding.py
similarity index 99%
rename from ater/rotary_embedding.py
rename to aiter/rotary_embedding.py
index 453f1c3f5a..ed530cf15c 100644
--- a/ater/rotary_embedding.py
+++ b/aiter/rotary_embedding.py
@@ -158,7 +158,7 @@ def forward(
         offsets: Optional[torch.Tensor] = None,
     ) -> Tuple[torch.Tensor, torch.Tensor]:
         # from vllm import _custom_ops as ops
-        import ater as ops
+        import aiter as ops
 
         self.cos_sin_cache = self.cos_sin_cache.to(query.device,
                                                    dtype=query.dtype)
diff --git a/ater/test_common.py b/aiter/test_common.py
similarity index 96%
rename from ater/test_common.py
rename to aiter/test_common.py
index a44e70c109..f2e7441265 100644
--- a/ater/test_common.py
+++ b/aiter/test_common.py
@@ -6,7 +6,7 @@
 import os
 import numpy as np
 import pandas as pd
-from ater import logger
+from aiter import logger
 
 
 def perftest(num_iters=101, num_warmup=10, testGraph=False):
@@ -15,7 +15,7 @@ def wrapper(*args, **kwargs):
             for _ in range(num_warmup):
                 data = func(*args, **kwargs)
 
-            if int(os.environ.get('ATER_LOG_MORE', 0)):
+            if int(os.environ.get('AITER_LOG_MORE', 0)):
                 latencies = []
                 start_event = torch.cuda.Event(enable_timing=True)
                 end_event = torch.cuda.Event(enable_timing=True)
@@ -46,7 +46,7 @@ def wrapper(*args, **kwargs):
                              with_modules=True,
                              #  record_shapes=True,
                              #  on_trace_ready=tpf.tensorboard_trace_handler(
-                             #      './ater_logs/'),
+                             #      './aiter_logs/'),
                              ) as prof:
                 data = run_iters(num_iters, func, *args, **kwargs)
             avg = get_trace_perf(prof, num_iters)
@@ -101,7 +101,7 @@ def get_trace_perf(prof, num_iters):
     avg_name = '[avg us/iter]'
     for el in timerList:
         df.at[avg_name, el] = df[el].sum()/num_iters
-    if int(os.environ.get('ATER_LOG_MORE', 0)):
+    if int(os.environ.get('AITER_LOG_MORE', 0)):
         logger.info(f'{df}')
     return df.at[avg_name, 'device_time_total']
 
diff --git a/ater/tuned_gemm.py b/aiter/tuned_gemm.py
similarity index 97%
rename from ater/tuned_gemm.py
rename to aiter/tuned_gemm.py
index a4f7c44178..ab7c4337dc 100644
--- a/ater/tuned_gemm.py
+++ b/aiter/tuned_gemm.py
@@ -20,9 +20,9 @@
 import pandas as pd
 import torch
 import torch.nn.functional as F
-from ater import hipb_create_extension, hipb_mm, getHipblasltKernelName
-from ater import rocb_create_extension, rocb_mm
-from ater import logger
+from aiter import hipb_create_extension, hipb_mm, getHipblasltKernelName
+from aiter import rocb_create_extension, rocb_mm
+from aiter import logger
 
 this_dir = os.path.dirname(os.path.abspath(__file__))
 
@@ -92,7 +92,7 @@ def query_sol(self, m, n, k, bias, dtype, otype, scaleAB=False):
         return soltype, solidx
 
     def apply_skinny(self, inp, weights, solidx, bias=None, otype=None, scale_a=None, scale_b=None, scale_c=None):
-        import ater as ops
+        import aiter as ops
         if solidx == 0:
             out = torch.empty(inp.shape[0],
                               weights.shape[0],
diff --git a/aiter_logs/readme.md b/aiter_logs/readme.md
new file mode 100644
index 0000000000..9558fad2b1
--- /dev/null
+++ b/aiter_logs/readme.md
@@ -0,0 +1 @@
+python3 ./aiter_logs/run.py ./aiter_logs/ ar_asm all
\ No newline at end of file
diff --git a/ater_logs/run.py b/aiter_logs/run.py
similarity index 100%
rename from ater_logs/run.py
rename to aiter_logs/run.py
diff --git a/ater_logs/readme.md b/ater_logs/readme.md
deleted file mode 100644
index e22d2a822e..0000000000
--- a/ater_logs/readme.md
+++ /dev/null
@@ -1 +0,0 @@
-python3 ./ater_logs/run.py ./ater_logs/ ar_asm all
\ No newline at end of file
diff --git a/csrc/ck_gemm_a8w8/README.md b/csrc/ck_gemm_a8w8/README.md
index 66f3c55a31..884a7d94ad 100644
--- a/csrc/ck_gemm_a8w8/README.md
+++ b/csrc/ck_gemm_a8w8/README.md
@@ -1,18 +1,18 @@
 # CK gemm a8w8 tune
 
-1. Install ater:  
+1. Install aiter:  
 `python3 setup.py develop`
 
 2. Tune gemm a8w8: 
- First add GEMM shapes in `ater/configs/a8w8_untuned_gemm.csv`, then run the following cmd to start tuning, please wait a few minutes as it will build gemm_a8w8_tune via jit:  
-`python3 csrc/ck_gemm_a8w8/gemm_a8w8_tune.py -i ater/configs/a8w8_untuned_gemm.csv -o ater/configs/a8w8_tuned_gemm.csv`  
+ First add GEMM shapes in `aiter/configs/a8w8_untuned_gemm.csv`, then run the following cmd to start tuning, please wait a few minutes as it will build gemm_a8w8_tune via jit:  
+`python3 csrc/ck_gemm_a8w8/gemm_a8w8_tune.py -i aiter/configs/a8w8_untuned_gemm.csv -o aiter/configs/a8w8_tuned_gemm.csv`  
 If you want to use split K kernels, you can add the `-k` parameter at the end, notice that should change `bias` to `bias/(2^k)`.
-You can find the results of the tuning in `ater/configs/a8w8_tuned_gemm.csv`.
+You can find the results of the tuning in `aiter/configs/a8w8_tuned_gemm.csv`.
 
-3. Test the performance, modify the test instance in `op_tests/test_gemm_a8w8.py` and run it, please wait a few minutes as it will build gemm_a8w8 kernels in `ater/configs/a8w8_tuned_gemm.csv` via jit：  
+3. Test the performance, modify the test instance in `op_tests/test_gemm_a8w8.py` and run it, please wait a few minutes as it will build gemm_a8w8 kernels in `aiter/configs/a8w8_tuned_gemm.csv` via jit：  
 `python3 op_tests/test_gemm_a8w8.py`
 
 
 ## More
-If you want to re-install gemm_a8w8, you should remove `ater/jit/module_gemm_a8w8.so` and `ater/jit/build/module_gemm_a8w8` first.
-If you use flag `PREBUILD_KERNELS=1 USE_CK_A8W8=1` when you install ater, it will build gemm a8w8 kernels in `ater/configs/a8w8_tuned_gemm.csv` by default. If you want to use the new result of gemm_a8w8_tune, please remove `build` and `*.so` first, then re-intall ater after finishing tune.
\ No newline at end of file
+If you want to re-install gemm_a8w8, you should remove `aiter/jit/module_gemm_a8w8.so` and `aiter/jit/build/module_gemm_a8w8` first.
+If you use flag `PREBUILD_KERNELS=1 USE_CK_A8W8=1` when you install aiter, it will build gemm a8w8 kernels in `aiter/configs/a8w8_tuned_gemm.csv` by default. If you want to use the new result of gemm_a8w8_tune, please remove `build` and `*.so` first, then re-intall aiter after finishing tune.
\ No newline at end of file
diff --git a/csrc/ck_gemm_a8w8/gemm_a8w8_tune.py b/csrc/ck_gemm_a8w8/gemm_a8w8_tune.py
index 8b907698fe..0d7993c4ff 100644
--- a/csrc/ck_gemm_a8w8/gemm_a8w8_tune.py
+++ b/csrc/ck_gemm_a8w8/gemm_a8w8_tune.py
@@ -2,12 +2,12 @@
 # Copyright (c) 2024, Advanced Micro Devices, Inc. All rights reserved.
 import os
 import sys
-import ater
+import aiter
 import pandas as pd
 import torch
 import torch.nn.functional as F
-import ater
-from ater.test_common import checkAllclose, perftest
+import aiter
+from aiter.test_common import checkAllclose, perftest
 from gemm_a8w8_common import kernelInstance, kernels_list
 import argparse
 
@@ -45,7 +45,7 @@ def get_tuned_gemm_list(tuned_gemm_file):
 
 @perftest()
 def kernel_instance_test(x, weight, x_scale, w_scale, out, kernel_id, splitK=0):
-    ater.gemm_a8w8_tune(x, weight, x_scale, w_scale, out, kernel_id, splitK)
+    aiter.gemm_a8w8_tune(x, weight, x_scale, w_scale, out, kernel_id, splitK)
     return out
 
 
@@ -66,7 +66,7 @@ def tune_gemm(m, n, k, useSplitK = False):
     best_time = -1
     for i in range(kernels_num):
         kernel = kernels_list[i]
-        maxsplitK = ater.compute_gemm_SplitK(m, n, k, kernel.MPerBLOCK, kernel.NPerBLOCK, kernel.KPerBLOCK) \
+        maxsplitK = aiter.compute_gemm_SplitK(m, n, k, kernel.MPerBLOCK, kernel.NPerBLOCK, kernel.KPerBLOCK) \
             if useSplitK else 0
         for splitK in range(maxsplitK+1):
             try:
@@ -129,7 +129,7 @@ def tune_gemm_list(untunedf, tunedf, issorted = False, useSplitK = False):
     parser.add_argument(
         "-i",
         "--untune_file",
-        default="ater/configs/a8w8_untuned_gemm.csv",
+        default="aiter/configs/a8w8_untuned_gemm.csv",
         required=False,
         help="input"
     )
@@ -137,7 +137,7 @@ def tune_gemm_list(untunedf, tunedf, issorted = False, useSplitK = False):
     parser.add_argument(
         "-o",
         "--tune_file",
-        default="ater/configs/a8w8_tuned_gemm.csv",
+        default="aiter/configs/a8w8_tuned_gemm.csv",
         required=False,
         help="output: tuning result store this file"
     )
diff --git a/csrc/ck_gemm_a8w8/gen_instances.py b/csrc/ck_gemm_a8w8/gen_instances.py
index 87855196a5..347e8b23c7 100644
--- a/csrc/ck_gemm_a8w8/gen_instances.py
+++ b/csrc/ck_gemm_a8w8/gen_instances.py
@@ -286,7 +286,7 @@ def get_tune_dict(tune_dict_csv):
     parser.add_argument(
         "-f",
         "--tune_file",
-        default="ater/configs/a8w8_tuned_gemm.csv",
+        default="aiter/configs/a8w8_tuned_gemm.csv",
         required=False,
         help="tune_file include the result after run gemm_a8w8_tune.py"
     )
diff --git a/csrc/include/ater_hip_common.h b/csrc/include/aiter_hip_common.h
similarity index 84%
rename from csrc/include/ater_hip_common.h
rename to csrc/include/aiter_hip_common.h
index ef56b5387d..0a78ce05a1 100644
--- a/csrc/include/ater_hip_common.h
+++ b/csrc/include/aiter_hip_common.h
@@ -24,7 +24,7 @@ struct p2
     unsigned int _p0;
     unsigned int _p1;
 };
-struct AterAsmKernelArgs
+struct AiterAsmKernelArgs
 {
     void *args_ptr;
     void *arg_size_ptr;
@@ -37,22 +37,22 @@ struct AterAsmKernelArgs
     const hipStream_t stream;
 };
 
-class AterAsmKernel
+class AiterAsmKernel
 {
 private:
     hipModule_t module;
     hipFunction_t kernel_func;
 
 public:
-    AterAsmKernel(const char *name, const char *hsaco)
+    AiterAsmKernel(const char *name, const char *hsaco)
     {
-        std::cout << "hipModuleLoad: " << (std::string(ATER_ASM_DIR) + hsaco).c_str() << " GetFunction: " << hsaco;
-        HIP_CALL(hipModuleLoad(&module, (std::string(ATER_ASM_DIR) + hsaco).c_str()));
+        std::cout << "hipModuleLoad: " << (std::string(AITER_ASM_DIR) + hsaco).c_str() << " GetFunction: " << name;
+        HIP_CALL(hipModuleLoad(&module, (std::string(AITER_ASM_DIR) + hsaco).c_str()));
         HIP_CALL(hipModuleGetFunction(&kernel_func, module, name));
         std::cout << " Success" << std::endl;
     };
 
-    void launch_kernel(const AterAsmKernelArgs &kargs)
+    void launch_kernel(const AiterAsmKernelArgs &kargs)
     {
         void *config[] = {HIP_LAUNCH_PARAM_BUFFER_POINTER, kargs.args_ptr,
                           HIP_LAUNCH_PARAM_BUFFER_SIZE, kargs.arg_size_ptr,
diff --git a/csrc/include/aiter_operator.h b/csrc/include/aiter_operator.h
new file mode 100644
index 0000000000..9594afadfa
--- /dev/null
+++ b/csrc/include/aiter_operator.h
@@ -0,0 +1,9 @@
+#pragma once
+// SPDX-License-Identifier: MIT
+// Copyright (c) 2024, Advanced Micro Devices, Inc. All rights reserved.
+#include <torch/extension.h>
+
+torch::Tensor aiter_add(torch::Tensor &input0, torch::Tensor &input1);
+torch::Tensor aiter_mul(torch::Tensor &input0, torch::Tensor &input1);
+torch::Tensor aiter_sub(torch::Tensor &input0, torch::Tensor &input1);
+torch::Tensor aiter_div(torch::Tensor &input0, torch::Tensor &input1);
\ No newline at end of file
diff --git a/csrc/include/ater_operator.h b/csrc/include/ater_operator.h
deleted file mode 100644
index 679dc5a4f0..0000000000
--- a/csrc/include/ater_operator.h
+++ /dev/null
@@ -1,9 +0,0 @@
-#pragma once
-// SPDX-License-Identifier: MIT
-// Copyright (c) 2024, Advanced Micro Devices, Inc. All rights reserved.
-#include <torch/extension.h>
-
-torch::Tensor ater_add(torch::Tensor &input0, torch::Tensor &input1);
-torch::Tensor ater_mul(torch::Tensor &input0, torch::Tensor &input1);
-torch::Tensor ater_sub(torch::Tensor &input0, torch::Tensor &input1);
-torch::Tensor ater_div(torch::Tensor &input0, torch::Tensor &input1);
\ No newline at end of file
diff --git a/csrc/kernels/ater_operator.cu b/csrc/kernels/aiter_operator.cu
similarity index 98%
rename from csrc/kernels/ater_operator.cu
rename to csrc/kernels/aiter_operator.cu
index 205de29613..7d889ad041 100644
--- a/csrc/kernels/ater_operator.cu
+++ b/csrc/kernels/aiter_operator.cu
@@ -554,7 +554,7 @@ namespace vllm
 }
 
 template <typename Operation>
-torch::Tensor ater_operation(torch::Tensor &input, torch::Tensor &other)
+torch::Tensor aiter_operation(torch::Tensor &input, torch::Tensor &other)
 {
   int dim = input.dim();
 
@@ -834,22 +834,22 @@ torch::Tensor ater_operation(torch::Tensor &input, torch::Tensor &other)
   }
 }
 
-torch::Tensor ater_add(torch::Tensor &input, torch::Tensor &other)
+torch::Tensor aiter_add(torch::Tensor &input, torch::Tensor &other)
 {
-  return ater_operation<vllm::AddOp>(input, other);
+  return aiter_operation<vllm::AddOp>(input, other);
 }
 
-torch::Tensor ater_sub(torch::Tensor &input, torch::Tensor &other)
+torch::Tensor aiter_sub(torch::Tensor &input, torch::Tensor &other)
 {
-  return ater_operation<vllm::SubOp>(input, other);
+  return aiter_operation<vllm::SubOp>(input, other);
 }
 
-torch::Tensor ater_mul(torch::Tensor &input, torch::Tensor &other)
+torch::Tensor aiter_mul(torch::Tensor &input, torch::Tensor &other)
 {
-  return ater_operation<vllm::MulOp>(input, other);
+  return aiter_operation<vllm::MulOp>(input, other);
 }
 
-torch::Tensor ater_div(torch::Tensor &input, torch::Tensor &other)
+torch::Tensor aiter_div(torch::Tensor &input, torch::Tensor &other)
 {
-  return ater_operation<vllm::DivOp>(input, other);
+  return aiter_operation<vllm::DivOp>(input, other);
 }
diff --git a/csrc/py_itfs_cu/asm_communication.cpp b/csrc/py_itfs_cu/asm_communication.cpp
index 292f60512f..07f7d297b4 100644
--- a/csrc/py_itfs_cu/asm_communication.cpp
+++ b/csrc/py_itfs_cu/asm_communication.cpp
@@ -7,7 +7,7 @@
 #include <c10/cuda/CUDAGuard.h>
 
 #include "communication_asm.h"
-#include "ater_hip_common.h"
+#include "aiter_hip_common.h"
 #include "custom_all_reduce.cuh"
 
 torch::Tensor all_reduce_asm(torch::Tensor &input,
@@ -92,7 +92,7 @@ torch::Tensor all_reduce_asm(torch::Tensor &input,
     args.stride_wave = stride_WV;
     args.loopcnt = 10;
 
-    static AterAsmKernel impl("allreduce_kernel_func", "all_reduce.co");
+    static AiterAsmKernel impl("allreduce_kernel_func", "all_reduce.co");
     impl.launch_kernel({&args,
                         &arg_size,
                         gdx, // gdx
@@ -232,7 +232,7 @@ std::tuple<torch::Tensor, torch::Tensor> all_reduce_rmsnorm(torch::Tensor &input
     args.tgs = TGs;
     args.loopcnt = 0;
 
-    static AterAsmKernel impl("allreduce_rmsnorm_N8192_kernel", "allreduce_rmsnorm_N8192.co");
+    static AiterAsmKernel impl("allreduce_rmsnorm_N8192_kernel", "allreduce_rmsnorm_N8192.co");
 
     impl.launch_kernel({&args,
                         &arg_size,
@@ -378,7 +378,7 @@ std::tuple<torch::Tensor, torch::Tensor, torch::Tensor> all_reduce_rmsnorm_quant
     args.tgs = TGs;
     args.loopcnt = 0;
 
-    static AterAsmKernel impl("allreduce_rmsnorm_qnt_N8192_kernel", "allreduce_rmsnorm_qnt_N8192.co");
+    static AiterAsmKernel impl("allreduce_rmsnorm_qnt_N8192_kernel", "allreduce_rmsnorm_qnt_N8192.co");
 
     impl.launch_kernel({&args,
                         &arg_size,
diff --git a/csrc/py_itfs_cu/asm_fmoe.cpp b/csrc/py_itfs_cu/asm_fmoe.cpp
index a11644e851..10b9b78079 100644
--- a/csrc/py_itfs_cu/asm_fmoe.cpp
+++ b/csrc/py_itfs_cu/asm_fmoe.cpp
@@ -4,7 +4,7 @@
 #include <hip/hip_fp16.h>
 #include <torch/all.h>
 #include <ATen/cuda/CUDAContext.h>
-#include "ater_hip_common.h"
+#include "aiter_hip_common.h"
 
 struct __attribute__((packed)) KernelArgs
 {
@@ -72,8 +72,8 @@ class FMoeKernel
 public:
     FMoeKernel(const char *name, const char *hsaco, uint32_t sub_GU = 512)
     {
-        std::cout << "hipModuleLoad: " << (std::string(ATER_ASM_DIR) + hsaco).c_str() << " GetFunction: " << hsaco;
-        HIP_CALL(hipModuleLoad(&module, (std::string(ATER_ASM_DIR) + hsaco).c_str()));
+        std::cout << "hipModuleLoad: " << (std::string(AITER_ASM_DIR) + hsaco).c_str() << " GetFunction: " << name;
+        HIP_CALL(hipModuleLoad(&module, (std::string(AITER_ASM_DIR) + hsaco).c_str()));
         HIP_CALL(hipModuleGetFunction(&kernel_func, module, name));
         std::cout << " Success" << std::endl;
         this->sub_GU = sub_GU;
diff --git a/csrc/py_itfs_cu/asm_gemm_a8w8.cpp b/csrc/py_itfs_cu/asm_gemm_a8w8.cpp
index d89691850c..2f8a95213a 100644
--- a/csrc/py_itfs_cu/asm_gemm_a8w8.cpp
+++ b/csrc/py_itfs_cu/asm_gemm_a8w8.cpp
@@ -4,7 +4,7 @@
 #include <hip/hip_fp16.h>
 #include <torch/all.h>
 #include <ATen/cuda/CUDAContext.h>
-#include "ater_hip_common.h"
+#include "aiter_hip_common.h"
 
 // start to prepare the input and output buffer
 struct __attribute__((packed)) KernelArgs
@@ -80,9 +80,9 @@ torch::Tensor gemm_a8w8_asm(torch::Tensor &A,       // A:[M, K] i8
     args.ks = ks;
 
     const cudaStream_t stream = at::cuda::getCurrentCUDAStream();
-    static AterAsmKernel splitK_impl("gemm_kernel_func", "gemm_a8w8_m128_splitK.co");
-    static AterAsmKernel noSplitK_impl("gemm_kernel_func", "gemm_a8w8_m128_noSplitK.co");
-    AterAsmKernel *impl_ptr = &noSplitK_impl;
+    static AiterAsmKernel splitK_impl("gemm_kernel_func", "gemm_a8w8_m128_splitK.co");
+    static AiterAsmKernel noSplitK_impl("gemm_kernel_func", "gemm_a8w8_m128_noSplitK.co");
+    AiterAsmKernel *impl_ptr = &noSplitK_impl;
     if (ks > 0)
         impl_ptr = &splitK_impl;
 
diff --git a/csrc/py_itfs_cu/asm_layernorm.cpp b/csrc/py_itfs_cu/asm_layernorm.cpp
index 9490f5a2f4..d9ef897af3 100644
--- a/csrc/py_itfs_cu/asm_layernorm.cpp
+++ b/csrc/py_itfs_cu/asm_layernorm.cpp
@@ -4,7 +4,7 @@
 #include <hip/hip_fp16.h>
 #include <torch/all.h>
 #include <ATen/cuda/CUDAContext.h>
-#include "ater_hip_common.h"
+#include "aiter_hip_common.h"
 
 struct __attribute__((packed)) KernelArgs
 {
@@ -67,7 +67,7 @@ void layernorm2d_with_add_asm(torch::Tensor &out,          // [m ,n]
 
     const cudaStream_t stream = at::cuda::getCurrentCUDAStream();
     int sub_M = 2;
-    static AterAsmKernel impl("layer_norm_kernel_func", "layer_norm.co");
+    static AiterAsmKernel impl("layer_norm_kernel_func", "layer_norm.co");
 
     impl.launch_kernel({&args,
                         &arg_size,
@@ -119,7 +119,7 @@ void layernorm2d_with_add_smoothquant_asm(torch::Tensor &out,          // [m ,n]
 
     const cudaStream_t stream = at::cuda::getCurrentCUDAStream();
     int sub_M = 2;
-    static AterAsmKernel impl("layer_norm_qnt", "layer_norm_qnt.co");
+    static AiterAsmKernel impl("layer_norm_qnt", "layer_norm_qnt.co");
 
     impl.launch_kernel({&args,
                         &arg_size,
diff --git a/csrc/py_itfs_cu/asm_pa.cpp b/csrc/py_itfs_cu/asm_pa.cpp
index bac5df825c..2cafad028d 100644
--- a/csrc/py_itfs_cu/asm_pa.cpp
+++ b/csrc/py_itfs_cu/asm_pa.cpp
@@ -4,7 +4,7 @@
 #include <hip/hip_fp16.h>
 #include <torch/all.h>
 #include <ATen/cuda/CUDAContext.h>
-#include "ater_hip_common.h"
+#include "aiter_hip_common.h"
 
 struct __attribute__((packed)) KernelArgs
 {
@@ -93,9 +93,9 @@ torch::Tensor pa_fwd(torch::Tensor &Q,            //   [num_seqs, num_heads, hea
     args.KVs = stride_KV_head;
 
     const cudaStream_t stream = at::cuda::getCurrentCUDAStream();
-    static AterAsmKernel impl_a16w16("pa_kernel_func", "pa_a16w16.co");
-    static AterAsmKernel impl_a16w8("pa_kernel_func", "pa_a16w8.co");
-    AterAsmKernel *impl_ptr = &impl_a16w16;
+    static AiterAsmKernel impl_a16w16("pa_kernel_func", "pa_a16w16.co");
+    static AiterAsmKernel impl_a16w8("pa_kernel_func", "pa_a16w8.co");
+    AiterAsmKernel *impl_ptr = &impl_a16w16;
 
     if (K_QScale)
         impl_ptr = &impl_a16w8;
diff --git a/csrc/pybind/aiter_operator_pybind.cu b/csrc/pybind/aiter_operator_pybind.cu
new file mode 100644
index 0000000000..594470e5c6
--- /dev/null
+++ b/csrc/pybind/aiter_operator_pybind.cu
@@ -0,0 +1,11 @@
+// SPDX-License-Identifier: MIT
+// Copyright (c) 2024, Advanced Micro Devices, Inc. All rights reserved.
+#include "aiter_operator.h"
+
+PYBIND11_MODULE(TORCH_EXTENSION_NAME, m)
+{
+    m.def("add", &aiter_add, "apply for add with transpose and broadcast.");
+    m.def("mul", &aiter_mul, "apply for mul with transpose and broadcast.");
+    m.def("sub", &aiter_sub, "apply for sub with transpose and broadcast.");
+    m.def("div", &aiter_div, "apply for div with transpose and broadcast.");
+}
\ No newline at end of file
diff --git a/csrc/pybind/ater_operator_pybind.cu b/csrc/pybind/ater_operator_pybind.cu
deleted file mode 100644
index 46fc802b12..0000000000
--- a/csrc/pybind/ater_operator_pybind.cu
+++ /dev/null
@@ -1,11 +0,0 @@
-// SPDX-License-Identifier: MIT
-// Copyright (c) 2024, Advanced Micro Devices, Inc. All rights reserved.
-#include "ater_operator.h"
-
-PYBIND11_MODULE(TORCH_EXTENSION_NAME, m)
-{
-    m.def("add", &ater_add, "apply for add with transpose and broadcast.");
-    m.def("mul", &ater_mul, "apply for mul with transpose and broadcast.");
-    m.def("sub", &ater_sub, "apply for sub with transpose and broadcast.");
-    m.def("div", &ater_div, "apply for div with transpose and broadcast.");
-}
\ No newline at end of file
diff --git a/csrc/rocm_ops.cpp b/csrc/rocm_ops.cpp
index 986a46b643..ed061be3be 100644
--- a/csrc/rocm_ops.cpp
+++ b/csrc/rocm_ops.cpp
@@ -14,7 +14,7 @@
 #include "pos_encoding.h"
 #include "rmsnorm.h"
 #include "smoothquant.h"
-#include "ater_operator.h"
+#include "aiter_operator.h"
 #include "asm_gemm_a8w8.h"
 #include <torch/extension.h>
 #include "gemm_a8w8.h"
@@ -168,10 +168,10 @@ PYBIND11_MODULE(TORCH_EXTENSION_NAME, m)
             py::arg("fc1_scale"), py::arg("fc2_scale"),
             py::arg("fc2_smooth_scale") = std::nullopt);
       m.def("fmoe_int8_g1u0_a16", &fmoe_int8_g1u0_a16);
-      m.def("add", &ater_add, "apply for add with transpose and broadcast.");
-      m.def("mul", &ater_mul, "apply for mul with transpose and broadcast.");
-      m.def("sub", &ater_sub, "apply for sub with transpose and broadcast.");
-      m.def("div", &ater_div, "apply for div with transpose and broadcast.");
+      m.def("add", &aiter_add, "apply for add with transpose and broadcast.");
+      m.def("mul", &aiter_mul, "apply for mul with transpose and broadcast.");
+      m.def("sub", &aiter_sub, "apply for sub with transpose and broadcast.");
+      m.def("div", &aiter_div, "apply for div with transpose and broadcast.");
       m.def("pa_fwd_asm", &pa_fwd, "pa_fwd",
             py::arg("Q"),
             py::arg("K"),
diff --git a/gradlib/README.md b/gradlib/README.md
index 94eb161a8d..65942833d5 100644
--- a/gradlib/README.md
+++ b/gradlib/README.md
@@ -13,17 +13,17 @@ By gradlib, we can confirm the parameter of GEMMs with best performance in the s
 
 ## How to use gradlib
 
-1. to get GEMM shapes to be tuned, replace F.linear by tgemm.mm under ater/tuned_gemm.py,
+1. to get GEMM shapes to be tuned, replace F.linear by tgemm.mm under aiter/tuned_gemm.py,
    run
 
    `
     VLLM_TUNE_GEMM=1 python {workload_tests}
-shapes will be captured in ater/configs/untuned_gemm.csv
+shapes will be captured in aiter/configs/untuned_gemm.csv
    `
-2. to tune GEMMs in ater/configs/untuned_gemm.csv,
+2. to tune GEMMs in aiter/configs/untuned_gemm.csv,
    run
    
    ` 
-    python3 gradlib/gradlib/gemm_tuner.py --tuned_file ater/configs/tuned_gemm.csv  --input_file ater/configs/untuned_gemm.csv
+    python3 gradlib/gradlib/gemm_tuner.py --tuned_file aiter/configs/tuned_gemm.csv  --input_file aiter/configs/untuned_gemm.csv
    `
 4. then run your test as normal~
diff --git a/gradlib/gradlib/GemmTuner.py b/gradlib/gradlib/GemmTuner.py
index a741e39b6e..0c91ae29a4 100644
--- a/gradlib/gradlib/GemmTuner.py
+++ b/gradlib/gradlib/GemmTuner.py
@@ -17,15 +17,15 @@
 import random
 from pathlib import Path
 
-import ater
+import aiter
 import pandas as pd
-import ater
+import aiter
 import torch
 import torch.nn.functional as F
-from ater.test_common import perftest
+from aiter.test_common import perftest
 
-ater.rocb_create_extension()
-ater.hipb_create_extension()
+aiter.rocb_create_extension()
+aiter.hipb_create_extension()
 
 rtol = 1e-5
 atol = 1
@@ -72,7 +72,7 @@ def __init__(self, m, n, k, bias, indtype, outdtype, scaleAB=False, rocblas_deco
         self.rocblas_decode = rocblas_decode
 
     def find_hipblas_sols(self):
-        sols = ater.hipb_findallsols(self.inp,
+        sols = aiter.hipb_findallsols(self.inp,
                                      self.weights.t(),
                                      bias=self.bias,
                                      out_dtype=self.outdtype,
@@ -106,7 +106,7 @@ def check_gemm_ref(self, libtype, solidx):
         else:
             ref = F.linear(self.inp, self.weights, self.bias)
         if libtype == 'hipblaslt':
-            c = ater.hipb_mm(self.inp,
+            c = aiter.hipb_mm(self.inp,
                              self.weights.t(),
                              solidx,
                              bias=self.bias,
@@ -114,7 +114,7 @@ def check_gemm_ref(self, libtype, solidx):
                              scaleA=scaleA,
                              scaleB=scaleB)
         elif libtype == 'rocblas':
-            c = ater.rocb_mm(self.inp, self.weights.t(), solidx)
+            c = aiter.rocb_mm(self.inp, self.weights.t(), solidx)
             if self.bias is not None:
                 c += self.bias
         if torch.allclose(c.to(self.outdtype),
@@ -136,7 +136,7 @@ def check_gemm_ref(self, libtype, solidx):
     def hipb_time_sol(self, solidx, cold_iters=2, warm_iters=10):
         @perftest(num_warmup=cold_iters, num_iters=warm_iters)
         def call_hipb_mm(input, weight, solidx, out_dtype, scale_a=None, scale_b=None):
-            ater.hipb_mm(input, weight, solidx, out_dtype=out_dtype, scaleA=scale_a, scaleB=scale_b)
+            aiter.hipb_mm(input, weight, solidx, out_dtype=out_dtype, scaleA=scale_a, scaleB=scale_b)
 
         scaleA = HALF if self.scaleAB else None
         scaleB = HALF if self.scaleAB else None
@@ -150,13 +150,13 @@ def call_hipb_mm(input, weight, solidx, out_dtype, scale_a=None, scale_b=None):
         gtime = gtime / 1000.0
         # print('>>>hipbtime',solidx)
         # for i in range(cold_iters):
-        #     ater.hipb_mm(self.inp,
+        #     aiter.hipb_mm(self.inp,
         #                            self.weights.t(),
         #                            solidx,
         #                            out_dtype=self.outdtype)
         # self.start.record()
         # for i in range(warm_iters):
-        #     ater.hipb_mm(self.inp,
+        #     aiter.hipb_mm(self.inp,
         #                            self.weights2[random.randint(
         #                                0, self.nb - 1)].t(),
         #                            solidx,
@@ -192,10 +192,10 @@ def hipb_time_all_sols(self, fast_mode=0, top_sols=0):
     def rocb_time_sol(self, solidx, cold_iters=2, warm_iters=10):
 
         def rocb_mm_bias(inp, w, solidx, bias):
-            return ater.rocb_mm(inp, w, solidx) + bias
+            return aiter.rocb_mm(inp, w, solidx) + bias
 
         def rocb_mm_nobias(inp, w, solidx, _):
-            return ater.rocb_mm(inp, w, solidx)
+            return aiter.rocb_mm(inp, w, solidx)
 
         rocb_fun = rocb_mm_bias if self.bias is not None else rocb_mm_nobias
 
@@ -229,7 +229,7 @@ def find_rocblas_sols(self):
         if self.scaleAB or self.bias is not None:
             sols = []
         else:
-            sols = ater.rocb_findallsols(self.inp, self.weights.t())
+            sols = aiter.rocb_findallsols(self.inp, self.weights.t())
         print('M N K dtype',
               self.m,
               self.n,
@@ -390,7 +390,7 @@ def find_best_sols(self):
             soldf.loc[i, 'libtype'] = gemmobj.best_libtype
             soldf.loc[i, 'solidx'] = gemmobj.best_solidx
             soldf.loc[i, 'soltimes'] = round(gemmobj.best_soltime*1000, 2)
-            soldf.loc[i, 'kernelName'] = ater.getHipblasltKernelName(
+            soldf.loc[i, 'kernelName'] = aiter.getHipblasltKernelName(
                 int(gemmobj.best_solidx)) if gemmobj.best_libtype == 'hipblaslt' else ""
 
             del gemmobj
diff --git a/gradlib/gradlib/gemm_tuner.py b/gradlib/gradlib/gemm_tuner.py
index d5812807a8..2a4da36c51 100644
--- a/gradlib/gradlib/gemm_tuner.py
+++ b/gradlib/gradlib/gemm_tuner.py
@@ -19,14 +19,14 @@
 from pathlib import Path
 
 import torch  # isort: split
-import ater
+import aiter
 import pandas as pd
-import ater
+import aiter
 
 from GemmTuner import GemmTuner
 
-ater.rocb_create_extension()
-ater.hipb_create_extension()
+aiter.rocb_create_extension()
+aiter.hipb_create_extension()
 
 
 def generate_mk_sets(model_dir, tp=1):
diff --git a/op_tests/test_ater_add.py b/op_tests/test_aiter_add.py
similarity index 95%
rename from op_tests/test_ater_add.py
rename to op_tests/test_aiter_add.py
index 3105785d7f..a138c54cee 100644
--- a/op_tests/test_ater_add.py
+++ b/op_tests/test_aiter_add.py
@@ -3,8 +3,8 @@
 
 import torch
 import torch.nn.functional as F
-import ater
-from ater.test_common import checkAllclose, perftest
+import aiter
+from aiter.test_common import checkAllclose, perftest
 from torch.profiler import profile, record_function, ProfilerActivity
 
 # input shape: torch.Size([4096, 64, 160]) (20480, 1, 128) 
@@ -58,7 +58,7 @@
     for j in range(100):
         #cache_flush1 = torch.randn(10000, 10000, requires_grad=True, device="cuda", dtype=torch.float32).to(torch.int32)
         # output = torch.empty_like(tensor1)
-        output = ater.add(tensor0, tensor1)
+        output = aiter.add(tensor0, tensor1)
 print(prof.key_averages().table(sort_by="cuda_time_total", row_limit=10))
 
 print(torch.equal(result_con, output))
diff --git a/op_tests/test_communication.py b/op_tests/test_communication.py
index aaff3a35b8..bb3af84c8c 100644
--- a/op_tests/test_communication.py
+++ b/op_tests/test_communication.py
@@ -5,14 +5,14 @@
 import torch.distributed as dist
 import torch.nn.functional as F
 import os
-import ater
-from ater.test_common import checkAllclose, perftest, tensor_dump, tensor_load
-from ater.dist.parallel_state import (graph_capture)
+import aiter
+from aiter.test_common import checkAllclose, perftest, tensor_dump, tensor_load
+from aiter.dist.parallel_state import (graph_capture)
 import sys
 import traceback
 import logging
 import multiprocessing as mp
-logger = logging.getLogger("ater")
+logger = logging.getLogger("aiter")
 
 NORM = 0
 DUMP = 1
@@ -26,7 +26,7 @@ def run_commun_fwd(tp_size, pp_size,  gpuID, input, withGraph=False):
     try:
         device = torch.device(f"cuda:{gpuID}")
         torch.cuda.set_device(device)
-        ater.init_dist_env(tp_size, gpuID)
+        aiter.init_dist_env(tp_size, gpuID)
 
         input = input.to(device)
         x = torch.empty_like(input)
@@ -43,7 +43,7 @@ def run_ca(graph):
                 with torch.cuda.graph(graph, stream=gc.stream):
                     # run inplace here, to test accuracy, we need this
                     x.copy_(input)
-                    out = ater.all_reduce_asm(x)
+                    out = aiter.all_reduce_asm(x)
             torch.cuda.synchronize()
             out.fill_(0)
             dist.barrier()
@@ -52,7 +52,7 @@ def run_ca(graph):
         else:
             @perftest()
             def run_ca(x):
-                return ater.all_reduce_asm(x)
+                return aiter.all_reduce_asm(x)
             out, us = run_ca(x)
         torch.cuda.synchronize()
         print(gpuID, 'finished')
@@ -62,7 +62,7 @@ def run_ca(x):
             ''.join(traceback.format_exception(*sys.exc_info()))
         ))
     finally:
-        ater.destroy_dist_env()
+        aiter.destroy_dist_env()
         return out, us
 
 
@@ -96,7 +96,7 @@ def run_all_reduce_rmsnorm(tp_size, pp_size,  gpuID, input, residual_in, weight,
     try:
         device = torch.device(f"cuda:{gpuID}")
         torch.cuda.set_device(device)
-        ater.init_dist_env(tp_size, gpuID)
+        aiter.init_dist_env(tp_size, gpuID)
 
         input = input.to(device)
         residual_in = residual_in.to(device)
@@ -111,7 +111,7 @@ def run_ca(graph):
             graph = torch.cuda.CUDAGraph()
             with graph_capture() as gc:
                 with torch.cuda.graph(graph, stream=gc.stream):
-                    out, residual_out = ater.all_reduce_rmsnorm(
+                    out, residual_out = aiter.all_reduce_rmsnorm(
                         input, residual_in, weight, bias, epsilon)
             torch.cuda.synchronize()
             out.fill_(0)
@@ -121,7 +121,7 @@ def run_ca(graph):
         else:
             @perftest()
             def run_ca(*args):
-                return ater.all_reduce_rmsnorm(*args)
+                return aiter.all_reduce_rmsnorm(*args)
             (out, residual_out), us = run_ca(
                 input, residual_in, weight, bias, epsilon)
         torch.cuda.synchronize()
@@ -133,7 +133,7 @@ def run_ca(*args):
             ''.join(traceback.format_exception(*sys.exc_info()))
         ))
     finally:
-        ater.destroy_dist_env()
+        aiter.destroy_dist_env()
         return (out, residual_out), us
 
 
@@ -141,7 +141,7 @@ def run_all_reduce_rmsnorm_quant(tp_size, pp_size,  gpuID, input, residual_in, x
     try:
         device = torch.device(f"cuda:{gpuID}")
         torch.cuda.set_device(device)
-        ater.init_dist_env(tp_size, gpuID)
+        aiter.init_dist_env(tp_size, gpuID)
 
         input = input.to(device)
         residual_in = residual_in.to(device)
@@ -157,7 +157,7 @@ def run_ca(graph):
             graph = torch.cuda.CUDAGraph()
             with graph_capture() as gc:
                 with torch.cuda.graph(graph, stream=gc.stream):
-                    out, residual_out, ysacle = ater.all_reduce_rmsnorm_quant(
+                    out, residual_out, ysacle = aiter.all_reduce_rmsnorm_quant(
                         input, residual_in, xscale, weight, bias, epsilon)
             torch.cuda.synchronize()
             out.fill_(0)
@@ -167,7 +167,7 @@ def run_ca(graph):
         else:
             @perftest()
             def run_ca(*args):
-                return ater.all_reduce_rmsnorm_quant(*args)
+                return aiter.all_reduce_rmsnorm_quant(*args)
             (out, residual_out, ysacle), us = run_ca(
                 input, residual_in, xscale, weight, bias, epsilon)
         torch.cuda.synchronize()
@@ -180,7 +180,7 @@ def run_ca(*args):
             ''.join(traceback.format_exception(*sys.exc_info()))
         ))
     finally:
-        ater.destroy_dist_env()
+        aiter.destroy_dist_env()
         return (out, residual_out, ysacle), us
 
 
@@ -223,7 +223,7 @@ def test_all_reduce_rmsnorm(tp_size, shape, dtype,  withGraph=False, perTKQuant=
     )+bias
     yscale = None
     if perTKQuant:
-        ref_out, yscale = ater.pertoken_quant(
+        ref_out, yscale = aiter.pertoken_quant(
             ref_out, torch.float32, x_scale=xscale)
 
     rets = [el.get() for el in rets]
diff --git a/op_tests/test_custom_allreduce.py b/op_tests/test_custom_allreduce.py
index 09f9433c77..465227eed5 100644
--- a/op_tests/test_custom_allreduce.py
+++ b/op_tests/test_custom_allreduce.py
@@ -6,21 +6,21 @@
 import torch
 import torch.distributed as dist
 
-from ater.dist.parallel_state import (ensure_model_parallel_initialized,
+from aiter.dist.parallel_state import (ensure_model_parallel_initialized,
                                       init_distributed_environment,
                                       set_custom_all_reduce,
                                       get_tp_group,
                                       graph_capture,
                                       destroy_model_parallel,
                                       destroy_distributed_environment)
-from ater.dist.utils import (get_open_port,
+from aiter.dist.utils import (get_open_port,
                              get_distributed_init_method,
                              get_ip)
-from ater.dist.communication_op import tensor_model_parallel_all_reduce
-from ater.test_common import checkAllclose, perftest, tensor_dump, tensor_load
+from aiter.dist.communication_op import tensor_model_parallel_all_reduce
+from aiter.test_common import checkAllclose, perftest, tensor_dump, tensor_load
 from multiprocessing import set_start_method, Pool, freeze_support
 import logging
-logger = logging.getLogger("ater")
+logger = logging.getLogger("aiter")
 
 set_start_method('spawn', force=True)
 
diff --git a/op_tests/test_gemm.py b/op_tests/test_gemm.py
index f6ed810fa4..a83961907d 100644
--- a/op_tests/test_gemm.py
+++ b/op_tests/test_gemm.py
@@ -1,7 +1,7 @@
 # SPDX-License-Identifier: MIT
 # Copyright (c) 2024, Advanced Micro Devices, Inc. All rights reserved.
 
-from ater.test_common import checkAllclose, perftest
+from aiter.test_common import checkAllclose, perftest
 import torch
 import torch.nn.functional as F
 import numpy as np
@@ -10,7 +10,7 @@
 if 1:
     _path = os.path.abspath(os.path.dirname(__file__))
     sys.path.insert(0, f'{_path}/../../')
-    from ater.tuned_gemm import tgemm
+    from aiter.tuned_gemm import tgemm
 
 
 @perftest()
diff --git a/op_tests/test_gemm_a8w8.py b/op_tests/test_gemm_a8w8.py
index 35ec58b82c..a3903f5ac1 100644
--- a/op_tests/test_gemm_a8w8.py
+++ b/op_tests/test_gemm_a8w8.py
@@ -1,14 +1,14 @@
 # SPDX-License-Identifier: MIT
 # Copyright (c) 2024, Advanced Micro Devices, Inc. All rights reserved.
 
-from ater.test_common import checkAllclose, perftest, tensor_dump
+from aiter.test_common import checkAllclose, perftest, tensor_dump
 import torch
 import torch.nn.functional as F
 import numpy as np
 import sys
 import os
-import ater
-from ater.ops.shuffle import shuffle_weight
+import aiter
+from aiter.ops.shuffle import shuffle_weight
 
 
 @perftest()
@@ -23,11 +23,11 @@ def run_torch(x, weight, x_scale, w_scale, bias=None, dtype=torch.bfloat16):
 
 @perftest()
 def run_gemm_ck(x, weight, x_scale, w_scale, bias=None, dtype=torch.bfloat16):
-    return ater.gemm_a8w8_CK(x, weight, x_scale, w_scale, bias)
+    return aiter.gemm_a8w8_CK(x, weight, x_scale, w_scale, bias)
 
 @perftest()
 def run_gemm_asm(x, weightshuffle, x_scale, w_scale, bias=None, dtype=torch.bfloat16):
-    return ater.gemm_a8w8_ASM(x, weightshuffle, x_scale, w_scale, bias)
+    return aiter.gemm_a8w8_ASM(x, weightshuffle, x_scale, w_scale, bias)
 
 
 def test_gemm(dtype, m, n, k):
diff --git a/op_tests/test_kvcache.py b/op_tests/test_kvcache.py
index 241344f204..db1158ffca 100644
--- a/op_tests/test_kvcache.py
+++ b/op_tests/test_kvcache.py
@@ -3,8 +3,8 @@
 
 import torch
 import torch.nn.functional as F
-import ater
-from ater.test_common import checkAllclose, perftest
+import aiter
+from aiter.test_common import checkAllclose, perftest
 from typing import List, Optional, Tuple, Union
 
 MAX_TOKEN_SUPPORTED = 16384
@@ -25,7 +25,7 @@ def run_torch(key, value, k_cache, v_cache, slot_mapping, block_size, x, asm_lay
     if quantCfg:
         k_scale = quantCfg['k_scale']
         v_scale = quantCfg['v_scale']
-        key, k_scale_ = ater.pertoken_quant(key,
+        key, k_scale_ = aiter.pertoken_quant(key,
                                             y_scale_dtype=quantCfg['y_scale_dtype'],
                                             quant_dtype=quantCfg['quant_dtype'])
         k_scale_ = k_scale_.permute(0, 1, 3, 2).view(
@@ -43,7 +43,7 @@ def run_torch(key, value, k_cache, v_cache, slot_mapping, block_size, x, asm_lay
                            head_size//x, x).permute(0, 2, 3, 1, 4)
 
     if quantCfg:
-        value, v_scale_ = ater.pertoken_quant(value,
+        value, v_scale_ = aiter.pertoken_quant(value,
                                               y_scale_dtype=quantCfg['y_scale_dtype'],
                                               quant_dtype=quantCfg['quant_dtype'])
         v_scale_ = v_scale_.permute(0, 1, 3, 2).view(
@@ -73,16 +73,16 @@ def run_torch(key, value, k_cache, v_cache, slot_mapping, block_size, x, asm_lay
 
 
 @perftest()
-def run_ater(key, value, k_cache, v_cache, slot_mapping, block_size, x, asm_layout, quantCfg={}):
+def run_aiter(key, value, k_cache, v_cache, slot_mapping, block_size, x, asm_layout, quantCfg={}):
     if quantCfg:
         k_scale = quantCfg['k_scale']
         v_scale = quantCfg['v_scale']
-        ater.reshape_and_cache_with_pertoken_quant(
+        aiter.reshape_and_cache_with_pertoken_quant(
             key, value, k_cache, v_cache, k_scale, v_scale, slot_mapping, asm_layout)
     else:
         k_scale = None
         v_scale = None
-        ater.reshape_and_cache(
+        aiter.reshape_and_cache(
             key, value, k_cache, v_cache, slot_mapping, 'auto', 1.0, 1.0, asm_layout)
     return k_cache, v_cache, k_scale, v_scale
 
@@ -137,10 +137,10 @@ def test_reshape_and_cache(ctx_lens: int,
     if quantCfg:
         quantCfg['k_scale'] = k_scale.clone()
         quantCfg['v_scale'] = v_scale.clone()
-    out_a, us_a = run_ater(key, value, k_cache_a, v_cache_a,
+    out_a, us_a = run_aiter(key, value, k_cache_a, v_cache_a,
                            slot_mapping, block_size, x, asm_layout, quantCfg)
 
-    print(f'prefill part: ref vs ater {us_ref:.2f}us vs {us_a:.2f}us')
+    print(f'prefill part: ref vs aiter {us_ref:.2f}us vs {us_a:.2f}us')
     names = ['k_cache', 'v_cache', 'k_scale', 'v_scale']
     for i, el in enumerate(out_ref):
         if el is None:
@@ -170,10 +170,10 @@ def test_reshape_and_cache(ctx_lens: int,
     if quantCfg:
         quantCfg['k_scale'] = k_scale.clone()
         quantCfg['v_scale'] = v_scale.clone()
-    out_a, us_a = run_ater(key, value, k_cache_a, v_cache_a,
+    out_a, us_a = run_aiter(key, value, k_cache_a, v_cache_a,
                            slot_mapping, block_size, x, asm_layout, quantCfg)
 
-    print(f'decode part: ref vs ater {us_ref:.2f}us vs {us_a:.2f}us')
+    print(f'decode part: ref vs aiter {us_ref:.2f}us vs {us_a:.2f}us')
     names = ['k_cache', 'v_cache', 'k_scale', 'v_scale']
     for i, el in enumerate(out_ref):
         if el is None:
diff --git a/op_tests/test_layernorm2d.py b/op_tests/test_layernorm2d.py
index d97cd76428..15be3a0d2b 100644
--- a/op_tests/test_layernorm2d.py
+++ b/op_tests/test_layernorm2d.py
@@ -3,8 +3,8 @@
 
 import torch
 import torch.nn.functional as F
-import ater
-from ater.test_common import checkAllclose, perftest
+import aiter
+from aiter.test_common import checkAllclose, perftest
 
 
 @perftest()
@@ -36,9 +36,9 @@ def run_torch(input, weight, bias, eps, residual=None, x_bias=None):
 def run_ck(input, weight, bias, eps, residual=None, x_bias=None):
     if residual is None:
         residual_out = None
-        output = ater.layer_norm(input, weight, bias, eps, x_bias)
+        output = aiter.layer_norm(input, weight, bias, eps, x_bias)
         # output = torch.empty_like(input)
-        # ater.layernorm2d_fwd(
+        # aiter.layernorm2d_fwd(
         #     output,
         #     input,
         #     weight,
@@ -48,7 +48,7 @@ def run_ck(input, weight, bias, eps, residual=None, x_bias=None):
     else:
         residual_out = torch.empty_like(input)
         output = torch.empty_like(input)
-        ater.layernorm2d_fwd_with_add(
+        aiter.layernorm2d_fwd_with_add(
             output,
             input,
             residual,
@@ -65,11 +65,11 @@ def run_ck(input, weight, bias, eps, residual=None, x_bias=None):
 def run_asm(input, weight, bias, eps, residual=None):
     if residual is None:
         residual_out = None
-        output = ater.layer_norm(input, weight, bias, eps)
+        output = aiter.layer_norm(input, weight, bias, eps)
     else:
         residual_out = torch.empty_like(input)
         output = torch.empty_like(input)
-        ater.layernorm2d_with_add_asm(
+        aiter.layernorm2d_with_add_asm(
             output,
             input,
             residual,
diff --git a/op_tests/test_layernorm2dFusedAddQuant.py b/op_tests/test_layernorm2dFusedAddQuant.py
index c93e2c8d3b..553e1d9df3 100644
--- a/op_tests/test_layernorm2dFusedAddQuant.py
+++ b/op_tests/test_layernorm2dFusedAddQuant.py
@@ -4,9 +4,9 @@
 import torch
 import torch.nn.functional as F
 import numpy as np
-import ater
+import aiter
 import argparse
-from ater.test_common import checkAllclose, perftest
+from aiter.test_common import checkAllclose, perftest
 
 
 @perftest()
@@ -32,7 +32,7 @@ def run_torch(input, weight, bias, eps, residual=None, x_scale = None, y_scale_d
     if y_scale_dtype is None:
         y_scale = None
     else:
-        output, y_scale = ater.pertoken_quant(output, y_scale_dtype, x_scale=x_scale)
+        output, y_scale = aiter.pertoken_quant(output, y_scale_dtype, x_scale=x_scale)
     return output, residual_out, y_scale
 
 @perftest()
@@ -41,7 +41,7 @@ def run_ck(input, weight, bias, eps, residual=None, x_scale = None, y_scale_dtyp
         y_scale = None
         if residual is None:
             residual_out = None
-            output = ater.layer_norm(
+            output = aiter.layer_norm(
                 input,
                 weight,
                 bias,
@@ -50,7 +50,7 @@ def run_ck(input, weight, bias, eps, residual=None, x_scale = None, y_scale_dtyp
         elif residual is not None:
             residual_out = torch.empty_like(input)
             output = torch.empty_like(input)
-            ater.layernorm2d_fwd_with_add(
+            aiter.layernorm2d_fwd_with_add(
                 output,
                 input,
                 residual,
@@ -64,7 +64,7 @@ def run_ck(input, weight, bias, eps, residual=None, x_scale = None, y_scale_dtyp
         output = torch.empty(input.shape, dtype=torch.int8, device="cuda")
         if residual is None:
             residual_out = None
-            ater.layernorm2d_fwd_with_dynamicquant(
+            aiter.layernorm2d_fwd_with_dynamicquant(
                 output,
                 input,
                 y_scale,
@@ -74,7 +74,7 @@ def run_ck(input, weight, bias, eps, residual=None, x_scale = None, y_scale_dtyp
             )
         elif residual is not None:
             residual_out = torch.empty_like(input)
-            ater.layernorm2d_fwd_with_add_dynamicquant(
+            aiter.layernorm2d_fwd_with_add_dynamicquant(
                 output,
                 input,
                 residual,
@@ -89,7 +89,7 @@ def run_ck(input, weight, bias, eps, residual=None, x_scale = None, y_scale_dtyp
         output = torch.empty(input.shape, dtype=torch.int8, device="cuda")
         if residual is None:
             residual_out = None
-            ater.layernorm2d_fwd_with_smoothquant(
+            aiter.layernorm2d_fwd_with_smoothquant(
                 output,
                 input,
                 x_scale,
@@ -100,7 +100,7 @@ def run_ck(input, weight, bias, eps, residual=None, x_scale = None, y_scale_dtyp
             )
         elif residual is not None:
             residual_out = torch.empty_like(input)
-            ater.layernorm2d_fwd_with_add_smoothquant(
+            aiter.layernorm2d_fwd_with_add_smoothquant(
                 output,
                 input,
                 residual,
@@ -121,7 +121,7 @@ def run_asm(input, weight, bias, eps, residual, x_scale, y_scale_dtype):
         input.shape[0], 1, dtype=y_scale_dtype, device="cuda")
     output = torch.empty(input.shape, dtype=torch.int8, device="cuda")
     residual_out = torch.empty_like(input)
-    ater.layernorm2d_with_add_smoothquant_asm(
+    aiter.layernorm2d_with_add_smoothquant_asm(
         output,
         input,
         residual,
diff --git a/op_tests/test_moe.py b/op_tests/test_moe.py
index e29da439e1..f69537221b 100644
--- a/op_tests/test_moe.py
+++ b/op_tests/test_moe.py
@@ -8,11 +8,11 @@
 import sys
 import os
 from typing import Any, Callable, Dict, Optional, Tuple
-from ater.test_common import checkAllclose, perftest
-from ater.fused_moe_bf16_asm import asm_moe, torch_moe, moe_sorting_ck
-from ater.fused_moe_gelu import fused_topk, moe_align_block_size, fused_experts
-from ater.ops.shuffle import shuffle_weight
-from ater import pertoken_quant, ck_moe
+from aiter.test_common import checkAllclose, perftest
+from aiter.fused_moe_bf16_asm import asm_moe, torch_moe, moe_sorting_ck
+from aiter.fused_moe_gelu import fused_topk, moe_align_block_size, fused_experts
+from aiter.ops.shuffle import shuffle_weight
+from aiter import pertoken_quant, ck_moe
 
 BLOCK_SIZE_M = 32
 
@@ -42,7 +42,7 @@ def moe_sorting_vllm(topk_ids: torch.Tensor,
     num_tokens_post_pad = torch.empty((1),
                                       dtype=torch.int32,
                                       device=topk_ids.device)
-    ater.moe_align_block_size(topk_ids, num_experts, block_size, sorted_ids,
+    aiter.moe_align_block_size(topk_ids, num_experts, block_size, sorted_ids,
                               expert_ids, token_nums, num_tokens_post_pad)
     return sorted_ids, expert_ids, token_nums, num_tokens_post_pad
 
@@ -287,21 +287,32 @@ def calculateTensorsSize(*args):
 
 
 print('test test_fmoe 16 bit')
-for dtype in [torch.float16, torch.bfloat16][1:]:
-    for m in [128, 256]:
-        for dim in [4096, 8192]:
-            for hdim in [1024]:
-                # test_fmoe(dtype, m, dim, hdim, 32, 5)
-                test_fmoe(dtype, m, dim, hdim, 32, 5, quant=True)
-
-for dtype in [torch.bfloat16]:
-    for m in [128, 256]:
-        for dim in [4096, 8192]:
-            for hdim in [1024]:
-                test_fmoe(dtype, m, dim, hdim, 32, 5, quant=True, use_g1u1=True)
-
+# for dtype in [torch.float16, torch.bfloat16][1:]:
+#     for m in [128, 256]:
+#         for dim in [4096, 8192]:
+#             for hdim in [1024]:
+#                 # test_fmoe(dtype, m, dim, hdim, 32, 5)
+#                 test_fmoe(dtype, m, dim, hdim, 32, 5, quant=True)
+
+# for dtype in [torch.bfloat16]:
+#     for m in [128, 256]:
+#         for dim in [4096, 8192]:
+#             for hdim in [1024]:
+#                 test_fmoe(dtype, m, dim, hdim, 32, 5, quant=True, use_g1u1=True)
+
+# for dtype in [torch.bfloat16]:
+#     for m in [128, 256]:
+#         for dim in [4096, 8192]:
+#             for hdim in [1024]:
+#                 test_fmoe(dtype, m, dim, hdim, 32, 5, quant=True, use_g1u1=True, quant_dtype=torch.float8_e4m3fnuz)
 for dtype in [torch.bfloat16]:
-    for m in [128, 256]:
-        for dim in [4096, 8192]:
+    for m in [128]:
+        for dim in [ 8192]:
             for hdim in [1024]:
                 test_fmoe(dtype, m, dim, hdim, 32, 5, quant=True, use_g1u1=True, quant_dtype=torch.float8_e4m3fnuz)
+# for dtype in [torch.bfloat16]:
+#     for m in [128]:
+#         for dim in [6144]:
+#             for hdim in [4096]:
+#                 test_fmoe(dtype, m, dim, hdim, 32, 5, quant=True,
+#                           use_g1u1=True, quant_dtype=torch.int8)
diff --git a/op_tests/test_moeTopkSoftmax.py b/op_tests/test_moeTopkSoftmax.py
index 38809ee578..35560482d0 100644
--- a/op_tests/test_moeTopkSoftmax.py
+++ b/op_tests/test_moeTopkSoftmax.py
@@ -3,8 +3,8 @@
 
 import torch
 import torch.nn.functional as F
-import ater
-from ater.test_common import checkAllclose, perftest
+import aiter
+from aiter.test_common import checkAllclose, perftest
 
 
 @perftest()
@@ -31,7 +31,7 @@ def test_nofuse(
                                         dtype=torch.int32,
                                         device=hidden_states.device)
 
-    ater.topk_softmax(
+    aiter.topk_softmax(
         topk_weights,
         topk_ids,
         token_expert_indicies,
@@ -51,7 +51,7 @@ def test_fuse(hidden_states: torch.Tensor,
               gating_output: torch.Tensor,
               topk: int,
               renormalize: bool):
-    from ater.fused_moe_gelu import fused_topk
+    from aiter.fused_moe_gelu import fused_topk
     return fused_topk(hidden_states, gating_output, topk, renormalize)
 
 
diff --git a/op_tests/test_pa.py b/op_tests/test_pa.py
index d4ccc90108..954ecef4a3 100644
--- a/op_tests/test_pa.py
+++ b/op_tests/test_pa.py
@@ -5,10 +5,10 @@
 from typing import List, Optional, Tuple, Union
 import itertools
 import torch
-import ater
-from ater import paged_attn as ops
-from ater.test_common import checkAllclose, perftest, tensor_dump, tensor_load
-from ater import pertoken_quant
+import aiter
+from aiter import paged_attn as ops
+from aiter.test_common import checkAllclose, perftest, tensor_dump, tensor_load
+from aiter import pertoken_quant
 
 uniform_range = (-1, 1)
 STR_DTYPE_TO_TORCH_DTYPE = {
@@ -19,6 +19,15 @@
     "fp8_e4m3": torch.uint8,
     "fp8_e5m2": torch.uint8,
 }
+ck_naive_quant_algo = [
+    'NO',
+    'KV_8BIT_PERHEAD',
+    # // FP8/INT8 quant for KVCache, per-token quant
+    # // [num_tokens, nhead, hdim] -> [nhead, num_tokens]
+    'KV_8BIT_PERTOKEN',
+    # // same as 8bit per token quant but 4 bit
+    'KV_4BIT_PERTOKEN',
+]
 
 
 def get_kv_cache_torch_dtype(
@@ -251,7 +260,7 @@ def run_native(query,
 
 
 @perftest()
-def run_ater(query,
+def run_aiter(query,
              key_cache,
              value_cache,
              block_tables,
@@ -280,7 +289,7 @@ def run_ater(query,
 
 
 @perftest()
-def run_ater_naive(query,
+def run_aiter_naive(query,
                    key_cache,
                    value_cache,
                    block_tables,
@@ -296,7 +305,7 @@ def run_ater_naive(query,
                    v_scale,
                    block_size,
                    quant_algo=0):
-    return ater.pa_fwd_naive(
+    return aiter.pa_fwd_naive(
         query,
         key_cache,
         value_cache,
@@ -315,7 +324,7 @@ def run_ater_naive(query,
 
 
 @perftest()
-def run_ater_asm(query,
+def run_aiter_asm(query,
                  key_cache,
                  value_cache,
                  block_tables,
@@ -328,7 +337,7 @@ def run_ater_asm(query,
                  max_num_blocks,
                  k_scale=None,
                  v_scale=None):
-    return ater.pa_fwd_asm(
+    return aiter.pa_fwd_asm(
         query,
         key_cache,
         value_cache,
@@ -367,7 +376,7 @@ def load_input():
     #         tensor_load('V_cache.bin'),
     #         tensor_load('block_tables.bin'),
     #         tensor_load('seq_lens.bin'),
-    #         tensor_load('out_ater.bin'))
+    #         tensor_load('out_aiter.bin'))
     # return (tensor_load('/mnt/raid0/ljin1/pa_data/x8_Kzero/Q_16.bin'),
     #         tensor_load('/mnt/raid0/ljin1/pa_data/x8_Kzero/K_16.bin'),
     #         tensor_load('/mnt/raid0/ljin1/pa_data/x8_Kzero/V_16.bin'),
@@ -462,7 +471,7 @@ def test_paged_attention(
                                                     device)
         key_cache, value_cache = key_caches[0], value_caches[0]
 
-    out_ater, time_ater = run_ater(
+    out_aiter, time_aiter = run_aiter(
         query,
         key_cache,
         value_cache,
@@ -477,12 +486,12 @@ def test_paged_attention(
         v_scale,
     )
     if debug_mode != VERIFY:
-        out_golden = out_ater
-    checkAllclose(out_golden, out_ater,
-                  msg=f'golden vs ater_shomy:{time_ater}')
-    tensor_dump(out_ater, 'out_ater')
-    
-    out_ater_asm, time_ater_asm = run_ater_asm(
+        out_golden = out_aiter
+    checkAllclose(out_golden, out_aiter,
+                  msg=f'golden vs aiter_shomy:{time_aiter}')
+    # tensor_dump(out_aiter, 'out_aiter')
+
+    out_aiter_asm, time_aiter_asm = run_aiter_asm(
         query,
         key_cache,
         asm_V_shuffle(value_cache),
@@ -496,9 +505,9 @@ def test_paged_attention(
         max_num_blocks_per_seq
     )
 
-    checkAllclose(out_golden, out_ater_asm,
-                  msg=f'golden vs ater_asm:{time_ater_asm}')
-    tensor_dump(out_ater, 'out_ater')
+    checkAllclose(out_golden, out_aiter_asm,
+                  msg=f'golden vs aiter_asm:{time_aiter_asm}')
+    # tensor_dump(out_aiter, 'out_aiter')
 
     for quant_algo_, cache_type_ in [(0, key_cache.dtype), (2, torch.float8_e4m3fnuz), (2, torch.int8)]:
         if quant_algo_ == 0:
@@ -507,7 +516,7 @@ def test_paged_attention(
         else:
             k_quant_, k_scale_, v_quant_, v_scale_ = pertoken_quant_kvcache_symm(
                 key_cache, value_cache, quant_dtype=cache_type_)
-        out_ater_naive, time_ater_naive = run_ater_naive(
+        out_aiter_naive, time_aiter_naive = run_aiter_naive(
             query,
             k_quant_,
             v_quant_,
@@ -525,11 +534,11 @@ def test_paged_attention(
             block_size,
             quant_algo_
         )
-        checkAllclose(out_ater_asm, out_ater_naive,
-                      msg=f'golden vs ck_naive(quant:{quant_algo_}, kvcache:{cache_type_}):{time_ater_naive}')
-  
+        checkAllclose(out_aiter_asm, out_aiter_naive,
+                      msg=f'golden vs ck_naive(quant:{ck_naive_quant_algo[quant_algo_]}, kvcache:{cache_type_}):{time_aiter_naive}')
+
         if cache_type_ == torch.int8:
-            out_ater_asm, time_ater_asm = run_ater_asm(
+            out_aiter_asm, time_aiter_asm = run_aiter_asm(
                 query,
                 k_quant_,
                 asm_V_shuffle(v_quant_),
@@ -544,8 +553,8 @@ def test_paged_attention(
                 k_scale_,
                 v_scale_,
             )
-            checkAllclose(out_golden, out_ater_asm,
-                          msg=f'golden vs ater_asm(quant:{quant_algo_}, kvcache:{cache_type_}):{time_ater_asm}')
+            checkAllclose(out_golden, out_aiter_asm,
+                          msg=f'golden vs aiter_asm(quant:{ck_naive_quant_algo[quant_algo_]}, kvcache:{cache_type_}):{time_aiter_asm}')
     if debug_mode == DUMP:
         dump_input(query,
                    key_cache,
@@ -579,10 +588,11 @@ def test_paged_attention(
     # tensor_dump(out_native, 'out_native')
 
     # atol, rtol = 1e-2, 1e-2
-    # msg = f"[perf] dim: {str((num_seqs, num_heads, head_size)):<20}, dtype: {dtype}, {time_native=:<8.2f} us, {time_ater=:<8.2f} us, uplift: {time_native/time_ater-1:<5.1%}"
-    # checkAllclose(out_native, out_ater, atol=atol, rtol=rtol, msg=msg)
+    # msg = f"[perf] dim: {str((num_seqs, num_heads, head_size)):<20}, dtype: {dtype}, {time_native=:<8.2f} us, {time_aiter=:<8.2f} us, uplift: {time_native/time_aiter-1:<5.1%}"
+    # checkAllclose(out_native, out_aiter, atol=atol, rtol=rtol, msg=msg)
     # print(
     #     f"[test] dim: {str((ctx_lens, num_seqs, num_heads, head_size)):<20}, dtype: {dtype}, finished)\n")
+    print(f'finish~ {ctx_lens=}, {num_seqs=}, {num_heads=}, {head_size=}, {use_alibi=}, {block_size=}, {dtype=}, {kv_cache_dtype=}\n')
 
 
 for ctx_len in [1, 26, 128, 4097]:
diff --git a/op_tests/test_quant.py b/op_tests/test_quant.py
index fe78687c06..cb4b3c7863 100644
--- a/op_tests/test_quant.py
+++ b/op_tests/test_quant.py
@@ -1,23 +1,23 @@
 # SPDX-License-Identifier: MIT
 # Copyright (c) 2024, Advanced Micro Devices, Inc. All rights reserved.
 
-from ater.test_common import checkAllclose, perftest, tensor_dump
+from aiter.test_common import checkAllclose, perftest, tensor_dump
 import torch
 import torch.nn.functional as F
 import numpy as np
 import sys
 import os
-import ater
-from ater.ops.shuffle import shuffle_weight
+import aiter
+from aiter.ops.shuffle import shuffle_weight
 
 @perftest()
-def test_ater_perTensorQuantFp8(input, scale=None):
+def test_aiter_perTensorQuantFp8(input, scale=None):
     out = torch.empty(input.shape, dtype=torch.float8_e4m3fnuz, device=input.device)
     if scale is None:
         scale = torch.zeros(1, dtype=torch.float32, device=input.device)
-        ater.dynamic_scaled_fp8_quant(out, input, scale)
+        aiter.dynamic_scaled_fp8_quant(out, input, scale)
     else:
-        ater.static_scaled_fp8_quant(out, input, scale)
+        aiter.static_scaled_fp8_quant(out, input, scale)
     return out, scale
 
 @perftest()
@@ -34,18 +34,18 @@ def test_torch_perTensorQuantFp8(input, scale=None):
     
 
 @perftest()
-def test_ater_perTokenQuantFp8(input):
+def test_aiter_perTokenQuantFp8(input):
     # orign_shape = input.shape
     token_num = input.view(-1, input.shape[-1]).shape[0]
     out = torch.empty(input.shape, dtype=torch.float8_e4m3fnuz, device=input.device)
     scales = torch.empty(token_num, dtype=torch.float32, device=input.device)
-    ater.dynamic_per_token_scaled_fp8_quant(out, input, scales)
+    aiter.dynamic_per_token_scaled_fp8_quant(out, input, scales)
     return out, scales
 
 
 @perftest()
 def test_torch_perTokenQuantFp8(input):
-    return ater.pertoken_quant(input, torch.float, quant_dtype=torch.float8_e4m3fnuz)
+    return aiter.pertoken_quant(input, torch.float, quant_dtype=torch.float8_e4m3fnuz)
 
 
 def test_quant(m, n, dtype=torch.bfloat16):
@@ -55,21 +55,21 @@ def test_quant(m, n, dtype=torch.bfloat16):
     print(f'{max_val=}')
 
     (a, a_scale), avg_a = test_torch_perTensorQuantFp8(input)
-    (b, b_scale), avg_b = test_ater_perTensorQuantFp8(input)
-    msg = f"[perf] dynamic_perTensorQuantFp8 dim: {str(dim):<20} dtype: {dtype}, torch avg: {avg_a:<8.2f} us, ater avg: {avg_b:<8.2f} us, uplift: {avg_a/avg_b-1:<5.1%}"
+    (b, b_scale), avg_b = test_aiter_perTensorQuantFp8(input)
+    msg = f"[perf] dynamic_perTensorQuantFp8 dim: {str(dim):<20} dtype: {dtype}, torch avg: {avg_a:<8.2f} us, aiter avg: {avg_b:<8.2f} us, uplift: {avg_a/avg_b-1:<5.1%}"
     checkAllclose(a.to(torch.float), b.to(torch.float), rtol=0.125, atol=1e-3, msg=msg)
     checkAllclose(a_scale, b_scale, msg='scale')
     # print(f'{a_scale=}, {b_scale=}')
 
     scale = a_scale
     (a, a_scale), avg_a = test_torch_perTensorQuantFp8(input, scale)
-    (b, b_scale), avg_b = test_ater_perTensorQuantFp8(input, scale)
-    msg = f"[perf] static_perTensorQuantFp8 dim: {str(dim):<20} dtype: {dtype}, torch avg: {avg_a:<8.2f} us, ater avg: {avg_b:<8.2f} us, uplift: {avg_a/avg_b-1:<5.1%}"
+    (b, b_scale), avg_b = test_aiter_perTensorQuantFp8(input, scale)
+    msg = f"[perf] static_perTensorQuantFp8 dim: {str(dim):<20} dtype: {dtype}, torch avg: {avg_a:<8.2f} us, aiter avg: {avg_b:<8.2f} us, uplift: {avg_a/avg_b-1:<5.1%}"
     checkAllclose(a.to(torch.float), b.to(torch.float), rtol=0.125, atol=1e-3, msg=msg)
 
     (a, a_scale), avg_a = test_torch_perTokenQuantFp8(input)
-    (b, b_scale), avg_b = test_ater_perTokenQuantFp8(input)
-    msg = f"[perf] dynamic_perTokenQuantFp8 dim: {str(dim):<20} dtype: {dtype}, torch avg: {avg_a:<8.2f} us, ater avg: {avg_b:<8.2f} us, uplift: {avg_a/avg_b-1:<5.1%}"
+    (b, b_scale), avg_b = test_aiter_perTokenQuantFp8(input)
+    msg = f"[perf] dynamic_perTokenQuantFp8 dim: {str(dim):<20} dtype: {dtype}, torch avg: {avg_a:<8.2f} us, aiter avg: {avg_b:<8.2f} us, uplift: {avg_a/avg_b-1:<5.1%}"
     checkAllclose(a.to(torch.float), b.to(torch.float), rtol=0.125, atol=1e-3, msg=msg)
     checkAllclose(a_scale.view(-1), b_scale, msg='scale')
 
diff --git a/op_tests/test_rmsnorm2d.py b/op_tests/test_rmsnorm2d.py
index 0c071fd135..d4a38a7337 100644
--- a/op_tests/test_rmsnorm2d.py
+++ b/op_tests/test_rmsnorm2d.py
@@ -3,8 +3,8 @@
 
 import torch
 import torch.nn.functional as F
-import ater
-from ater.test_common import checkAllclose, perftest
+import aiter
+from aiter.test_common import checkAllclose, perftest
 
 
 @perftest()
@@ -32,7 +32,7 @@ def run_torch(input, weight, eps, residual=None):
 def run_ck(input, weight, eps, residual=None):
     if residual is None:
         residual_out = None
-        output = ater.rms_norm(
+        output = aiter.rms_norm(
             input,
             weight,
             eps
@@ -40,7 +40,7 @@ def run_ck(input, weight, eps, residual=None):
     else:
         residual_out = torch.empty_like(input)
         output = torch.empty_like(input)
-        ater.rmsnorm2d_fwd_with_add(
+        aiter.rmsnorm2d_fwd_with_add(
             output,
             input,
             residual,
@@ -56,9 +56,9 @@ def run_cu(input, weight, eps, residual=None):
     if residual is None:
         residual_out = None
         output = torch.empty_like(input)
-        ater.rms_norm_cu(output, input, weight, eps)
+        aiter.rms_norm_cu(output, input, weight, eps)
     else:
-        ater.fused_add_rms_norm_cu(
+        aiter.fused_add_rms_norm_cu(
             input,
             residual,
             weight,
diff --git a/op_tests/test_rmsnorm2dFusedAddQuant.py b/op_tests/test_rmsnorm2dFusedAddQuant.py
index 816ccf923c..dfa4d38db6 100644
--- a/op_tests/test_rmsnorm2dFusedAddQuant.py
+++ b/op_tests/test_rmsnorm2dFusedAddQuant.py
@@ -4,9 +4,9 @@
 import torch
 import torch.nn.functional as F
 import numpy as np
-import ater
+import aiter
 import argparse
-from ater.test_common import checkAllclose, perftest
+from aiter.test_common import checkAllclose, perftest
 
 
 @perftest()
@@ -30,7 +30,7 @@ def run_torch(input, weight, eps, residual=None, x_scale = None, y_scale_dtype =
     if y_scale_dtype is None:
         y_scale = None
     else:
-        output, y_scale = ater.pertoken_quant(output, y_scale_dtype, x_scale=x_scale)
+        output, y_scale = aiter.pertoken_quant(output, y_scale_dtype, x_scale=x_scale)
     return output, residual_out, y_scale
 
 @perftest()
@@ -39,7 +39,7 @@ def run_ck(input, weight, eps, residual=None, x_scale = None, y_scale_dtype = No
         y_scale = None
         if residual is None:
             residual_out = None
-            output = ater.rms_norm(
+            output = aiter.rms_norm(
                 input,
                 weight,
                 eps
@@ -47,7 +47,7 @@ def run_ck(input, weight, eps, residual=None, x_scale = None, y_scale_dtype = No
         elif residual is not None:
             residual_out = torch.empty_like(input)
             output = torch.empty_like(input)
-            ater.rmsnorm2d_fwd_with_add(
+            aiter.rmsnorm2d_fwd_with_add(
                 output,
                 input,
                 residual,
@@ -60,7 +60,7 @@ def run_ck(input, weight, eps, residual=None, x_scale = None, y_scale_dtype = No
         output = torch.empty(input.shape, dtype=torch.int8, device="cuda")
         if residual is None:
             residual_out = None
-            ater.rmsnorm2d_fwd_with_dynamicquant(
+            aiter.rmsnorm2d_fwd_with_dynamicquant(
                 output,
                 input,
                 y_scale,
@@ -69,7 +69,7 @@ def run_ck(input, weight, eps, residual=None, x_scale = None, y_scale_dtype = No
             )
         elif residual is not None:
             residual_out = torch.empty_like(input)
-            ater.rmsnorm2d_fwd_with_add_dynamicquant(
+            aiter.rmsnorm2d_fwd_with_add_dynamicquant(
                 output,
                 input,
                 residual,
@@ -83,7 +83,7 @@ def run_ck(input, weight, eps, residual=None, x_scale = None, y_scale_dtype = No
         output = torch.empty(input.shape, dtype=torch.int8, device="cuda")
         if residual is None:
             residual_out = None
-            ater.rmsnorm2d_fwd_with_smoothquant(
+            aiter.rmsnorm2d_fwd_with_smoothquant(
                 output,
                 input,
                 x_scale,
@@ -93,7 +93,7 @@ def run_ck(input, weight, eps, residual=None, x_scale = None, y_scale_dtype = No
             )
         elif residual is not None:
             residual_out = torch.empty_like(input)
-            ater.rmsnorm2d_fwd_with_add_smoothquant(
+            aiter.rmsnorm2d_fwd_with_add_smoothquant(
                 output,
                 input,
                 residual,
diff --git a/op_tests/test_smoothquant.py b/op_tests/test_smoothquant.py
index 872751bac4..5d1c59fb2f 100644
--- a/op_tests/test_smoothquant.py
+++ b/op_tests/test_smoothquant.py
@@ -4,14 +4,14 @@
 import torch
 import torch.nn.functional as F
 import numpy as np
-import ater
-from ater.test_common import checkAllclose, perftest
+import aiter
+from aiter.test_common import checkAllclose, perftest
 import argparse
 
 
 @perftest()
 def run_torch(input, x_scale, y_scale_dtype=torch.float32):
-    output, y_scale = ater.pertoken_quant(
+    output, y_scale = aiter.pertoken_quant(
         input, x_scale=x_scale, y_scale_dtype=y_scale_dtype)
     return output, y_scale
 
@@ -23,7 +23,7 @@ def run_ck(input, x_scale, y_scale_dtype=torch.float32):
                          layout=input.layout, device=input.device)
     y_scale = torch.empty(
         input.shape[0], 1, device="cuda", dtype=y_scale_dtype)
-    ater.smoothquant_fwd(output,
+    aiter.smoothquant_fwd(output,
                          input,
                          x_scale,
                          y_scale)
diff --git a/setup.py b/setup.py
index 7a60d0cf72..b672cdf936 100644
--- a/setup.py
+++ b/setup.py
@@ -8,7 +8,7 @@
 
 from setuptools import setup, find_packages
 from packaging.version import parse, Version
-from ater.jit import core
+from aiter.jit import core
 import torch
 from torch.utils.cpp_extension import (
     BuildExtension,
@@ -23,7 +23,7 @@
 ck_dir = os.environ.get("CK_DIR", f"{this_dir}/3rdparty/composable_kernel")
 bd_dir = f"{this_dir}/build"
 blob_dir = f"{bd_dir}/blob"
-PACKAGE_NAME = 'ater'
+PACKAGE_NAME = 'aiter'
 BUILD_TARGET = os.environ.get("BUILD_TARGET", "auto")
 
 if BUILD_TARGET == "auto":
@@ -73,12 +73,12 @@ def validate_and_update_archs(archs):
 
     # Check, if ATen/CUDAGeneratorImpl.h is found, otherwise use ATen/cuda/CUDAGeneratorImpl.h
     # See https://github.com/pytorch/pytorch/pull/70650
-    generator_flag = [f'-DATER_ASM_DIR="{this_dir}/hsa/"']
+    generator_flag = [f'-DAITER_ASM_DIR="{this_dir}/hsa/"']
     torch_dir = torch.__path__[0]
     if os.path.exists(os.path.join(torch_dir, "include", "ATen", "CUDAGeneratorImpl.h")):
         generator_flag.append("-DOLD_GENERATOR_PATH")
     assert os.path.exists(
-        ck_dir), f'CK is needed by ater, please make sure clone by "git clone --recursive https://github.com/ROCm/ater.git" or "git submodule sync ; git submodule update --init --recursive"'
+        ck_dir), f'CK is needed by aiter, please make sure clone by "git clone --recursive https://github.com/ROCm/aiter.git" or "git submodule sync ; git submodule update --init --recursive"'
     generator_flag.append("-DFIND_CK")
 
     shutil.copytree(ck_dir, f'{bd_dir}/ck', dirs_exist_ok=True)
@@ -118,7 +118,7 @@ def validate_and_update_archs(archs):
         new_list=[el for el in all_opts_args_build["srcs"] if "pybind.cu" not in el]
         all_opts_args_build["srcs"] = new_list
 
-        core.build_module(md_name = "ater_", 
+        core.build_module(md_name = "aiter_", 
                     srcs = all_opts_args_build["srcs"] + [f"{this_dir}/csrc"],
                     flags_extra_cc = all_opts_args_build["flags_extra_cc"],
                     flags_extra_hip = all_opts_args_build["flags_extra_hip"],
