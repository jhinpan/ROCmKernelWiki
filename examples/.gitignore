# build artifacts — regenerate with each example's build.sh
*.o
*.out
a.out
*.exe
*.hsaco
*.s.tmp
# compiled binaries (no extension, executable) are not tracked; sources + build.sh are
bandwidth-microbench/bwread
bandwidth-microbench/bwread.s
rmsnorm/rmsnorm
